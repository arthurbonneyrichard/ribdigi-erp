'use client';import {useEffect,useState} from 'react';import Shell from '../../components/Shell';import {api} from '../../lib/api';
export default function Page(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{api('/users').then(r=>setRows(r.data||[])).catch(()=>{})},[]);return <Shell><h1>User Management</h1><p className="muted">RIBDIGI ERP module</p><table className="table"><thead><tr><th>Full Name</th><th>Email</th><th>Role</th><th>Is Active</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td>{String(r.full_name??"")}</td><td>{String(r.email??"")}</td><td>{String(r.role??"")}</td><td>{String(r.is_active??"")}</td></tr>)}</tbody></table></Shell>}
