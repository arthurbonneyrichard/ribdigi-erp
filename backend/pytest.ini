[pytest]
asyncio_mode = auto
pythonpath = .
markers =
    security: OWASP / production security / auth hardening proofs
    isolation: tenant isolation matrix and cross-tenant denials
    store_scope: living store-scoped RBAC matrix (cross-store deny / membership soak / ALLOWs)
