# RIBDIGI ERP — Production Readiness Gate

RIBDIGI is intended to be a commercial ERP, not a demo application. A feature is **production-ready only when implementation, automated tests, security controls, and operational requirements are verified**.

## Non-negotiable release rules

- No demo tenant, sample company, fake transaction, pre-filled login, or known default password in production.
- No endpoint may return success for work that was not actually completed.
- No hard-coded secrets or development credentials in production.
- No protected tenant data endpoint without authentication, tenant isolation and authorization.
- No database schema change outside versioned migrations.
- No financial workflow is complete until stock, ledger, tax, credit and audit side effects are transactionally correct where applicable.
- No production claim until the Phase 5 launch gates in `docs/DEVELOPMENT_ROADMAP.md` are passed.

## Required launch gates

### Platform & tenancy
- [x] Schema-per-tenant strategy implemented as specified, or architecture documents formally revised to an approved equivalent.
  - Approved equivalent: shared-schema + `tenant_id` (see `docs/ADR_001_TENANCY.md`). Schema-per-tenant remains post-MVP.
- [ ] Cross-tenant isolation integration tests pass for every tenant-owned resource.
  - Partial: SQLite suite covers product list scoping, foreign sales invoice 404, mismatched `X-Tenant-ID` (products, credit aging, users), suspend/activate login block, plus matrix coverage for customers/suppliers, credit statement/limit, product image, expenses, purchase invoices, tax rates, store inventory, POS sessions/drawer, notifications, backups, accounting accounts/journals, audit logs, bank connections, report schedules, cheques, stock transfers, media-key tenant mismatch, and tenant-scoped AI insights (chat remains 503 until provider). Document/attachment paths: expense + purchase-invoice + journal attachment GET/POST/DELETE (+ expense/PI OCR), product legacy/gallery image mutate + IDOR image_id, backup download/restore, and poisoned cross-tenant storage keys (`test_attachment_path_isolation.py`). Remaining: expand matrix as new tenant-owned resources land.
- [x] Tenant provisioning, suspension, activation and lifecycle management complete.
  - Complete (MVP lifecycle + first-run): register + defaults, company profile GET/PATCH (including `legal_name`, `registration_number`, `contact_person`, HQ/billing/shipping addresses, tax ID, **formatting BR-20.2** `date_format`/`decimal_separator`/`thousand_separator`/`time_format` + Alembic `20260813_0088`), suspend (revokes sessions) / activate, super_admin cross-tenant lifecycle, trial `trial_ends_at` (default 14d) + 7/3/1 reminders + grace read-only (`status=grace`) then auto-suspend, company logo upload/serve/delete via `STORAGE_BACKEND=local|s3` (MinIO/S3-compatible object store; keys remain tenant-scoped). First-run onboarding checklist (`GET /onboarding/checklist`, skip/unskip/dismiss/restore) auto-detects company profile, products, supplier, stock, and first sale; dismissible at ≥80% progress. **Subscription packages (BR-1.3):** software owner assigns `package_code` + term months/years (`POST /tenants/{ref}/subscription`), sees used/remaining until renewal, and controls feature modules (`PATCH /tenants/{ref}/modules` → `PACKAGE_FEATURE_DISABLED`); Platform UI Manage panel. **Platform staff roles + reports:** `platform_owner|admin|support|finance` (+ legacy `super_admin`); `POST /platform/staff`, grant/revoke software-owner dashboard for existing app users (`POST /platform/staff/grant`, `POST /platform/staff/{id}/revoke`, `GET /platform/app-users`), `GET /platform/reports` (tenant summary, packages, subscriptions, trial/renewal window). Payment gateway billing remains post-MVP.

### Identity & security
- [x] Login/logout/refresh token flow complete. **Session management (BR-19.3):** `GET|DELETE /auth/sessions` (self-scoped) + Security UI revoke; refresh rotation; client idle logout via tenant `inactivity_timeout_minutes` (default 30; Company UI + `PATCH /tenants/me` / `GET /me`; Alembic `20260813_0089`).
- [x] Email verification and password reset complete.
  - Tokens issued; SMTP delivery with console fallback in dev; production requires `SMTP_HOST` + `SMTP_FROM_EMAIL` when `EMAIL_ENABLED=true`.
- [x] 2FA/TOTP complete for required roles.
  - Complete: TOTP setup/QR/confirm, backup codes, encrypted secrets, company_admin/super_admin enrollment gate; WebAuthn/passkeys (register/list/delete + login challenge with `methods`); MFA satisfied by TOTP and/or passkeys.
- [x] Session listing and revocation complete.
- [x] Granular module/menu/record RBAC complete.
  - Module+action RBAC enforced on routes; `GET /roles` catalog (includes default `record_scope` + tenant custom roles); custom role CRUD (`POST/PATCH/DELETE /roles`, clone from system `base_role`, sync assigned users on permission update); user create/patch/deactivate + `record_scope` override + **branch/department assignment (BR-3.1)** on Users UI (`branch_id` / `department_id` create + inline PATCH); bulk user CSV import (`GET /users/import/template`, `POST /users/import` dry-run/commit) with Users UI; Shell menu filtered from `/me` permissions; record scope `own`|`department`|`branch`|`all` via org units (`branches` / `departments`, user + store `branch_id` / `department_id`, `scope_user_ids` in claims) enforced on expenses, sales invoices/quotations/orders/returns, and purchasing requests/orders/GRNs/invoices/returns. Approvals intentionally bypass own-scope. Remaining: none for department/branch record scopes.
- [x] Tenant API keys for service integrations (BR-18.1). Admin UI: **Integrations** (`/integrations`) create/revoke/usage.
  - Complete: `POST/GET/DELETE /api-keys` (company_admin/super_admin); secret shown once (`rdk_…`); auth via `X-API-Key` or `Authorization: Bearer rdk_…`; permission-scoped like custom roles; revoke + cross-tenant header blocked; hashed at rest.
- [x] Outbound webhooks with HMAC signatures (BR-18.6). Admin UI: **Integrations** create/test/rotate/disable/delete.
  - Complete: `POST/GET/PATCH/DELETE /webhooks` + `POST .../test`; encrypted `whsec_…` signing secret; `X-Ribdigi-Signature` HMAC-SHA256; HTTPS-only URLs (localhost http allowed); event fan-out (`sale.created` on invoice post); Celery `retry_due_webhooks` with exponential backoff (`pending_retry` → delivered/failed).
- [x] Rate limiting, security headers and production CORS complete.
  - Complete (MVP): Redis sliding-window rate limits (`RATE_LIMIT_BACKEND=auto|redis|memory`) with memory fallback, auth vs API caps (login/refresh/2FA/password-reset on auth bucket), `429 RATE_LIMIT_EXCEEDED` + `Retry-After` / `X-RateLimit-*` headers exposed via CORS, buckets keyed by IP + route class + `X-Tenant-ID` scope, security headers including CSP (`default-src 'none'`), COOP, production HSTS + `Cache-Control: no-store`, production CORS whitelist (no `*`), production validators reject weak JWT/DEBUG/empty CORS/disabled limits / `RATE_LIMIT_REQUIRE_REDIS`+memory; recommend `RATE_LIMIT_REQUIRE_REDIS=true` for multi-instance. Celery/RabbitMQ scheduled workloads remain under Reliability.
- [x] OWASP/security tests completed.
  - Complete (MVP automated): smoke (`test_owasp_smoke.py`) plus beyond-smoke suites (`test_owasp_suite_o1.py`, `test_owasp_suite_t1.py`) covering A01 access control/IDOR/privilege escalation, A02 secret leakage + tampered JWT, A03 injection/XSS-as-JSON, A05 safe 404 errors, A07 missing/garbage/expired tokens and garbage API keys (alongside lockout and RBAC deny). SMTP settings expose `has_password` only (never the secret). Vendor pen test / live ZAP remain post-MVP.

### ERP operations
- [ ] Inventory catalog, variants, batches/expiry, stock movements and adjustments complete.
  - Partial: product GET/PATCH + create with category/brand/unit FKs + image upload, **product details (BR-5.1)** (`description`, `weight` kg, `length`/`width`/`height` cm; CSV import columns; Inventory UI; Alembic `20260813_0083`), **SKU auto/manual (BR-5.1)** (omit `sku` → `SKU-YYYY-NNNN` on product/variant create), hierarchical categories (`parent_id`), **brand logo + description (BR-5.1)** (`brands.logo_url`; `POST|GET|DELETE /catalog/brands/{id}/logo`; Alembic `20260813_0085`), **units of measure with conversion ratios (BR-5.1)** (`base_unit_id` + `conversion_ratio`; stock-in/out optional `unit_id` converts to `product.unit_id`; `POST /catalog/units/convert` preview), variants (SKU/size/color/flavor/**dosage**; Inventory Variants UI; Alembic `20260813_0087`) with PATCH/soft-delete, multi-image gallery (`product_images`, max 5, primary synced to `products.image_url`; legacy `/image` kept), batches with manufacturing_date + expiry + FEFO stock-out (Inventory Batches/Opening UI), stock in/out/adjust + immutable `stock_movements` (variant/batch refs; movements filterable by product/warehouse/type), physical stock counts (`stock_counts` / `stock_count_items`, draft→count→complete posts warehouse variances), expiring-batches report/UI + **Reports expiry export** (`inventory_expiry`), sales/POS lines accept `variant_id`. **Document-line alternate UoM:** optional `unit_id` on PO / GRN / sales quote·order·invoice (+ POS `LineItem`); GRN accept / invoice post / order reserve / POS checkout convert via `to_stock_qty`; Purchasing + Sales unit pickers. **Opening stock (BR-5.2):** `POST /inventory/opening-stock` multi-line init with warehouse/batch/unit + optional Dr 1200 / Cr 3000 journal; Inventory → Opening stock UI. **Stock status lights (BR-5.5):** `stock_status`/`stock_status_label` on product serialize (red ≤ reorder / out; yellow ≤ reorder×1.5; green OK); Inventory products Stock badge. **Low-stock role notify (BR-5.5):** product + warehouse-policy alerts email `inventory_officer`/`store_manager` (+ admins); default `low_stock.email=true`. Remaining: deep category trees/UI polish. CSV bulk import: `GET /products/import/template` + `POST /products/import` (dry-run validate then all-or-nothing commit) with Inventory → Import UI.
- [ ] Purchasing/PO/GRN/supplier workflow complete.
  - Partial: supplier CRUD + **supplier profile (BR-6.1)** (`code`/`profile_type`/`category`/`status`/`address`/GPS on `parties`; `GET /suppliers/{id}`; Alembic `20260813_0080`) + **multi-contact (BR-6.1)** (`party_contacts` via `/suppliers|customers/{id}/contacts`; primary syncs party email/phone; Alembic `20260813_0090`) + **PO delivery address (BR-6.3)** (`purchase_orders.delivery_address` create/amend/email; Alembic `20260813_0081`) + **supplier history (BR-6.1)** (`GET /suppliers/{id}/history`), purchase requests (draft→pending→approved/rejected→converted) with configurable N-level role-chain approval matrix (`tenants.purchase_approval_matrix` / `GET|PATCH /purchasing/requests/settings`; default Store Manager → Company Admin; no self-approve / no duplicate-step actor; action log + L-step UI) and one-click convert to draft PO, PO draft→email supplier (SMTP/console) then status=sent + resend with `emailed_at`/`emailed_to`, PO amendment tracking (`POST .../amend`, revision history, optional supplier notify; blocked after receipts), partial/full received, GRN posts stock + supplier balance, **GRN reject/damage (BR-6.4)** (`rejected_qty` + required `rejection_reason`; accepted-only stock-in; full received qty closes PO outstanding; Purchasing receive UI + GRN reject columns), purchase invoices (from GRN or manual) draft→approve with unpaid/partial/paid/overdue + payment allocation, purchase returns draft→post with stock out + AP/debit note journal, supplier invoice file upload/download/delete on invoices, suggest-only supplier-invoice OCR (`POST .../ocr-suggest` + draft `PATCH` for supplier # / date / notes).
- [ ] Sales/invoice/payment/customer workflow complete.
  - Partial: customer CRUD + **customer profile & contact (BR-7.1)** (`code`/`profile_type` walk_in|registered/`status`/`address`/GPS; `GET /customers/{id}`; Alembic `20260813_0080`) + **customer groups** (Retail/Wholesale/VIP defaults, CRUD, `customer_group_id` on party; list-price discount when `unit_price` omitted on sales invoices/quotes/orders/POS; `GET /products/{id}/price` preview), quotations (send emails customer via SMTP/console then status=sent + resend, accept/reject/convert), sales orders (confirm soft-reserves store warehouse stock; cancel releases through processing; fulfillment `confirmed→processing→shipped→delivered` with timestamps; invoice from any pre-invoiced fulfillment status; invoice post consumes reservation then hard stock-out; delivery date/address + store_id), invoices draft→posted/sent/partial/overdue/paid|cancelled (email marks `sent`; due-date overdue via payment-due scan) with optional `store_id` (posts deduct that store’s warehouse stock) + print A4/thermal (`GET /sales/invoices/{id}/print`) + email billed invoices to customer (SMTP/console) with `emailed_at`/`emailed_to` + resend, payments, sales returns with restock/discard + credit note number on post + `settlement_method=adjust|refund` (refund cash/bank when return exceeds open AR) + AR/journal, line `variant_id` on quote/order/invoice/return, salesperson + store performance, **customer sales (BR-14.1)** (`GET /reports/sales/customers` revenue/frequency; optional `store_id`; Reports Customers tab + export `sales_customers`) + **sales returns summary** (`GET /reports/sales/returns`; by reason/customer; Reports Sales tab + export `sales_returns`) + **customer history (BR-7.1)** (`GET /customers/{id}/history`) + **product sales filters** (`store_id`/`category_id` on `/reports/sales/products`) reports with CSV/PDF/Excel export. Remaining: none for BR-20.4 (numbering + print branding).
- [ ] POS cart, barcode, payment, receipt, shift and stock deduction complete.
  - Partial: cart checkout with stock out (incl. variant lines; store session deducts linked warehouse), shift open/close + cash reconciliation/variance notification + **shift report summary (BR-8.2)** (`GET /pos/sessions/{id}/report` with discounts/returns/`summary`), payment methods, thermal receipt (JSON/text/PDF 58/80mm) + email/SMS send; hardware cash drawer (`stores.drawer_mode` none|mock|network|browser_bridge, `PATCH /stores/{id}/drawer`, `POST /pos/sessions/{id}/drawer/open`, auto-pulse on cash sales, ESC/POS kick bytes on receipt JSON). Remaining: vendor-specific USB/serial drivers beyond TCP ESC/POS / browser bridge.
- [ ] Expenses and approval workflow complete.
  - Partial: categories + default seed, **category budgets (BR-9.1 / BR-14.4)** (`ExpenseCategory.budget_amount`; `PATCH /expenses/categories/{id}`; `GET /reports/expenses/budget-vs-actual` scales monthly budget by `period_days/30` vs approved spend; Expenses UI + Reports budget/actual/variance; export `expenses_budget_vs_actual`), threshold auto-approve, configurable N-level approval matrix (`tenants.expense_approval_matrix` / `PATCH /expenses/settings` levels + role gates per step; legacy `expense_approval_threshold` / `expense_l2_threshold` kept in sync), action log + no self-approve / no duplicate-step actor, approve/reject + journal, recurring create/generate + Celery beat automation, receipt file upload/download/delete via local or S3/MinIO media store, suggest-only receipt OCR (`POST .../ocr-suggest` PDF text + Tesseract images; human `PATCH /expenses/{id}` apply). **Expense org dims (BR-9.2):** `branch_id`/`department_id`/`store_id` on create/PATCH (`clear_store`); Expenses UI pickers; Alembic `20260813_0091`. **Category COA (BR-9.2):** `expense_categories.account_id` → expense GL; `post_expense_journal` debits linked account else `6000`; Expenses category GL picker; Alembic `20260813_0092`. **Approver email (BR-9.3):** pending create / level-advance notify emails current-step matrix roles (`create_notification(..., roles=...)`; default `expense_approval.email=true`; creator excluded). Remaining: none for expense OCR.
- [ ] Double-entry accounting, journals, COA and financial statements complete.
  - Partial: COA defaults (Cash 1000 + Bank 1010 + Cheques Receivable 1020 + Cheques Payable 2015 + Owner's Equity 3000), balanced journals, auto-post from invoice/GRN/purchase invoice (manual)/purchase return/payment/expense approval, trial balance + P&L, cash-flow across liquid accounts, **standard-cost COGS + gross profit (BR-10.6)** (sales invoice / POS / restock return journals append Dr 5000 COGS ↔ Cr 1200 Inventory at `product`/`variant.cost_price` × stock qty; `GET /accounting/profit-loss` returns `revenue`/`cogs`/`gross_profit`/`operating_expenses`/`net_profit`; Accounting P&L card), **P&L date/store/branch filters (BR-10.6 / BR-14.5)** (`from_date`/`to_date`/`store_id`/`branch_id` on `/accounting/profit-loss` + `/reports/profit-loss` + export; journal-line mode for filtered queries; Accounting + Reports P&L UI), **trial balance as-of (BR-10.6 / BR-14.5)** (`as_of` on `/accounting/trial-balance` + `/reports/trial-balance` + export; journal reconstruction; Accounting + Reports UI), **balance sheet as-of + compare (BR-14.5)** (`as_of` reconstructs posted journals through date; `compare=prior_period|prior_year` with prior balances/deltas; `/reports/balance-sheet` + `/accounting/balance-sheet` + export; Reports Balance sheet UI), **cash flow O/I/F (BR-10.6 / BR-14.5)** (`GET /reports/cash-flow` sections operating/investing/financing/transfers by `source_type` + cash_transfer kind; optional `store_id`/`branch_id` attributable filter; Reports Cash flow UI + export), **period close / books lock (BR-10.2)** (`tenants.books_closed_through`; `GET /accounting/period`, `POST .../close|reopen`; post/unpost blocked on/before closed date; Accounting UI), **journal unpost + attachments (BR-10.2)** (`POST /accounting/journal-entries/{id}/unpost` reverses balances for manual JEs within current fiscal period from `tenants.fiscal_year_start`; blocks auto `source_type`; attachment GET/POST/DELETE on `journal_entries.attachment_url`; Accounting → Recent journals UI), **COA opening balances (BR-10.1)** (`POST /accounting/opening-balances` balanced JE `source_type=coa_opening` with equity plug to 3000; `accounts.opening_balance`; `PATCH /accounting/accounts/{id}`; Accounting → Opening balances UI), **cash/bank transfers (BR-10.3)** (`POST /accounting/accounts` for cash/bank tills, `POST /accounting/transfers` transfer|deposit|withdrawal with `cash_transfers` + journal `source_type=cash_transfer`; Accounting → Cash & Bank UI), bank statement import + match/ignore/complete against journal lines (`/accounting/bank-statements`); CSV/OFX-QFX feed upload (`POST .../import`); confidence-scored auto-match + `auto-clear`; multi-line clearing groups (`clear-group`); customer/supplier payments, expenses, and POS settle to Cash vs Bank by `payment_method` (card/transfer→1010, cash→1000, cheque→1020/2015 clearing, POS credit→AR); optional per-payment `liquid_account_id` GL override; cheque lifecycle deposit/clear/bounce/cancel; direct bank API connectors (`bank_account_connections`, providers `mock`|`http_json`, `POST .../bank-connections/{id}/sync`, Celery `sync_bank_feeds`, external_ref dedupe into recon statements). Remaining: native Open Banking/Plaid vendor adapters (http_json covers aggregator feeds).
- [x] Credit limits, balances, payments and aging complete.
  - Complete: AR/AP aging buckets (AP prefers purchase invoices, falls back to uninvoiced POs), statements, auto-allocate payments oldest-first, supplier payments + journals, credit-limit warnings, **credit-limit override (BR-11.1)** (`override_credit_limit` + optional `override_reason` on sales invoice post / POS checkout / legacy `/sales`; requires `credit:approve` for store_manager·accountant·admin; 409 `CREDIT_LIMIT_EXCEEDED` / 403 `CREDIT_OVERRIDE_FORBIDDEN`; audit `credit_limit_override`; Sales + POS confirm UX), **per-party payment terms (BR-6.1 / BR-7.1)** (`parties.payment_terms_days` Net N; sales invoice post / PO send / purchase invoice create use party terms for `due_date`; create/PATCH customers & suppliers; credit-limit PATCH optional terms; Alembic `20260812_0078`), **customer/supplier credit info (BR-7.1 / BR-6.1)** (`GET /customers/{id}/credit`, `GET /suppliers/{id}/credit` + Credit UI Balance) + **customer/supplier history (BR-7.1 / BR-6.1)** (`GET /customers/{id}/history`, `GET /suppliers/{id}/history` purchases/returns/payments + Credit UI History) + **AP payment schedule + due notifications (BR-10.5 / BR-11.2)** (`GET /suppliers/{id}/payment-schedule` sorted open bills + uninvoiced POs with days_until_due / early-discount quote; `scan_payment_due` covers purchase invoices via `refresh_overdue_purchase_invoices` + `payment_due` notifications `entity_type=purchase_invoice`; Credit → Payables schedule UI; Celery beat unchanged), early-payment discounts for AR give and AP take (`early_pay_discount_pct`/`days`, quote APIs, settle with Dr/Cr Sales Discounts 4100 or Purchase Discounts Taken 4200), multi-currency credit (tenant base + `exchange_rates`, document `currency`/`exchange_rate` on sales/purchase invoices and payments, GL + party balances in base, FX gain/loss 4300 on settle rate variance), live FX feed / auto-update (`POST /credit/exchange-rates/refresh`, `FX_PROVIDER` open_er_api|frankfurter, Celery `refresh_fx_rates`, tenant `fx_auto_refresh`).
- [ ] VAT/tax calculation and reporting complete.
  - Partial: tax types/modes, default rate, auto-calc on sales invoices + POS, **purchase tax auto-resolve (BR-12.2)** (PO create/amend + manual PI omit `tax_rate` → `resolve_product_tax` product → category → default; explicit 0 wins; Purchasing UI omits blank rates), product tax override/exempt, **category-specific tax (BR-12.1 / BR-2.8)** (`product_categories.tax_rate_id`; resolve order product → category walk parents → tenant default; Inventory → Catalog category tax picker; Alembic `20260812_0077`), compound rate components (`basis=net|compound`), **sales invoice line tax breakdown (BR-12.2)** (`sales_invoice_items.line_tax` + `tax_components` + `tax_breakdown` on serialize; Sales UI + print; Alembic `20260813_0093`), **purchase invoice line tax breakdown (BR-12.2)** (`purchase_invoice_items.line_subtotal`/`line_tax`/`tax_components` + `tax_breakdown` on serialize; Purchasing UI; Alembic `20260813_0094`), sales reverse charge (memo tax excluded from customer total + filing box 2a), purchase reverse charge (`is_reverse_charge` → AP=net, self-assess Dr 1300 / Cr 2100 on approve, filing input+output), output/input tax report, jurisdiction-neutral filing pack + Ghana GRA VAT return template (`tax_jurisdiction`/`tax_registration_number`, `GET /reports/tax/filing`, export `tax_filing_gh`). Supply class splits: products `tax_supply_class` (standard|zero_rated|exempt) snapshotted on sales invoice/POS lines; filing boxes 1/1a/1b + Ghana GH3/GH4. Remaining: other jurisdictions, GRA e-file portal submission.
- [ ] Multi-store/warehouse inventory and transfer workflow complete.
  - Partial: stores with linked warehouses, **branch management (BR-2.2)** (`GET|POST|PATCH /branches` with code/manager/address/phone/email + soft `is_active`; Multi-Store Branches UI) + **department setup (BR-2.5)** (`GET|POST|PATCH /departments` with `head_user_id` / optional `branch_id` + soft `is_active`; Multi-Store Departments UI; reporting filters via `GET /reports/sales/by-department` + `department_id` on salesperson/by-store) + **store manager + branch + operating hours (BR-2.3)** (`manager_id` / `branch_id` / `operating_hours` on create/`PATCH /stores/{id}`; Multi-Store UI; Alembic `20260813_0084` for hours) + **warehouse setup (BR-2.4)** (`warehouse_type` retail|bulk|cold_storage|other, `manager_id`, `address`, `capacity`; `GET|POST|PATCH /warehouses`; Stores UI; Alembic `20260813_0082`), warehouse stock balances, transfer draft→requested→(source mgr approve→dest mgr approve)→in_transit→received/cancelled with stock movements + dual-manager approval gate before ship; store sales reporting (`GET /reports/sales/by-store`); invoice post + POS sale deduct store warehouse stock when `store_id` / session store is set (unlocated stock auto-allocated); per-store warehouse reorder_level/reorder_qty + low-stock report/notifications; one-click draft PRs from low-stock suggestions (`GET /purchasing/suggestions/low-stock`, `POST /purchasing/requests/from-low-stock`) with Reports → Inventory UI; optional `fefo_strict_warehouse` (FEFO only from warehouse-tagged batches). **Inter-store transfer history (BR-13.2)** (`GET /reports/inventory/transfers` + export `inventory_transfers`). Remaining: advanced multi-bin locations.
- [ ] Reports and exports complete.
  - Partial: sales/inventory/purchase/expense/cash-flow reports, **pending POs (BR-14.3)** + **purchase returns summary** (`GET /reports/purchases/returns`; export `purchases_returns`) (`GET /reports/purchases/pending-orders`; export `purchases_pending_orders`) + **purchase location filters (BR-14.3 / BR-14.5)** (`warehouse_id`/`store_id` on summary + suppliers + pending-orders + returns + export; Reports Purchases pickers) + **sales returns summary (BR-14.1)** (`GET /reports/sales/returns`; optional `store_id` via invoice store; export `sales_returns`) + **daily/monthly sales store (BR-14.1 / BR-14.5)** (`store_id` on `/reports/sales/daily` + `/reports/sales/monthly` + returns; Reports Sales tab) + **product sales filters (BR-14.1)** (`store_id`/`category_id` on `/reports/sales/products` + export) + **customer sales store (BR-14.1)** (`store_id` on `/reports/sales/customers`) + **salesperson store (BR-14.1 / BR-14.5)** (`store_id` on `/reports/sales/salesperson` + export; Reports Salespeople picker) + **expense org filters (BR-14.4 / BR-14.5)** (`branch_id`/`department_id`/`store_id` on `/reports/expenses/summary` + `budget-vs-actual` + export; Reports Expenses store picker), salesperson + store performance, balance sheet, **expiry report (BR-14.2)** (`GET /reports/inventory/expiry`; export `inventory_expiry`; Reports Inventory) + **standard-cost stock valuation (BR-14.2 / BR-5.4)** (`GET /reports/inventory/valuation?method=standard` qty × `product.cost_price`; optional `warehouse_id`; FIFO/LIFO/average → 400; Reports Inventory UI + export `inventory_valuation`), unified CSV/PDF/Excel (`xlsx`) export including tax filing pack + Ghana VAT return (`tax_filing_gh`); scheduled report email (`report_schedules` + Celery `run_due_report_emails`). Remaining: FIFO/LIFO/weighted-average costing; additional jurisdiction filing templates.
- [ ] In-app notifications and channel preferences complete.
  - Partial: categorized alerts, unread count/bell, mark read/all, preferences, low-stock (BR-5.5 role emails to inventory_officer/store_manager; default email on) + payment-due scan + **quotation expiry (BR-7.2)** (`quotation_expiry` T−1 day via Celery `scan_quotation_expiry` + `POST /notifications/scan-due`) + **new order alerts (BR-15.1)** (`new_order` on sales order create/confirm), email channel (SMTP), SMS channel (Twilio with console fallback) + profile phone + `/settings/sms`. Remaining: none for auth-linked MFA channels (WebAuthn lives under Identity).

### Reliability & operations
- [x] Automated backup and tested restore complete (logical MVP).
  - Complete (MVP logical): encrypted tenant logical backups (`.ribbak`), SHA-256 integrity, download, dry-run + guarded restore, retention prune, schedule settings + `/backup/run-due` + Celery beat due runner, admin UI; runbook `docs/DR_LOGICAL_BACKUP_RUNBOOK.md`. Offsite mirror script `ops/backup/sync-ribbak-offsite.sh.example` (Stage 26 W1). Remaining post-MVP: automatic in-app `.ribbak` S3 upload wiring, audit cold archives in logical pack optional.
- [x] Point-in-time recovery/WAL strategy complete.
  - Complete (MVP) (Stage 26 W1 + Stage 28 R1 pack): WAL archiving + S3-compatible offsite strategy documented (`docs/DR_WAL_PITR_RUNBOOK.md`); versioned configs `ops/postgres/` (`postgresql-wal-archive.conf.example`, `archive-wal-to-s3.sh.example`) + `.ribbak` offsite sync `ops/backup/sync-ribbak-offsite.sh.example` + compose sketch `ops/backup/docker-compose.wal-drill.example.yml`; evidence `/opt/cursor/artifacts/dr/stage26_w1_wal_pitr_strategy.json` (`test_wal_pitr_w1.py`). Operator PITR drill pack Complete (MVP packaging) — `docs/PITR_DRILL_PACK_MVP.md`, `ops/postgres/pitr-drill-checklist.json`, evidence `/opt/cursor/artifacts/dr/stage28_r1_pitr_drill_pack.json` (`test_pitr_drill_pack_r1.py`); packaging only (`operator_pitr_drill_executed: false`). Remaining post-MVP: operator staging PITR drill **execution** (base + WAL replay).
- [x] Immutable audit logging for sensitive operations complete.
  - Complete (MVP Stage-1 / BR-17.1–17.2): append-only APIs, hash-chained integrity verify, filtered query + CSV export, login/logout/user-create audited; HTTP mutation middleware auto-logs successful POST/PUT/PATCH/DELETE under `/api/v1` as `http_write` (`AUDIT_HTTP_MIDDLEWARE_ENABLED`, skip `/audit-logs`); 7-year retention policy with checksummed JSONL cold archive (`POST /audit-logs/archive-cold`, `GET /audit-logs/archives`, Celery `archive_cold_audit_logs`; rows marked `archived_at`, never purged). Remaining post-MVP: hot-table physical prune after retention window.
- [x] Redis/Celery/RabbitMQ used for intended production workloads.
  - Complete (MVP packaging, Celery reliability R1): Redis for distributed API/auth rate limiting; Celery worker + beat on RabbitMQ broker (Redis results) for the 11 non-AI intended jobs (low-stock, payment-due, quotation-expiry, recurring expenses, due backups, trial lifecycle, report emails, FX refresh, bank feed sync, webhook retries, audit cold archive) plus rule-based `scan_ai_security_alerts` (BR-21.10); admin `GET /jobs` beat map parity + `POST /jobs/{name}/run`; deep health `GET /health?deep=true` + `/health/ready` (DB/Redis/broker); runbook `docs/CELERY_RELIABILITY_RUNBOOK.md` + `ops/celery/` checklist; evidence `/opt/cursor/artifacts/ops/celery_reliability_r1.json` (`test_celery_reliability_r1.py`). Honesty: `live_broker_soak_executed: false`, `ci_queue_drained_claimed: false`. Remaining: AI nightly / LLM forecast jobs (AI functions gate); live staging soak + alerting (Monitoring).
- [x] Monitoring, metrics, logging and alerting complete.
  - Complete (MVP) (Stage 5 H5 + Stage 18 L1 + Stage 26 M1 + Stage 28 A1): deep `/api/v1/health?deep=true` + `/health/ready` (database / Redis / Celery broker; 503 on hard failure); Prometheus-text `/api/v1/metrics` (`METRICS_ENABLED`) with request counters via `MetricsMiddleware`; structured JSON request/error logs (`request_id`, `tenant_id`, `user_id`, `status`, `latency_ms`, safe `error_code`; `X-Request-ID`) via `RequestLoggingMiddleware` (`test_request_logging_l1.py`). Stage 26 M1: versioned Prometheus scrape `ops/prometheus/prometheus.yml`, alert rules `ops/prometheus/alerts/ribdigi.yml` (`RibdigiDown` / `RibdigiHighErrorRate` / `RibdigiHighLatency` / `RibdigiNotReady` / optional RabbitMQ depth), Fluent Bit example `ops/logging/fluent-bit-ribdigi.conf.example`, doc `docs/OPS_MONITORING_MVP.md` (`test_ops_monitoring_m1.py`). Stage 28 A1 Grafana/Alertmanager operator pack — `docs/GRAFANA_PACK_MVP.md`, `ops/grafana/`, evidence `/opt/cursor/artifacts/monitoring/stage28_a1_grafana_pack.json` (`test_grafana_pack_a1.py`); packaging only (`hosted_grafana_claimed: false`, `pagerduty_wired: false`, `siem_claimed: false`). Remaining post-MVP: hosted Grafana/Alertmanager/PagerDuty **as SaaS**, live on-call rota / incident drills, centralized SIEM deployment.
- [x] Kubernetes production deployment reviewed.
  - Complete (MVP) (Stage 26 K1): Helm chart `helm/ribdigi/` (backend/frontend/Celery/migration + staging/production values) with liveness `/api/v1/health` and readiness `/api/v1/health/ready` probes, resource limits, `ribdigi-secrets` refs; hardened flat `k8s/` manifests; operator install/smoke `ops/k8s/`; secret template `.env.production.example`; doc `docs/K8S_DEPLOY_MVP.md`; evidence `/opt/cursor/artifacts/k8s/stage26_k1_deploy_fidelity.json` (`test_k8s_deploy_k1.py`). Main `ci.yml` stays deploy-free. Honesty: `live_staging_apply_claimed: false`, `gha_staging_deploy_deferred: true`. Remaining post-MVP: live staging/production cluster apply, TLS cutover, GHA deploy wiring.
- [x] Load/performance tests meet documented targets.
  - Complete (MVP) (Stage 5 L1 + Stage 26 C1 + Stage 28 C1 pack): baseline scripts + targets in `docs/LOAD_TEST_BASELINE.md`; httpx harness (`backend/loadtest/`, `--smoke` + `--ci-capacity`); ASGI smoke + CI capacity profile (health/login/products/dashboard, 0% errors, ASGI-honest p95 gate); evidence `/opt/cursor/artifacts/loadtest/stage26_c1_capacity_evidence.json` (`test_load_capacity_c1.py`); MVP doc `docs/LOAD_CAPACITY_MVP.md`; optional Locust `backend/loadtest/locustfile.py`. Stage 28 C1 operator ~1000-VU cert pack — `docs/LOAD_CERT_PACK_MVP.md`, `ops/loadtest/`, evidence `/opt/cursor/artifacts/loadtest/stage28_c1_load_cert_pack.json` (`test_load_cert_pack_c1.py`); packaging only (`operator_1000vu_executed: false`, `ci_1000vu_certificate_claimed: false`). Remaining post-MVP: operator staging ~1000 VU **execution** / p95 < 500 ms on sized infra with rate limits tuned.
- [x] Disaster recovery drill passes.
  - Complete (MVP logical): Stage 23 B1 automated quarterly drill evidence — create → corrupt → dry-run → guarded `confirm_text=RESTORE` apply → verify proof + audits; foreign-tenant backup restore/verify/download → 404; durable artifact `/opt/cursor/artifacts/dr/stage23_b1_logical_drill.json` (`test_logical_dr_drill_b1.py`; runbook `docs/DR_LOGICAL_BACKUP_RUNBOOK.md`). `POST /backup/{id}/verify` proves field match vs live data. Stage 26 W1 WAL/PITR strategy packaging Complete (MVP) — `docs/DR_WAL_PITR_RUNBOOK.md` (`test_wal_pitr_w1.py`). Stage 28 R1 operator PITR drill pack Complete (MVP packaging) — `docs/PITR_DRILL_PACK_MVP.md` (`test_pitr_drill_pack_r1.py`); not live CI replay. Honesty: `wal_pitr_deferred: true`, `operator_pitr_drill_executed: false`. Remaining post-MVP: operator staging infrastructure PITR drill **execution** (base + WAL replay).

### AI
- [x] AI provider configured securely.
  - Complete (MVP packaging): optional `AI_ENABLED` / `AI_PROVIDER` / `AI_API_KEY` in settings; production rejects weak keys and `mock`; chat fail-closed (503) until configured; `GET /ai/status` never leaks secrets. Live OpenAI HTTP client still Incomplete (`provider_pending` when key present). See `docs/AI_SECURITY_MVP.md`.
- [x] Tenant-safe data access enforced.
  - Complete (MVP packaging): insights built from tenant-scoped dashboard only; `ai_queries` rows and `/ai/queries` list are tenant-filtered; cross-tenant previews must not leak. Full RAG/LLM context isolation remains Incomplete until provider chat lands.
- [ ] AI functions use real tenant data and satisfy documented acceptance criteria.
  - Partial: insights, Security Monitor, inventory predictions, sales/expense analysis, report generator, customer assistant, **document assistant** (`POST /ai/documents/analyze`) — see `docs/AI_DOCUMENT_MVP.md`. Chat / free-form LLM NL remain Incomplete.
- [x] AI audit logging and prompt/data protections complete.
  - Complete (MVP packaging): `ai_queries` table + `audit_logs` module=`ai`; prompt SHA-256 + redacted preview (no raw secrets); max message length; injection deny-list → 400. **AI Security Monitor (BR-21.10)** Complete (MVP rule-based): `GET /ai/security/alerts`, `POST /ai/security/scan`, Celery `scan_ai_security_alerts`, in-app `security` notifications above threshold — see `docs/AI_SECURITY_MVP.md`. Output PII scanner / Isolation-Forest ML remain post-MVP.

## Current repository rule

Until every applicable item above is verified, Cursor and developers must describe unfinished items as `PARTIAL`, `MISSING`, or `BLOCKED` — never as complete or production-ready.
