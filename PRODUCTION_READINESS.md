# RIBDIGI ERP — Production Readiness Gate

RIBDIGI is intended to be a commercial ERP, not a demo application. A feature is **production-ready only when implementation, automated tests, security controls, and operational requirements are verified**.

## Non-negotiable release rules

- No demo tenant, sample company, fake transaction, pre-filled login, or known default password in production.
- No endpoint may return success for work that was not actually completed.
- No hard-coded secrets or development credentials in production.
- No protected tenant data endpoint without authentication, tenant isolation and authorization.
- No database schema change outside versioned migrations.
- No financial workflow is complete until stock, ledger, tax, credit and audit side effects are transactionally correct where applicable.
- No production claim until the Phase 5 launch gates in `docs/DEVELOPMENT_ROADMAP.md` are passed.

## Required launch gates

### Platform & tenancy
- [x] Schema-per-tenant strategy implemented as specified, or architecture documents formally revised to an approved equivalent.
  - Approved equivalent: shared-schema + `tenant_id` (see `docs/ADR_001_TENANCY.md`). Schema-per-tenant remains post-MVP.
- [x] Cross-tenant isolation integration tests pass for every tenant-owned resource.
  - Complete (shared-schema MVP): SQLite suite covers product list scoping, foreign sales invoice 404, mismatched `X-Tenant-ID` (products, credit aging, users), suspend/activate login block, plus matrix coverage for customers/suppliers, credit statement/limit, product image, expenses (+ attachment + OCR-suggest + OCR-apply + foreign `store_id`/`liquid_account_id` 404), purchase invoices (+ attachment + OCR-suggest + OCR-apply), tax rates, store inventory, POS sessions/drawer/receipt (+ receipt-send), notifications, backups (+ download), accounting accounts/journals, audit logs, bank connections (+ create with foreign `account_id` 404), bank-statement create/import foreign `account_id` 404, stock-in foreign `warehouse_id` 404 (tenant-scoped warehouse guard on stock engine); Stage 18 S1: API keys/webhooks/OCR-apply/stock-counts/warehouse-transfers/quotations/orders/product warehouse-stock + mismatched header on keys/webhooks (`test_isolation_matrix_s1.py`), customer/supplier payment foreign `liquid_account_id` 404 (eager settlement GL resolve), store create foreign `manager_id` 404, report schedules, cheques, stock transfers, PO print, product labels, media-key tenant mismatch (unit + planted wrong key on owned expense/logo HTTP 403), tenant-scoped AI insights (chat remains 503 until provider), and Stage 1 B6 paths: users CSV import (header mismatch + tenant-scoped commit + foreign branch_code), `GET/PATCH /settings/email` (header mismatch + no cross-tenant SMTP mutate), warehouse PATCH (foreign id/manager + header mismatch). Stage 21 I1: BR-1.4–1.5 isolation + seed fidelity (`test_tenant_isolation_seeds_i1.py`). Stage 21 D1: `docs/STAGE_21_FIDELITY.md` (`test_stage21_fidelity_d1.py`). Stage 23 I1: finance/report residual matrix — liquid accounts/transfers, expense categories/recurring, foreign branch + BS/P&L/cash-flow `store_id`/`branch_id`, mismatched header on financial reports/liquid/budgets (`test_isolation_matrix_i1.py`). Stage 23 G1: commercial MVP gate closure marks shared-schema isolation Complete (`test_mvp_gate_closure_g1.py`). Remaining post-MVP: schema-per-tenant (ADR-001).
- [x] Tenant provisioning, suspension, activation and lifecycle management complete.
  - Complete (MVP): register + defaults, company profile GET/PATCH, suspend (revokes sessions) / activate, super_admin cross-tenant lifecycle, trial `trial_ends_at` (default 14d) + 7/3/1 reminders + grace read-only (`status=grace`) then auto-suspend, company logo upload/serve/delete via `STORAGE_BACKEND=local|s3` (MinIO/S3-compatible object store; keys remain tenant-scoped). Invoice/receipt/quotation/credit-note prints embed logo (HTML data-URI) and prefer `legal_name` on documents (BR-20.1). Document numbering prefixes/series; invoice print template (`a4`/`thermal_*`); POS receipt print template (`thermal_80`/`thermal_58` default paper); optional `document_header`/`document_footer` on prints (BR-20.4). `plan_code` is commercial metadata only with `billing_deferred` (ADR-002); no fake payment success on upgrade. Stage 21 closed (ADR-048): Tenant Lifecycle, Org & Dashboard Fidelity — `docs/STAGE_21_PLAN.md`, exit `docs/STAGE_21_EXIT_CRITERIA.md`. Stage 21 T1: BR-1.1–1.3 lifecycle fidelity (`test_tenant_lifecycle_t1.py`). Stage 21 I1: BR-1.4–1.5 isolation + seeds (`test_tenant_isolation_seeds_i1.py`). Stage 21 O1–N1: org/users/dashboard/notifications fidelity proofs. Stage 21 D1/H21x: BR-1–4 / readiness / USER_MANUAL / API / launch §§1–2 sync + freeze (`docs/STAGE_21_FIDELITY.md`, `test_stage21_fidelity_d1.py`, `test_stage21_exit_h21x.py`, ADR-048). Stage 22 closed (ADR-050). Stage 23 G1: MVP lifecycle gate Complete (`test_mvp_gate_closure_g1.py`). Remaining post-MVP: paid billing provider (ADR-002); richer WYSIWYG template designer.

### Identity & security
- [x] Login/logout/refresh token flow complete.
  - Stage 19 K1: live JWT login/refresh rotation + API-key regression + tenant-scoped rate-limit headers (`test_auth_api_fidelity_k1.py`; BR-18.1).
  - Stage 19 P1: products/catalog CRUD + import + stock/barcode lookup and customers/groups/balance/history via JWT + X-API-Key reads (`test_products_customers_api_p1.py`; BR-18.2–18.3).
  - Stage 19 S1: sales quote/order/invoice/payment/return/POS and purchasing PR/PO/GRN/PI/supplier payment via JWT + X-API-Key reads (`test_sales_purchases_api_s1.py`; BR-18.4–18.5).
  - Stage 19 A1: API standards — `env()` envelope, `/api/v1`, list `limit`, OpenAPI (non-prod), webhooks (`test_api_standards_a1.py`; BR-18.6).
  - Stage 19 U1: BR-19 auth/2FA/sessions fidelity (`test_auth_session_br19_u1.py`).
  - Stage 19 C1: BR-20 company/settings fidelity — legal/logo/formats/SMTP/numbering/print templates (`test_company_settings_br20_c1.py`; WYSIWYG designer deferred).
  - Stage 19 R1: LAUNCH §5 reliability — Redis cache soft-fail, permissions invalidation, Celery beat matrix, admin jobs dry-run, logical DR runbook (`test_reliability_cache_r1.py`; `docs/DR_LOGICAL_BACKUP_RUNBOOK.md`).
  - Stage 19 D1: BR-18–20 / SECURITY_GUIDE / API docs / readiness / launch §§1–5 fidelity (`docs/STAGE_19_FIDELITY.md`, `test_stage19_fidelity_d1.py`).
  - Stage 19 H19x: exit met — `docs/STAGE_19_EXIT_CRITERIA.md`, freeze `docs/ADR_044_STAGE19_FREEZE.md` (`test_stage19_exit_h19x.py`).
- [x] Email verification and password reset complete.
  - Tokens issued; SMTP delivery with console fallback in dev; production requires `SMTP_HOST` + `SMTP_FROM_EMAIL` when `EMAIL_ENABLED=true`; tenants may override SMTP via encrypted `PATCH /settings/email`. Login blocked until email verified (`EMAIL_NOT_VERIFIED` + `POST /auth/resend-verification`). Authenticated `POST /auth/change-password` revokes other sessions.
- [x] 2FA/TOTP complete for required roles.
  - Complete: TOTP setup/QR/confirm, backup codes, encrypted secrets, company_admin/super_admin enrollment gate; WebAuthn/passkeys (register/list/delete + login challenge with `methods`); MFA satisfied by TOTP and/or passkeys.
- [x] Session listing and revocation complete.
  - Complete: list/revoke sessions; Shell idle timer calls `POST /auth/idle-logout` to revoke the current session JWT server-side (audit `idle_logout`) before clearing client tokens (BR-19.3 inactivity auto-logout).
- [x] Granular module/menu/record RBAC complete.
  - Complete for Stage 1 Identity/RBAC: module+action RBAC; system + custom roles; user lifecycle including bulk CSV import (`GET/POST /users/import[/template]`); soft-delete only via `DELETE /users/{id}` → `is_active=false` + session revoke (ADR-003; BR-3.1 hard-delete/archival deferred post-MVP); Shell menu visibility = module read/write (ADR-004); user store membership deferred in favor of branch/department + `stores.manager_id` (ADR-005); org units (branches/departments with manager/phone/email + department head); stores PATCH + operating hours; warehouses typed attrs + PATCH; tenant plan/legal/multi-address/contact/inactivity; regional date/number/time formats applied on dashboard/audit/company dates; invoice + receipt print templates with document header/footer; tenant SMTP override CRUD (`GET/PATCH /settings/email`, encrypted password); record scopes; executive dashboard MoM/DoD (`yesterday_revenue`/`dod_change_pct`) + OOS/expiring. Audit CSV+PDF export. English-only UI with i18n scaffold + ADR-006 (BR-2.7 multi-language packs post-MVP); executive dashboard revenue charts (30d/12m), KPI click-through, notifications panel (BR-4.1–4.4); audit HTTP middleware + 7-year cold archive (ADR-007). Stage 1 foundation exit criteria met (`docs/STAGE_1_EXIT_CRITERIA.md`); Stage 1 freeze ADR-008; Stage 2 track opened ADR-009 / `docs/STAGE_2_PLAN.md`. BR-7.3 sales-order soft allocation + logistics delivered under Sales gate. Stage 21 U1/V1/N1/D1: `test_users_roles_u1.py`, `test_dashboard_kpis_v1.py`, `test_dashboard_notifications_n1.py`, `docs/STAGE_21_FIDELITY.md` (`test_stage21_fidelity_d1.py`).
- [x] Rate limiting, security headers and production CORS complete.
  - Complete for Stage 5 S1: Redis sliding-window rate limits (`RATE_LIMIT_BACKEND=auto|redis|memory`) with memory fallback, auth vs API caps, `429 RATE_LIMIT_EXCEEDED` + `Retry-After` / Remaining/Backend headers exposed via CORS, security headers including CSP (`default-src 'none'`), COOP, production HSTS + `Cache-Control: no-store`, production CORS whitelist (no `*`), OpenAPI disabled in production, `/api/v1/health` security posture; production validators reject weak JWT/DEBUG/empty CORS/disabled limits; recommend `RATE_LIMIT_REQUIRE_REDIS=true` for multi-instance. Stage 19 K1: rate-limit key includes `X-Tenant-ID` scope + refresh on auth bucket (`test_auth_api_fidelity_k1.py`). Stage 18 C1: CI runs security/isolation marker pytest + full suite + frontend build (no K8s deploy); `.env.production.example` + `docker-compose.prod.yml` align Redis-required rate limit / CORS / secrets posture (`test_ci_prod_config_c1.py`). Celery/RabbitMQ scheduled workloads remain under Reliability.
- [x] OWASP/security tests completed.
  - Complete (MVP) (Stage 5 O1 + Stage 18 T1 + Stage 27 S1): smoke (`test_owasp_smoke.py`) plus beyond-smoke (`test_owasp_suite_o1.py`, `test_owasp_suite_t1.py`) covering A01 access control/IDOR/privilege escalation, A02 secret leakage + tampered JWT, A03 injection/XSS-as-JSON, A05 safe 404 errors, A07 missing/garbage/expired tokens (alongside lockout and RBAC deny). Stage 27 S1 durable baseline evidence `/opt/cursor/artifacts/security/stage27_s1_security_scan.json` (`test_security_scan_s1.py`); doc `docs/SECURITY_SCAN_MVP.md`; CI via `pytest -m "security or isolation"` (main `ci.yml` deploy-free). Stage 29 V1 vendor pen-test / ZAP staging pack Complete (MVP) — `docs/PENTEST_PACK_MVP.md`, `ops/security/pentest-engagement-checklist.json`, evidence `/opt/cursor/artifacts/security/stage29_v1_pentest_pack.json` (`test_pentest_pack_v1.py`); packaging only (`vendor_pen_test_purchased: false`, `live_zap_executed: false`). Remaining post-MVP: vendor / third-party penetration test **purchase/execution**; live ZAP-in-CI against authenticated staging (operator template `ops/security/zap-baseline.example.yml` — not wired into main CI).

### ERP operations
- [x] Inventory catalog, variants, batches/expiry, stock movements and adjustments complete.
  - Complete (MVP): product GET/PATCH + create with category/brand/unit FKs + image upload + opening `stock_qty` on create UI, hierarchical categories (`parent_id`, tree API + cycle-safe parent PATCH, indented Catalog UI), brands, units of measure (+ tenant seed defaults) with PATCH/soft-delete, variants (SKU/size/color/flavor) with PATCH/soft-delete, multi-image gallery (`product_images`, max 5, primary synced to `products.image_url`; legacy `/image` kept), barcode validation (EAN-8/13, UPC-A, Code 128) + generate endpoints for products/variants (tenant-unique), printable barcode labels (`GET /products/{id}/labels`, `POST /inventory/labels` html/png/pdf with name/SKU/price + barcode graphic; Catalog print/download UI), CSV product import (`GET /products/import/template`, `POST /products/import` dry-run/commit with opening stock), dedicated stock-only CSV import (`GET/POST /inventory/stock/import[/template]`, match sku/barcode, `adjust`/`set` modes, optional `warehouse_code`, dry-run + sequential deltas + stock movements), batches with expiry + FEFO stock-out, stock in/out/adjust (+ optional `warehouse_id` on adjust; Inventory Stock ops UI) + immutable `stock_movements` (variant/batch refs; movements filterable by product/warehouse/type/`from_date`/`to_date`; Inventory Movements tab), inter-warehouse transfers (`GET/POST /inventory/stock-transfers` + ship/receive/cancel; store FKs nullable; Inventory Transfers tab), product warehouse stock grid (`GET /products/{id}/warehouse-stock`; Inventory Stock tab), low-stock list with `suggested_order_qty` + `POST /inventory/low-stock/reorder-po` draft PO (purchasing write; Inventory Low stock tab), physical stock counts (`stock_counts` / `stock_count_items`, draft→count→complete posts warehouse variances), expiring-batches report/UI, sales/POS lines accept `variant_id`, POS search resolves scanned barcodes, browser camera scanner UX (`BarcodeCameraScanner` via Barcode Detection API + getUserMedia on POS and stock counts; USB/Bluetooth keyboard-wedge Enter-to-add), inventory barcode lookup (`GET /inventory/products/lookup`). Stage 2 I1–I6 complete + exit freeze ADR-010: opening stock; ops scan + reasons; minimum_stock traffic lights; count variance export; movement audit integrity; UoM conversion, brand logo, product weight/dimensions. Optional P2 PO Kanban polish parked (P1 return multi-line UI delivered in Stage 8). Stage 17 open (ADR-039): `docs/STAGE_17_PLAN.md`. Stage 17 C1: catalog fidelity proof (`test_catalog_fidelity_c1.py`; BR-5.1 sync). Stage 17 S1: stock-in → warehouse qty + movements, adjust reasons, opening stock (`test_stock_ops_chain_s1.py`; BR-5.2 Stock In/Adjustment/Opening sync). Stage 17 S2: stock count → complete posts adjustments + variance report (`test_stock_count_chain_s2.py`; BR-5.2 Stock Count sync). Stage 17 W1: warehouse-stock grid + inter-warehouse ship/receive (`test_warehouse_transfer_chain_w1.py`; BR-5.2 Stock Transfer + BR-5.4 view/transfer sync). Stage 17 L1: low-stock traffic lights + `suggested_order_qty` + draft reorder-PO (`test_low_stock_reorder_l1.py`; BR-5.4 warehouse reorder + BR-5.5 sync). Stage 17 A1: product create/update/deactivate + stock_* domain audits with before/after (`test_inventory_audit_a1.py`; BR-17.1 Product Changes). Stage 17 D1: BR-5.1–5.5 / API / readiness / USER_MANUAL fidelity (`docs/STAGE_17_FIDELITY.md`, `test_stage17_fidelity_d1.py`). Stage 17 exit (H17x): `docs/STAGE_17_EXIT_CRITERIA.md`, ADR-040 (`test_stage17_exit_h17x.py`). Stage 18 COMPLETE (ADR-042): `docs/STAGE_18_EXIT_CRITERIA.md`, `docs/STAGE_18_FIDELITY.md`. Stage 19 open (ADR-043): `docs/STAGE_19_PLAN.md` (API / Settings / Operator Reliability Fidelity). Stage 18 A1: BR-17 login/logout/user/purchases/financial + filter/export/verify/retention (`test_security_hardening_a1.py`). Stage 24 G1: Inventory gate Complete for MVP (`test_commerce_gate_closure_g1.py`). Stage 24 D1: `docs/STAGE_24_FIDELITY.md` (`test_stage24_fidelity_d1.py`). Stage 24 H24x / ADR-054 freeze recorded (`docs/STAGE_24_EXIT_CRITERIA.md`, `test_stage24_exit_h24x.py`). Remaining post-MVP: advanced multi-bin; Stage 2 P2 PO Kanban optional polish.
- [x] Purchasing/PO/GRN/supplier workflow complete.
  - Complete (MVP): purchase requests (`purchase_requests` / items; draft→pending→approved/rejected/cancelled→converted; no self-approve; convert→draft PO; Requests UI tab), multi-level PR approval matrix (`tenants.purchase_request_approval_matrix`, `purchase_request_approval_actions`, amount steps + role gates, no duplicate-step actor; default Store Manager → Company Admin; `GET/PATCH /purchasing/settings`), supplier profile (code/type/category/status/address/notes/`payment_terms_days`, optional `early_pay_discount_pct`/`days` override with tenant fallback, contacts CRUD, deactivate, purchase history; Suppliers UI tab), PO draft→sent→partial/full received (+ cancel) with `delivery_address` + line `tax_rate`/`discount` (tax on net after discount; create/amend/print/email; Purchasing UI), optional PO email on send (SMTP/console + `sent_at`/`emailed_to`) and print text (`GET .../orders/{id}/print`), PO amendment tracking (`purchase_orders.revision`, `purchase_order_amendments` before/after snapshots; draft `PATCH` without history; sent/partial `POST .../amend` requires reason; qty cannot fall below received), GRN posts stock + supplier balance with per-line accepted/rejected + reason, batch/expiry on GRN lines (`goods_receipt_items.batch_*`, stock into `product_batches`, required when product `tracks_batches`), partial-receive UI, purchase invoices (from GRN or manual) draft→approve with unpaid/partial/paid/overdue + payment allocation, purchase returns draft→post with stock out + AP/debit note journal + configurable `purchase_return`/`purchase_debit_note` numbering + printable debit note (`GET /purchasing/returns/{id}/print`; Returns UI), supplier invoice file upload/download/delete on invoices, supplier-invoice OCR suggest (`POST .../ocr-suggest`) + Stage 10 A1 human-confirmed apply on draft (`POST .../ocr-apply` with `confirm: true`; Purchasing UI). Stage 8 P1: purchase return multi-line UI (qty per GRN line). Stage 11 C1: GRN valuation uses PO discount/tax math; PI-from-GRN proportional discount; AP aging uninvoiced = received value; E2E `test_purchasing_chain_c1.py`. Stage 11 C2: GRN-linked reverse-charge PI posts Dr 1300/Cr 2100 only (`test_grn_linked_rc_c2.py`). Stage 11 COMPLETE (ADR-028): C1 chain E2E + valuation; C2 GRN-linked RC; A1 purchasing audit; D1 fidelity; H11x exit (`docs/STAGE_11_EXIT_CRITERIA.md`). Stage 24 G1: Purchasing gate Complete for MVP (`test_commerce_gate_closure_g1.py`). Remaining post-MVP: Stage 2 P2 PO Kanban optional polish.
- [x] Sales/invoice/payment/customer workflow complete.
  - Complete (MVP): customer profile (code, `party_type` walk-in|registered, status active/inactive, address/notes/GPS `latitude`/`longitude`, `payment_terms_days`, contacts CRUD, deactivate, sales history; `GET/PATCH/DELETE /customers/{id}`, `GET .../history`, list `active_only`; customer groups CRUD `/customers/groups` with `discount_percent` + tenant defaults Retail/Wholesale/VIP; assign via `customer_group_id` or name; group pricing when catalog unit price is used on quotes/orders/invoices/POS; Sales Customers + Customer Groups tabs + POS group price display; POS credit rejects walk-in), quotations (send emails customer via SMTP/console then status=sent + resend, accept/reject/convert; branded print `GET /sales/quotations/{id}/print` text|html|pdf × a4|thermal with company/customer/product labels + Sales UI Print/PDF; expiry reminders + auto-expire via `scan_quotation_expiry` / `/notifications/scan-due`), sales orders with BR-7.3 soft allocation + logistics (`stock_reservations`, product/warehouse `reserved_qty`; confirm reserves; cancel from draft/confirmed/processing releases; convert-to-invoice from draft through delivered; invoice post consumes then stock-out once; optional order `store_id`/`warehouse_id`; `delivery_date`/`delivery_address` with customer-address default + `PATCH /sales/orders/{id}`; status flow confirmed→processing→shipped→delivered via `/process|/ship|/deliver`; Sales Orders delivery + reserved UI), invoices draft→post→sent/partial/paid/overdue/cancelled with optional `store_id` (posts deduct that store’s warehouse stock) + configurable invoice numbering (`tenants.document_numbering` prefix/year/pad/next series, e.g. `INV-34535` or `INV-2026-0001`; Company UI) + branded multi-format print (`GET /sales/invoices/{id}/print` a4|thermal_80|thermal_58 × text|html|pdf with company/customer/product labels; tenant default `invoice_print_template`; Sales UI Print + PDF) + email send (`POST .../send` → `emailed_at`/`emailed_to`, status posted→sent without re-posting stock/GL; overdue refresh on list/get when past `due_date`; Sales UI Print/Email/Pay on sent/overdue), payments, sales returns with restock/discard + AR/journal + configurable `sales_credit_note` numbering allocated on post + branded credit note print (`GET /sales/returns/{id}/print` text|html|pdf; Sales UI Print/PDF), line `variant_id` on quote/order/invoice/return, salesperson + store performance reports with CSV/PDF/Excel export. Stage 12 COMPLETE (ADR-030): C1 OTC E2E + tax-on-net; C2 POS chain E2E; A1 POS audits; D1 fidelity; H12x exit (`docs/STAGE_12_EXIT_CRITERIA.md`). Stage 15 COMPLETE (ADR-036): C1 invoice stock→AR→tax→JE (`test_sales_inventory_ledger_chain_c1.py`); I1 standard-cost COGS `5000`/Inventory `1200`; H1 post atomicity `INSUFFICIENT_STOCK`; R1 return warehouse restock + FX-safe AR; T1 live tax→filing; A1 enriched `invoice_posted` + `sales_return_posted`; D1/H15x exit (`docs/STAGE_15_EXIT_CRITERIA.md`, `docs/STAGE_15_FIDELITY.md`). Stage 24 N1: shared document numbering series fidelity (`test_document_numbering_n1.py`). Stage 24 G1: Sales gate Complete for MVP (`test_commerce_gate_closure_g1.py`). Remaining post-MVP: percentage discount UI polish.
- [x] POS cart, barcode, payment, receipt, shift and stock deduction complete.
  - Complete (MVP): cart checkout with stock out (incl. variant lines; store session deducts linked warehouse), shift open/close + cash reconciliation/variance notification, payment methods + Stage 3 P1 split tender (`pos_payments`, `payments[]` on `/pos/sales`, per-method GL + session buckets), thermal receipt (JSON/text/PDF 58/80mm) + email/SMS send; hardware cash drawer (`stores.drawer_mode` none|mock|network|browser_bridge, `PATCH /stores/{id}/drawer`, `POST /pos/sessions/{id}/drawer/open`, auto-pulse on cash sales / any cash tender, ESC/POS kick bytes on receipt JSON); barcode entry via USB/Bluetooth wedge (Enter auto-add) and camera scan (Chromium BarcodeDetector); cart qty/remove, line + cart discounts, walk-in/customer selection, wallet + credit (credit requires active customer + limit check) with receipt discount/customer lines. Stage 12 COMPLETE POS chain + domain audit. Stage 13 COMPLETE (ADR-032): H1 atomic stock guard; H2 multi-tender + `pos_receipt_sent` + cash-portion drawer; D1 fidelity; H13x exit (`docs/STAGE_13_EXIT_CRITERIA.md`, `docs/STAGE_13_FIDELITY.md`). Stage 24 G1: POS gate Complete for MVP (`test_commerce_gate_closure_g1.py`). Remaining post-MVP: vendor-specific USB/serial drivers beyond TCP ESC/POS / browser bridge.
- [x] Expenses and approval workflow complete.
  - Complete: categories + default seed + category budget allocation (`PATCH /expenses/categories/{id}` `budget_amount`; `GET /expenses/budgets` period variance vs approved/pending; Expenses UI Categories & budgets; report summary embeds budgets), threshold auto-approve, configurable N-level approval matrix (`tenants.expense_approval_matrix` / `PATCH /expenses/settings` levels + role gates per step; legacy `expense_approval_threshold` / `expense_l2_threshold` kept in sync), action log + no self-approve / no duplicate-step actor, approve/reject + journal, role-targeted expense approval email/dashboard notify (`expense_approval` email default on), recurring create/generate + Celery beat automation with notify-before (`recurring_expense` prefs / `scan_recurring_expense_upcoming` on `/notifications/scan-due` + generate job) and skip/modify next occurrence (`skip_next`, `next_amount`/`next_description` via `PATCH /expenses/recurring/{id}`; Expenses UI Recurring section), receipt file upload/download/delete via local or S3/MinIO media store, receipt OCR suggest (`POST .../ocr-suggest` PDF text + Tesseract images) + Stage 10 A1 human-confirmed apply (`POST .../ocr-apply` with `confirm: true`; Expenses UI). Stage 14 COMPLETE (ADR-034): E1 category → COA (`expense_categories.account_id`, fallback `6000`); E2 `store_id`/`department_id` on expenses and recurring; A3 domain audit `expense_submitted` / `expense_auto_approved` / `expense_level_approved` / `expense_approved` / `expense_rejected`; D1/H14x exit (`docs/STAGE_14_EXIT_CRITERIA.md`, `docs/STAGE_14_FIDELITY.md`). Stage 22 closed (ADR-050): Expenses, Ledger, Credit & Tax Surface Fidelity — `docs/STAGE_22_PLAN.md`, exit `docs/STAGE_22_EXIT_CRITERIA.md`. Stage 22 D1/H22x: BR-9.1–9.3/9.5 surface fidelity + docs sync + freeze (`test_expense_categories_entry_e1.py`, `test_expense_approval_recurring_a1.py`, `docs/STAGE_22_FIDELITY.md`, `test_stage22_fidelity_d1.py`, `test_stage22_exit_h22x.py`). Stage 23 G1: Expenses gate Complete — documented BR-9 ACs met (`test_mvp_gate_closure_g1.py`).
- [x] Double-entry accounting, journals, COA and financial statements complete.
  - Complete (MVP): COA defaults (Cash 1000 + Bank 1010 + Cheques Receivable 1020 + Cheques Payable 2015), balanced journals, auto-post from invoice/GRN/purchase invoice (manual)/purchase return/payment/expense approval, trial balance + P&L, cash-flow across liquid accounts, liquid cash/bank CRUD (`POST/PATCH /accounting/liquid-accounts` with `bank_name`/`account_number`/`bank_branch`) + deposit/withdrawal/transfer (`POST /accounting/liquid-transfers` → balanced journals `liquid_deposit|liquid_withdrawal|liquid_transfer`; Accounting Ledger UI), bank statement import + match/ignore/complete against journal lines (`/accounting/bank-statements`); CSV/OFX-QFX feed upload (`POST .../import`); confidence-scored auto-match + `auto-clear`; multi-line clearing groups (`clear-group`); customer/supplier payments, expenses, and POS settle to Cash vs Bank by `payment_method` (card/transfer→1010, cash→1000, cheque→1020/2015 clearing, POS credit→AR); optional per-payment `liquid_account_id` GL override; cheque lifecycle deposit/clear/bounce/cancel; direct bank API connectors (`bank_account_connections`, providers `mock`|`http_json`, `POST .../bank-connections/{id}/sync`, Celery `sync_bank_feeds`, external_ref dedupe into recon statements). Stage 8 A1: per-account ledger `GET /accounting/accounts/{id}/transactions` with running balance + Ledger UI drill-down. Stage 9 J1: journal supporting documents (`POST/GET/DELETE /accounting/journal-entries/{id}/attachment`, Alembic `0083`, Ledger UI). Stage 14 COMPLETE (ADR-034): A1 `journal_entries.store_id` + store-filtered P&L/cash-flow/JE list; A2 trial balance / balance sheet `as_of_date` from posted journals; D1/H14x exit (`docs/STAGE_14_EXIT_CRITERIA.md`, `docs/STAGE_14_FIDELITY.md`). Stage 15 I1: sales invoice / POS / restocked return journals post Dr COGS `5000` / Cr Inventory `1200` at qty × standard `cost_price` (`test_sales_cogs_inventory_i1.py`; `docs/STAGE_15_FIDELITY.md`). Stage 22 C1/B1/P1/D1/H22x: BR-10.1/10.3–10.6 fidelity — industry-agnostic COA, liquid/recon/cheques, AR/AP aging + `scan_payment_due` for sales and purchase invoices, P&L/TB PDF+Excel export (`test_coa_fidelity_c1.py`, `test_cash_bank_recon_b1.py`, `test_ar_ap_export_p1.py`, `docs/STAGE_22_FIDELITY.md`, `test_stage22_fidelity_d1.py`, `docs/STAGE_22_EXIT_CRITERIA.md`, ADR-050). Stage 23 G1: Accounting gate Complete for MVP (`test_mvp_gate_closure_g1.py`). Remaining post-MVP: native Open Banking/Plaid vendor adapters (http_json covers aggregator feeds). Stage 3 A1: journal unpost within open fiscal period (BR-10.2). Stage 3 A2: COA CRUD + `parent_id` hierarchy + opening balances vs 3900 (`POST/PATCH /accounting/accounts`, `.../opening-balance`, Alembic `0075`). Stage 3 A3: dated P&L buckets + cash-flow O/I/F (`from_date`/`to_date`, opening/closing cash). Stage 3 P1: POS split tender `pos_payments` (Alembic `0076`, `payments[]` on `/pos/sales`). Stage 18 I1: cross-module integrity proofs — inventory qty=Σ movements (Stage 17 stock ops), TB balanced with Inventory 1200 / AR 1100 / COGS 5000 sanity, POS sale/payment/JE linked with no orphans (`test_cross_module_integrity_i1.py`).
- [x] Credit limits, balances, payments and aging complete.
  - Complete: AR/AP aging buckets (AP prefers purchase invoices, falls back to uninvoiced POs), statements, auto-allocate payments oldest-first, supplier payments + journals, credit-limit warnings + Stage 3 C1 hard gate/override (`CREDIT_LIMIT_EXCEEDED` on invoice post and POS credit; override with `credit:approve` + reason ≥3 chars; audit `credit_limit_override`; Alembic `0077` invoice override columns), early-payment discounts for AR give and AP take (`early_pay_discount_pct`/`days`, quote APIs, settle with Dr/Cr Sales Discounts 4100 or Purchase Discounts Taken 4200), multi-currency credit (tenant base + `exchange_rates`, document `currency`/`exchange_rate` on sales/purchase invoices and payments, GL + party balances in base, FX gain/loss 4300 on settle rate variance), live FX feed / auto-update (`POST /credit/exchange-rates/refresh`, `FX_PROVIDER` open_er_api|frankfurter, Celery `refresh_fx_rates`, tenant `fx_auto_refresh`). Stage 8 COMPLETE (ADR-022): S1 payment schedule; S2 outstanding UI; A1 account ledger transactions; P1 purchase return multi-line UI; H8x exit (`docs/STAGE_8_EXIT_CRITERIA.md`). Stage 14 COMPLETE (ADR-034): R1 Credit UI allocate payment to selected invoice/bill/PO (or Auto); payload `sales_invoice_id` / `purchase_invoice_id` / `purchase_order_id` (`docs/STAGE_14_EXIT_CRITERIA.md`, `docs/STAGE_14_FIDELITY.md`). Stage 22 R1/D1/H22x: BR-11.1 surface fidelity + docs sync + freeze (`test_customer_credit_r1.py`, `docs/STAGE_22_FIDELITY.md`, `test_stage22_fidelity_d1.py`, `docs/STAGE_22_EXIT_CRITERIA.md`, ADR-050).
- [x] VAT/tax calculation and reporting complete.
  - Complete (MVP): tax types/modes, default rate, auto-calc on sales invoices + POS, product tax override/exempt, compound rate components (`basis=net|compound`), sales reverse charge (memo tax excluded from customer total + filing box 2a), purchase reverse charge (`is_reverse_charge` → AP=net, self-assess Dr 1300 / Cr 2100 on approve, filing input+output), output/input tax report, jurisdiction-neutral filing pack + Ghana GRA + Nigeria FIRS + Kenya KRA VAT return templates (`tax_jurisdiction`/`tax_registration_number`, `GET /reports/tax/filing`, export `tax_filing_gh` / `tax_filing_ng` / `tax_filing_ke`), standard/zero/exempt supply splits (`sales_invoice_items.supply_category` + POS line category; filing boxes `1`/`1z`/`1e` → GH1/GH3/GH4, NG1/NG3/NG4, KE1/KE3/KE4). Stage 10 T1: category-level tax (`product_categories.tax_rate_id`, Alembic `0084`). Stage 10 T2: Kenya KRA manual filing workbook (no portal e-file). Stage 10 COMPLETE (ADR-026): T1 category tax; T2 KE filing; A1 OCR apply; B1 backup media; H10x exit (`docs/STAGE_10_EXIT_CRITERIA.md`). Stage 14 COMPLETE (ADR-034): T1 `PATCH /tax/rates/{id}` edit/deactivate; `active_only`; report/filing `period=monthly|quarterly|annually`; D1/H14x exit (`docs/STAGE_14_EXIT_CRITERIA.md`, `docs/STAGE_14_FIDELITY.md`). Stage 15 T1: live HTTP-posted invoices (standard / reverse-charge memo / zero-or-exempt splits) feed `/reports/tax` and `/reports/tax/filing` (`test_sales_tax_filing_t1.py`; `docs/STAGE_15_FIDELITY.md`). Stage 22 T1/D1/H22x: BR-12.1 types/inclusive-exclusive/compound fidelity + docs sync + freeze (`test_tax_config_fidelity_t1.py`, `docs/STAGE_22_FIDELITY.md`, `test_stage22_fidelity_d1.py`, `docs/STAGE_22_EXIT_CRITERIA.md`, ADR-050). Stage 23 G1: Tax gate Complete for MVP (`test_mvp_gate_closure_g1.py`). Remaining post-MVP: more jurisdictions as needed; GRA/FIRS/KRA e-file portal submission.
- [x] Multi-store/warehouse inventory and transfer workflow complete.
  - Complete (MVP): stores with linked warehouses, warehouse stock balances, inter-store transfers + Stage 2 inter-warehouse transfers (`/inventory/stock-transfers`, draft→requested→in_transit→received/cancelled with stock movements); Stage 4 T1 dual-manager approval on inter-store ship/receive (`TRANSFER_SHIP_FORBIDDEN` / `TRANSFER_RECEIVE_FORBIDDEN`, admin override audit `transfer_manager_override`); Stage 4 M1 store sales `GET /stores/{id}/sales` + Shell global store context switcher (`selected_store_id`); consolidated store sales reporting (`GET /reports/sales/by-store`); invoice post + POS sale deduct store warehouse stock when `store_id` / session store is set (unlocated stock auto-allocated); per-store warehouse reorder_level/reorder_qty + low-stock report/notifications; optional `fefo_strict_warehouse` (FEFO only from warehouse-tagged batches). Stage 16 M1: inter-store transfer → warehouse qty + `stock_movements` chain (`test_multistore_transfer_chain_m1.py`). Stage 16 M2: transfer history filters + `GET /reports/transfers` + export `transfer_history` + Reports → Transfers (`test_transfer_history_m2.py`). Stage 16 COMPLETE (ADR-038): M1–N2 / D1 / H16x — `docs/STAGE_16_EXIT_CRITERIA.md`, `docs/STAGE_16_FIDELITY.md`, `test_stage16_fidelity_d1.py`, `test_stage16_exit_h16x.py`. Stage 24 G1: Multi-store gate Complete for MVP (`test_commerce_gate_closure_g1.py`). Remaining post-MVP: advanced multi-bin locations; user↔store membership (ADR-005).
- [x] Reports and exports complete.
  - Complete (MVP): sales/inventory/purchase/expense/cash-flow reports, salesperson + store performance, Stage 4 R1 customer sales (`/reports/sales/customers`), product sales `store_id`/`category_id` filters, daily/monthly comparative `change_pct`, balance sheet, unified CSV/PDF/Excel (`xlsx`) export including tax filing pack + Ghana/Nigeria VAT returns (`tax_filing_gh` / `tax_filing_ng`); scheduled report email (`report_schedules` + Celery `run_due_report_emails`). Stage 9 COMPLETE (ADR-024): J1 journal attachments; R1 pending POs + purchase return summary; R2 stock valuation at standard cost; D1 fidelity (`docs/STAGE_9_FIDELITY.md`); H9x exit (`docs/STAGE_9_EXIT_CRITERIA.md`). Stage 16 R1: suite fidelity for Sales/Inventory/Low Stock/Purchasing/Expenses/Financial/Store Performance (`test_reports_suite_r1.py`; BR-14.1/14.4/14.5 sync). Stage 16 R2: Reports UI Credit/Tax tabs + `credit_aging` export packaging existing `/credit/aging` and `/reports/tax*` (`test_credit_tax_reports_r2.py`). Stage 16 M2: transfer history report/export packaging (`transfer_history`). Stage 16 D1/H16x: `docs/STAGE_16_FIDELITY.md`, `docs/STAGE_16_EXIT_CRITERIA.md` (ADR-038). Stage 23 F1: financial `store_id`/`branch_id` on balance sheet / P&L / cash-flow (`test_financial_report_filters_f1.py`, `docs/STAGE_23_PLAN.md`, ADR-051). Stage 23 C1: financial comparative `compare=true` on P&L / cash-flow / BS (`test_financial_comparative_c1.py`). Stage 23 G1: Reports gate Complete for MVP BR-14 (`test_mvp_gate_closure_g1.py`). Stage 23 D1: `docs/STAGE_23_FIDELITY.md` (`test_stage23_fidelity_d1.py`). Stage 23 H23x: exit met — `docs/STAGE_23_EXIT_CRITERIA.md`, freeze `docs/ADR_052_STAGE23_FREEZE.md` (`test_stage23_exit_h23x.py`). Stage 24 closed (ADR-054): Commerce & Ops Gate Fidelity — `docs/STAGE_24_PLAN.md`, exit `docs/STAGE_24_EXIT_CRITERIA.md` (`test_stage24_exit_h24x.py`; historical open ADR-053). Remaining post-MVP: additional jurisdiction filing templates beyond GH/NG/KE; FIFO/LIFO/WA costing.
- [x] In-app notifications and channel preferences complete.
  - Complete for Stage 1 / BR-4.4: unread count/bell, categorized groups (`stock`/`orders`/`payments`/`system` via `?group=`), mark read/unread + mark-all, 90-day history window, dashboard notifications panel, channel preferences; low-stock + payment-due + quotation-expiry scan (`quotation_expiry` prefs; marks past `valid_until` draft/sent as expired; Celery `scan_quotation_expiry`) + recurring-expense upcoming scan (`recurring_expense` prefs); Stage 4 N1 `new_order` on sales-order create/confirm (group `orders`, channel prefs); email channel (SMTP), SMS channel (Twilio with console fallback) + profile phone + `/settings/sms`. Stage 16 N1: emission matrix for outline buckets (`test_notification_emission_n1.py`). Stage 16 N2: email/SMS prefs honored for outline categories with console send-attempt proof (`test_notification_channel_delivery_n2.py`; no fake carrier delivery). Stage 16 D1/H16x: BR-15 sync + exit (`docs/STAGE_16_FIDELITY.md`, `docs/STAGE_16_EXIT_CRITERIA.md`, ADR-038). Stage 21 N1/D1: panel fidelity + BR-4.4 sync (`test_dashboard_notifications_n1.py`, `docs/STAGE_21_FIDELITY.md`, `test_stage21_fidelity_d1.py`).

### Reliability & operations
- [x] Automated backup and tested restore complete (logical MVP).
  - Complete (Stage 5 B1 + Stage 10 B1 + Stage 18 B1 + Stage 18 D1 / H18x): encrypted tenant logical backups (`.ribbak`), SHA-256 integrity, download, dry-run + guarded restore (`confirm_text=RESTORE`), post-restore/verify integrity proof (`POST /backup/{id}/verify`, `proof` on apply), retention prune, schedule + Celery due runner, admin UI, automated drill tests, runbook `docs/DR_LOGICAL_BACKUP_RUNBOOK.md`. Stage 10 B1: uploaded media bytes (logos, product images, expense/PI/journal attachments) embedded in `.ribbak` `media` map and rehydrated on apply (`test_backup_media_b1.py`). Stage 18 B1: schedule daily/weekly + retention proofs, failure → admin `Backup failed` notification (no fake success on `run-due`), restore dry-run/verify remain green (`test_backup_schedule_b1.py`). Stage 18 D1/H18x: BR-16/17 + SECURITY_GUIDE + readiness + launch fidelity + exit (`docs/STAGE_18_FIDELITY.md`, `docs/STAGE_18_EXIT_CRITERIA.md`, ADR-042, `test_stage18_fidelity_d1.py`, `test_stage18_exit_h18x.py`). Stage 26 W1: S3-compatible offsite mirror script for `.ribbak` (`ops/backup/sync-ribbak-offsite.sh.example`) + WAL strategy (`docs/DR_WAL_PITR_RUNBOOK.md`). Stage 27 B1: opt-in automatic `.ribbak` upload from `create_backup` (`BACKUP_OFFSITE_UPLOAD_ENABLED` + `BACKUP_OFFSITE_S3_*`; `test_backup_offsite_b1.py`; failure → `Backup failed`, no fake success). Remaining post-MVP: audit cold archives in logical pack optional.
- [x] Point-in-time recovery/WAL strategy complete.
  - Complete (MVP) (Stage 26 W1): WAL archiving + S3-compatible offsite strategy documented (`docs/DR_WAL_PITR_RUNBOOK.md`); versioned configs `ops/postgres/` (`postgresql-wal-archive.conf.example`, `archive-wal-to-s3.sh.example`) + `.ribbak` offsite sync `ops/backup/sync-ribbak-offsite.sh.example`; evidence `/opt/cursor/artifacts/dr/stage26_w1_wal_pitr_strategy.json` (`test_wal_pitr_w1.py`). Logical `.ribbak` DR remains Complete (Stages 5/10/18/23). Stage 27 B1 closes automatic `.ribbak` upload Remaining (`BACKUP_OFFSITE_UPLOAD_ENABLED`; `test_backup_offsite_b1.py`). Stage 28 R1 operator PITR drill pack Complete (MVP) — `docs/PITR_DRILL_PACK_MVP.md`, `ops/postgres/pitr-drill-checklist.json`, evidence `/opt/cursor/artifacts/dr/stage28_r1_pitr_drill_pack.json` (`test_pitr_drill_pack_r1.py`); packaging only (`operator_pitr_drill_executed: false`). Remaining post-MVP: operator staging PITR drill **execution** (base + WAL replay) with ops change-log evidence, managed-cloud PITR automation. Stage 26 D1 production platform fidelity: `docs/STAGE_26_FIDELITY.md` (`test_stage26_fidelity_d1.py`).
- [x] Immutable audit logging for sensitive operations complete.
  - Complete for Stage 1 / BR-17.1–17.2: append-only APIs, hash-chained integrity verify, filtered query + CSV/PDF export; domain `audit.record_event` coverage for auth/MFA/users/roles/bank/stock/backup/purchasing/sales/accounting writes; HTTP mutation middleware auto-coverage (`http_write` on successful `/api/v1` POST/PUT/PATCH/DELETE); 7-year retention policy (ADR-007) with cold JSONL archive (`POST /audit-logs/archive-cold`, Celery `archive_cold_audit_logs`), `archived_at` markers, no purge. Stage 15 A1: enriched `invoice_posted` (stock/tax/AR) and `sales_return_posted` (`test_sales_audit_a1.py`; `docs/STAGE_15_FIDELITY.md`). Stage 17 A1: product create/update/deactivate + stock_* before/after (`test_inventory_audit_a1.py`). Stage 18 A1: BR-17 login/logout/user/purchases/financial + filter/export/verify/retention (`test_security_hardening_a1.py`). Stage 18 D1/H18x: fidelity sync + exit (`docs/STAGE_18_FIDELITY.md`, `docs/STAGE_18_EXIT_CRITERIA.md`, ADR-042, `test_stage18_fidelity_d1.py`, `test_stage18_exit_h18x.py`). Remaining post-MVP: hot-table physical prune after retention window.
- [x] Redis/Celery/RabbitMQ used for intended production workloads.
  - Complete (MVP): Redis for distributed API/auth rate limiting; Celery worker + beat on RabbitMQ broker (Redis results) for low-stock scan, payment-due scan, quotation-expiry scan, recurring expenses, due backups, report emails, FX rate refresh, bank feed sync (`sync_bank_feeds`), and Stage 7 W2 webhook retries (`retry_due_webhooks`); admin `GET /jobs` + `POST /jobs/{name}/run`; AI jobs `generate_ai_low_stock_predictions` + `generate_ai_insights` (Celery beat; weekly digest email via `ai_insight` prefs). Stage 6 COMPLETE (ADR-018): K1 API keys; W1 HMAC webhooks; N2 onboarding; P2 Redis app cache. Stage 7 COMPLETE (ADR-020): W2 webhook retries; C2 permissions Redis cache; K2 API key usage statistics; L7x launch checklist + exit (`docs/LAUNCH_CHECKLIST.md`, `docs/STAGE_7_EXIT_CRITERIA.md`). Stage 19 R1: LAUNCH §5 fidelity — dashboard/catalog Redis soft-fail, permissions invalidate on role/`record_scope`, beat matrix + admin jobs dry-run, logical DR packaging (`test_reliability_cache_r1.py`). Stage 24 O1: Redis/Celery/RabbitMQ gate Complete for MVP intended workloads (`test_ops_ai_gate_closure_o1.py`). Stage 24 D1: `docs/STAGE_24_FIDELITY.md` (`test_stage24_fidelity_d1.py`). Stage 24 H24x / ADR-054 freeze recorded. Stage 27 P1: PgBouncer Complete (MVP) — `ops/postgres/pgbouncer.ini.example`, `docs/PGBOUNCER_MVP.md` (`test_pgbouncer_p1.py`). Stage 29 B2 soak/pooler pack Complete (MVP) — `docs/PGBOUNCER_SOAK_PACK_MVP.md`, evidence `/opt/cursor/artifacts/db/stage29_b2_pgbouncer_soak.json` (`test_pgbouncer_soak_b2.py`); packaging only (`live_soak_executed: false`, `helm_pooler_default_claimed: false`). Remaining post-MVP: live soak **execution** / in-cluster Helm pooler as **default** data plane. Remaining post-MVP: vendor pen test, certified 1000-VU ops run (K8s/WAL strategy already Complete MVP under Stage 26).
- [x] Monitoring, metrics, logging and alerting complete.
  - Complete (MVP) (Stage 5 H5 + Stage 18 L1 + Stage 26 M1): deep `/api/v1/health?deep=true` + `/health/ready` (database / Redis / Celery broker checks, 503 on hard failure); Prometheus-text `/api/v1/metrics` with request counters (`METRICS_ENABLED`). Stage 18 L1: structured JSON request/error logs (`request_id`, `tenant_id`, `user_id`, `status`, `latency_ms`, safe `error_code`; `X-Request-ID`) via `RequestLoggingMiddleware` (`test_request_logging_l1.py`). Stage 26 M1: versioned Prometheus scrape `ops/prometheus/prometheus.yml`, alert rules `ops/prometheus/alerts/ribdigi.yml` (`RibdigiDown` / `RibdigiHighErrorRate` / `RibdigiHighLatency` / `RibdigiNotReady`), Fluent Bit log-ship example `ops/logging/fluent-bit-ribdigi.conf.example`, doc sync `docs/OPS_MONITORING_MVP.md` (`test_ops_monitoring_m1.py`). Stage 28 A1 Grafana/Alertmanager operator pack Complete (MVP) — `docs/GRAFANA_PACK_MVP.md`, `ops/grafana/`, evidence `/opt/cursor/artifacts/monitoring/stage28_a1_grafana_pack.json` (`test_grafana_pack_a1.py`); packaging only (`hosted_grafana_claimed: false`, `pagerduty_wired: false`). Stage 30 I1 incident / on-call pack Complete (MVP) — `docs/INCIDENT_PACK_MVP.md`, `ops/incident/`, evidence `/opt/cursor/artifacts/monitoring/stage30_i1_incident_pack.json` (`test_incident_pack_i1.py`); packaging only (`pagerduty_hosted_claimed: false`, `oncall_rota_live: false`, `incident_drill_executed: false`). Stage 30 S1 support/Admin runbook fidelity Complete (MVP) — `docs/SUPPORT_RUNBOOK_MVP.md`, `ops/support/admin-ops-map.json`, evidence `/opt/cursor/artifacts/launch/stage30_s1_support_runbook.json` (`test_support_runbook_s1.py`); packaging only (`live_ops_success_claimed: false`, `support_sla_claimed: false`). Remaining post-MVP: hosted Grafana/Alertmanager/PagerDuty **as SaaS**, live on-call rota / incident drills, centralized SIEM deployment. Stage 26 D1 production platform fidelity: `docs/STAGE_26_FIDELITY.md` (`test_stage26_fidelity_d1.py`).
- [x] Kubernetes production deployment reviewed.
  - Complete (MVP) (Stage 26 K1): Helm chart `helm/ribdigi/` (backend/frontend/Celery/migration + staging/production values) with liveness `/api/v1/health` and readiness `/api/v1/health/ready` probes, resource limits, `ribdigi-secrets` refs; hardened flat `k8s/` manifests; operator install/smoke `ops/k8s/`; doc `docs/K8S_DEPLOY_MVP.md`; evidence `/opt/cursor/artifacts/k8s/stage26_k1_deploy_fidelity.json` (`test_k8s_deploy_k1.py`). Stage 18 C1 Compose/env path remains valid; main `ci.yml` stays deploy-free. Stage 28 G1 staging GHA template Complete (MVP) — `docs/STAGING_GHA_MVP.md`, `ops/k8s/deploy-staging.example.yml`, evidence `/opt/cursor/artifacts/k8s/stage28_g1_staging_gha.json` (`test_staging_gha_g1.py`); packaging only (`live_staging_apply_claimed: false`; not in main `ci.yml`). Stage 29 T1 cert-manager / TLS ingress pack Complete (MVP) — `docs/TLS_INGRESS_PACK_MVP.md`, `ops/k8s/cluster-issuer.example.yaml`, evidence `/opt/cursor/artifacts/k8s/stage29_t1_tls_ingress.json` (`test_tls_ingress_t1.py`); packaging only (`letsencrypt_issued: false`, `tls_cutover_claimed: false`). Stage 29 X1 production cutover pack Complete (MVP) — `docs/CUTOVER_PACK_MVP.md`, `ops/launch/cutover-checklist.json`, `ops/k8s/deploy-production.example.yml`, evidence `/opt/cursor/artifacts/launch/stage29_x1_cutover_pack.json` (`test_cutover_pack_x1.py`); packaging only (`production_cutover_claimed: false`, `section_7_signed: false`; not in main `ci.yml`). Stage 29 D1 fidelity: `docs/STAGE_29_FIDELITY.md` (`test_stage29_fidelity_d1.py`) maps V1–X1. Stage 29 H29x exit + freeze: `docs/STAGE_29_EXIT_CRITERIA.md`, ADR-064 (`test_stage29_exit_h29x.py`). Stage 30 open (ADR-065): Go-Live Support Fidelity — `docs/STAGE_30_PLAN.md` (`test_stage30_open.py`). Stage 30 D1 fidelity: `docs/STAGE_30_FIDELITY.md` (`test_stage30_fidelity_d1.py`). Stage 30 H30x exit + freeze: `docs/STAGE_30_EXIT_CRITERIA.md`, ADR-066 (`test_stage30_exit_h30x.py`). Stage 30 L1 operator evidence ledger Complete (MVP) — `docs/EVIDENCE_LEDGER_MVP.md`, `ops/evidence/ledger.json`, evidence `/opt/cursor/artifacts/launch/stage30_l1_evidence_ledger.json` (`test_evidence_ledger_l1.py`); packaging only (`live_runs_certified: false`, `attestation_claimed: false`). Stage 30 A1 go-live attestation matrix Complete (MVP) — `docs/ATTESTATION_PACK_MVP.md`, `ops/launch/attestation-matrix.json`, evidence `/opt/cursor/artifacts/launch/stage30_a1_attestation_pack.json` (`test_attestation_pack_a1.py`); packaging only (`attestation_claimed: false`, `section_7_signed: false`, `sections_1_3_verified: false`). Stage 30 D1 go-live support fidelity: `docs/STAGE_30_FIDELITY.md` (`test_stage30_fidelity_d1.py`) maps L1–A1. Stage 30 H30x exit + freeze: `docs/STAGE_30_EXIT_CRITERIA.md`, ADR-066 (`test_stage30_exit_h30x.py`). Stage 31 open (ADR-067): Commercial MVP Closeout Fidelity — `docs/STAGE_31_PLAN.md` (`test_stage31_open.py`). Stage 31 G1 MVP gate honesty matrix Complete (MVP) — `docs/MVP_GATE_MATRIX_MVP.md`, `ops/mvp/gate-matrix.json`, evidence `/opt/cursor/artifacts/launch/stage31_g1_mvp_gate_matrix.json` (`test_mvp_gate_matrix_g1.py`); packaging only (`go_live_claimed: false`, `section_7_signed: false`, `attestation_claimed: false`). Stage 31 R1 deferred ADR register Complete (MVP) — `docs/DEFERRED_ADR_REGISTER_MVP.md`, `ops/mvp/deferred-adr-register.json`, evidence `/opt/cursor/artifacts/launch/stage31_r1_deferred_adr_register.json` (`test_deferred_adr_register_r1.py`); packaging only (`deferred_implemented_claimed: false`, `billing_complete_claimed: false`, `schema_per_tenant_claimed: false`, `i18n_packs_claimed: false`). Stage 31 O1 operator Remaining register Complete (MVP) — `docs/OPERATOR_REMAINING_MVP.md`, `ops/mvp/operator-remaining-register.json`, evidence `/opt/cursor/artifacts/launch/stage31_o1_operator_remaining.json` (`test_operator_remaining_o1.py`); packaging only (`live_runs_certified: false`, `attestation_claimed: false`, `section_7_signed: false`). Stage 31 C1 commercial MVP declaration Complete (MVP) — `docs/MVP_DECLARATION_MVP.md`, `ops/mvp/mvp-declaration.json`, evidence `/opt/cursor/artifacts/launch/stage31_c1_mvp_declaration.json` (`test_mvp_declaration_c1.py`); packaging Complete declared (`packaging_complete: true`) while `go_live_claimed: false`, `section_7_signed: false`, `attestation_claimed: false`. Stage 31 D1 closeout fidelity: `docs/STAGE_31_FIDELITY.md` (`test_stage31_fidelity_d1.py`) maps G1–C1; no go-live signed claim. Stage 31 H31x exit + freeze: `docs/STAGE_31_EXIT_CRITERIA.md`, ADR-068 (`test_stage31_exit_h31x.py`). Stage 32 open (ADR-069): Commercial MVP Handoff Fidelity — `docs/STAGE_32_PLAN.md` (`test_stage32_open.py`). Stage 32 A1 MVP acceptance archive Complete (MVP) — `docs/ACCEPTANCE_ARCHIVE_MVP.md`, `ops/mvp/acceptance-archive.json`, evidence `/opt/cursor/artifacts/launch/stage32_a1_acceptance_archive.json` (`test_acceptance_archive_a1.py`); packaging only (`go_live_claimed: false`, `section_7_signed: false`, `attestation_claimed: false`). Stage 32 H1 operator handoff Complete (MVP) — `docs/OPERATOR_HANDOFF_MVP.md`, `ops/mvp/operator-handoff.json`, evidence `/opt/cursor/artifacts/launch/stage32_h1_operator_handoff.json` (`test_operator_handoff_h1.py`); packaging only (`handoff_complete_claimed: false`, `go_live_claimed: false`, `section_7_signed: false`). Stage 32 N1 commercial release notes Complete (MVP) — `docs/RELEASE_NOTES_MVP.md`, `ops/mvp/release-notes.json`, evidence `/opt/cursor/artifacts/launch/stage32_n1_release_notes.json` (`test_release_notes_n1.py`); packaging only (`production_live_claimed: false`, `go_live_claimed: false`, `section_7_signed: false`). Stage 32 B1 post-MVP backlog Complete (MVP) — `docs/POST_MVP_BACKLOG_MVP.md`, `ops/mvp/post-mvp-backlog.json`, evidence `/opt/cursor/artifacts/launch/stage32_b1_post_mvp_backlog.json` (`test_post_mvp_backlog_b1.py`); packaging only (`deferred_implemented_claimed: false`, `billing_complete_claimed: false`, `go_live_claimed: false`). Stage 32 D1 handoff fidelity: `docs/STAGE_32_FIDELITY.md` (`test_stage32_fidelity_d1.py`) maps A1–B1; no go-live signed claim. Stage 32 H32x exit + freeze: `docs/STAGE_32_EXIT_CRITERIA.md`, ADR-070 (`test_stage32_exit_h32x.py`). Stage 33 open (ADR-071): Commercial MVP Continuity Fidelity — `docs/STAGE_33_PLAN.md` (`test_stage33_open.py`). Stage 33 K1 residual risk register Complete (MVP) — `docs/RESIDUAL_RISK_MVP.md`, `ops/mvp/residual-risk-register.json`, evidence `/opt/cursor/artifacts/launch/stage33_k1_residual_risk.json` (`test_residual_risk_k1.py`); packaging only (`risks_closed_claimed: false`, `go_live_claimed: false`, `section_7_signed: false`). Stage 33 C1 compliance readiness Complete (MVP) — `docs/COMPLIANCE_READINESS_MVP.md`, `ops/mvp/compliance-readiness-register.json`, evidence `/opt/cursor/artifacts/launch/stage33_c1_compliance_readiness.json` (`test_compliance_readiness_c1.py`); packaging only (`soc2_complete_claimed: false`, `iso27001_complete_claimed: false`, `certification_complete_claimed: false`). Stage 33 F1 first-tenant onboarding Complete (MVP) — `docs/FIRST_TENANT_ONBOARDING_MVP.md`, `ops/mvp/first-tenant-onboarding.json`, evidence `/opt/cursor/artifacts/launch/stage33_f1_first_tenant_onboarding.json` (`test_first_tenant_onboarding_f1.py`); packaging only (`first_tenant_onboarded_claimed: false`, `live_onboarding_success_claimed: false`, `go_live_claimed: false`). Stage 33 T1 knowledge transfer Complete (MVP) — `docs/KNOWLEDGE_TRANSFER_MVP.md`, `ops/mvp/knowledge-transfer.json`, evidence `/opt/cursor/artifacts/launch/stage33_t1_knowledge_transfer.json` (`test_knowledge_transfer_t1.py`); packaging only (`live_training_claimed: false`, `training_complete_claimed: false`). Stage 33 D1 continuity fidelity: `docs/STAGE_33_FIDELITY.md` (`test_stage33_fidelity_d1.py`) maps K1–T1; packaging only — no go-live signed claim. Stage 33 H33x exit + freeze: `docs/STAGE_33_EXIT_CRITERIA.md`, ADR-072 (`test_stage33_exit_h33x.py`). Stage 34 open (ADR-073): Commercial Customer Assurance Fidelity — `docs/STAGE_34_PLAN.md` (`test_stage34_open.py`). Stage 34 A1 assurance evidence Complete (MVP) — `docs/ASSURANCE_EVIDENCE_MVP.md`, `ops/mvp/assurance-evidence.json`, evidence `/opt/cursor/artifacts/launch/stage34_a1_assurance_evidence.json` (`test_assurance_evidence_a1.py`); packaging only (`customer_assurance_claimed: false`, `attestation_claimed: false`, `section_7_signed: false`). Stage 34 C1 compliance questionnaire Complete (MVP) — `docs/COMPLIANCE_QUESTIONNAIRE_MVP.md`, `ops/mvp/compliance-questionnaire.json`, evidence `/opt/cursor/artifacts/launch/stage34_c1_compliance_questionnaire.json` (`test_compliance_questionnaire_c1.py`); packaging only (`soc2_complete_claimed: false`, `iso27001_complete_claimed: false`, `questionnaire_answers_certified: false`). Stage 34 D1 assurance fidelity: `docs/STAGE_34_FIDELITY.md` (`test_stage34_fidelity_d1.py`) maps A1–C1; S1/B1 deferred; packaging only — no go-live signed claim. Stage 34 H34x exit + freeze: `docs/STAGE_34_EXIT_CRITERIA.md`, ADR-074 (`test_stage34_exit_h34x.py`). Stage 35 open (ADR-075): Commercial End-to-End Operational Smoke Fidelity — `docs/STAGE_35_PLAN.md` (`test_stage35_open.py`). Stage 35 T1 org bootstrap Complete (MVP) — `docs/E2E_ORG_BOOTSTRAP_MVP.md`, `ops/mvp/e2e-org-bootstrap.json`, evidence `/opt/cursor/artifacts/launch/stage35_t1_e2e_org_bootstrap.json` (`test_e2e_org_bootstrap_t1.py`); packaging only (`e2e_smoke_executed_claimed: false`, `live_bootstrap_claimed: false`, `demo_tenant_claimed: false`). Stage 35 U1 users + RBAC Complete (MVP) — `docs/E2E_USERS_RBAC_MVP.md`, `ops/mvp/e2e-users-rbac.json`, evidence `/opt/cursor/artifacts/launch/stage35_u1_e2e_users_rbac.json` (`test_e2e_users_rbac_u1.py`); packaging only (`live_users_provisioned_claimed: false`, `e2e_smoke_executed_claimed: false`, `demo_tenant_claimed: false`, `store_membership_claimed: false`). Stage 35 P1 purchase-to-stock Complete (MVP) — `docs/E2E_PURCHASE_STOCK_MVP.md`, `ops/mvp/e2e-purchase-stock.json`, evidence `/opt/cursor/artifacts/launch/stage35_p1_e2e_purchase_stock.json` (`test_e2e_purchase_stock_p1.py`); packaging only (`live_purchase_stock_claimed: false`, `e2e_smoke_executed_claimed: false`, `demo_tenant_claimed: false`, `po_kanban_claimed: false`). Stage 35 S1 sale-to-payment Complete (MVP) — `docs/E2E_SALE_PAYMENT_MVP.md`, `ops/mvp/e2e-sale-payment.json`, evidence `/opt/cursor/artifacts/launch/stage35_s1_e2e_sale_payment.json` (`test_e2e_sale_payment_s1.py`); packaging only (`live_sale_payment_claimed: false`, `e2e_smoke_executed_claimed: false`, `demo_tenant_claimed: false`, `usb_serial_drivers_claimed: false`). Stage 35 V1 verify financials Complete (MVP) — `docs/E2E_VERIFY_FINANCIALS_MVP.md`, `ops/mvp/e2e-verify-financials.json`, evidence `/opt/cursor/artifacts/launch/stage35_v1_e2e_verify_financials.json` (`test_e2e_verify_financials_v1.py`); packaging only (`live_verify_financials_claimed: false`, `e2e_smoke_executed_claimed: false`, `demo_tenant_claimed: false`, `tax_efile_claimed: false`). Stage 35 R1 backup + restore Complete (MVP) — `docs/E2E_BACKUP_RESTORE_MVP.md`, `ops/mvp/e2e-backup-restore.json`, evidence `/opt/cursor/artifacts/launch/stage35_r1_e2e_backup_restore.json` (`test_e2e_backup_restore_r1.py`); packaging only (`live_backup_restore_claimed: false`, `e2e_smoke_executed_claimed: false`, `demo_tenant_claimed: false`, `live_pitr_drill_claimed: false`). Stage 35 D1 E2E operational smoke fidelity: `docs/STAGE_35_FIDELITY.md` (`test_stage35_fidelity_d1.py`) maps T1–R1; packaging only — no live E2E smoke / go-live signed claim. Stage 35 H35x exit + freeze: `docs/STAGE_35_EXIT_CRITERIA.md`, ADR-076 (`test_stage35_exit_h35x.py`). Stage 36 open (ADR-077): Commercial Assurance Completion Fidelity — `docs/STAGE_36_PLAN.md` (`test_stage36_open.py`). Stage 36 S1 support SLA boundary Complete (MVP) — `docs/SUPPORT_SLA_BOUNDARY_MVP.md`, `ops/mvp/support-sla-boundary.json`, evidence `/opt/cursor/artifacts/launch/stage36_s1_support_sla_boundary.json` (`test_support_sla_boundary_s1.py`); packaging only (`support_sla_claimed: false`, `pagerduty_hosted_claimed: false`, `oncall_rota_live: false`, `incident_drill_executed: false`). Stage 36 B1 billing-deferred honesty Complete (MVP) — `docs/BILLING_DEFERRED_HONESTY_MVP.md`, `ops/mvp/billing-deferred-honesty.json`, evidence `/opt/cursor/artifacts/launch/stage36_b1_billing_deferred_honesty.json` (`test_billing_deferred_honesty_b1.py`); packaging only (`billing_complete_claimed: false`, `payment_provider_claimed: false`, `checkout_success_claimed: false`, `deferred_implemented_claimed: false`). Stage 36 D1 assurance completion fidelity: `docs/STAGE_36_FIDELITY.md` (`test_stage36_fidelity_d1.py`) maps S1–B1; packaging only — no live SLA / paid billing / go-live signed claim. Stage 36 H36x exit + freeze: `docs/STAGE_36_EXIT_CRITERIA.md`, ADR-078 (`test_stage36_exit_h36x.py`). Stage 37 open (ADR-079): Commercial Data Protection Fidelity — `docs/STAGE_37_PLAN.md` (`test_stage37_open.py`). Stage 37 P1 data subject access / portability Complete (MVP) — `docs/DATA_PORTABILITY_MVP.md`, `ops/mvp/data-portability.json`, evidence `/opt/cursor/artifacts/launch/stage37_p1_data_portability.json` (`test_data_portability_p1.py`); packaging only (`gdpr_complete_claimed: false`, `dsar_portal_claimed: false`, `live_portability_workflow_claimed: false`, `consent_management_claimed: false`). Stage 37 E1 erasure / soft-delete honesty Complete (MVP) — `docs/ERASURE_HONESTY_MVP.md`, `ops/mvp/erasure-honesty.json`, evidence `/opt/cursor/artifacts/launch/stage37_e1_erasure_honesty.json` (`test_erasure_honesty_e1.py`); packaging only (`hard_delete_claimed: false`, `erasure_complete_claimed: false`, `anonymize_workflow_claimed: false`, `deferred_implemented_claimed: false`). Stage 37 D1 data protection fidelity: `docs/STAGE_37_FIDELITY.md` (`test_stage37_fidelity_d1.py`) maps P1–E1; packaging only — no GDPR / hard-delete / go-live signed claim. Stage 37 H37x exit + freeze: `docs/STAGE_37_EXIT_CRITERIA.md`, ADR-080 (`test_stage37_exit_h37x.py`). Stage 38 open (ADR-081): Commercial Security Disclosure Fidelity — `docs/STAGE_38_PLAN.md` (`test_stage38_open.py`). Stage 38 V1 vulnerability disclosure Complete (MVP) — `docs/VULN_DISCLOSURE_MVP.md`, `ops/mvp/vuln-disclosure.json`, evidence `/opt/cursor/artifacts/launch/stage38_v1_vuln_disclosure.json` (`test_vuln_disclosure_v1.py`); packaging only (`disclosure_program_claimed: false`, `bug_bounty_claimed: false`, `continuous_disclosure_claimed: false`, `researcher_intake_live: false`). Stage 38 B1 breach notification / security contact Complete (MVP) — `docs/BREACH_NOTIFICATION_MVP.md`, `ops/mvp/breach-notification.json`, evidence `/opt/cursor/artifacts/launch/stage38_b1_breach_notification.json` (`test_breach_notification_b1.py`); packaging only (`breach_drill_claimed: false`, `regulatory_filing_claimed: false`, `customer_notify_saas_claimed: false`, `security_mailbox_live: false`). Stage 38 D1 security disclosure fidelity: `docs/STAGE_38_FIDELITY.md` (`test_stage38_fidelity_d1.py`) maps V1–B1; packaging only — no live disclosure / breach drill / go-live signed claim. Stage 38 H38x exit + freeze: `docs/STAGE_38_EXIT_CRITERIA.md`, ADR-082 (`test_stage38_exit_h38x.py`). Stage 39 open (ADR-083): Commercial Contract Evidence Fidelity — `docs/STAGE_39_PLAN.md` (`test_stage39_open.py`). Stage 39 P1 DPA / subprocessor honesty Complete (MVP) — `docs/DPA_SUBPROCESSOR_MVP.md`, `ops/mvp/dpa-subprocessor.json`, evidence `/opt/cursor/artifacts/launch/stage39_p1_dpa_subprocessor.json` (`test_dpa_subprocessor_p1.py`); packaging only (`dpa_signed_claimed: false`, `subprocessor_register_live: false`, `legal_counsel_claimed: false`, `contract_execution_claimed: false`). Stage 39 A1 MSA security addendum honesty Complete (MVP) — `docs/MSA_ADDENDUM_MVP.md`, `ops/mvp/msa-addendum.json`, evidence `/opt/cursor/artifacts/launch/stage39_a1_msa_addendum.json` (`test_msa_addendum_a1.py`); packaging only (`msa_signed_claimed: false`, `security_exhibit_signed: false`, `legal_counsel_claimed: false`, `contract_execution_claimed: false`). Stage 39 D1 contract evidence fidelity: `docs/STAGE_39_FIDELITY.md` (`test_stage39_fidelity_d1.py`) maps P1–A1; packaging only — no signed DPA/MSA / go-live signed claim. Stage 39 H39x exit + freeze: `docs/STAGE_39_EXIT_CRITERIA.md Stage 40 open: `docs/STAGE_40_PLAN.md`, ADR-085 (`test_stage40_open.py`). Stage 40 U1 status/uptime honesty Complete (MVP) Stage 40 S1 SBOM/dependency disclosure honesty Complete (MVP) Stage 40 D1 availability & supply-chain fidelity Complete (MVP) Stage 40 exit met — `docs/STAGE_40_EXIT_CRITERIA.md`, ADR-086 (`test_stage40_exit_h40x.py`) Stage 41 open: `docs/STAGE_41_PLAN.md`, ADR-087 (`test_stage41_open.py`). Stage 41 A1 accessibility statement honesty Complete (MVP) Stage 41 C1 change/maintenance governance honesty Complete (MVP) Stage 41 D1 accessibility & change governance fidelity Complete (MVP) Stage 41 exit met — `docs/STAGE_41_EXIT_CRITERIA.md`, ADR-088 (`test_stage41_exit_h41x.py`) Stage 42 open: `docs/STAGE_42_PLAN.md`, ADR-089 (`test_stage42_open.py`). Stage 42 A1 AI use disclosure honesty Complete (MVP) Stage 42 P1 AI model/provider boundary honesty Complete (MVP) Stage 42 D1 AI transparency fidelity Complete (MVP) Stage 42 exit met — `docs/STAGE_42_EXIT_CRITERIA.md`, ADR-090 (`test_stage42_exit_h42x.py`) Stage 43 open: `docs/STAGE_43_PLAN.md`, ADR-091 (`test_stage43_open.py`). Stage 43 T1 ToS / AUP honesty Complete (MVP) — `docs/TOS_AUP_MVP.md`, `ops/mvp/tos-aup.json` (`test_tos_aup_t1.py`). Stage 43 C1 Cookie / privacy notice honesty Complete (MVP) — `docs/COOKIE_PRIVACY_NOTICE_MVP.md`, `ops/mvp/cookie-privacy-notice.json` (`test_cookie_privacy_notice_c1.py`). Stage 43 D1 commercial legal notice fidelity Complete (MVP) — `docs/STAGE_43_FIDELITY.md` (`test_stage43_fidelity_d1.py`). Stage 43 exit met — `docs/STAGE_43_EXIT_CRITERIA.md`, ADR-092 (`test_stage43_exit_h43x.py`). Stage 44 open: `docs/STAGE_44_PLAN.md`, ADR-093 (`test_stage44_open.py`).; Stages 1–42 frozen for Stage 42 scope; external LLM / AI certification Remaining. — `docs/STAGE_42_FIDELITY.md` (`test_stage42_fidelity_d1.py`); maps A1–P1; `ai_certification_claimed` / `external_llm_claimed` remain false; external LLM / AI certification Remaining. — `docs/AI_PROVIDER_BOUNDARY_MVP.md`, `ops/mvp/ai-provider-boundary.json` (`test_ai_provider_boundary_p1.py`); external LLM Remaining. — `docs/AI_USE_DISCLOSURE_MVP.md`, `ops/mvp/ai-use-disclosure.json` (`test_ai_use_disclosure_a1.py`); AI certification Remaining.; Stages 1–41 frozen for Stage 41 scope; WCAG AA audit / public change calendar Remaining. — `docs/STAGE_41_FIDELITY.md` (`test_stage41_fidelity_d1.py`); maps A1–C1; `wcag_aa_claimed` / `change_calendar_live` remain false; WCAG AA audit / public change calendar Remaining. — `docs/CHANGE_GOVERNANCE_MVP.md`, `ops/mvp/change-governance.json` (`test_change_governance_c1.py`); public change calendar Remaining. — `docs/ACCESSIBILITY_STATEMENT_MVP.md`, `ops/mvp/accessibility-statement.json` (`test_accessibility_statement_a1.py`); WCAG AA audit Remaining.; Stages 1–40 frozen for Stage 40 scope; live status page / SBOM pipeline Remaining. — `docs/STAGE_40_FIDELITY.md` (`test_stage40_fidelity_d1.py`); maps U1–S1; `status_page_live` / `sbom_pipeline_live` remain false; live status page / SBOM pipeline Remaining. — `docs/SBOM_DISCLOSURE_MVP.md`, `ops/mvp/sbom-disclosure.json` (`test_sbom_disclosure_s1.py`); live SBOM pipeline Remaining. — `docs/STATUS_UPTIME_MVP.md`, `ops/mvp/status-uptime.json` (`test_status_uptime_u1.py`); live status page / 99.9% SLA Remaining.`, ADR-084 (`test_stage39_exit_h39x.py`). Remaining post-MVP: live operator run certification / go-live attestation / LAUNCH §7 Name/Date sign-off. Remaining post-MVP: live GHA → staging cluster **apply**, in-cluster data plane, cert-manager / TLS production **cutover** (live ACME issuance), live production **cutover execution** / LAUNCH §7 Name/Date sign-off. Stage 26 D1 production platform fidelity: `docs/STAGE_26_FIDELITY.md` (`test_stage26_fidelity_d1.py`).
- [x] Load/performance tests meet documented targets.
  - Complete (MVP) (Stage 5 L1 + Stage 18 T1 + Stage 26 C1): baseline scripts + targets in `docs/LOAD_TEST_BASELINE.md`; httpx harness (`backend/loadtest/`, `--smoke` + `--ci-capacity`); ASGI smoke + CI capacity profile (health/login/products/dashboard, 0% errors, ASGI-honest p95 gate); evidence `/opt/cursor/artifacts/loadtest/stage26_c1_capacity_evidence.json` (`test_load_capacity_c1.py`); Stage 18 T1 smoke artifact retained; MVP doc `docs/LOAD_CAPACITY_MVP.md`; optional Locust for operator staging. Stage 28 C1 operator ~1000-VU cert pack Complete (MVP) — `docs/LOAD_CERT_PACK_MVP.md`, `ops/loadtest/`, evidence `/opt/cursor/artifacts/loadtest/stage28_c1_load_cert_pack.json` (`test_load_cert_pack_c1.py`); packaging only (`operator_1000vu_executed: false`, `ci_1000vu_certificate_claimed: false`). Remaining post-MVP: operator staging ~1000 VU **execution** / p95 < 500 ms on sized infra with rate limits tuned; live ZAP-under-load (vendor pen test / staging ZAP remain Remaining under OWASP gate / Stage 27 S1). Stage 27 P1 PgBouncer packaging Complete (MVP) (`docs/PGBOUNCER_MVP.md`, `test_pgbouncer_p1.py`). Stage 29 B2 soak pack: `docs/PGBOUNCER_SOAK_PACK_MVP.md` (`test_pgbouncer_soak_b2.py`). Stage 26 D1 production platform fidelity: `docs/STAGE_26_FIDELITY.md` (`test_stage26_fidelity_d1.py`). Stage 26 H26x exit + freeze: `docs/STAGE_26_EXIT_CRITERIA.md`, ADR-058 (`test_stage26_exit_h26x.py`). Stage 27 open (ADR-059): Commercial MVP Release Fidelity — `docs/STAGE_27_PLAN.md` (`test_stage27_open.py`); B1–D1 / H27x complete (closed ADR-060). Stage 27 D1 release fidelity: `docs/STAGE_27_FIDELITY.md` (`test_stage27_fidelity_d1.py`). Stage 27 L1 launch cert packaging: `docs/LAUNCH_CERT_MVP.md` (`test_launch_cert_l1.py`) — not production sign-off. Stage 29 X1 cutover pack: `docs/CUTOVER_PACK_MVP.md` (`test_cutover_pack_x1.py`) — not live cutover / forged §7. Stage 29 D1: `docs/STAGE_29_FIDELITY.md` (`test_stage29_fidelity_d1.py`). Stage 27 H27x exit + freeze: `docs/STAGE_27_EXIT_CRITERIA.md`, ADR-060 (`test_stage27_exit_h27x.py`). Stage 28 open (ADR-061): Staging Certification Fidelity — `docs/STAGE_28_PLAN.md` (`test_stage28_open.py`). Stage 28 R1 PITR drill pack: `docs/PITR_DRILL_PACK_MVP.md` (`test_pitr_drill_pack_r1.py`). Stage 28 G1 staging GHA: `docs/STAGING_GHA_MVP.md` (`test_staging_gha_g1.py`). Stage 28 A1 Grafana pack: `docs/GRAFANA_PACK_MVP.md` (`test_grafana_pack_a1.py`). Stage 28 C1 1000-VU cert pack: `docs/LOAD_CERT_PACK_MVP.md` (`test_load_cert_pack_c1.py`). Stage 28 D1 staging certification fidelity: `docs/STAGE_28_FIDELITY.md` (`test_stage28_fidelity_d1.py`). Stage 28 H28x exit + freeze: `docs/STAGE_28_EXIT_CRITERIA.md`, ADR-062 (`test_stage28_exit_h28x.py`). Stage 29 open (ADR-063): Operator Hardening & Production Cutover Fidelity — `docs/STAGE_29_PLAN.md` (`test_stage29_open.py`). Stage 29 V1 pen-test pack: `docs/PENTEST_PACK_MVP.md` (`test_pentest_pack_v1.py`). Stage 29 B2 PgBouncer soak pack: `docs/PGBOUNCER_SOAK_PACK_MVP.md` (`test_pgbouncer_soak_b2.py`). Stage 29 T1 TLS ingress pack: `docs/TLS_INGRESS_PACK_MVP.md` (`test_tls_ingress_t1.py`); X1 next.
- [x] Disaster recovery drill passes.
  - Complete (MVP logical): Stage 23 B1 automated quarterly drill evidence — create → corrupt → dry-run → guarded `confirm_text=RESTORE` apply → verify proof + audits; foreign-tenant backup restore/verify/download → 404; durable artifact `/opt/cursor/artifacts/dr/stage23_b1_logical_drill.json` (`test_logical_dr_drill_b1.py`; runbook `docs/DR_LOGICAL_BACKUP_RUNBOOK.md`). Stage 23 D1 fidelity: `docs/STAGE_23_FIDELITY.md` (`test_stage23_fidelity_d1.py`). Stage 23 H23x / ADR-052 freeze recorded. Stage 26 W1: WAL/PITR strategy + offsite packaging Complete (MVP) — `docs/DR_WAL_PITR_RUNBOOK.md` (`test_wal_pitr_w1.py`). Stage 28 R1: operator PITR drill pack Complete (MVP) — `docs/PITR_DRILL_PACK_MVP.md` (`test_pitr_drill_pack_r1.py`); not live CI replay. Remaining post-MVP: operator staging infrastructure PITR drill **execution** (base + WAL replay). Stage 28 D1 fidelity: `docs/STAGE_28_FIDELITY.md` (`test_stage28_fidelity_d1.py`). Stage 29 D1 fidelity: `docs/STAGE_29_FIDELITY.md` (`test_stage29_fidelity_d1.py`).

### AI
- [x] AI provider configured securely.
  - Complete (MVP): optional external LLM provider may remain unset; rule-based chat (BR-21.1) works without a provider; deterministic inventory prediction needs no external provider. Stage 24 O1: AI provider gate Complete for MVP (`test_ops_ai_gate_closure_o1.py`). Remaining post-MVP: configure/secure external LLM provider when enabled.
- [x] Tenant-safe data access enforced.
  - Complete (MVP): AI routes use `require_permission("ai", …)` and tenant-scoped queries; insights + low-stock prediction + chat history isolation covered in tests. Stage 24 O1 (`test_ops_ai_gate_closure_o1.py`).
- [x] AI functions use real tenant data and satisfy documented acceptance criteria.
  - Complete (MVP) (Phase 4 / BR-21.1–21.10): rule-based ERP chat; NL report generator + templates; document analyze `POST /ai/documents/analyze` (OCR extract/match/discrepancies); customer assistant `POST /ai/customer/assist` + `GET /ai/customers/insights` (churn, best customers, promotions); insights + weekly digest; demand forecast / dead stock / low-stock; sales + expense analysis; security monitor. Stage 10 A1: human-confirmed OCR apply-to-draft for expenses + purchase invoices (`ocr-apply`, no silent auto-write). Stage 20 closed (ADR-046): AI Business Assistant Fidelity — `docs/STAGE_20_PLAN.md`, exit `docs/STAGE_20_EXIT_CRITERIA.md`. Evidence: `test_ai_chat_fidelity_c1.py`, `test_ai_insights_fidelity_i1.py`, `test_ai_inventory_intel_v1.py`, `test_ai_low_stock_prediction_l1.py`, `test_ai_sales_analysis_s1.py`, `test_ai_report_generator_r1.py`, `test_ai_customer_security_u1.py`. Stage 20 D1/H20x: `docs/STAGE_20_FIDELITY.md`, `test_stage20_fidelity_d1.py`, `test_stage20_exit_h20x.py`. Stage 24 O1: AI functions gate Complete for MVP (`test_ops_ai_gate_closure_o1.py`). Stage 24 D1: `docs/STAGE_24_FIDELITY.md` (`test_stage24_fidelity_d1.py`). Stage 24 H24x / ADR-054 freeze recorded. Stage 25 closed (ADR-056): Actuals → AI Analysis → Business Insights — `docs/STAGE_25_PLAN.md`, exit `docs/STAGE_25_EXIT_CRITERIA.md` (`test_stage25_exit_h25x.py`; historical open ADR-055). Stage 25 P1: purchases AI analysis `GET /ai/purchases/analysis` (`test_ai_purchases_analysis_p1.py`; BR-21.11). Stage 25 X1: cross-domain `GET /ai/cross-domain/analysis` (`test_ai_cross_domain_x1.py`; BR-21.12). Stage 25 B1: business insights four-actual surface (`test_ai_business_insights_b1.py`; BR-21.2). Stage 25 U1: AI UI purchases/cross-domain/document panels (`test_ai_ui_fidelity_u1.py`). Stage 25 D1: `docs/STAGE_25_FIDELITY.md` (`test_stage25_fidelity_d1.py`). Remaining post-MVP: Prophet/ML upgrade, optional LLM provider; PO OCR apply deferred.
- [x] AI audit logging and prompt/data protections complete.
  - Complete (Stage 5 A1): `ai_guard` max-length + injection/exfil blocks on free-text AI prompts; domain `audit.record_event` with `module=ai` and redacted prompt previews on chat / NL reports / templates / customer assist / document analyze; rejected prompts audited as `ai_prompt_rejected`. Remaining post-MVP: output PII scanner for external LLM providers, anomaly volume alerts.

## Current repository rule

Until every applicable item above is verified, Cursor and developers must describe unfinished items as `PARTIAL`, `MISSING`, or `BLOCKED` — never as complete or production-ready.
