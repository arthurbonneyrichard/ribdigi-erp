# Agent notes

## Cursor Cloud specific instructions

### Services
- **Backend** (FastAPI): typically `cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000` with `DATABASE_URL`, `REDIS_URL`, `RABBITMQ_URL` pointing at localhost in this VM.
- **Frontend** (Next.js): `cd frontend && npm run dev` (port 3000).
- **Redis + RabbitMQ + Celery worker/beat**: required for rate limits and scheduled jobs. Compose definitions live in root `docker-compose.yml`; ops sketches in `ops/celery/`.
- Standard lint/test/run commands: see root README / `backend` and `frontend` package scripts. Reliability packaging: `docs/CELERY_RELIABILITY_RUNBOOK.md`, `docs/DR_WAL_PITR_RUNBOOK.md`.

### Non-obvious gotchas
- **Disk full from stuck `ffmpeg`:** Screen recordings can leave an `ffmpeg` process holding a deleted multi‑GB file under `/opt/cursor/recording-staging/...` (space not freed until that PID exits). If `df` shows ~100% but `du -x /` is tiny, find the PID with `lsof` + `(deleted)` and `kill <pid>` (then `kill -9` if needed). Clear `/opt/cursor/recording-staging` after saves. Also wipe Chrome ML caches under `~/.config/google-chrome/OptGuideOnDeviceModel` if they balloon.
- **Next.js static chunk 404s:** If Multi-Store/Inventory buttons stay SSR-`disabled` despite filled inputs, check DevTools for `/_next/static/chunks/*.js` **404** (React never hydrates). Restart `cd frontend && npm run dev`.
- **Next.js stale / no live updates:** A long-lived `next dev` (esp. orphan under pid 1) can stop reflecting edits in the browser. `frontend/next.config.mjs` enables webpack **poll** watching for cloud VMs. If the UI still looks stale: stop the old process, `rm -rf frontend/.next`, then `cd frontend && WATCHPACK_POLLING=true npm run dev`, and hard-refresh the tab (Ctrl/Cmd+Shift+R).
- **Cloud VM infra:** Docker is often unavailable here. Use host **Postgres / Redis / RabbitMQ** with `.env` URLs pointed at `localhost` (not compose hostnames). Start API with `cd backend && PYTHONPATH=. .venv/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 8000`, frontend with `cd frontend && npm run dev`, Celery worker/beat from `backend` against the same `.env`.
- **POS store↔warehouse uniqueness:** `stores.warehouse_for_store` uses `scalar_one_or_none`. If a store (e.g. idle2021 **CF Live A**) has two warehouses with the same `store_id`, POS sales 500 with `MultipleResultsFound`. Unlink the duplicate warehouse’s `store_id` (or delete it) before demo sales.
- **Sidebar default logo:** Shell brand area uses uploaded `GET /tenants/me/logo` when `has_logo`; otherwise falls back to transparent **writing/wordmark** SVG `/brand/logo-sidebar.svg` (RIBDIGI | ERP + tagline; no RD mark, not the white-sticker PNG).
- **Migrate after pull:** live Postgres can lag feature migrations. From `backend`: `PYTHONPATH=. .venv/bin/alembic upgrade head` (export `DATABASE_URL` from `.env` carefully — quote values with spaces like `WEBAUTHN_RP_NAME="RIBDIGI ERP"` before `source .env`).
- **Login 2FA:** `LOGIN_2FA_ENABLED` (default `false`) controls whether enrolled TOTP/passkeys are challenged at `/auth/login` and whether `must_enroll_2fa` blocks APIs. Set `true` in production (see `.env.production.example`). Setup endpoints under `/auth/2fa` and `/auth/webauthn` remain available either way.
- **Passkey name OpenAPI (BR-19 / WebAuthn):** `WebAuthnRegisterVerify.name` ∈ `PasskeyNameValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`); omit/`null` → service default `"Passkey"`; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently became `"Passkey"` via strip-or-default; punctuation/URL could persist on `WebAuthnCredential.name` String(120)). Security **Passkey name** input (`aria-label`); register sends `null` when blank. Service `(name or "").strip() or "Passkey"` remains defense-in-depth for omit/`null`.
- **2FA code OpenAPI (BR-19):** `TwoFactorConfirm` / `TwoFactorVerify` / `TwoFactorDisable.code` + `Login.totp_code` ∈ `TwoFactorCodeValue` (strip; 4–64; ≥1 letter/digit; no `://`/`@`); confirm/verify/disable required; login omit/`null` → no TOTP field; blank/`!!!`/`http://…`/`abc` → **422** (was free `str`; empty/garbage reached service verify). Security **2FA setup code** / **2FA code** + Login **2FA code** inputs (`aria-label`s); Login/Security sends trim (`totpCode.trim()` / `code.trim()`; login `totp_code: totpCode.trim() || null`). Service `verify_totp` / `hash_backup_code` remain defense-in-depth for authenticity.
- **Celery does not auto-reload.** After changing `backend/app/jobs.py`, `tasks.py`, or beat schedule, restart `celery_worker` and `celery_beat` or workers keep stale handler maps / crash on unknown jobs.
- **`run_async` uses one event loop per worker process** so sequential Celery tasks do not hit async SQLAlchemy “Future attached to a different loop” errors. Do not switch back to bare `asyncio.run()` per task without disposing the engine.
- Deep readiness: `GET /api/v1/health/ready` (and `?deep=true`) probes DB + Redis + Celery broker; shallow `/health` stays liveness-only.
- Admin job triggers: `GET /api/v1/jobs` (company_admin / super_admin / platform_owner) and `POST /jobs/{name}/run` (**super_admin** / **platform_owner** only). UI: Shell **Jobs** (`/jobs`). Pass `X-Tenant-ID` as the JWT `tenant_id` UUID (`UuidIdValue`; blank/slug/`!!!`/`http://…` → **422**; UUID mismatch vs JWT → cross-tenant **403**).
- **Jobs Path job_name OpenAPI (BR-Celery):** `POST /jobs/{job_name}/run` Path `job_name` ∈ `JOB_HANDLERS` keys (`JobNameValue`); blank/unknown → **422** (was late **404**). Allow-list defense-in-depth on handler. Jobs **Run sync** / **Enqueue** (`aria-label`s).
- **Onboarding Path step_id OpenAPI:** `POST /onboarding/checklist/steps/{step_id}/skip|unskip` Path `step_id` ∈ setup_company|add_products|create_supplier|stock_ready|first_sale (`OnboardingStepIdValue`); blank/unknown → **422** (was late service **400**). Service `VALID_STEP_IDS` remains defense-in-depth. Shell Getting started **Skip** / **Undo skip** (`aria-label`s).
- **Platform subscriptions:** `GET /packages`, `POST /tenants/{slug}/subscription` (package + months/years), `PATCH /tenants/{slug}/modules`, `GET /tenants/{slug}/usage`. Sidebar hides modules not in `enabled_modules` from `/me`. Run Alembic `20260812_0070` after pull.
- **Subscription-based store limits:** Company == Tenant in this product (no child Company table). Package catalog `max_stores` (`NULL`=unlimited) + tenant `max_stores_override` (platform) + tenant `store_limit` (company_admin allocation ≤ entitlement). Enforce on `POST /stores` and activate (`PATCH is_active=true`) via `store_entitlements` (active count; tenant `FOR UPDATE`). Downgrade never deletes stores; sets `over_entitlement`. APIs: `GET /stores/entitlement`, `PATCH /tenants/me/store-limit`, `PATCH /tenants/{ref}/store-entitlement`. Migration `20260816_0104`. UI: Multi-Store usage banner + Platform override.
- **Sales UI needs `stores` module:** Sales page `storeContext` calls `GET /stores` on load; if `stores` is not in the tenant `enabled_modules`, the page surfaces that error and lists (invoices/returns) stay empty. For idle2021 starter demos, enable `stores` (and usually `credit` for Credit page, `reports` for Reports/export) via platform `PATCH /tenants/{id}/modules`.
- **Subscription term_unit OpenAPI:** `TenantSubscriptionAssign.term_unit` ∈ months|years (`Literal`); omit → `months`; blank/invalid → **422** (no silent months from `""`). Service `assign_subscription` remains defense-in-depth. Platform **Subscription term unit** select (`aria-label`).
- **Subscription start_at OpenAPI:** `TenantSubscriptionAssign.start_at` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit/`null` → now; blank/`not-a-date`/`01/02/2024` → **422** (was free `datetime`; OpenAPI date-time; padded dates inconsistent). API `reports.parse_date` remains defense-in-depth. Platform **Subscription start date** input (`aria-label`); assign omits blank.
- **Journal entry_date OpenAPI (BR-10.2):** `JournalCreate.entry_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit/`null` → now; blank/`not-a-date`/`01/02/2024` → **422** (was free `date`; OpenAPI date; padded dates inconsistent). API `reports.parse_date` remains defense-in-depth. Accounting **Journal entry date** input (`aria-label`); create sends `null` when blank.
- **Journal description OpenAPI (BR-10.2):** `JournalCreate.description` ∈ `JournalDescriptionValue` (strip; 2–500; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422** (was free `str`; empty/garbage could persist on the ledger). Accounting **Journal description** input (`aria-label`).
- **Journal line description OpenAPI (BR-10.2):** `JournalLineCreate.description` ∈ `JournalLineDescriptionValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no line narrative; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on `JournalEntryLine.description`). Accounting Manual journal **Journal line N description** inputs (`aria-label`); post sends `null` when blank.
- **Journal reference OpenAPI (BR-10.2):** `JournalCreate.reference` ∈ `JournalReferenceValue` (strip; 1–100; ≥1 letter/digit; no `://`/`@`); omit/`null` → no reference; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently dropped / garbage could persist on `JournalEntry.reference` String(100)). Accounting **Journal reference** input (`aria-label`); Post sends `null` when blank.
- **Period through_date OpenAPI (BR-10.2):** `PeriodCloseBody.through_date` (required) + `PeriodReopenBody.through_date` (optional) ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); close blank/invalid → **422**; reopen omit/`null` → clear closed-through; blank/invalid → **422** (was bare `date`; OpenAPI date; padded dates inconsistent). API `reports.parse_date` + `as_calendar_date` defense-in-depth. Accounting **Period close through date** input (`aria-label`).
- **Subscription package_code OpenAPI:** `TenantSubscriptionAssign.package_code` ∈ trial|starter|professional|enterprise (`Literal` + strip/lower); blank/invalid → **422**. Service `VALID_PACKAGE_CODES` remains defense-in-depth. Platform **Subscription package** select (`aria-label`).
- **Enabled modules OpenAPI:** `TenantSubscriptionAssign` / `TenantModulesUpdate.enabled_modules` items ∈ `PACKAGEABLE_MODULES` (`Literal` + strip/lower); blank/unknown/`platform` → **422**. Service still merges always-on + defense-in-depth. Platform Feature modules checkboxes.
- **Backup frequency OpenAPI (BR-16):** `BackupSettingsUpdate.frequency` ∈ daily|weekly (`Literal` + strip/lower); omit = no change; blank/invalid → **422**. Service `update_settings` remains defense-in-depth. Backup **Backup frequency** select (`aria-label`).
- **Backup create/verify/restore bodies OpenAPI (BR-16):** `POST /backup` `BackupCreateBody` (`notes` ∈ `BackupNotesValue` — strip; 1–500; ≥1 letter/digit; no `://`/`@`; omit/`null` → no notes; blank/`!!!`/`http://…` → **422** — was free `str` max_length=500), `POST /backup/{id}/verify` `BackupVerifyBody` (`sample_limit` 1–500), `POST /backup/{id}/restore` `BackupRestoreBody` (`extra=forbid` each). Unknown keys → **422**. Destructive apply requires `confirm_text="RESTORE"` at schema (**422**; was late route **400**). `sample_limit` out of range → **422** (was silent clamp). Backup UI **Backup notes** + Create / Dry-run / Restore aria-labels; Create sends `null` when blank.
- **Backup job status Query OpenAPI (BR-16):** `GET /backup` Query `status` ∈ pending|completed|failed|restoring (`Literal` + strip/lower); omit → all; blank/invalid → **422**. Service `list_backups` remains defense-in-depth. Backup **Backup job status filter** (`backupManageFilter`; client filter over full list cache).
- **Report schedule frequency/format/report_type OpenAPI (BR-14):** `ReportScheduleCreate` / `Update.frequency` ∈ daily|weekly; `format` ∈ csv|pdf|xlsx; `report_type` ∈ `EXPORTABLE` (`Literal` + strip/lower). Create omit → daily/xlsx; blank/invalid → **422** (no silent daily from `""`). Service remains defense-in-depth. Reports Email schedules **Report schedule report type** / **Report schedule format** / **Report schedule frequency** selects (`aria-label`s; aligned with `EXPORTABLE`).
- **Report schedule recipients OpenAPI (BR-14):** `ReportScheduleCreate.recipients` (required) + `Update.recipients` (optional) ∈ `ReportScheduleRecipientsValue` (`list[EmailStr]` or comma/`;` string); blank/`bad`/`ops@x.com, bad` → **422**; update omit/`null` → no change (was free `list[str]|str`; service soft-dropped non-`@` then late **400** if empty). Service `_normalize_recipients` remains defense-in-depth. Reports **Report schedule recipients** input (`aria-label`).
- **Report schedule name OpenAPI (BR-14):** `ReportScheduleCreate.name` (required) + `Update.name` (optional) ∈ `ReportScheduleNameValue` (strip; 2–120; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str` min_length=2; whitespace late **400**; punctuation/URL could persist). Service strip remains defense-in-depth. Reports **Report schedule name** input (`aria-label`).
- **Document numbering prefix OpenAPI (BR-20.4):** `DocumentNumberingFields.prefix` (+ legacy sales flat `prefix`) ∈ `DocumentPrefixValue` (strip + upper; `^[A-Za-z0-9][A-Za-z0-9_-]{0,19}$`); blank/`!!!`/`JE!`/`a b` → **422** (was free `str` min_length=1; service `normalize_prefix` late **400**). Nested `DocumentNumberingFields` `extra=forbid` (unknown keys → **422**). Shared by accounting/sales/purchasing/inventory/expenses/POS settings numbering. Inputs (`aria-label`s): Accounting **Journal number prefix** / **Cash transfer number prefix**; Sales **Invoice number prefix** / **Quotation number prefix** / **Sales order number prefix** / **Sales return number prefix** / **Credit note number prefix** / **Payment receipt number prefix**; Purchasing **Purchase order number prefix** / **GRN number prefix** / **Purchase invoice number prefix** / **Purchase request number prefix** / **Purchase return number prefix** / **Debit note number prefix** / **Supplier payment number prefix**; Expenses **Expense number prefix**; Inventory **Stock transfer number prefix** / **Stock count number prefix** / **Opening stock number prefix**; POS **POS sale number prefix** / **POS session number prefix** (matching next-number labels).
- **Report schedule list Query OpenAPI (BR-14):** `GET /reports/schedules` Query `enabled` ∈ true|false (omit → all); Query `frequency` ∈ daily|weekly (`ScheduleFrequencyValue`); blank/invalid frequency → **422**. Service `list_schedules` remains defense-in-depth. Reports Email schedules **enabled** / **frequency** filters (`scheduleManageFilter` / `scheduleFrequencyFilter`; client filter over full list cache).
- **Report export query OpenAPI (BR-14):** `GET /reports/export` `report_type` ∈ `EXPORTABLE`; `format` ∈ csv|pdf|xlsx (`Literal` + strip/lower). Format omit → `csv`; blank/unknown → **422** (no silent csv from `""`). Service `export_report` / `build_report_payload` remain defense-in-depth. Reports Export CSV/Excel/PDF buttons.
- **Balance sheet compare OpenAPI (BR-14.5):** Query `compare` on `/reports/balance-sheet`, `/accounting/balance-sheet`, and `/reports/export` ∈ prior_period|prior_year (`Literal` + strip/lower). Omit → no compare; blank/invalid → **422**. Service `balance_sheet` remains defense-in-depth. Reports **Balance sheet compare** select (`aria-label`).
- **Bank recon auto-clear confidence OpenAPI (BR-10.3):** `BankAutoClearBody.min_confidence` ∈ high|medium|low (`Literal` + strip/lower); omit → `high`; blank/invalid → **422** (no silent high from `""`/garbage). Service `apply_auto_matches` remains defense-in-depth. Accounting Reconcile Auto-clear buttons.
- **Bank recon match/clear-group OpenAPI (BR-10.3):** `POST .../lines/{line_id}/match` body `BankStatementMatchBody` (`extra=forbid`; required `journal_line_id` ∈ `UuidIdValue` — blank/`!!!`/`http://…`/non-UUID → **422**; was free `str` `min_length=1` / late **404**) and `POST .../clear-group` body `BankClearGroupBody` (`extra=forbid`; non-empty `statement_line_ids` + `journal_line_ids` ∈ `list[UuidIdValue]` — blank/`!!!`/`http://…`/non-UUID element → **422**; was free `list[str]` with blank-strip only). Unknown keys / blank ids / empty lists → **422** (was free `dict` with late 404/400). Service `match_line` / `create_clearing_group` remain defense-in-depth. Accounting Reconcile **Match bank line to journal line** / Clear group controls; Match sends `journal_line_id` trim; Clear group sends id lists trimmed.
- **Bank clear-group statement_line_ids OpenAPI (BR-10.3):** `BankClearGroupBody.statement_line_ids` ∈ `list[UuidIdValue]` (min 1 after blank-strip); blank/`!!!`/`http://…`/non-UUID element → **422** (was free `list[str]`). Existence remains tenant-scoped (**404**/400). Accounting Clear group sends trim.
- **Bank clear-group journal_line_ids OpenAPI (BR-10.3):** `BankClearGroupBody.journal_line_ids` ∈ `list[UuidIdValue]` (min 1 after blank-strip); blank/`!!!`/`http://…`/non-UUID element → **422** (was free `list[str]`). Same honesty as statement_line_ids; Accounting Clear group sends trim.
- **Bank statement status Query OpenAPI (BR-10.3):** `GET /accounting/bank-statements` Query `status` ∈ draft|in_progress|reconciled (`Literal` + strip/lower); omit → all; blank/invalid → **422**. Service `list_statements` remains defense-in-depth. Accounting Reconcile **Bank statement status filter** (`statementManageFilter`; client filter over full list cache).
- **Bank statement create body OpenAPI (BR-10.3):** `POST /accounting/bank-statements` body typed `BankStatementCreateBody` (`extra=forbid`; required `account_id` ∈ `UuidIdValue`; optional balances/notes/`lines` of `BankStatementLineCreate`). Unknown keys / blank/non-UUID `account_id` / zero line amount → **422** (was free `dict` with late 404/400). Service `create_statement` remains defense-in-depth. Accounting Reconcile create controls (`aria-label` account/opening/closing/line/create).
- **Bank statement date OpenAPI (BR-10.3):** `POST /accounting/bank-statements` body `statement_date` + line `txn_date`, and import Query `statement_date`, ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → today / line default / import max txn; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent default; invalid was uncaught **500** via `_parse_dt`). Accounting **Statement date** / **Statement line txn date** inputs (`aria-label`s).
- **Bank statement line description OpenAPI (BR-10.3):** `BankStatementLineCreate.description` ∈ `BankStatementLineDescriptionValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no description; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently dropped via strip-to-None / garbage could persist). Accounting Reconcile **Statement line description** input (`aria-label`); create sends `null` when blank.
- **Bank statement line external_ref OpenAPI (BR-10.3):** `BankStatementLineCreate.external_ref` ∈ `BankStatementLineExternalRefValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`); omit/`null` → no ref; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently dropped via strip-to-None / garbage could persist). Accounting Reconcile **Statement line external ref** input (`aria-label`); create sends `null` when blank.
- **Bank statement notes OpenAPI (BR-10.3):** `BankStatementCreateBody.notes` ∈ `BankStatementNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently dropped via strip-to-None / garbage could persist). Accounting Reconcile **Statement notes** input (`aria-label`); create sends `null` when blank (no longer dual-uses line description).
- **Bank clear-group notes OpenAPI (BR-10.3):** `BankClearGroupBody.notes` ∈ `BankClearGroupNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on clearing group). Accounting Reconcile **Clear-group notes** input (`aria-label`); clear sends `null` when blank.
- **AI document draft date OpenAPI (BR-21.8):** `POST /ai/documents/create-expense` body `expense_date` + `POST /ai/documents/create-purchase-invoice` body `invoice_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → service default; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent default; invalid was late service **400** via `_parse_expense_date` / `_parse_invoice_date`). Bodies `extra=forbid`. AI **AI document draft date** input (`aria-label`).
- **AI document expense category_id OpenAPI (BR-21.8 / BR-9.2):** `AiDocumentExpenseCreate.category_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → label-only / MISC path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach category lookup). Existence remains tenant-scoped expense-category lookup (**404**/400). Same honesty as `ExpenseCreate.category_id`. AI **AI document expense category** select (`aria-label`); Create draft expense sends trim or `null` when blank (prefilled from Analyze suggestion).
- **AI document expense store_id OpenAPI (BR-21.8 / BR-9.2):** `AiDocumentExpenseCreate.store_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no store; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach store lookup). Existence remains tenant-scoped store lookup (**404**). Same honesty as `ExpenseCreate.store_id`. AI **AI document expense store** select (`aria-label`); Create draft expense sends trim or `null` when blank.
- **AI document expense branch_id OpenAPI (BR-21.8 / BR-9.2):** `AiDocumentExpenseCreate.branch_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no branch; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach branch lookup). Existence remains tenant-scoped branch lookup (**404**). Same honesty as `ExpenseCreate.branch_id`. AI **AI document expense branch** select (`aria-label`); Create draft expense sends trim or `null` when blank.
- **AI document expense department_id OpenAPI (BR-21.8 / BR-9.2):** `AiDocumentExpenseCreate.department_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no department; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach department lookup). Existence remains tenant-scoped department lookup (**404**). Same honesty as `ExpenseCreate.department_id`. AI **AI document expense department** select (`aria-label`); Create draft expense sends trim or `null` when blank.
- **AI document purchase_order_id OpenAPI (BR-21.8 / BR-6.4):** `AiDocumentPurchaseInvoiceCreate.purchase_order_id` ∈ `UuidIdValue` (strip; lower; valid UUID; **required**); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach PO lookup). Existence remains tenant-scoped purchase-order lookup (**404**). Same honesty as `GrnCreate.purchase_order_id`. AI **AI document purchase order** select (`aria-label`; prefilled from Analyze PO matches); Create draft purchase invoice sends trim.
- **AI document supplier_id OpenAPI (BR-21.8):** `AiDocumentPurchaseInvoiceCreate.supplier_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → PO's supplier; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach party lookup). Service still requires supplier match the PO (**400**). AI **AI document supplier** select (`aria-label`; prefilled from matched PO); Create draft purchase invoice sends trim or `null` when blank.
- **Document email send Query `to` OpenAPI (BR-7.4 / BR-7.2 / BR-6.3):** `POST /sales/invoices/{id}/send`, `POST /sales/quotations/{id}/send`, `POST /purchasing/orders/{id}/send` Query `to` ∈ `EmailStr`; omit → customer/supplier email; blank/`not-an-email` → **422** (blank was silent fallthrough; garbage was accepted). `PurchaseOrderAmend.to` same when notifying. Sales/Purchasing **Document / PO email override to** inputs (`aria-label`s).
- **Company from_email OpenAPI (BR-20.3):** `PATCH /settings/email` body `from_email` ∈ `EmailStr`; omit/`null` → no change; blank/`not-an-email` → **422** (was free `str`; blank/garbage accepted into tenant SMTP). Body `extra=forbid`. Company **Company from email** input (`aria-label`).
- **Company SMTP host OpenAPI (BR-20.3):** `PATCH /settings/email` body `host` ∈ `SmtpHostValue` (strip/lower; DNS hostname / IPv4 / `localhost`; no `://` / `@` / spaces); omit/`null` → no change; blank/`http://smtp…`/`not a host` → **422** (was free `str`; blank/garbage accepted into tenant SMTP). Body `extra=forbid`. Company **Company SMTP host** input (`aria-label`); Save omits blank host.
- **Company SMTP from_name OpenAPI (BR-20.3):** `PATCH /settings/email` body `from_name` ∈ `SmtpFromNameValue` (strip; 1–120 chars; at least one letter/digit; no `://` / `@`); omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage accepted into tenant SMTP From display name). Body `extra=forbid`. Company **Company from name** input (`aria-label`); Save omits blank from_name.
- **Company SMTP username OpenAPI (BR-20.3):** `PATCH /settings/email` body `username` ∈ `SmtpUsernameValue` (strip; 1–200 chars; at least one letter/digit; no `://`; email-shaped logins OK); omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage accepted into tenant SMTP username). Body `extra=forbid`. Company **Company SMTP username** input (`aria-label`); Save omits blank username.
- **Company SMTP password OpenAPI (BR-20.3):** `PATCH /settings/email` body `password` ∈ `SmtpPasswordValue` (strip; 1–128; ≥1 letter/digit; no `://` / `@` / spaces); omit/`null` → keep prior encrypted password; blank/`!!!`/`http://…` → **422** (was free `str`; blank was a silent no-op via service; punctuation/URL could be encrypted into `email_settings.password_enc`). Body `extra=forbid`. `clear_password: true` still clears. Company **Company SMTP password** input (`aria-label`); Save omits blank password.
- **Company SMS from_number OpenAPI (BR-15.2):** `PATCH /settings/sms` body `from_number` ∈ `E164PhoneValue` (`+` + 8–15 digits); omit/`null` → no change; blank/`not-a-phone`/`123` → **422** (was free `str`; blank/garbage accepted into tenant Twilio). Body `extra=forbid`. `SmsTestRequest.to` same. Company **Company SMS from number** input (`aria-label`).
- **Company SMS account_sid OpenAPI (BR-15.2):** `PATCH /settings/sms` body `account_sid` ∈ `TwilioAccountSidValue` (strip; alphanumeric 1–64; not strict `AC`+32hex); omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage accepted into tenant Twilio SID). Body `extra=forbid`. Company **Company SMS account SID** input (`aria-label`); Save omits blank SID.
- **Company SMS auth_token OpenAPI (BR-15.2):** `PATCH /settings/sms` body `auth_token` ∈ `TwilioAuthTokenValue` (strip; 1–128; ≥1 letter/digit; no `://` / `@` / spaces); omit/`null` → keep prior encrypted token; blank/`!!!`/`http://…` → **422** (was free `str`; blank was a silent no-op via service; punctuation/URL could be encrypted into `sms_settings.auth_token_enc`). Body `extra=forbid`. `clear_auth_token: true` still clears. Company **Company SMS auth token** input (`aria-label`); Save omits blank token.
- **Profile phone OpenAPI (BR-15.2 / BR-19):** `PATCH /me` body `phone` ∈ `E164PhoneValue` (`+` + 8–15 digits); omit/`null` → no change; blank/`not-a-phone`/`123` → **422** (was free `str`; blank silently cleared phone; garbage was late **400** via `normalize_phone`). Company **Profile phone for SMS test** + **Save my profile** (`aria-label`s); Save omits blank phone (same as full_name).
- **Profile full name OpenAPI (BR-19):** `PATCH /me` body `full_name` ∈ `UserFullNameValue` (`ProfileUpdate`; strip; 1–150; ≥1 letter/digit; no `://`/`@`); omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank reached service **400**; punctuation/URL could persist on `User.full_name`). Same Value type as `UserCreate` / `UserUpdate`. Company **Profile full name** + **Save my profile** (`aria-label`s); Save omits blank name.
- **Batch number OpenAPI (BR-5.2 / BR-6.4):** `StockMove` + `OpeningStockLine` + `GrnItemCreate.batch_number` ∈ `BatchNumberValue` (strip; 1–80; ≥1 letter/digit; no `://`/`@`); omit/`null` → no lot (service still requires a lot when `product.tracks_batches`); blank/`!!!`/`http://…` → **422** (was free `str`; blank silently stripped to None / punctuation/URL could persist on `ProductBatch.batch_number`). Inventory **Stock-in batch number** + **Opening stock batch number** + Purchasing **GRN batch number** inputs (`aria-label`s); stock-in trims; opening/GRN omit blank.
- **Journal line account code OpenAPI (BR-10.2 / BR-10.1):** `JournalLineCreate` + `OpeningBalanceLine.account_code` ∈ `AccountCodeValue` (strip; 1–30; `^[A-Za-z0-9][A-Za-z0-9_-]{0,29}$`); omit/`null` OK when `account_id` set; blank/`!!!`/`a b`/`http://…` → **422** (was free `str`; blank reached service **400**; malformed codes were late **404** Account not found). Same Value type as `AccountCreate.code`. Manual journal / opening lines may still send `account_code` via API when `account_id` omitted.
- **Journal line account_id OpenAPI (BR-10.2):** `JournalLineCreate.account_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` OK when `account_code` set; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach COA lookup). Existence remains tenant-scoped account lookup (**404**). Accounting Manual journal **Journal line N account** select (`aria-label`); Post balanced entry sends `account_id` trim or `null` when blank.
- **Opening balance account_id OpenAPI (BR-10.1):** `OpeningBalanceLine.account_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` OK when `account_code` set; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach COA lookup). Existence remains tenant-scoped account lookup (**404**). Accounting Ledger **Opening balance account** select (`aria-label`); Post opening balances sends `account_id` trim.
- **Product SKU OpenAPI (BR-5.1):** `ProductCreate` / `Update` + `ProductVariantCreate` / `Update.sku` ∈ `ProductSkuValue` (strip + upper; `^[A-Za-z0-9][A-Za-z0-9._-]{0,99}$` aligned with `catalog.normalize_sku`); create omit/`null` → auto `SKU-YYYY-NNNN`; PATCH omit/`null` → no change; blank/`!!!`/`a b`/`http://…` → **422** (was free `str`; create blank also auto-allocated; PATCH blank silently ignored / variant blank late **400**; garbage late service **400**). Inventory **Product SKU** + **Variant SKU** inputs (`aria-label`s); create sends `null` when blank. Service `normalize_sku` + uniqueness **409** remain defense-in-depth.
- **Company phone OpenAPI:** `PATCH /tenants/me` body `phone` ∈ `E164PhoneValue` (`+` + 8–15 digits); omit/`null` → no change; blank/`not-a-phone`/`123` → **422** (was free `str`; blank silently cleared; garbage could persist). Company **Company phone** input (`aria-label`); Save omits blank phone.
- **Company website OpenAPI:** `PATCH /tenants/me` body `website` ∈ `WebhookUrlValue` (strip; absolute `http(s)` + netloc; `http` only for localhost/127.0.0.1/testserver/host.docker.internal); omit/`null` → no change; blank/`ftp://`/`not-a-url`/`www.x`/plain-http remote → **422** (was free `str`; blank silently cleared; garbage could persist). Company **Company website** input (`aria-label`); Save omits blank website.
- **Company tax_registration_number OpenAPI (BR-20.1):** `PATCH /tenants/me` body `tax_registration_number` ∈ `TaxRegistrationNumberValue` (strip; alphanumeric + optional spaces/hyphens; max 40); omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently cleared TIN; garbage could persist). Company **TIN / VAT registration number** input (`aria-label`); Save omits blank TIN.
- **Company registration_number OpenAPI (BR-20.1):** `PATCH /tenants/me` body `registration_number` ∈ `RegistrationNumberValue` (strip; alphanumeric + optional spaces/hyphens; max 80); omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently cleared; garbage could persist; length>80 was late **400**). Company **Company registration number** input (`aria-label`); Save omits blank.
- **Company contact_person OpenAPI (BR-20.1):** `PATCH /tenants/me` body `contact_person` ∈ `ContactPersonValue` (strip; 1–150 chars; at least one letter/digit; no `://` / `@`); omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently cleared; garbage could persist; length>150 was late **400**). Company **Company contact person** input (`aria-label`); Save omits blank.
- **Company legal_name OpenAPI (BR-20.1):** `PATCH /tenants/me` body `legal_name` ∈ `LegalNameValue` (strip; 2–200 chars; at least one letter/digit; no `://` / `@`); omit/`null` → no change; blank/`!!!`/`http://…`/`X` → **422** (was free `str`; blank silently cleared; garbage could persist; len<2 or >200 was late **400**). Company **Company legal name** input (`aria-label`); Save omits blank.
- **Company company_name OpenAPI (BR-20.1):** `POST /tenants` + `PATCH /tenants/me` body `company_name` ∈ `CompanyNameValue` (strip; 2–200 chars; at least one letter/digit; no `://` / `@`); create required; PATCH omit/`null` → no change; blank/`!!!`/`http://…`/`X` → **422** (was free `str`; create unchecked; PATCH blank/`X` late **400**; garbage could persist). Company **Company trading name** input (`aria-label`); Save sends trim (required).
- **Tenant slug OpenAPI (BR-1):** `TenantCreate.slug` ∈ `TenantSlugValue` (strip/lower; 2–80; `^[a-z0-9][a-z0-9-]{1,79}$`); blank/`!!!`/`http://…`/`a b`/`X`/`-bad` → **422** (was free `str`; blank/punctuation/URL/uppercase-with-symbols could persist on `Tenant.slug` String(80)). Uniqueness remains create_tenant **409**. Platform console **Tenant slug** input (`aria-label`; create sends trim/lower).
- **Company address OpenAPI (BR-20.1):** `PATCH /tenants/me` body `address` / `billing_address` / `shipping_address` ∈ `AddressValue` (strip; 1–500 chars; at least one letter/digit; no `://` / `@`); omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently cleared; garbage could persist). Company **Company headquarters address** / **Company billing address** / **Company shipping address** textareas (`aria-label`s); Save omits blank.
- **Store address OpenAPI (BR-2.3):** `StoreCreate` / `StoreUpdate.address` ∈ `AddressValue` (strip; 1–500 chars; at least one letter/digit; no `://` / `@`); create omit/`null` → no address; PATCH omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently cleared on PATCH; garbage could persist). Multi-Store **Store address** input (`aria-label`); create/edit omit blank.
- **Branch address OpenAPI (BR-2.2):** `BranchCreate` / `BranchUpdate.address` ∈ `AddressValue` (strip; 1–500 chars; at least one letter/digit; no `://` / `@`); create omit/`null` → no address; PATCH omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently cleared on PATCH; garbage could persist). Multi-Store **Branch address** input (`aria-label`); create/edit omit blank.
- **Warehouse address OpenAPI (BR-2.4):** `WarehouseCreate` / `WarehouseUpdate.address` ∈ `AddressValue` (strip; 1–500 chars; at least one letter/digit; no `://` / `@`); create omit/`null` → no address; PATCH omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently cleared on PATCH; garbage could persist). Multi-Store **Warehouse address** input (`aria-label`); create/edit omit blank.
- **User phone OpenAPI (BR-3):** `UserCreate` / `UserUpdate.phone` ∈ `E164PhoneValue` (`+` + 8–15 digits); create omit/`null` → no phone; PATCH omit/`null` → no change; blank/`not-a-phone`/`123` → **422** (was free `str`; blank silently cleared on PATCH; garbage could persist). Users **User phone** input (`aria-label`); create sends `phone: form.phone.trim() || null`.
- **User create branch_id OpenAPI (BR-3):** `UserCreate.branch_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no branch; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach branch lookup). Existence remains tenant-scoped branch lookup (**404**). Users **User branch** select (`aria-label`); Create user sends trim or `null` when blank.
- **User create department_id OpenAPI (BR-3):** `UserCreate.department_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no department; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach department lookup). Existence remains tenant-scoped department lookup (**404**). Users **User department** select (`aria-label`); Create user sends trim or `null` when blank.
- **User update branch_id OpenAPI (BR-3):** `UserUpdate.branch_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach branch lookup). `clear_branch` remains explicit clear. Existence remains tenant-scoped branch lookup (**404**). Users row **Edit user branch** select (`aria-label`); assignment sends trim.
- **User update department_id OpenAPI (BR-3):** `UserUpdate.department_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach department lookup). `clear_department` remains explicit clear. Existence remains tenant-scoped department lookup (**404**). Users row **Edit user department** select (`aria-label`); assignment sends trim.
- **User full name OpenAPI (BR-3):** `UserCreate.full_name` (required) + `UserUpdate.full_name` (optional) ∈ `UserFullNameValue` (strip; 1–150; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; empty/whitespace/`!!!`/URL could persist). Users **User full name** input (`aria-label`); create requires trim.
- **Branch phone OpenAPI (BR-2.2):** `BranchCreate` / `BranchUpdate.phone` ∈ `E164PhoneValue` (`+` + 8–15 digits); create omit/`null` → no phone; PATCH omit/`null` → no change; blank/`not-a-phone`/`123` → **422** (was free `str`; blank silently cleared on PATCH; garbage could persist). Multi-Store **Branch phone** input (`aria-label`); create sends `null` when blank; edit omits blank phone.
- **Store phone OpenAPI (BR-2.3):** `StoreCreate` / `StoreUpdate.phone` ∈ `E164PhoneValue` (`+` + 8–15 digits); create omit/`null` → no phone; PATCH omit/`null` → no change; blank/`not-a-phone`/`123` → **422** (was free `str`; blank silently cleared on PATCH; garbage could persist). Multi-Store **Store phone** input (`aria-label`); create sends `null` when blank; edit omits blank phone.
- **Cash drawer host OpenAPI (BR-8.1):** `StoreDrawerSettingsUpdate.drawer_host` ∈ `SmtpHostValue` (strip/lower; DNS hostname / IPv4 / `localhost`; no `://` / `@` / spaces); omit/`null` OK; blank/`http://…`/`not a host` → **422** (was free `str`; blank silent→null; garbage could persist). Service still requires host for `network` mode (**400**). Multi-Store **Cash drawer host** input (`aria-label`; network mode); Save sends `null` when blank.
- **Party phone OpenAPI (BR-6.1 / BR-7.1):** `PartyCreate` / `PartyUpdate.phone` ∈ `E164PhoneValue` (`+` + 8–15 digits) for `POST|PATCH /customers` and `/suppliers`; create omit/`null` → no phone; PATCH omit/`null` → no change; blank/`not-a-phone`/`123` → **422** (was free `str`; blank/garbage could persist). Sales **Customer phone** + Purchasing **Supplier phone** inputs (`aria-label`s); create sends `null` when blank.
- **Party name OpenAPI (BR-6.1 / BR-7.1):** `PartyCreate.name` (required) + `PartyUpdate.name` (optional) ∈ `PartyNameValue` (strip; 1–180; ≥1 letter/digit; no `://`/`@`) for `POST|PATCH /customers` and `/suppliers`; blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist). Sales **Customer name** + Purchasing **Supplier name** inputs (`aria-label`s).
- **Party category OpenAPI (BR-6.1 / BR-7.1):** `PartyCreate` / `PartyUpdate.category` ∈ `PartyCategoryValue` (strip; 1–80; ≥1 letter/digit; no `://`/`@`) for `POST|PATCH /customers` and `/suppliers`; create omit/`null` → no category; PATCH omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently cleared via `_normalize_party_profile`; punctuation/URL could persist on `Party.category`). Sales **Customer category** + Purchasing **Supplier category** inputs (`aria-label`s); create sends `null` when blank.
- **Party code OpenAPI (BR-6.1 / BR-7.1):** `PartyCreate` / `PartyUpdate.code` ∈ `PartyCodeValue` (strip; 1–64; ≥1 letter/digit; no `://`/`@`) for `POST|PATCH /customers` and `/suppliers`; create omit/`null` → no code; PATCH omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently cleared via `_normalize_party_profile`; punctuation/URL could persist on `Party.code`). Tenant uniqueness remains `_ensure_party_code_unique`. Sales **Customer code** + Purchasing **Supplier code** inputs (`aria-label`s); create sends `null` when blank.
- **Product name OpenAPI (BR-5.1):** `ProductCreate.name` (required) + `ProductUpdate.name` (optional) ∈ `ProductNameValue` (strip; 1–200; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist on catalog). Inventory **Product name** input (`aria-label`).
- **Product category label OpenAPI (BR-5.1):** `ProductCreate.category` (default `"General"`) + `ProductUpdate.category` (optional) ∈ `ProductCategoryLabelValue` (strip; 1–100; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank silently fell through to `"General"` in `resolve_product_refs`; punctuation/URL could persist on `Product.category`). Prefer `category_id` (catalog FK); when set, service overwrites the denormalized label from the category name. Inventory **Product category** select (`aria-label`).
- **Product barcode OpenAPI (BR-5.1):** `ProductCreate` / `Update` + `ProductVariantCreate` / `Update.barcode` ∈ `ProductBarcodeValue` (strip + upper; `^[A-Za-z0-9\-._]{4,48}$` aligned with `barcodes.BARCODE_PATTERN`); omit/`null` → no barcode / no change; blank/`!!!!`/`http://…`/`ab` → **422** (was free `str`; blank silently cleared; garbage late service **400**). Inventory **Product barcode** / **Edit product barcode** / **Variant barcode** inputs (`aria-label`s); create/edit send `null` when blank. Service `normalize_barcode` (+ GTIN check digits) remains defense-in-depth.
- **Product description OpenAPI (BR-5.1):** `ProductCreate` / `Update.description` ∈ `ProductDescriptionValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no description / no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently cleared / garbage could persist on `Product.description` Text). Inventory **Product description** / **Edit product description** inputs (`aria-label`s); create/edit send `null` when blank.
- **Store name OpenAPI (BR-13.1):** `StoreCreate.name` (required) + `StoreUpdate.name` (optional) ∈ `StoreNameValue` (strip; 1–150; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist on multi-store). Multi-Store **Store name** + **Edit store name** inputs (`aria-label`s).
- **Store code OpenAPI (BR-13.1):** `StoreCreate.code` (required) ∈ `StoreCodeValue` (strip; 1–50; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422** (was free `str`; blank/punctuation/URL could persist on `Store.code`). Tenant uniqueness remains UniqueConstraint `(tenant_id, code)`. Multi-Store **Store code** input (`aria-label`). `StoreUpdate` does not accept `code`.
- **Warehouse name OpenAPI (BR-2.4 / BR-13):** `WarehouseCreate.name` (required) + `WarehouseUpdate.name` (optional) ∈ `WarehouseNameValue` (strip; 1–150; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist). Multi-Store **Warehouse name** / **Edit warehouse name** inputs (`aria-label`s).
- **Warehouse code OpenAPI (BR-2.4 / BR-13):** `WarehouseCreate.code` (required) ∈ `WarehouseCodeValue` (strip; 1–50; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422** (was free `str`; blank reached service **400**; punctuation/URL could persist on `Warehouse.code`). Tenant uniqueness remains create_warehouse **409**. Multi-Store **Warehouse code** input (`aria-label`). `WarehouseUpdate` does not accept `code`.
- **Branch name OpenAPI (BR-2.3 / BR-13):** `BranchCreate.name` (required) + `BranchUpdate.name` (optional) ∈ `BranchNameValue` (strip; 1–150; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist). Multi-Store **Branch name** / **Edit branch name** inputs (`aria-label`s).
- **Branch code OpenAPI (BR-2.2 / BR-13):** `BranchCreate.code` (required) ∈ `BranchCodeValue` (strip; 1–40; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422** (was free `str`; blank reached service **400**; punctuation/URL could persist on `Branch.code`). Tenant uniqueness remains create_branch **409**. Multi-Store **Branch code** input (`aria-label`). `BranchUpdate` does not accept `code`.
- **Brand name OpenAPI (BR-5.1):** `BrandCreate.name` (required) + `BrandUpdate.name` (optional) ∈ `BrandNameValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist on catalog). Inventory Catalog **Brand name** input (`aria-label`).
- **Brand code OpenAPI (BR-5.1):** `BrandCreate.code` (required) + `BrandUpdate.code` (optional) ∈ `BrandCodeValue` (strip; 1–40; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank reached service **400**; punctuation/URL could persist on `Brand.code`). Tenant uniqueness remains create_brand **409**. Inventory Catalog **Brand code** input (`aria-label`).
- **Brand description OpenAPI (BR-5.1):** `BrandCreate` / `Update.description` ∈ `BrandDescriptionValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no description / no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently cleared / garbage could persist on `Brand.description` Text). Inventory Catalog **Brand description** input (`aria-label`); create sends `null` when blank.
- **Stock transfer notes OpenAPI (BR-5.2):** `StockTransferCreate.notes` ∈ `StockTransferNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently dropped / garbage could persist on `StockTransfer.notes` Text). Inventory Transfers **Stock transfer notes** input (`aria-label`); create sends `null` when blank.
- **Stock adjustment notes OpenAPI (BR-5.2):** `StockAdjust.notes` ∈ `StockAdjustNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently dropped / garbage could persist on `StockMovement.notes` Text). Inventory Adjust **Stock adjustment notes** input (`aria-label`); post sends `null` when blank.
- **Stock adjustment warehouse_id OpenAPI (BR-5.2):** `StockAdjust.warehouse_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → company / product stock path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach warehouse lookup). Existence remains tenant-scoped warehouse lookup (**404**). Inventory Adjust **Stock adjustment warehouse** select (`aria-label`); Post stock adjustment sends trim or `null` when blank.
- **Stock-out notes OpenAPI (BR-5.2):** `StockOut.notes` ∈ `StockOutNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently dropped / garbage could persist on `StockMovement.notes` Text). Inventory Stock Out **Stock-out notes** input (`aria-label`); post sends `null` when blank.
- **Stock movement reference_id OpenAPI (BR-5.2):** `StockMove` / `StockOut.reference_id` ∈ `StockMovementReferenceIdValue` (strip; 1–36; ≥1 letter/digit; no `://`/`@`); omit/`null` → no external ref; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on `StockMovement.reference_id` String(36)). Inventory Stock Out **Stock-out reference id** + Stock In **Stock-in reference id** inputs (`aria-label`s); post sends `null` when blank.
- **Stock-in reference_type OpenAPI (BR-5.2):** `StockMove.reference_type` ∈ `StockInReferenceTypeValue` (strip/lower; 1–50; ≥1 letter/digit; no `://`/`@`); omit/`null` → no coded source; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on `StockMovement.reference_type` String(50)). Stock-out keeps required `Literal` on `StockOut.reference_type`. Inventory Batches **Stock-in reference type** input (`aria-label`); Receive batch sends `null` when blank. `POST /inventory/stock-in` passes type/id through to `stock_in_with_batch`.
- **Stock-in notes OpenAPI (BR-5.2):** `StockMove.notes` ∈ `StockInNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently dropped / garbage could persist on `StockMovement.notes` Text). Inventory Batches **Stock-in notes** input (`aria-label`); Receive batch sends `null` when blank.
- **Category name OpenAPI (BR-5.1):** `ProductCategoryCreate.name` (required) + `ProductCategoryUpdate.name` (optional) ∈ `CategoryNameValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist on catalog tree). Inventory Catalog **Category name** input (`aria-label`).
- **Category code OpenAPI (BR-5.1):** `ProductCategoryCreate.code` (required) + `ProductCategoryUpdate.code` (optional) ∈ `CategoryCodeValue` (strip; 1–40; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank reached service **400**; punctuation/URL could persist on `ProductCategory.code`). Tenant uniqueness remains create_category **409**. Inventory Catalog **Category code** input (`aria-label`).
- **Category create parent_id OpenAPI (BR-5.1):** `ProductCategoryCreate.parent_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → root; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach parent lookup). Existence remains tenant-scoped category lookup (**404**). Inventory Catalog **Category parent** select (`aria-label`); Add category sends trim or `null` when blank.
- **Category create tax_rate_id OpenAPI (BR-5.1 / BR-12.1):** `ProductCategoryCreate.tax_rate_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → tenant default path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach tax-rate lookup). Existence remains tenant-scoped tax-rate lookup (**404**). Inventory Catalog **Category tax rate** select (`aria-label`); Add category sends trim or `null` when blank.
- **Category update parent_id OpenAPI (BR-5.1):** `ProductCategoryUpdate.parent_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit → no change; `null` → root; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach parent lookup). Existence / cycle checks remain tenant-scoped (**404**/400). Same honesty as create. Inventory Catalog **Edit category parent** select (`aria-label`); reparent sends trim or `null` when blank.
- **Category update tax_rate_id OpenAPI (BR-5.1 / BR-12.1):** `ProductCategoryUpdate.tax_rate_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit → no change; `null` → clear; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach tax-rate lookup). Existence remains tenant-scoped tax-rate lookup (**404**). Same honesty as create. Inventory Catalog **Edit category tax rate** select (`aria-label`); update sends trim or `null` when blank.
- **Unit name OpenAPI (BR-5.1):** `UnitOfMeasureCreate.name` (required) + `UnitOfMeasureUpdate.name` (optional) ∈ `UnitNameValue` (strip; 1–80; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist on catalog UoM). Inventory Catalog **Unit name** input (`aria-label`).
- **Unit code OpenAPI (BR-5.1):** `UnitOfMeasureCreate.code` (required) + `UnitOfMeasureUpdate.code` (optional) ∈ `UnitCodeValue` (strip; 1–20; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank reached service **400**; punctuation/URL could persist on `UnitOfMeasure.code`). Tenant uniqueness remains create_unit **409**. Inventory Catalog **Unit code** input (`aria-label`).
- **Unit create base_unit_id OpenAPI (BR-5.1):** `UnitOfMeasureCreate.base_unit_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → root unit; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach base lookup). Existence / root-base rules remain `validate_unit_base` (**404**/400). Inventory Catalog **Unit base unit** select (`aria-label`); Add unit sends trim or `null` when blank.
- **Unit update base_unit_id OpenAPI (BR-5.1):** `UnitOfMeasureUpdate.base_unit_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach base lookup). `clear_base` remains explicit root clear. Existence / root-base rules remain `validate_unit_base` (**404**/400). Same honesty as create (API clients; Catalog manage UI activates/deactivates only).
- **Department name OpenAPI (BR-2.4 / BR-13):** `DepartmentCreate.name` (required) + `DepartmentUpdate.name` (optional) ∈ `DepartmentNameValue` (strip; 1–150; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist). Multi-Store **Department name** / **Edit department name** inputs (`aria-label`s).
- **Department code OpenAPI (BR-2.5 / BR-13):** `DepartmentCreate.code` (required) ∈ `DepartmentCodeValue` (strip; 1–40; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422** (was free `str`; blank/invalid reached service **400** via `_clean_code`; punctuation/URL could otherwise slip past OpenAPI). Tenant uniqueness remains create_department **409**. Multi-Store **Department code** input (`aria-label`). `DepartmentUpdate` does not accept `code`.
- **Variant name OpenAPI (BR-5.1):** `ProductVariantCreate.name` (required) + `ProductVariantUpdate.name` (optional) ∈ `VariantNameValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`) for `POST|PATCH /products/{id}/variants`; blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist). Inventory Variants **Variant name** input (`aria-label`).
- **Variant attributes OpenAPI (BR-5.1):** `ProductVariantCreate` / `Update` `size`|`color`|`flavor`|`dosage` ∈ `VariantAttrValue` (strip; 1–80; ≥1 letter/digit; no `://`/`@`); create omit/`null` → no attr; update omit → no change; `null` → clear; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently `_clean_attr`→null; punctuation/URL could persist on `ProductVariant`). Inventory Variants **Variant size** / **color** / **flavor** / **dosage** inputs (`aria-label`s); create sends `null` when blank. Service `_clean_attr` remains defense-in-depth.
- **Customer group name OpenAPI (BR-7.1):** `CustomerGroupCreate.name` (required) + `CustomerGroupUpdate.name` (optional) ∈ `CustomerGroupNameValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`) for `POST|PATCH /customers/groups`; blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist). Sales **Customer group name** input (`aria-label`).
- **Customer create customer_group_id OpenAPI (BR-7.1):** `PartyCreate.customer_group_id` ∈ `UuidIdValue` (strip; lower; valid UUID) for `POST /customers`; omit/`null` → no group; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach group lookup). Existence / active-group rules remain `require_active_group` (**404**/400). Sales **Customer group** select (`aria-label`); Add customer sends trim or `null` when blank.
- **Customer update customer_group_id OpenAPI (BR-7.1):** `PartyUpdate.customer_group_id` ∈ `UuidIdValue` (strip; lower; valid UUID) for `PATCH /customers/{id}`; omit → no change; `null` → clear; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach group lookup). Existence / active-group rules remain `require_active_group` (**404**/400). Same select as create; **Assign group** sends trim.
- **Customer group code OpenAPI (BR-7.1):** `CustomerGroupCreate.code` ∈ `CustomerGroupCodeValue` (strip; 1–40; ≥1 letter/digit; no `://`/`@`); omit/`null` → service slugs from name; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently fell through to name-slug; punctuation/URL could persist on `CustomerGroup.code`). Uniqueness remains create_group **409**. Sales **Customer group code** input (`aria-label`); create sends `null` when blank.
- **Tax rate name OpenAPI (BR-12.1):** `TaxCreate.name` (required) + `TaxUpdate.name` (optional) ∈ `TaxRateNameValue` (strip; 1–80; ≥1 letter/digit; no `://`/`@`) for `POST|PATCH /tax/rates`; blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist). Tax **Tax rate name** input (`aria-label`).

- **Expense category name OpenAPI (BR-9.1):** `ExpenseCategoryCreate.name` (required) + `ExpenseCategoryUpdate.name` (optional) ∈ `ExpenseCategoryNameValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`) for `POST|PATCH /expenses/categories`; blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist). Expenses **Expense category name** input (`aria-label`).
- **Expense category code OpenAPI (BR-9.1):** `ExpenseCategoryCreate.code` (required) ∈ `ExpenseCategoryCodeValue` (strip; 1–40; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422** (was free `str`; blank/punctuation/URL could persist on `ExpenseCategory.code`). Tenant uniqueness remains create **409**. Expenses **Expense category code** input (`aria-label`). `ExpenseCategoryUpdate` does not accept `code`.

- **Party contact name OpenAPI (BR-6.1 / BR-7.1):** `PartyContactCreate.name` (required) + `PartyContactUpdate.name` (optional) ∈ `PartyContactNameValue` (strip; 1–150; ≥1 letter/digit; no `://`/`@`) for nested `POST|PATCH /customers|suppliers/{id}/contacts`; blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist). Sales/Purchasing **Party contact name** input (`aria-label`).

- **Party contact designation OpenAPI (BR-6.1 / BR-7.1):** `PartyContactCreate` / `Update.designation` ∈ `PartyContactDesignationValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`); omit/`null` OK (create) / no change (PATCH); blank/`!!!`/`http://…` → **422** (was free `str`; blank silently None / garbage could persist). Sales/Purchasing **Party contact designation** input (`aria-label`); create sends `null` when blank.

- **Custom role label OpenAPI (BR-3.2):** `CustomRoleCreate.label` (required) + `CustomRoleUpdate.label` (optional) ∈ `CustomRoleLabelValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`) for `POST|PATCH /roles`; blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank/garbage could persist). Users **Custom role label** input (`aria-label`).

- **Bank connection display_name OpenAPI (BR-10.3):** `BankConnectionCreate` / `Update.display_name` ∈ `BankConnectionDisplayNameValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`); omit/`null` OK (create) / no change (PATCH); blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist). Accounting Reconcile **Bank connection display name** input (`aria-label`); create sends `null` when blank.
- **Bank external_account_id OpenAPI (BR-10.3):** `BankConnectionCreate` / `Update.external_account_id` ∈ `BankExternalAccountIdValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`); omit/`null` OK (create) / no change (PATCH); blank/`!!!`/`http://…` → **422** (was free `str`; blank silent→null via service strip; garbage could persist on `BankConnection.external_account_id` String(120)). Accounting Reconcile **Bank external account id** input (`aria-label`); create sends `null` when blank. Service strip remains defense-in-depth.

- **POS customer_name OpenAPI (BR-8.1):** `PosSaleCreate.customer_name` ∈ `PosCustomerNameValue` (strip; 1–180; ≥1 letter/digit; no `://`/`@`); omit/`null` → walk-in; blank/`!!!`/`http://…` → **422** (was free `str` max 180; blank/garbage could persist on receipt). POS **POS customer name** input (`aria-label`); checkout sends `null` when blank/whitespace (`name.trim() || null`).

- **API key name OpenAPI (BR-18.1):** `ApiKeyCreate.name` ∈ `ApiKeyNameValue` (strip; 2–120; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…`/`x` → **422** (was free `str` min_length=2; punctuation/URL could persist). Integrations **API key name** input (`aria-label`); create requires trim (no silent default label).

- **AI report template name OpenAPI (BR-21.7):** `AiReportTemplateCreateBody.name` ∈ `AiReportTemplateNameValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422** (was free `str` min_length=1; punctuation/URL could persist). AI **AI report template name** input (`aria-label`); Save requires trim (no silent “Saved report” default).

- **Party address OpenAPI (BR-6.1 / BR-7.1):** `PartyCreate` / `PartyUpdate.address` ∈ `AddressValue` (strip; 1–500 chars; at least one letter/digit; no `://` / `@`) for `POST|PATCH /customers` and `/suppliers`; create omit/`null` → no address; PATCH omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist). Sales **Customer address** + Purchasing **Supplier address** inputs (`aria-label`s); create sends `null` when blank.
- **PO delivery_address OpenAPI (BR-6.3):** `PurchaseOrderCreate` / `PurchaseOrderAmend.delivery_address` ∈ `AddressValue` (strip; 1–500 chars; at least one letter/digit; no `://` / `@`); create omit/`null` → no ship-to; amend omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silent→null / cleared; garbage could persist). Purchasing **PO delivery address** + **PO amend delivery address** inputs (`aria-label`s); create sends `null` when blank; amend omits blank.
- **PO notes OpenAPI (BR-6.3):** `PurchaseOrderCreate` / `PurchaseOrderAmend.notes` ∈ `PurchaseOrderNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); create omit/`null` → no notes; amend `null` clears; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist). Purchasing **PO notes** / **PO amend notes** inputs (`aria-label`s); create/amend send `null` when blank.
- **PO amend due_date OpenAPI (BR-6.3):** `PurchaseOrderAmend.due_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit/`null` → no change; `clear_due_date=true` clears; blank/`not-a-date`/`01/02/2024` → **422** (was free `datetime`; OpenAPI date-time; padded dates inconsistent). API `reports.parse_date` remains defense-in-depth. Purchasing **PO amend due date** input (`aria-label`); amend omits blank.
- **PR required_date OpenAPI (BR-6.2):** `PurchaseRequestCreate.required_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit/`null` → no needed-by date; blank/`not-a-date`/`01/02/2024` → **422** (was free `datetime`; OpenAPI date-time; padded dates inconsistent). API `reports.parse_date` remains defense-in-depth. Purchasing **Purchase request required date** input (`aria-label`); create sends `null` when blank.
- **PR notes OpenAPI (BR-6.2):** `PurchaseRequestCreate.notes` + `PurchaseRequestItemCreate.notes` ∈ `PurchaseRequestNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on `PurchaseRequest.notes` / `PurchaseRequestItem.notes`). Purchasing **Purchase request notes** + **Purchase request line notes** inputs (`aria-label`s); Create draft PR sends `null` when blank.
- **PR department OpenAPI (BR-6.2):** `PurchaseRequestCreate.department` + `LowStockSuggestionsCreate.department` ∈ `PurchaseRequestDepartmentValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`); omit/`null` → no department label; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on `PurchaseRequest.department`). Purchasing **Purchase request department** input (`aria-label`); create sends `null` when blank.
- **PR preferred_supplier_id OpenAPI (BR-6.2):** `PurchaseRequestCreate.preferred_supplier_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no preference; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach supplier lookup). Existence / active-supplier rules remain `require_active_supplier` (**404**/400). Purchasing **Purchase request preferred supplier** select (`aria-label`); Create draft PR sends trim or `null` when blank.
- **PR warehouse_id OpenAPI (BR-6.2):** `PurchaseRequestCreate.warehouse_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no warehouse; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach warehouse lookup). Existence remains tenant-scoped warehouse lookup (**404**). API clients (Purchasing create form does not yet expose warehouse).
- **PR item product_id OpenAPI (BR-6.2):** `PurchaseRequestItemCreate.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog lookup). Existence remains tenant-scoped product lookup (**404**). Purchasing **Purchase request product** select (`aria-label`); Create draft PR sends trim.
- **PR item variant_id OpenAPI (BR-6.2):** `PurchaseRequestItemCreate.variant_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no variant; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach variant lookup). Existence / product match remain `create_request` (**404**). API clients (Purchasing create form does not yet expose variant).
- **Low-stock suggestion product_id OpenAPI (BR-6.2):** `LowStockSuggestionLine.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog lookup). Existence remains tenant-scoped product lookup (**404**). Reports **Create draft PR from low-stock suggestions** sends trim.
- **Low-stock suggestion warehouse_id OpenAPI (BR-6.2):** `LowStockSuggestionLine.warehouse_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no warehouse; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach warehouse lookup). Reports Create draft PR sends trim or `null`.
- **Low-stock suggestion preferred_supplier_id OpenAPI (BR-6.2):** `LowStockSuggestionLine.preferred_supplier_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no preference; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach supplier lookup). Reports Create draft PR sends trim or `null`.
- **SO delivery_address OpenAPI (BR-7.3):** `SalesOrderCreate` / `SalesOrderConfirm.delivery_address` ∈ `AddressValue` (strip; 1–500 chars; at least one letter/digit; no `://` / `@`); create omit/`null` → no ship-to; confirm omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silent→null; garbage could persist). Sales **SO delivery address** input (`aria-label`); create sends `null` when blank; confirm omits blank.
- **SO delivery_date OpenAPI (BR-7.3):** `SalesOrderCreate` / `SalesOrderConfirm.delivery_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); create omit/`null` → no promised date; confirm omit/`null` → no change; blank/`not-a-date`/`01/02/2024` → **422** (was free `datetime`; OpenAPI date-time; padded dates inconsistent). API `reports.parse_date` remains defense-in-depth. Sales **SO delivery date** input (`aria-label`); create/confirm send `null` when blank.
- **Party contact phone OpenAPI (BR-6.1):** `PartyContactCreate` / `PartyContactUpdate.phone` ∈ `E164PhoneValue` (`+` + 8–15 digits) for nested `/customers|suppliers/{id}/contacts`; create omit/`null` → no phone; PATCH omit/`null` → no change; blank/`not-a-phone`/`123` → **422** (was free `str`; blank/garbage could persist). Sales/Purchasing **Party contact phone** input (`aria-label`); create sends `null` when blank.
- **POS receipt send Query `to` OpenAPI (BR-8):** `POST /pos/sales/{id}/receipt/send` Query `to` ∈ `ReceiptOverrideToValue` (strip; email or E.164); omit/`null` → cashier email/phone; blank/`!!!`/`not-an-email`/`not-a-phone` → **422** (was free `str`; blank was silent fallthrough; garbage reached soft send failure). Channel refine remains in API (email channel rejects E.164-only; sms rejects email). POS **POS receipt override to** + Email/SMS (`aria-label`s); send trims/`omits` blank.
- **Platform staff email OpenAPI:** `POST /platform/staff` body `PlatformStaffCreate.email` ∈ `EmailStr`; blank/`not-an-email`/`abc` → **422** (was free `str` with `min_length=3`; `"abc"` accepted). Body `extra=forbid`. Platform Staff **Platform staff email** input (`aria-label`).
- **Platform staff full name OpenAPI:** `PlatformStaffCreate.full_name` (required) + `PlatformStaffUpdate.full_name` (optional) ∈ `PlatformStaffFullNameValue` (strip; 1–150; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str` min_length=1; whitespace/`!!!`/URL could persist). Platform Staff **Platform staff full name** input (`aria-label`); create requires trim.
- **Platform staff phone OpenAPI:** `PlatformStaffCreate` / `PlatformStaffUpdate.phone` ∈ `E164PhoneValue` (`+` + 8–15 digits); create omit/`null` → no phone; PATCH omit/`null` → no change; blank/`not-a-phone`/`123` → **422** (was free `str`; blank/garbage could persist). Platform Staff **Platform staff phone** input (`aria-label`); create sends `null` when blank.
- **Platform staff password OpenAPI:** `PlatformStaffCreate.password` ∈ `PlatformStaffPasswordValue` (strip; 1–128; ≥1 letter/digit; no `://` / `@` / spaces); blank/`!!!`/`http://…` → **422** (was free `str` min_length=1; whitespace/`!!!`/URL could reach hash path). Body `extra=forbid`. Service `validate_password_strength` remains defense-in-depth (**400**). Platform Staff **Platform staff password** input (`aria-label`); create requires trim.
- **User password OpenAPI (BR-3):** `UserCreate.password` (required) + `UserUpdate.password` (optional) ∈ `UserPasswordValue` (strip; 1–128; ≥1 letter/digit; no `://` / `@` / spaces); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; whitespace/`!!!`/URL could reach hash path). Service `validate_password_strength` remains defense-in-depth (**400**). Users **User password** input (`aria-label`); create requires trim.
- **Tenant admin password OpenAPI (BR-1):** `TenantCreate.admin_password` ∈ `TenantAdminPasswordValue` (strip; 1–128; ≥1 letter/digit; no `://` / `@` / spaces); blank/`!!!`/`http://…` → **422** (was free `str`; whitespace/`!!!`/URL could reach hash path). Service `validate_password_strength` remains defense-in-depth (**400**). Platform console **Tenant admin password** input (`aria-label`); create requires trim.
- **Password reset new_password OpenAPI (BR-19):** `PasswordResetConfirm.new_password` ∈ `PasswordResetNewPasswordValue` (strip; 1–128; ≥1 letter/digit; no `://` / `@` / spaces); blank/`!!!`/`http://…` → **422** (was free `str`; whitespace/`!!!`/URL could reach hash path). Service `validate_password_strength` remains defense-in-depth (**400**). Reset password **Password reset new password** input (`aria-label`); submit requires trim.
- **Password reset token OpenAPI (BR-19):** `PasswordResetConfirm.token` ∈ `PasswordResetTokenValue` (strip; 1–200; ≥1 letter/digit; no `://` / `@` / spaces); blank/`!!!`/`http://…` → **422** (was free `str`; whitespace/`!!!`/URL reached `hash_token` / invalid-token **400**). Authenticity remains AuthToken lookup (**400**). Reset password **Password reset token** input (`aria-label`); submit sends trim.
- **Email verify token OpenAPI (BR-19):** `EmailVerifyConfirm.token` ∈ `EmailVerifyTokenValue` (strip; 1–200; ≥1 letter/digit; no `://` / `@` / spaces); blank/`!!!`/`http://…` → **422** (was free `str`; whitespace/`!!!`/URL reached `hash_token` / invalid-token **400**). Authenticity remains AuthToken lookup (**400**). Verify email **Email verification token** input (`aria-label`); submit sends trim.
- **Login tenant OpenAPI (BR-19 / BR-1):** `Login.tenant_id` + `PasswordResetRequest.tenant_id` + `ResendVerificationRequest.tenant_id` ∈ `TenantRefValue` (strip; UUID or slug `^[a-z0-9][a-z0-9-]{1,79}$`; UUID/slug normalized lower); blank/`!!!`/`http://…`/`a b`/`X` → **422** (was free `str`; whitespace/`!!!`/URL reached `resolve_tenant` as **404**). Existence remains `resolve_tenant` (**404**). Login **Login tenant** + Forgot password **Password reset tenant** inputs (`aria-label`s); submit/resend send trim.
- **2FA challenge token OpenAPI (BR-19):** `TwoFactorVerify` / `WebAuthnLoginOptions` / `WebAuthnLoginVerify.challenge_token` ∈ `ChallengeTokenValue` (strip; 1–2048; ≥1 letter/digit; no `://` / `@` / spaces); blank/`!!!`/`http://…` → **422** (was free `str`; whitespace/`!!!`/URL reached `decode_challenge_token` as **401**). Authenticity remains JWT decode (**401**). Login **2FA challenge token** input (`aria-label`; hidden when challenge issued); verify/passkey send trim.
- **Refresh token OpenAPI (BR-19):** `RefreshRequest.refresh_token` ∈ `RefreshTokenValue` (strip; 1–200; ≥1 letter/digit; no `://` / `@` / spaces); blank/`!!!`/`http://…` → **422** (was free `str`; whitespace/`!!!`/URL reached refresh lookup as invalid). Authenticity remains hashed refresh-token lookup (**401**). Frontend `refreshSession` sends trim (no dedicated form; stored in `localStorage`).
- **Sales invoice customer_id OpenAPI (BR-7.4):** `SalesInvoiceCreate.customer_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach party lookup). Existence remains tenant-scoped customer lookup (**404**). Sales **Sale customer** select (`aria-label`); Create invoice sends trim.
- **Stock count warehouse_id OpenAPI (BR-5.2):** `StockCountCreate.warehouse_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach warehouse lookup). Existence remains tenant-scoped warehouse lookup (**404**). Inventory Counts **Stock count warehouse** select (`aria-label`); Create draft sends trim.
- **Stock count product_ids OpenAPI (BR-5.2):** `StockCountCreate.product_ids` ∈ `list[UuidIdValue]`; omit/`null` → all warehouse stock; blank/`!!!`/`http://…`/non-UUID element → **422** (was free `list[str]`; garbage could reach product lookup). Existence remains tenant-scoped (**404**). Inventory Create draft omits (full warehouse).
- **Expense category account_id OpenAPI (BR-9.1):** `ExpenseCategoryCreate.account_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → default GL 6000; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach COA lookup). Existence remains tenant-scoped account lookup (**404**). Expenses **Expense category GL account** select (`aria-label`); Add category sends trim or `null` when blank.
- **Expense category update account_id OpenAPI (BR-9.1):** `ExpenseCategoryUpdate.account_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach COA lookup). Existence remains tenant-scoped account lookup (**404**). Use `clear_account` to remove. Expenses **Edit expense category GL account** select (`aria-label`); Save sends trim, or `clear_account` when cleared.
- **Transaction create party_id OpenAPI (BR-8.1):** `TransactionCreate.party_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → walk-in / no party; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach party lookup). Existence remains tenant-scoped customer lookup (**404**). Legacy sale create path (POS uses `PosSaleCreate.party_id`).
- **Tax calculate tax_rate_id OpenAPI (BR-12.1):** `TaxCalculateRequest.tax_rate_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → use `rate` / tenant default; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach tax-rate lookup). Existence remains tenant-scoped tax-rate lookup (**404**).
- **Sales return post liquid_account_id OpenAPI (BR-7.5):** `SalesReturnPost.liquid_account_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → payment-method default; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach liquid-account lookup). Existence remains tenant-scoped (**404**/400).
- **Customer payment liquid_account_id OpenAPI (BR-11.1):** `CustomerPaymentCreate.liquid_account_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → payment-method default GL; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach liquid-account lookup). Existence remains tenant-scoped (**404**/400). Credit **Credit payment liquid account** select (`aria-label`); Record payment sends trim or `null` when blank.
- **Supplier payment liquid_account_id OpenAPI (BR-11.2):** `SupplierPaymentCreate.liquid_account_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → payment-method default GL; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach liquid-account lookup). Existence remains tenant-scoped (**404**/400). Same honesty as customer payments; Credit **Credit payment liquid account** select; Record payment sends trim or `null` when blank.
- **POS payment line liquid_account_id OpenAPI (BR-8.1):** `PosPaymentLine.liquid_account_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → tender-method default; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach liquid-account lookup). Existence remains tenant-scoped (**404**/400).
- **Bank match journal_line_id OpenAPI (BR-10.3):** `BankStatementMatchBody.journal_line_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str` `min_length=1`; whitespace still reached service **404**; non-UUID garbage could reach journal-line lookup). Existence remains tenant-scoped (**404**). Accounting Match sends trim.
- **Expense create category_id OpenAPI (BR-9.2):** `ExpenseCreate.category_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → label-only / MISC path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach category lookup). Existence remains tenant-scoped expense-category lookup (**404**/400). Expenses **Expense spend category** select (`aria-label`); Submit expense sends trim or `null` when blank.
- **Expense update category_id OpenAPI (BR-9.2):** `ExpenseUpdate.category_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach category lookup). Existence remains tenant-scoped expense-category lookup (**404**/400). Same honesty as create. Expenses **Edit expense category** select (`aria-label`); Save changes sends trim or `null` when blank.
- **Expense update store_id OpenAPI (BR-9.2):** `ExpenseUpdate.store_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach store lookup). Existence remains tenant-scoped store lookup (**404**). Same honesty as create; use `clear_store` to remove. Expenses **Edit expense store** select (`aria-label`); Save changes sends trim, or `clear_store` when cleared.
- **Expense update branch_id OpenAPI (BR-9.2):** `ExpenseUpdate.branch_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach branch lookup). Existence remains tenant-scoped branch lookup (**404**). Same honesty as create; use `clear_branch` to remove. Expenses **Edit expense branch** select (`aria-label`); Save changes sends trim, or `clear_branch` when cleared.
- **Expense update department_id OpenAPI (BR-9.2):** `ExpenseUpdate.department_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach department lookup). Existence remains tenant-scoped department lookup (**404**). Same honesty as create; use `clear_department` to remove. Expenses **Edit expense department** select (`aria-label`); Save changes sends trim, or `clear_department` when cleared.
- **Expense create liquid_account_id OpenAPI (BR-9.2):** `ExpenseCreate.liquid_account_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → payment-method default GL; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach liquid-account lookup). Existence remains tenant-scoped liquid account lookup (**404**/400). Expenses **Expense liquid account** select (`aria-label`); Submit expense sends trim or `null` when blank.
- **Expense create branch_id OpenAPI (BR-9.2):** `ExpenseCreate.branch_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no branch; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach branch lookup). Existence remains tenant-scoped branch lookup (**404**). Expenses **Expense branch** select (`aria-label`); Submit expense sends trim or `null` when blank.
- **Expense create department_id OpenAPI (BR-9.2):** `ExpenseCreate.department_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no department; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach department lookup). Existence remains tenant-scoped department lookup (**404**). Expenses **Expense department** select (`aria-label`); Submit expense sends trim or `null` when blank.
- **Expense create store_id OpenAPI (BR-9.2):** `ExpenseCreate.store_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no store; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach store lookup). Existence remains tenant-scoped store lookup (**404**). Expenses **Expense store** select (`aria-label`); Submit expense sends trim or `null` when blank.
- **Recurring expense category_id OpenAPI (BR-9.5):** `RecurringExpenseCreate.category_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → label-only path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach category lookup). Existence remains tenant-scoped expense-category lookup (**404**/400). Expenses **Recurring expense category** select (`aria-label`); Create schedule sends trim or `null` when blank.
- **Recurring expense update category_id OpenAPI (BR-9.5):** `RecurringExpenseUpdate.category_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach category lookup). Existence remains tenant-scoped expense-category lookup (**404**/400). Same honesty as create. Expenses **Edit recurring expense category** select (`aria-label` when editing); Save schedule sends trim or `null` when blank.
- **Recurring expense create branch_id OpenAPI (BR-9.5):** `RecurringExpenseCreate.branch_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no branch; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach branch lookup). Existence remains tenant-scoped branch lookup (**404**). Same honesty as ExpenseCreate.branch_id. Expenses **Recurring expense branch** select (`aria-label`); Create schedule sends trim or `null` when blank.
- **Recurring expense create department_id OpenAPI (BR-9.5):** `RecurringExpenseCreate.department_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no department; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach department lookup). Existence remains tenant-scoped department lookup (**404**). Same honesty as ExpenseCreate.department_id. Expenses **Recurring expense department** select (`aria-label`); Create schedule sends trim or `null` when blank.
- **Recurring expense update branch_id OpenAPI (BR-9.5):** `RecurringExpenseUpdate.branch_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach branch lookup). Existence remains tenant-scoped branch lookup (**404**). Same honesty as create; use `clear_branch` to remove. Expenses **Edit recurring expense branch** select (`aria-label` when editing); Save schedule sends trim, or `clear_branch` when cleared.
- **Recurring expense update department_id OpenAPI (BR-9.5):** `RecurringExpenseUpdate.department_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach department lookup). Existence remains tenant-scoped department lookup (**404**). Same honesty as create; use `clear_department` to remove. Expenses **Edit recurring expense department** select (`aria-label` when editing); Save schedule sends trim, or `clear_department` when cleared.
- **Sales order customer_id OpenAPI (BR-7.3):** `SalesOrderCreate.customer_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach party lookup). Existence remains tenant-scoped customer lookup (**404**). Sales **Sale customer** select (`aria-label`; shared create form); Create order sends trim.
- **Sales quotation customer_id OpenAPI (BR-7.2):** `SalesQuotationCreate.customer_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach party lookup). Existence remains tenant-scoped customer lookup (**404**). Sales **Sale customer** select (`aria-label`; shared create form); Create quotation sends trim.
- **Sales return sales_invoice_id OpenAPI (BR-7.5):** `SalesReturnCreate.sales_invoice_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach invoice lookup). Existence remains tenant-scoped sales invoice lookup (**404**). Sales **Return from invoice** select (`aria-label`); Create return sends trim.
- **Sales return item product_id OpenAPI (BR-7.5):** `SalesReturnItemCreate.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog lookup). Existence remains tenant-scoped product lookup (**404**). Sales Create return sends `product_id` trim (shared **Sales product** select).
- **Sales return item variant_id OpenAPI (BR-7.5):** `SalesReturnItemCreate.variant_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no variant; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach variant lookup). Sales Create return sends trim or `null` when blank (shared **Sales variant** select).
- **Platform grant user_id OpenAPI:** `PlatformGrantAccess.user_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach user lookup). Existence remains platform-scoped user lookup (**404**). Platform Staff **Grant dashboard** (`aria-label`); grant sends `user_id` trim.
- **GRN item po_item_id OpenAPI (BR-6.4):** `GrnItemCreate.po_item_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach PO-line lookup). Existence remains tenant-scoped purchase-order item lookup (**404**/400). Purchasing Post GRN / Receive all send `po_item_id` trim.
- **Purchase return item goods_receipt_item_id OpenAPI (BR-6.6):** `PurchaseReturnItemCreate.goods_receipt_item_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach GRN line lookup). Existence remains tenant-scoped goods-receipt item lookup (**404**/400). Purchasing **Purchase return GRN line** select (`aria-label`); Draft return sends trim.
- **Sales invoice store_id OpenAPI (BR-7.4):** `SalesInvoiceCreate.store_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no store / HQ path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach store lookup). Existence remains tenant-scoped store lookup (**404**). Sales **Sale store** select (`aria-label`); Create invoice sends trim or `null` when blank.
- **Sales order store_id OpenAPI (BR-7.3):** `SalesOrderCreate.store_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no store until confirm; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach store lookup). Existence remains tenant-scoped store lookup (**404**). Sales **Sale store** select (`aria-label`; shared create form); Create order sends trim or `null` when blank.
- **Sales order confirm store_id OpenAPI (BR-7.3):** `SalesOrderConfirm.store_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → keep order store / require when confirming without one; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach store lookup). Existence remains tenant-scoped store lookup (**404**). Sales **Sale store** select; Confirm sends trim when set.
- **POS session open store_id OpenAPI (BR-8.2):** `PosSessionOpen.store_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no store / HQ path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach store lookup). Existence remains tenant-scoped store lookup (**404**). POS **POS store** select (`aria-label`); Open shift sends trim or `null` when blank.
- **POS sale session_id OpenAPI (BR-8.1):** `PosSaleCreate.session_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → current session path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach session lookup). Existence remains tenant-scoped POS session lookup (**404**/400). POS Complete sale sends `session_id` trim when a shift is open.
- **POS sale party_id OpenAPI (BR-8.1):** `PosSaleCreate.party_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → walk-in; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach party lookup). Existence remains tenant-scoped customer lookup (**404**). POS **POS customer** select (`aria-label`); Complete sale sends trim or `null` when blank.
- **Purchase invoice supplier_id OpenAPI (BR-6.5):** `PurchaseInvoiceCreate.supplier_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` OK when from-GRN / PO path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach party lookup). Existence remains tenant-scoped supplier lookup (**404**). Purchasing **Purchase invoice supplier** select (`aria-label`); Draft manual PI sends trim.
- **Purchase invoice goods_receipt_id OpenAPI (BR-6.5):** `PurchaseInvoiceCreate.goods_receipt_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → manual / PO path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach GRN lookup). Existence remains tenant-scoped goods-receipt lookup (**404**). Purchasing **Purchase invoice GRN** select (`aria-label`); Draft from GRN sends trim.
- **Purchase invoice purchase_order_id OpenAPI (BR-6.5):** `PurchaseInvoiceCreate.purchase_order_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → manual / GRN path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach PO lookup). Existence remains tenant-scoped purchase-order lookup (**404**).
- **Purchase invoice item product_id OpenAPI (BR-6.5):** `PurchaseInvoiceItemCreate.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog lookup). Existence remains tenant-scoped product lookup (**404**). Purchasing **Purchase invoice product** select (`aria-label`); Draft manual PI sends trim.
- **Sales order quotation_id OpenAPI (BR-7.3):** `SalesOrderCreate.quotation_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → standalone order; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach quotation lookup). Existence remains tenant-scoped quotation lookup (**404**). UI convert-order uses path id; API create may pass body trim.
- **Customer payment sales_invoice_id OpenAPI (BR-11.1):** `CustomerPaymentCreate.sales_invoice_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → apply oldest-open; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach invoice lookup). Existence remains tenant-scoped sales-invoice lookup (**404**). Credit Record payment may omit (FIFO apply).
- **Supplier payment purchase_order_id OpenAPI (BR-11.2):** `SupplierPaymentCreate.purchase_order_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → apply oldest-open; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach PO lookup). Existence remains tenant-scoped purchase-order lookup (**404**).
- **Supplier payment purchase_invoice_id OpenAPI (BR-11.2):** `SupplierPaymentCreate.purchase_invoice_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → apply oldest-open; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach PI lookup). Existence remains tenant-scoped purchase-invoice lookup (**404**).
- **Store reorder product_id OpenAPI (BR-5.4 / BR-13):** `StoreReorderPolicyUpdate.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog lookup). Existence remains tenant-scoped product lookup (**404**). Multi-Store **Store reorder product** select (`aria-label`); Save store reorder policy sends trim.
- **Warehouse reorder warehouse_id OpenAPI (BR-5.4):** `WarehouseReorderPolicyUpdate.warehouse_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach warehouse lookup). Existence remains tenant-scoped warehouse lookup (**404**). Inventory **Warehouse stock warehouse** select (`aria-label`); Save warehouse reorder policy sends trim.
- **Warehouse reorder product_id OpenAPI (BR-5.4):** `WarehouseReorderPolicyUpdate.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog lookup). Existence remains tenant-scoped product lookup (**404**). Inventory **Warehouse reorder product** select (`aria-label`); Save warehouse reorder policy sends trim.
- **Purchase request convert supplier_id OpenAPI (BR-6.2):** `PurchaseRequestConvert.supplier_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → preferred / first-line supplier path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach party lookup). Existence remains tenant-scoped supplier lookup (**404**). Purchasing **Convert purchase request** (`aria-label`); Convert to PO sends `{}` when no override.
- **Stock count item product_id OpenAPI (BR-5.2):** `StockCountItemUpdate.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog / count-line lookup). Existence remains tenant-scoped product on count (**404**/400). Inventory Counts save sends `product_id` trim.
- **Stock-in unit_id OpenAPI (BR-5.2):** `StockMove.unit_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → product stock unit; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach UoM lookup). Inventory Batches **Stock-in unit** select (`aria-label`); Receive batch sends trim or `null` when blank.
- **Stock-in variant_id OpenAPI (BR-5.2):** `StockMove.variant_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no variant; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach variant lookup). Inventory Batches **Stock-in variant** select (`aria-label`); Receive batch sends trim or `null` when blank.
- **Stock-in batch_id OpenAPI (BR-5.2):** `StockMove.batch_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → create/resolve via `batch_number`; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach batch lookup). API clients (UI prefers `batch_number`).
- **Stock-out unit_id OpenAPI (BR-5.2):** `StockOut.unit_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → product stock unit; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach UoM lookup). Inventory Stock Out **Stock-out unit** select (`aria-label`); Post stock out sends trim or `null` when blank.
- **Stock-out variant_id OpenAPI (BR-5.2):** `StockOut.variant_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no variant; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach variant lookup). Inventory Stock Out **Stock-out variant** select (`aria-label`); Post stock out sends trim or `null` when blank.
- **Stock-out batch_id OpenAPI (BR-5.2):** `StockOut.batch_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → FEFO; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach batch lookup). Inventory Stock Out **Stock-out batch** select (`aria-label`); Post stock out sends trim or `null` when blank.
- **Opening stock unit_id OpenAPI (BR-5.2):** `OpeningStockLine.unit_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → product stock unit; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach UoM lookup). Inventory Opening **Opening stock unit** select (`aria-label`); Post opening stock sends trim or `null` when blank.
- **Opening stock variant_id OpenAPI (BR-5.2):** `OpeningStockLine.variant_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no variant; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach variant lookup). Inventory Opening **Opening stock variant** select (`aria-label`); Post opening stock sends trim or `null` when blank.
- **Purchase return goods_receipt_id OpenAPI (BR-6.6):** `PurchaseReturnCreate.goods_receipt_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach GRN lookup). Existence remains tenant-scoped goods-receipt lookup (**404**). Purchasing **Return from GRN** select (`aria-label`); Create purchase return / Draft return sends trim.
- **Product create category_id OpenAPI (BR-5.1):** `ProductCreate.category_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → label-only / General path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach category lookup). Existence remains tenant-scoped catalog category lookup (**404**/400). Inventory **Product category** select (`aria-label`); Create product sends trim or `null` when blank.
- **Product update category_id OpenAPI (BR-5.1):** `ProductUpdate.category_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach category lookup). Existence remains tenant-scoped catalog category lookup (**404**/400). Same honesty as create. Inventory **Edit product category** select (`aria-label`); Save product sends trim or `null` when blank.
- **Product create tax_rate_id OpenAPI (BR-5.1 / BR-12.1):** `ProductCreate.tax_rate_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → category/tenant default path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach tax-rate lookup / FK). Existence remains tenant-scoped tax-rate lookup (**404**/integrity). Inventory **Product tax rate** select (`aria-label`); Create product sends trim or `null` when blank.
- **Product update tax_rate_id OpenAPI (BR-5.1 / BR-12.1):** `ProductUpdate.tax_rate_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach tax-rate lookup / FK). Existence remains tenant-scoped tax-rate lookup (**404**/integrity). Same honesty as create. Inventory **Edit product tax rate** select (`aria-label`); Save product sends trim or `null` when blank.
- **Product create brand_id OpenAPI (BR-5.1):** `ProductCreate.brand_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no brand; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach brand lookup / FK). Existence remains tenant-scoped brand lookup (**404**/integrity). Inventory **Product brand** select (`aria-label`); Create product sends trim or `null` when blank.
- **Product update brand_id OpenAPI (BR-5.1):** `ProductUpdate.brand_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach brand lookup / FK). Existence remains tenant-scoped brand lookup (**404**/integrity). Same honesty as create. Inventory **Edit product brand** select (`aria-label`); Save product sends trim or `null` when blank.
- **Product create unit_id OpenAPI (BR-5.1):** `ProductCreate.unit_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no unit / default path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach unit lookup / FK). Existence remains tenant-scoped unit lookup (**404**/integrity). Inventory **Product unit** select (`aria-label`); Create product sends trim or `null` when blank.
- **Product update unit_id OpenAPI (BR-5.1):** `ProductUpdate.unit_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach unit lookup / FK). Existence remains tenant-scoped unit lookup (**404**/integrity). Same honesty as create. Inventory **Edit product unit** select (`aria-label`); Save product sends trim or `null` when blank.
- **StockMove product_id OpenAPI (BR-5.2):** `StockMove.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog lookup). Existence remains tenant-scoped product lookup (**404**). Inventory **Selected product** select (`aria-label`); Receive batch (stock-in) sends trim.
- **StockMove warehouse_id OpenAPI (BR-5.2):** `StockMove.warehouse_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → company / product stock path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach warehouse lookup). Existence remains tenant-scoped warehouse lookup (**404**). Inventory Batches **Stock-in warehouse** select (`aria-label`); Receive batch sends trim or `null` when blank.
- **Opening stock line product_id OpenAPI (BR-5.2):** `OpeningStockLine.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog lookup). Existence remains tenant-scoped product lookup (**404**). Inventory **Selected product** select (`aria-label`; shared with stock-in/out); Post opening stock sends trim.
- **Opening stock line warehouse_id OpenAPI (BR-5.2):** `OpeningStockLine.warehouse_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → company / product stock path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach warehouse lookup). Existence remains tenant-scoped warehouse lookup (**404**). Inventory Opening **Opening stock warehouse** select (`aria-label`); Post opening stock sends trim or `null` when blank.
- **StockOut product_id OpenAPI (BR-5.2):** `StockOut.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog lookup). Existence remains tenant-scoped product lookup (**404**). Inventory **Selected product** select (`aria-label`); Stock out sends trim.
- **StockOut warehouse_id OpenAPI (BR-5.2):** `StockOut.warehouse_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → company / product stock path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach warehouse lookup). Existence remains tenant-scoped warehouse lookup (**404**). Inventory Stock Out **Stock-out warehouse** select (`aria-label`); Post stock out sends trim or `null` when blank.
- **Stock transfer item product_id OpenAPI (BR-5.2 / BR-13.2):** `StockTransferItemCreate.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog lookup). Shared by `POST /inventory/stock-transfers` + `POST /stores/transfers` nested `items[]`. Existence remains tenant-scoped product lookup (**404**). Inventory **Selected product** select (`aria-label`; shared product picker) + Multi-Store **Stock transfer product** select (`aria-label`); Create transfer sends trim.
- **Stock transfer warehouse_id OpenAPI (BR-5.2 / BR-13.2):** `StockTransferCreate.from_warehouse_id` / `to_warehouse_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` OK when store pair set; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach warehouse lookup). Existence remains tenant-scoped warehouse lookup (**404**). Inventory **Stock transfer from warehouse** / **Stock transfer to warehouse** selects (`aria-label`s); Create transfer sends trim.
- **Stock transfer from_store_id OpenAPI (BR-5.2 / BR-13.2):** `StockTransferCreate.from_store_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` OK when warehouse pair set; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach store lookup). Existence remains tenant-scoped store lookup (**404**). Multi-Store **Stock transfer from store** select (`aria-label`); Create transfer sends trim.
- **Stock transfer to_store_id OpenAPI (BR-5.2 / BR-13.2):** `StockTransferCreate.to_store_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` OK when warehouse pair set; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach store lookup). Existence remains tenant-scoped store lookup (**404**). Multi-Store **Stock transfer to store** select (`aria-label`); Create transfer sends trim.
- **Department branch_id OpenAPI (BR-2.4 / BR-13):** `DepartmentCreate.branch_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no branch; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach branch lookup). Existence remains tenant-scoped branch lookup (**404**). Multi-Store **Department branch** select (`aria-label`); Create department sends trim or `null` when blank.
- **Department update branch_id OpenAPI (BR-2.5 / BR-13):** `DepartmentUpdate.branch_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach branch lookup). Existence remains tenant-scoped branch lookup (**404**). Same honesty as create; use `clear_branch` to remove. Multi-Store **Edit department branch** select (`aria-label` when editing); Save department sends trim, or `clear_branch` when cleared.
- **Store create manager_id OpenAPI (BR-2.3 / BR-13):** `StoreCreate.manager_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no manager; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach user lookup). Existence remains tenant-scoped user lookup (**404**). Multi-Store **Store manager** select (`aria-label`); Create store sends trim or `null` when blank.
- **Store create branch_id OpenAPI (BR-2.3 / BR-13):** `StoreCreate.branch_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no branch; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach branch lookup). Existence remains tenant-scoped branch lookup (**404**). Multi-Store **Store branch** select (`aria-label`); Create store sends trim or `null` when blank.
- **Store update manager_id OpenAPI (BR-2.3 / BR-13):** `StoreUpdate.manager_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach user lookup). Existence remains tenant-scoped user lookup (**404**). Same honesty as create; use `clear_manager` to remove. Multi-Store **Edit store manager** select (`aria-label`); Save store sends trim, or `clear_manager` when cleared.
- **Store update branch_id OpenAPI (BR-2.3 / BR-13):** `StoreUpdate.branch_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach branch lookup). Existence remains tenant-scoped branch lookup (**404**). Same honesty as create; use `clear_branch` to remove. Multi-Store **Edit store branch** select (`aria-label`); Save store sends trim, or `clear_branch` when cleared.
- **Warehouse create manager_id OpenAPI (BR-2.4 / BR-13):** `WarehouseCreate.manager_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no manager; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach user lookup). Existence remains tenant-scoped user lookup (**404**). Multi-Store **Warehouse manager** select (`aria-label`); Create warehouse sends trim or `null` when blank.
- **Warehouse create store_id OpenAPI (BR-2.4 / BR-13):** `WarehouseCreate.store_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no store; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach store lookup). Existence remains tenant-scoped store lookup (**404**). Multi-Store **Warehouse store** select (`aria-label`); Create warehouse sends trim or `null` when blank.
- **Warehouse update manager_id OpenAPI (BR-2.4 / BR-13):** `WarehouseUpdate.manager_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach user lookup). Existence remains tenant-scoped user lookup (**404**). Same honesty as create; use `clear_manager` to remove. Multi-Store **Edit warehouse manager** select (`aria-label` when editing); Save warehouse sends trim, or `clear_manager` when cleared.
- **Warehouse update store_id OpenAPI (BR-2.4 / BR-13):** `WarehouseUpdate.store_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach store lookup). Existence remains tenant-scoped store lookup (**404**). Same honesty as create; use `clear_store` to remove. Multi-Store **Edit warehouse store** select (`aria-label` when editing); Save warehouse sends trim, or `clear_store` when cleared.
- **Branch create manager_id OpenAPI (BR-2.2 / BR-13):** `BranchCreate.manager_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no manager; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach user lookup). Existence remains tenant-scoped user lookup (**404**). Multi-Store **Branch manager** select (`aria-label`); Create branch sends trim or `null` when blank.
- **Branch update manager_id OpenAPI (BR-2.2 / BR-13):** `BranchUpdate.manager_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach user lookup). Existence remains tenant-scoped user lookup (**404**). Same honesty as create; use `clear_manager` to remove. Multi-Store **Edit branch manager** select (`aria-label` when editing); Save branch sends trim, or `clear_manager` when cleared.
- **Department create head_user_id OpenAPI (BR-2.5 / BR-13):** `DepartmentCreate.head_user_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no head; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach user lookup). Existence remains tenant-scoped user lookup (**404**). Multi-Store **Department head** select (`aria-label`); Create department sends trim or `null` when blank.
- **Department update head_user_id OpenAPI (BR-2.5 / BR-13):** `DepartmentUpdate.head_user_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no change; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach user lookup). Existence remains tenant-scoped user lookup (**404**). Same honesty as create; use `clear_head` to remove. Multi-Store **Edit department head** select (`aria-label` when editing); Save department sends trim, or `clear_head` when cleared.
- **Cash transfer from_account_id OpenAPI (BR-10.3):** `CashTransferCreate.from_account_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → service requires for transfer/withdrawal; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach liquid-account lookup). Existence remains tenant-scoped account lookup (**404**). Accounting **Cash transfer from account** select (`aria-label`); Post cash transfer sends trim or `null` when blank.
- **Cash transfer to_account_id OpenAPI (BR-10.3):** `CashTransferCreate.to_account_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → service requires for transfer/deposit; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach liquid-account lookup). Existence remains tenant-scoped account lookup (**404**). Accounting **Cash transfer to account** select (`aria-label`); Post cash transfer sends trim or `null` when blank.
- **Customer payment customer_id OpenAPI (BR-11.1):** `CustomerPaymentCreate.customer_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach party lookup). Existence remains tenant-scoped customer lookup (**404**). Credit **Credit payment party** select (`aria-label`); Record payment sends trim.
- **Supplier payment supplier_id OpenAPI (BR-11):** `SupplierPaymentCreate.supplier_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach party lookup). Existence remains tenant-scoped supplier lookup (**404**). Credit **Credit payment party** select (`aria-label`; shared AR/AP); Record payment sends trim.
- **GRN purchase_order_id OpenAPI (BR-6.4):** `GrnCreate.purchase_order_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach PO lookup). Existence remains tenant-scoped purchase-order lookup (**404**). Purchasing **GRN purchase order** control (`aria-label`); Post GRN / Receive all send trim.
- **GRN warehouse_id OpenAPI (BR-6.4):** `GrnCreate.warehouse_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → PO warehouse / default path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach warehouse lookup). Existence remains tenant-scoped warehouse lookup (**404**). API clients (Purchasing Post GRN does not yet expose warehouse override).
- **Unit convert product_id OpenAPI (BR-5.1):** `UnitConvertPreview.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog lookup). Existence remains tenant-scoped product lookup (**404**). API `POST /catalog/units/convert`.
- **Unit convert from_unit_id OpenAPI (BR-5.1):** `UnitConvertPreview.from_unit_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → product stock unit; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach UoM lookup). Existence / conversion remain `to_stock_qty` (**404**/400). API `POST /catalog/units/convert`.
- **Line item unit_id OpenAPI (BR-7.4 / BR-8.1):** `LineItem.unit_id` + `SalesInvoiceItemCreate.unit_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → product stock unit; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach UoM lookup). Shared by SI/QT/SO + POS/legacy sale lines. Sales **Sales unit** select (`aria-label`); Create sale sends trim or `null` when blank.
- **Line item variant_id OpenAPI (BR-7.4 / BR-8.1):** `LineItem.variant_id` + `SalesInvoiceItemCreate.variant_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no variant; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach variant lookup). Shared by SI/QT/SO + POS/legacy sale lines. Sales **Sales variant** select (`aria-label`); Create sale sends trim or `null` when blank.
- **Purchase order supplier_id OpenAPI (BR-6.3):** `PurchaseOrderCreate.supplier_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach party lookup). Existence remains tenant-scoped supplier lookup (**404**). Purchasing **PO supplier** select (`aria-label`); Create PO sends trim.
- **PO line product_id OpenAPI (BR-6.3):** `PurchaseOrderItemCreate.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog lookup). Shared by PO create/amend line items. Existence remains tenant-scoped product lookup (**404**). Purchasing **PO product** select (`aria-label`); Create PO sends trim.
- **PO warehouse_id OpenAPI (BR-6.3):** `PurchaseOrderCreate.warehouse_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no warehouse; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach warehouse lookup). Existence / active-warehouse rules remain `require_active_warehouse` (**404**/400). API clients (Purchasing create form does not yet expose warehouse).
- **PO line unit_id OpenAPI (BR-6.3):** `PurchaseOrderItemCreate.unit_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → product stock unit; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach UoM lookup). Existence / conversion remain `resolve_line_unit` (**404**/400). Shared by create/amend. Purchasing **PO unit** select (`aria-label`) on Create and Amend; send trim or `null` when blank.
- **Line item product_id OpenAPI (BR-7.4 / BR-8.1):** `LineItem.product_id` + `SalesInvoiceItemCreate.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach catalog lookup). Shared by SI/QT/SO create lines + `TransactionCreate` / `PosSaleCreate` items. Existence remains tenant-scoped product lookup (**404**). Sales **Sales product** select (`aria-label`); Create sale sends `product_id` trim.
- **Bank statement account_id OpenAPI (BR-10.3):** `BankStatementCreateBody.account_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str` min_length=1; garbage could reach liquid-account lookup). Existence remains tenant-scoped liquid account lookup (**404**). Accounting Reconcile **Reconcile liquid account** select (`aria-label`); Create statement sends trim.
- **Bank connection account_id OpenAPI (BR-10.3):** `BankConnectionCreate.account_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach liquid-account lookup). Existence remains tenant-scoped liquid account lookup (**404**). Accounting Reconcile **Reconcile liquid account** select (`aria-label`; shared with statement create); Connect bank account sends trim.
- **Login password OpenAPI (BR-19):** `Login.password` ∈ `LoginPasswordValue` (strip; 1–128; ≥1 letter/digit; no `://` / `@` / spaces); blank/`!!!`/`http://…` → **422** (was free `str`; whitespace/`!!!`/URL reached `verify_password` as **401**). Authenticity remains `verify_password` (**401**). Login **Login password** input (`aria-label`); submit sends trim.
- **2FA disable password OpenAPI (BR-19):** `TwoFactorDisable.password` ∈ `TwoFactorDisablePasswordValue` (strip; 1–128; ≥1 letter/digit; no `://` / `@` / spaces); blank/`!!!`/`http://…` → **422** (was free `str`; whitespace/`!!!`/URL reached `verify_password` as **401**). Authenticity remains `verify_password` (**401**); TOTP/backup still `TwoFactorCodeValue`. Security **Disable 2FA password** input (`aria-label`); Disable sends trim.
- **Webhook events OpenAPI:** `WebhookCreate` / `WebhookUpdate.events` items ∈ `VALID_EVENTS` (`Literal` + strip/lower); empty list / blank / unknown → **422**. Service `normalize_events` remains defense-in-depth. Integrations webhook event checkboxes.
- **Webhook URL OpenAPI (BR-18.6):** `WebhookCreate` / `WebhookUpdate.url` ∈ `WebhookUrlValue` (strip; absolute `http(s)` + netloc; `http` only for localhost/127.0.0.1/testserver/host.docker.internal); blank/`ftp://`/`not-a-url`/plain-http remote → **422** (was free `str`; late service **400**). Service `validate_url` remains defense-in-depth. Integrations **Webhook endpoint URL** input (`aria-label`); create sends `url: hookUrl.trim()`.
- **Webhook description OpenAPI (BR-18.6):** `WebhookCreate` / `Update.description` ∈ `WebhookDescriptionValue` (strip; 1–255; ≥1 letter/digit; no `://`/`@`); omit/`null` OK (create) / no change (PATCH); blank/`!!!`/`http://…` → **422** (was free `str`; blank silently None / garbage could persist). Integrations **Webhook description** input (`aria-label`); create sends `null` when blank.
- **Webhook secret OpenAPI (BR-18.6):** `WebhookCreate.secret` ∈ `WebhookSecretValue` (strip; 1–128; ≥1 letter/digit; no `://` / `@` / spaces); omit/`null` → auto-generate `whsec_…`; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently auto-generated via strip-or-generate; punctuation/URL could be encrypted into `WebhookEndpoint.secret_enc`). Service min-length for non-`whsec_` remains defense-in-depth (**400**). Integrations **Webhook signing secret** input (`aria-label`); create sends `null` when blank.
- **Audit module Query OpenAPI (BR-17):** `GET /audit-logs` + `GET /audit-logs/export` Query `module` ∈ `AuditModuleValue` / `audit.AUDIT_MODULES` (strip/lower); omit → all; blank/unknown → **422** (blank was silent omit; unknown returned empty). Service `query_logs` remains defense-in-depth. Audit **Audit module filter** select (`aria-label`).
- **Audit date Query OpenAPI (BR-17):** `GET /audit-logs` + export Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was uncaught **500**). Service `parse_date` remains defense-in-depth (**400**). Audit **From/To date** inputs (`aria-label`s).
- **Bank feed URL OpenAPI (BR-10.3):** `BankConnectionCreate` / `Update.feed_url` ∈ `WebhookUrlValue` (strip; absolute `http(s)` + netloc; `http` only for localhost/127.0.0.1/testserver/host.docker.internal); omit/`null` OK; blank/`ftp://`/`not-a-url`/plain-http remote → **422** (was free `str`; blank silent→null; garbage could persist). Service still requires URL for `http_json` (**400**). Accounting Reconcile **Bank feed URL** input (`aria-label`); Connect sends `connFeedUrl.trim() || null` for `http_json`.
- **Bank connection access_token OpenAPI (BR-10.3):** `BankConnectionCreate` / `Update.access_token` ∈ `BankAccessTokenValue` (strip; 1–128; ≥1 letter/digit; no `://` / `@` / spaces); omit/`null` → no token / no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank was a silent no-op via service; punctuation/URL could be encrypted into `BankConnection.credentials_enc`). `clear_credentials: true` still clears. Accounting Reconcile **Bank connection access token** input (`aria-label`; http_json); create sends `null` when blank.
- **Bank account number OpenAPI (BR-10.3):** `AccountCreate` / `AccountUpdate.account_number` ∈ `BankAccountNumberValue` (strip; alphanumeric + optional spaces/hyphens; max 64); omit/`null` OK; blank/`not-an-account`/`http://…` → **422** (was free `str`; blank silent→null; garbage could persist). Accounting **Bank account number** input (`aria-label`; bank liquid accounts); create sends `null` when blank.
- **Bank name OpenAPI (BR-10.3):** `AccountCreate` / `AccountUpdate.bank_name` ∈ `BankNameValue` (strip; 1–120 chars; at least one letter/digit; no `://` / `@`); omit/`null` OK; blank/`!!!`/`http://…` → **422** (was free `str`; blank silent→null then late service **400** when `liquid_kind=bank`). Service still requires bank_name for bank accounts (**400**). Accounting **Bank name** input (`aria-label`); create omits blank.
- **Account name OpenAPI (BR-10.3):** `AccountCreate.name` (required) + `AccountUpdate.name` (optional) ∈ `AccountNameValue` (strip; 1–150; ≥1 letter/digit; no `://`/`@`); blank/`!!!`/`http://…` → **422**; update omit/`null` → no change (was free `str`; blank late service **400**; garbage could persist on COA). Service strip remains defense-in-depth. Accounting **Account name** + **Edit account name** inputs (`aria-label`s).
- **Account code OpenAPI (BR-10.3):** `AccountCreate.code` ∈ `AccountCodeValue` (strip; 1–30; `^[A-Za-z0-9][A-Za-z0-9_-]{0,29}$`; no forced upper); blank/`!!!`/`a b`/`http://…` → **422** (was free `str`; blank late service **400**; garbage could persist as COA identity). Service strip/uniqueness remain defense-in-depth. Accounting **Account code** input (`aria-label`).
- **Bank branch OpenAPI (BR-10.3):** `AccountCreate` / `AccountUpdate.bank_branch` ∈ `BankBranchValue` (strip; 1–120 chars; at least one letter/digit; no `://` / `@`); omit/`null` OK; blank/`!!!`/`http://…` → **422** (was free `str`; blank silent→null; garbage could persist). Accounting **Bank branch** input (`aria-label`; bank liquid accounts); create sends `null` when blank.
- **Payment reference OpenAPI (BR-11 / BR-8.1):** `CustomerPaymentCreate` / `SupplierPaymentCreate.reference` + `PosPaymentLine.reference` ∈ `PaymentReferenceValue` (strip; 1–100; ≥1 letter/digit; no `://`/`@`); omit/`null` → no reference; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on payment `reference`). Credit **Payment reference** + POS **POS payment reference** inputs (`aria-label`s); Record payment / Charge sends `null` when blank (POS wraps a single tender in `payments[]` when a reference is set).
- **Payment notes OpenAPI (BR-11):** `CustomerPaymentCreate` / `SupplierPaymentCreate.notes` ∈ `PaymentNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on payment `notes`). Credit **Payment notes** input (`aria-label`); Record payment sends `null` when blank.
- **Payment cheque_number OpenAPI (BR-11 / BR-10.4):** `CustomerPaymentCreate` / `SupplierPaymentCreate.cheque_number` ∈ `ChequeNumberValue` (strip; alphanumeric + optional spaces/hyphens; max 50); omit/`null` → service falls back to reference/payment_number; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist). Credit **Payment cheque number** input (`aria-label`; shown when Method = cheque); Pay sends `null` when blank.
- **Payment cheque bank_name OpenAPI (BR-11 / BR-10.4):** `CustomerPaymentCreate` / `SupplierPaymentCreate.bank_name` ∈ `BankNameValue` (strip; 1–120 chars; at least one letter/digit; no `://` / `@`); omit/`null` OK; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on cheque payments). Credit **Payment cheque bank name** input (`aria-label`; shown when Method = cheque); Pay sends `null` when blank.
- **Payment cheque_date OpenAPI (BR-11 / BR-10.4):** `CustomerPaymentCreate` / `SupplierPaymentCreate.cheque_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit/`null` OK; blank/`not-a-date`/`01/02/2024` → **422** (was free `datetime`; OpenAPI date-time; padded dates rejected; Credit UI never set → always null). API `reports.parse_date` remains defense-in-depth (**400**). Credit **Payment cheque date** input (`aria-label`; Method = cheque); Pay sends `null` when blank.
- **Expense expense_date OpenAPI (BR-9.2):** `ExpenseCreate` / `ExpenseUpdate.expense_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); create omit/`null` → today; PATCH omit/`null` → no change; blank/`not-a-date`/`01/02/2024` → **422** (was free `datetime`; OpenAPI date-time; padded dates inconsistent). API `reports.parse_date` remains defense-in-depth. Expenses **Expense date** + **Expense OCR date** inputs (`aria-label`s); create/OCR Apply send `null`/omit when blank after trim.
- **Expense description OpenAPI (BR-9.2 / BR-9.5):** `ExpenseCreate` / `Update` + `RecurringExpenseCreate` / `Update` + `AiDocumentExpenseCreate.description` ∈ `ExpenseDescriptionValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); create omit/`null` → empty narrative; PATCH omit/`null` → no change; AI draft omit/`null` → no description; blank/`!!!`/`http://…` → **422** (was free `str` default `""`; blank/garbage could persist). Expenses **Expense description** + **Recurring description** + **Edit description** inputs (`aria-label`s); AI **AI document expense description**; create/edit/AI draft send `null` when blank.
- **Expense payee OpenAPI (BR-9.2 / BR-9.5):** `ExpenseCreate` / `Update` + `RecurringExpenseCreate` / `Update` + `AiDocumentExpenseCreate.payee` ∈ `ExpensePayeeValue` (strip; 1–150; ≥1 letter/digit; no `://`/`@`); omit/`null` OK (create) / no change (PATCH; recurring may `clear_payee`); blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist). Expenses **Expense payee** + **Recurring payee** + **Edit payee** inputs (`aria-label`s); create/edit send `null` when blank.
- **Expense category label OpenAPI (BR-9.2 / BR-9.5):** `ExpenseCreate` / `Update` + `RecurringExpenseCreate` / `Update` + `AiDocumentExpenseCreate.category` ∈ `ExpenseCategoryLabelValue` (strip; 1–100; ≥1 letter/digit; no `://`/`@`); omit/`null` OK when `category_id` set (prefer FK) / no change on PATCH; blank/`!!!`/`http://…` → **422** (was free `str`; blank without `category_id` reached service **400**; punctuation/URL could persist on `Expense.category` / recurring). When `category_id` is set, service overwrites the denormalized label from `ExpenseCategory.name`. Expenses **Expense spend category** + **Recurring expense category** selects (`aria-label`s).
- **Expense reference OpenAPI (BR-9.2):** `ExpenseCreate` / `Update` + `AiDocumentExpenseCreate.reference` ∈ `ExpenseReferenceValue` (strip; 1–100; ≥1 letter/digit; no `://`/`@`); create omit/`null` → auto `EXP-YYYY-NNNN`; PATCH omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently auto-numbered / cleared; garbage could persist). Expenses **Expense reference** + **Edit reference** inputs (`aria-label`s); create/edit send `null` when blank.
- **Purchase invoice date OpenAPI (BR-6.5):** `PurchaseInvoiceUpdate.invoice_date` / `due_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); PATCH omit/`null` → no change; blank/`not-a-date`/`01/02/2024` → **422** (was free `datetime`; OpenAPI date-time; padded dates inconsistent). API `reports.parse_date` remains defense-in-depth. Purchasing **Purchase invoice OCR date** input (`aria-label`); OCR Apply omits blank date.
- **Purchase invoice notes OpenAPI (BR-6.5):** `PurchaseInvoiceCreate` / `PurchaseInvoiceUpdate` / `AiDocumentPurchaseInvoiceCreate.notes` ∈ `PurchaseInvoiceNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); create omit/`null` → no notes; PATCH omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently dropped/cleared; garbage could persist). Purchasing **Purchase invoice notes** + **Purchase invoice OCR notes** inputs (`aria-label`s); create/OCR Apply send `null`/omit when blank.
- **Supplier invoice number OpenAPI (BR-6.5):** `PurchaseInvoiceCreate` / `PurchaseInvoiceUpdate` / `AiDocumentPurchaseInvoiceCreate.supplier_invoice_number` ∈ `SupplierInvoiceNumberValue` (strip; 1–100; ≥1 letter/digit; no `://`/`@`); create omit/`null` → no number; PATCH omit/`null` → no change; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently cleared via strip→null; punctuation/URL could persist on `PurchaseInvoice.supplier_invoice_number`). Purchasing **Supplier invoice number** inputs (`aria-label`; from-GRN + manual + OCR); create sends `null` when blank; OCR Apply omits blank.
- **Purchase invoice attachment URL OpenAPI (BR-6.5):** `PurchaseInvoiceCreate.attachment_url` ∈ `WebhookUrlValue` (strip; absolute `http(s)` + netloc; `http` only for localhost/127.0.0.1/testserver/host.docker.internal); omit/`null` → no external URL; blank/`ftp://`/`not-a-url`/`www.x`/plain-http remote → **422** (was free `str`; blank/`""` could persist; garbage URLs could persist on `PurchaseInvoice.attachment_url`). Multipart `POST …/invoices/{id}/attachment` still stores a tenant media key (not this create field). Purchasing **Purchase invoice attachment URL** inputs (`aria-label`; from-GRN + manual); Draft sends `null` when blank.
- **GRN batch date OpenAPI (BR-6.4):** `GrnItemCreate.manufacturing_date` / `expiry_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit/`null` → no batch dates; blank/`not-a-date`/`01/02/2024` → **422** (was free `datetime`; OpenAPI date-time; padded dates inconsistent). API `reports.parse_date` remains defense-in-depth. Purchasing Orders receive **GRN manufacturing date** / **GRN expiry date** inputs (`aria-label`s); post omits blank.
- **GRN notes OpenAPI (BR-6.4):** `GrnCreate.notes` ∈ `GrnNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on `GoodsReceipt.notes` Text). Purchasing Orders receive **GRN notes** input (`aria-label`); Post GRN / Receive all send `null` when blank.
- **Stock-in / opening-stock batch date OpenAPI (BR-5.2):** `StockMove` + `OpeningStockLine.manufacturing_date` / `expiry_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit/`null` → no batch dates; blank/`not-a-date`/`01/02/2024` → **422** (was free `datetime`; OpenAPI date-time; padded dates inconsistent). API `reports.parse_date` remains defense-in-depth. Inventory **Stock-in manufacturing/expiry date** + **Opening stock manufacturing/expiry date** inputs (`aria-label`s); post sends `null` when blank.
- **Opening stock reference OpenAPI (BR-5.2):** `OpeningStockCreate.reference` ∈ `OpeningStockReferenceValue` (strip; 1–100; ≥1 letter/digit; no `://`/`@`); omit/`null` → auto `OS-YYYY-NNNN`; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently auto-numbered / garbage could persist on journal/audit ref). Inventory Opening **Opening stock reference** input (`aria-label`); Post sends `null` when blank.
- **Opening stock notes OpenAPI (BR-5.2):** `OpeningStockCreate.notes` + `OpeningStockLine.notes` ∈ `OpeningStockNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently dropped / garbage could persist on movement notes). Header + line notes merge onto `StockMovement.notes`. Inventory Opening **Opening stock notes** + **Opening stock line notes** inputs (`aria-label`s); Post sends `null` when blank.
- **Movement date Query OpenAPI (BR-5.3):** `GET /inventory/movements` + `GET /reports/inventory/movements` Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Inventory **Movements** **From/To date** inputs (`aria-label`s).
- **Movement product_id Query OpenAPI (BR-5.3):** same movement list endpoints Query `product_id` ∈ `UuidIdValue`; omit/`null` → all products; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach product lookup). Inventory **Movement selected product only** (`aria-label`); load sends trim when filtering.
- **Movement warehouse_id Query OpenAPI (BR-5.3):** same endpoints Query `warehouse_id` ∈ `UuidIdValue`; omit/`null` → all warehouses; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach warehouse lookup). Inventory **Movement warehouse filter** + Reports **Report inventory warehouse filter** (`aria-label`s); load/qs send trim when set.
- **Movement store_id Query OpenAPI (BR-5.3):** same endpoints Query `store_id` ∈ `UuidIdValue`; omit/`null` → all stores; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach store lookup). Reports **Report inventory store filter** (`aria-label`); qs sends trim when set.
- **Movement created_by Query OpenAPI (BR-5.3):** same movement list endpoints Query `created_by` ∈ `UuidIdValue`; omit/`null` → all users; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach user filter). Inventory Movements FE currently omits (shows attribution columns only).
- **Audit user_id Query OpenAPI (BR-17):** `GET /audit-logs` + `GET /audit-logs/export` Query `user_id` ∈ `UuidIdValue`; omit/`null` → all (non-admin roles still forced to self); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Audit **Audit user filter** (`aria-label`); list/export send trim when set.
- **Product path product_id OpenAPI (BR-5.1):** Path `product_id` ∈ `UuidIdValue` on product/get/patch/image/barcode/variant/batch/stock/price routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach product lookup; existence remains tenant-scoped **404**). FE uses list/select ids.
- **Catalog path category_id OpenAPI (BR-5.1):** Path `category_id` ∈ `UuidIdValue` on category patch/delete; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Catalog path brand_id OpenAPI (BR-5.1):** Path `brand_id` ∈ `UuidIdValue` on brand patch/delete/logo; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Catalog path unit_id OpenAPI (BR-5.1):** Path `unit_id` ∈ `UuidIdValue` on unit patch/delete; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **User path user_id OpenAPI (BR-3):** Path `user_id` ∈ `UuidIdValue` on tenant `GET|PATCH|deactivate` + platform staff revoke/update; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Branch path branch_id OpenAPI (BR-2.5):** Path `branch_id` ∈ `UuidIdValue` on `PATCH /branches/{branch_id}`; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Department path department_id OpenAPI (BR-2.5):** Path `department_id` ∈ `UuidIdValue` on `PATCH /departments/{department_id}`; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Warehouse path warehouse_id OpenAPI (BR-5.2):** Path `warehouse_id` ∈ `UuidIdValue` on `GET|PATCH /warehouses/{warehouse_id}`; required Query `warehouse_id` on `GET /inventory/warehouse-stock` also ∈ `UuidIdValue`; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Variant path variant_id OpenAPI (BR-5.1):** Path `variant_id` ∈ `UuidIdValue` on product variant + barcode routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Product image path image_id OpenAPI (BR-5.1):** Path `image_id` ∈ `UuidIdValue` on product image patch/delete; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Customer path customer_id OpenAPI (BR-7.1):** Path `customer_id` ∈ `UuidIdValue` on customer get/patch/contacts/history/payments routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; existence remains tenant-scoped **404**). FE uses list/select ids.
- **Supplier path supplier_id OpenAPI (BR-6.1):** Path `supplier_id` ∈ `UuidIdValue` on supplier get/patch/contacts/history/payments routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). FE uses list/select ids.
- **Party contact path contact_id OpenAPI (BR-6.1 / BR-7.1):** Path `contact_id` ∈ `UuidIdValue` on customer/supplier contact patch/delete; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Customer group path group_id OpenAPI (BR-7.2):** Path `group_id` ∈ `UuidIdValue` on `PATCH /customers/groups/{group_id}` (+ bank clear-group dissolve Path `group_id`); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Sales invoice path invoice_id OpenAPI (BR-7):** Path `invoice_id` ∈ `UuidIdValue` on sales invoice get/print/post/send/cancel (+ related invoice path routes); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Quotation path quotation_id OpenAPI (BR-7.3):** Path `quotation_id` ∈ `UuidIdValue` on quotation get/send/accept/reject/convert; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Sales order path order_id OpenAPI (BR-7):** Path `order_id` ∈ `UuidIdValue` on sales order get/confirm/process/ship/deliver/cancel/convert; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Return path return_id OpenAPI (BR-7 / BR-6):** Path `return_id` ∈ `UuidIdValue` on sales/purchase return get/post/cancel routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Expense path expense_id OpenAPI (BR-9):** Path `expense_id` ∈ `UuidIdValue` on expense get/patch/approve/reject/attach routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Store path store_id OpenAPI (BR-2.3):** Path `store_id` ∈ `UuidIdValue` on store patch/drawer/reorder routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Stock count path count_id OpenAPI (BR-5.2):** Path `count_id` ∈ `UuidIdValue` on stock-count get/items/complete/cancel; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **POS session path session_id OpenAPI (BR-8):** Path `session_id` ∈ `UuidIdValue` on POS session get/close/sale routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Transfer path transfer_id OpenAPI (BR-13 / cash):** Path `transfer_id` ∈ `UuidIdValue` on cash transfers + store/inventory stock-transfer get/submit/approve/reject/ship/receive/cancel; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Bank statement path statement_id OpenAPI (BR-10):** Path `statement_id` ∈ `UuidIdValue` on bank-statement get/match/clear-group routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Webhook path webhook_id OpenAPI (BR-17):** Path `webhook_id` ∈ `UuidIdValue` on webhook get/patch/delete/test/deliveries; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Purchase request path request_id OpenAPI (BR-6.2):** Path `request_id` ∈ `UuidIdValue` on PR get/approve/reject/convert; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Purchase order path po_id OpenAPI (BR-6.3):** Path `po_id` ∈ `UuidIdValue` on PO get/send/receive/cancel routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Cheque path cheque_id OpenAPI (BR-10):** Path `cheque_id` ∈ `UuidIdValue` on cheque get/lifecycle routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Journal entry path entry_id OpenAPI (BR-10):** Path `entry_id` ∈ `UuidIdValue` on journal unpost/attachment routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Backup path backup_id OpenAPI:** Path `backup_id` ∈ `UuidIdValue` on backup get/download/verify/restore; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **COA path account_id OpenAPI (BR-10):** Path `account_id` ∈ `UuidIdValue` on accounting account patch/routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Bank connection path connection_id OpenAPI (BR-10):** Path `connection_id` ∈ `UuidIdValue` on bank-connection patch/sync/delete; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Bank statement line path line_id OpenAPI (BR-10):** Path `line_id` ∈ `UuidIdValue` on statement line match/unmatch; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Report schedule path schedule_id OpenAPI (BR-14):** Path `schedule_id` ∈ `UuidIdValue` on report schedule patch/delete; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Tax rate path rate_id OpenAPI:** Path `rate_id` ∈ `UuidIdValue` on tax rate get/patch/default; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **API key path key_id OpenAPI:** Path `key_id` ∈ `UuidIdValue` on API key get/revoke; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **POS sale path sale_id OpenAPI (BR-8):** Path `sale_id` ∈ `UuidIdValue` on POS receipt/send routes; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Recurring expense path recurring_id OpenAPI (BR-9):** Path `recurring_id` ∈ `UuidIdValue` on recurring expense patch/delete; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Audit log path log_id OpenAPI (BR-17):** Path `log_id` ∈ `UuidIdValue` on blocked audit delete route; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **WebAuthn path credential_id OpenAPI:** Path `credential_id` ∈ `UuidIdValue` on WebAuthn credential delete; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **GRN path grn_id OpenAPI (BR-6.4):** Path `grn_id` ∈ `UuidIdValue` on GRN get; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Webhook delivery path delivery_id OpenAPI (BR-17):** Path `delivery_id` ∈ `UuidIdValue` on webhook delivery retry; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **AI report template path template_id OpenAPI (BR-21.7):** Path `template_id` ∈ `UuidIdValue` on AI report template delete; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Inventory report store_id Query OpenAPI (BR-14.2):** `GET /reports/inventory/low-stock` + `/expiry` + `/balance` + `/valuation` Query `store_id` ∈ `UuidIdValue`; omit/`null` → all stores; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach store lookup). Reports **Report inventory store filter** (`aria-label`); qs sends trim.
- **Inventory report warehouse_id Query OpenAPI (BR-14.2):** same inventory report endpoints Query `warehouse_id` ∈ `UuidIdValue`; omit/`null` → all warehouses; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage could reach warehouse lookup). Reports **Report inventory warehouse filter** (`aria-label`); qs sends trim.
- **Transfer report store_id Query OpenAPI (BR-13.2):** `GET /reports/inventory/transfers` Query `store_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; matches source or destination). Reports qs sends trim.
- **Transfer report from_store_id Query OpenAPI (BR-13.2):** same endpoint Query `from_store_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Transfer report to_store_id Query OpenAPI (BR-13.2):** same endpoint Query `to_store_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Stock count report warehouse_id Query OpenAPI (BR-5.2):** `GET /reports/inventory/stock-counts` Query `warehouse_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Reports inventory warehouse filter; qs sends trim.
- **Stock count report store_id Query OpenAPI (BR-5.2):** same endpoint Query `store_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Reports inventory store filter; qs sends trim.
- **Financial report store_id Query OpenAPI (BR-14.5):** `GET /accounting|/reports` `profit-loss` / `trial-balance` / `balance-sheet` / `cash-flow` + `GET /reports/export` Query `store_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Accounting **P&L store filter** + Reports **Report financial store filter** (`aria-label`s); qs/pnlQuery send trim.
- **Financial report branch_id Query OpenAPI (BR-14.5):** same financial endpoints + export Query `branch_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Accounting **P&L branch filter** + Reports **Report financial branch filter** (`aria-label`s); qs/pnlQuery send trim.
- **Report export category_id Query OpenAPI (BR-14):** `GET /reports/export` Query `category_id` ∈ `UuidIdValue`; omit/`null` OK; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Report export department_id Query OpenAPI (BR-14):** `GET /reports/export` Query `department_id` ∈ `UuidIdValue`; omit/`null` OK; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Reports qs sends department trim.
- **Report export warehouse_id Query OpenAPI (BR-14):** `GET /reports/export` Query `warehouse_id` ∈ `UuidIdValue`; omit/`null` OK; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Sales report store_id Query OpenAPI (BR-14.1):** `GET /reports/sales/daily|monthly|products|customers|returns|salesperson` Query `store_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Reports **Report financial store filter** (`aria-label`); qs sends trim.
- **Sales products category_id Query OpenAPI (BR-14.1):** `GET /reports/sales/products` (+ export `sales_products`) Query `category_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Reports **Report sales category filter** (`aria-label`); qs/export send trim.
- **Sales returns customer_id Query OpenAPI (BR-14.1):** `GET /reports/sales/returns` Query `customer_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Sales department_id Query OpenAPI (BR-14.1 / BR-2.5):** `GET /reports/sales/salesperson|by-store|by-department` Query `department_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Reports **Report department filter** (`aria-label`); qs sends trim.
- **Purchases report location Query OpenAPI (BR-14.3):** `GET /reports/purchases/summary|suppliers|pending-orders|returns` Query `warehouse_id` / `store_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Reports **Report purchases store/warehouse filter** (`aria-label`s); qs sends trim.
- **Purchases report supplier_id Query OpenAPI (BR-14.3):** `GET /reports/purchases/suppliers|pending-orders|returns` Query `supplier_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`).
- **Expenses report UUID Query OpenAPI (BR-14.4):** `GET /reports/expenses/summary` + `budget-vs-actual` Query `category_id` / `branch_id` / `department_id` / `store_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Reports financial store/branch + department filters (`aria-label`s); qs sends trim.
- **Purchasing suggestions location Query OpenAPI:** `GET /purchasing/suggestions/low-stock` Query `store_id` / `warehouse_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Reports Inventory passes shared location qs (trim).
- **Tax report store_id Query OpenAPI:** `GET /reports/tax` + `/reports/tax/filing` Query `store_id` ∈ `UuidIdValue`; omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Tax **Tax report store filter** (`aria-label`); qs/export send trim.
- **Product price Query OpenAPI (BR-5.1):** `GET /products/{product_id}/price` Query `customer_id` / `variant_id` ∈ `UuidIdValue`; omit/`null` → list/base price path; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Sales **Sale customer** / **Sales variant** (`aria-label`s); price preview sends trim or omits blank.
- **Departments list branch_id Query OpenAPI (BR-2.5):** `GET /departments` Query `branch_id` ∈ `UuidIdValue`; omit/`null` → all branches; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). FE currently omits (client-side branch filter).
- **P&L date Query OpenAPI (BR-14):** `GET /accounting/profit-loss` + `GET /reports/profit-loss` Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Accounting **P&L From/To date** + Reports shared **Report From/To date** inputs (`aria-label`s).
- **Cash-flow date Query OpenAPI (BR-14):** `GET /reports/cash-flow` Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Reports **Cash flow** tab uses shared **Report From/To date** inputs (`aria-label`s).
- **BS/TB as_of Query OpenAPI (BR-14):** `GET /accounting|/reports` `trial-balance` + `balance-sheet` Query `as_of` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → live balances; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent live; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Accounting **Trial balance as of date** + Reports **Report as of date** inputs (`aria-label`s).
- **Audit action Query OpenAPI (BR-17):** `GET /audit-logs` + export Query `action` ∈ `AuditActionValue` (strip/lower; snake_case; digit-start OK for `2fa_*`); omit → all; blank/`login!`/`A` → **422** (blank was silent omit; malformed returned empty). Unknown well-shaped actions still empty (no closed Literal). Audit **Audit action filter** applies on Filter (draft vs `appliedAction`; avoids auto-refresh 422 while typing) (`aria-label`).
- **Audit entity Query OpenAPI (BR-17):** `GET /audit-logs` + export Query `entity` ∈ `AuditEntityValue` (strip/lower; snake_case starting with a letter); omit → all; blank/`!!!`/`Invoice`/`2fa` → **422** (blank was silent omit; malformed returned empty). Unknown well-shaped entities still empty (no closed Literal). Audit **Audit entity filter** applies on Filter (draft vs `appliedEntity`) (`aria-label`).
- **Product lookup barcode Query OpenAPI (BR-18.2 / BR-5.1):** `GET /inventory/products/lookup` Query `barcode` ∈ `ProductBarcodeValue` (strip + upper; 4–48); omit/`null` → name/SKU `q` path; blank/`!!!!`/`ab`/`http://…` → **422** (was free `str`). Inventory **Product lookup barcode** (`aria-label`); lookup omits blank.
- **POS search barcode Query OpenAPI (BR-8 / BR-5.1):** `GET /pos/products/search` Query `barcode` ∈ `ProductBarcodeValue` (same honesty); omit/`null` → `q` / wedge path; blank/invalid → **422** (was free `str`). POS search omits blank barcode param.
- **Product lookup search Query OpenAPI (BR-18.2 / BR-5.1):** `GET /inventory/products/lookup` Query `q` ∈ `ProductSearchQueryValue` (strip; empty OK; non-empty max 120; ≥1 letter/digit; no `://`); default `""` → empty search / barcode path; `!!!`/`http://…`/punctuation-only → **422** (was free `str`). Inventory **Product lookup search** (`aria-label`); lookup omits blank `q`.
- **POS product search Query OpenAPI (BR-8 / BR-5.1):** `GET /pos/products/search` Query `q` ∈ `ProductSearchQueryValue` (same honesty); empty OK for browse-all; garbage/URL → **422** (was free `str`). POS **Barcode scan or product search** (`aria-label`); browse sends trim.
- **Bank statement import notes OpenAPI (BR-10):** `POST /accounting/bank-statements/import` Query `notes` ∈ `BankStatementNotesValue`; omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`). Accounting import passes **Statement notes** trim when set.
- **Notification path nid OpenAPI (BR-4.4):** Path `nid` ∈ `UuidIdValue` on `PATCH /notifications/{nid}/read|unread`; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`). Notifications UI uses list ids.
- **Role path role OpenAPI (BR-3):** Path `role` ∈ `RoleKeyValue` on `GET|PATCH|DELETE /roles/{role}`; blank/`A`/`Cashier!` → **422** (was free `str`). Existence remains catalog/custom lookup **404**.
- **Tenant path tenant_ref OpenAPI (BR-19):** Path `tenant_ref` ∈ `TenantRefValue` (UUID or slug) on platform tenant suspend/activate/subscription/modules/usage/store-entitlement; blank/`!!!`/`http://…` → **422** (was free `str`; existence remains resolve_tenant **404**).
- **Auth session path session_id OpenAPI (BR-19.3):** `DELETE /auth/sessions/{session_id}` Path `session_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; garbage reached session lookup **404**). Existence remains own-session lookup **404**. Security **Revoke session** / **Sign out this device** (`aria-label`s); uses list ids.
- **Platform trials within_days Query OpenAPI:** `GET /platform/reports/trials` Query `within_days` ∈ 1–365 (omit → 45); `0`/`-1`/`366` → **422** (was free `int`; service silently clamped 1–365). Service clamp remains defense-in-depth. Platform Reports uses default 45.
- **Barcode label copies Query OpenAPI (BR-5.1):** `GET /products/{id}/barcode/label` + variant label Query `copies` ∈ 1–40 (omit → 1); `0`/`-1`/`41` → **422** (was free `int`; service silently clamped 1–40). Service clamp remains defense-in-depth. Inventory **Label copies** (`aria-label`); print clamps 1–40.
- **Opening stock list limit Query OpenAPI (BR-5.2):** `GET /inventory/opening-stock` Query `limit` ∈ 1–500 (omit → 100); `0`/`-1`/`501` → **422** (was free `int`; service silently clamped 1–500). Service clamp remains defense-in-depth.
- **Product lookup limit Query OpenAPI (BR-18.2):** `GET /inventory/products/lookup` Query `limit` ∈ 1–100 (omit → 48); `0`/`-1`/`101` → **422** (was free `int`; service silently clamped 1–100). Service clamp remains defense-in-depth. Inventory Lookup omits (default 48).
- **Inventory expiry days Query OpenAPI (BR-5.1 / BR-14.2):** `GET /inventory/batches/expiring` + `GET /reports/inventory/expiry` + export Query `days` ∈ 1–365 (omit → 30 / export default); `0`/`-1`/`366` → **422** (was free `int`; Reports UI allowed max 3650). Reports **Inventory expiry days** (`aria-label`); load/export clamp 1–365. Inventory Batches uses `days=60`.
- **Notifications limit Query OpenAPI (BR-4.4):** `GET /notifications` Query `limit` ∈ 1–200 (omit → 100); `0`/`-1`/`201` → **422** (was free `int`; API silently clamped 1–200). API clamp remains defense-in-depth. Shell notification center uses `limit=8`.
- **AI low-stock days_ahead Query OpenAPI (BR-21.4):** `GET /ai/inventory/low-stock-prediction` Query `days_ahead` ∈ 1–90 (omit → 14); `0`/`-1`/`91` → **422** (was free `int`; service silently clamped 1–90). Service clamp remains defense-in-depth. AI uses `days_ahead=14`.
- **Audit logs limit Query OpenAPI (BR-17):** `GET /audit-logs` Query `limit` ∈ 1–1000 (omit → 200); `0`/`-1`/`1001` → **422** (was free `int`; service only capped high at 1000). Service `min(limit, 1000)` remains defense-in-depth. Audit Filter omits (default 200).
- **Audit archives limit Query OpenAPI (BR-17.2):** `GET /audit-logs/archives` Query `limit` ∈ 1–200 (omit → 50); `0`/`-1`/`201` → **422** (was free `int`; service capped at 200). Audit archives list omits (default 50).
- **Audit archive-cold older_than_days Query OpenAPI (BR-17.2):** `POST /audit-logs/archive-cold` Query `older_than_days` ∈ 1–3650; omit/`null` → retention policy days; `0`/`-1`/`3651` → **422** (was free `int`; service `max(1, days)`). Audit **Archive cold audit logs** (`aria-label`); sends policy `cold_archive_after_days` when > 0.
- **Webhook deliveries limit Query OpenAPI (BR-18):** `GET /webhooks/{id}/deliveries` Query `limit` ∈ 1–200 (omit → 50); `0`/`-1`/`201` → **422** (was free `int`; service silently clamped 1–200). Integrations loads `limit=30`.
- **AI queries limit Query OpenAPI (BR-21):** `GET /ai/queries` Query `limit` ∈ 1–200 (omit → 50); `0`/`-1`/`201` → **422** (was free `int`; service silently clamped 1–200).
- **AI security alerts Query OpenAPI (BR-21.10):** `GET /ai/security/alerts` Query `limit` ∈ 1–200 (omit → 50); Query `min_score` ∈ 0–100 (omit/`null` → no floor); out-of-range → **422** (was free `int`). Service limit clamp remains defense-in-depth.
- **AI report templates limit Query OpenAPI (BR-21.7):** `GET /ai/reports/templates` Query `limit` ∈ 1–200 (omit → 50); `0`/`-1`/`201` → **422** (was free `int`; service silently clamped 1–200).
- **Sales monthly / export year-month Query OpenAPI (BR-14.1):** `GET /reports/sales/monthly` + `GET /reports/export` Query `year` ∈ 2000–2100; Query `month` ∈ 1–12; omit/`null` → current UTC / export default; out-of-range → **422** (was free `int`). Reports Sales monthly omits (current month).
- **Bank statement money OpenAPI (BR-10.3):** `BankStatementCreateBody.opening_balance` / `closing_balance` ∈ `FiniteMoneyValue` (finite; ±1e15; omit → 0); `nan`/`inf`/out-of-range → **422** (was unconstrained `float`). Accounting **Statement opening/closing balance** (`aria-label`s).
- **Bank statement import money Query OpenAPI (BR-10.3):** `POST /accounting/bank-statements/import` Query `opening_balance` / `closing_balance` ∈ `FiniteMoneyValue`; omit/`null` → feed/default; `nan`/`inf`/out-of-range → **422** (was free `float`). Same FE balance inputs.
- **AI document expected_amount Form OpenAPI (BR-21.8):** `POST /ai/documents/analyze` Form `expected_amount` ∈ `FiniteMoneyValue`; omit/`null` → no expected; `nan`/`inf`/out-of-range → **422** (was free `float`). AI **AI document expected amount** (`aria-label`); analyze omits blank.
- **AI report filters/params OpenAPI (BR-21.7):** `AiReportsGenerateBody` / `ExportBody` `filters` / `params` ∈ `AiReportFilters` (`extra=forbid`; dates ∈ `IsoDateQueryValue`; UUIDs ∈ `UuidIdValue`; `year` 2000–2100; `month` 1–12; `days` 1–365; `jurisdiction` / `compare` Literals); omit/`null` OK; unknown keys / non-UUID / bad dates → **422** (was free `dict[str, Any]`). AI Generate/Export currently omit filters (prompt path).
- **Positive / non-negative money OpenAPI:** Introduce `PositiveMoneyValue` (`gt=0`; finite; ≤1e15) + `NonNegativeMoneyValue` (`ge=0`; finite; ≤1e15); relocate `FiniteMoneyValue` beside them. `nan`/`inf`/out-of-range → **422** (`Field(gt=0)`/`ge=0` alone still allowed Inf).
- **Expense / recurring / AI draft amount OpenAPI (BR-9 / BR-21.8):** `ExpenseCreate` / `Update` / `RecurringExpenseCreate` / `Update` / `AiDocumentExpenseCreate.amount` ∈ `PositiveMoneyValue`; `nan`/`inf`/≤0 → **422** (was `Field(gt=0)` only). Expenses **Expense amount** / **Recurring amount** / **Edit amount** / **OCR expense amount** (`aria-label`s).
- **Payment / cash transfer / POS tender amount OpenAPI (BR-11 / BR-10.3 / BR-8.1):** `CustomerPaymentCreate` / `SupplierPaymentCreate` / `CashTransferCreate` / `PosPaymentLine.amount` ∈ `PositiveMoneyValue`; `nan`/`inf`/≤0 → **422**. Credit **Payment amount** + Accounting **Cash transfer amount** (`aria-label`s).
- **Opening balance / tax / approval threshold money OpenAPI (BR-10.1 / BR-12 / BR-9.3):** `OpeningBalanceLine.amount` / `TaxCalculateRequest.amount` / `ApprovalLevelUpdate.min_amount` / `ExpenseThresholdUpdate` thresholds ∈ `PositiveMoneyValue`; `nan`/`inf`/≤0 → **422**. Accounting **Opening balance amount** (`aria-label`); Expenses approval **min amount** already labeled.
- **Journal debit/credit + POS session cash OpenAPI (BR-10.2 / BR-8):** `JournalLineCreate.debit`/`credit` + `PosSessionOpen.opening_cash` + `PosSessionClose.actual_cash`/`closing_cash` ∈ `NonNegativeMoneyValue`; `nan`/`inf`/<0 → **422** (was `Field(ge=0)` only — Inf could pass). Accounting journal line debit/credit + POS **Opening cash** / **Counted cash** (`aria-label`s).
- **Bank statement line amount OpenAPI (BR-10.3):** `BankStatementLineCreate.amount` ∈ `FiniteMoneyValue` (signed; zero still rejected by validator); `nan`/`inf`/out-of-range → **422** (was unconstrained `float`). Accounting **Statement line amount** (`aria-label`).
- **Expense category budget OpenAPI (BR-9.1):** `ExpenseCategoryCreate` / `Update.budget_amount` ∈ `NonNegativeMoneyValue`; omit create → 0; omit update → no change; `nan`/`inf`/<0 → **422** (was `Field(ge=0)` only). Expenses **Expense category monthly budget** / **Edit expense category budget** (`aria-label`s).
- **Document header discount_amount OpenAPI:** `SalesInvoiceCreate` / `SalesQuotationCreate` / `SalesOrderCreate` / `PurchaseInvoiceCreate` / `PosSaleCreate.discount_amount` ∈ `NonNegativeMoneyValue`; `nan`/`inf`/<0 → **422**. Sales **Header discount**, Purchasing **Purchase invoice header discount**, POS **POS cart discount** (`aria-label`s).
- **Credit limit OpenAPI (BR-7.1 / BR-11):** `PartyCreate` / `Update.credit_limit` + `CreditLimitUpdate.credit_limit` ∈ `NonNegativeMoneyValue`; `nan`/`inf`/<0 → **422** (was unconstrained / `Field(ge=0)` only). Credit **Credit limit** (`aria-label`).
- **Tax rate money OpenAPI (BR-12):** `TaxCreate` / `Update` / `TaxComponent.rate` + `TaxCalculateRequest.rate` + line `tax_rate` ∈ `NonNegativeMoneyValue`; omit/`null` OK where optional; `nan`/`inf`/<0 → **422**. Tax **Tax rate percent** + Sales **Line tax percent** (`aria-label`s).
- **FX exchange rate OpenAPI (BR-2.6):** `ExchangeRateUpsert.rate_to_base` + payment/invoice `exchange_rate` ∈ `PositiveMoneyValue`; omit/`null` → resolve_rate; `nan`/`inf`/≤0 → **422** (was `Field(gt=0)` only). Credit **FX rate to base** (`aria-label`).
- **Line discount + percent rate OpenAPI:** Line `discount` ∈ `NonNegativeMoneyValue`; `CustomerGroupCreate` / `Update.discount_percent` + `EarlyPaySettingsUpdate.early_pay_discount_pct` ∈ `PercentRateValue` (0–100 finite); `nan`/`inf`/out-of-range → **422**. Sales **Line discount** / **Customer group discount percent**, Credit **Early pay discount percent**, Purchasing line discount (`aria-label`s).
- **Qty / geo Value aliases OpenAPI:** Introduce `PositiveQtyValue` (`gt=0` finite), `NonNegativeQtyValue` (`ge=0` finite), `FiniteQtyValue` (signed finite), `LatitudeValue` (−90…90), `LongitudeValue` (−180…180) — `nan`/`inf`/out-of-range → **422**.
- **Line / movement quantity OpenAPI:** Document/stock line `quantity` + GRN `received_qty` + UoM convert qty ∈ `PositiveQtyValue`; `StockAdjust.quantity` ∈ `FiniteQtyValue`; `nan`/`inf`/≤0 (where positive) → **422** (was `Field(gt=0)` / unconstrained — Inf could pass). Sales **Line quantity** (`aria-label`).
- **Product price OpenAPI (BR-5.1):** `ProductCreate` / `Update` / variant `cost_price` / `selling_price` + line `unit_price` / opening `unit_cost` ∈ `NonNegativeMoneyValue`; `nan`/`inf`/<0 → **422**. Inventory **Product selling price** / **Edit product selling price** + Sales **Line unit price** (`aria-label`s).
- **Stock / reorder / capacity / dims OpenAPI:** `stock_qty` / `reorder_level` / `reorder_qty` / warehouse `capacity` / `counted_qty` / `rejected_qty` / weight+dims ∈ `NonNegativeQtyValue`; `nan`/`inf`/<0 → **422**. Inventory / Multi-Store reorder + capacity + weight/dim controls (`aria-label`s).
- **Party GPS + UoM ratio OpenAPI:** `PartyCreate` / `Update.latitude` / `longitude` ∈ `LatitudeValue` / `LongitudeValue`; UoM `conversion_ratio` ∈ `PositiveQtyValue`; omit/`null` OK where optional; out-of-range → **422**. Sales/Purchasing **Customer/Supplier latitude/longitude** + Inventory **Unit conversion ratio** (`aria-label`s).
- **Unit interval confidence OpenAPI (BR-21.4):** Introduce `UnitIntervalValue` (0–1 finite). `AiLowStockPredictionLine.confidence` + `AiLowStockPredictionRequestsBody.min_confidence` ∈ `UnitIntervalValue`; omit/`null` OK / omit → 0; `nan`/`inf`/out-of-range → **422** (was `Field(ge=0, le=1)` Inf/NaN edge cases). AI **AI prediction min confidence** (`aria-label`); Create draft PR(s) omits blank.
- **POS / legacy sale totals OpenAPI (BR-8.1):** `PosSaleCreate` / `TransactionCreate` `subtotal` / `tax` / `total` ∈ `NonNegativeMoneyValue`; omit → 0; `nan`/`inf`/<0 → **422** (was unconstrained `float`; POS UI usually omits — server computes). POS **POS cart totals** (`aria-label`).
- **Approval matrix step OpenAPI (BR-9.3 / BR-6.x):** `ApprovalLevelUpdate` / `PurchaseApprovalLevelUpdate.step` ∈ 1–20; omit/`null` → list-order; `0`/`-1`/`21` → **422** (was unconstrained `int`).
- **Legacy sale payload OpenAPI (BR-8.1):** `TransactionCreate.payload` ∈ `LegacyTransactionPayload` (`extra=forbid`; optional `items[]` ∈ `LineItem`); omit/`null` OK; unknown keys → **422** (was free `dict`; poison keys persisted on `transactions.payload`). Prefer top-level `items`. Server stores only typed items.
- **POS sale payload OpenAPI (BR-8.1):** `PosSaleCreate` no longer accepts client `payload` (`extra=forbid` body); server builds receipt bag. Unknown top-level keys → **422** (was free `payload: dict` merge). POS Complete sale omits payload.
- **WebAuthn credential OpenAPI (BR-19):** `WebAuthnRegisterVerify.credential` ∈ `WebAuthnRegistrationCredential`; `WebAuthnLoginVerify.credential` ∈ `WebAuthnAuthenticationCredential` (`extra=forbid`; `type` ∈ `public-key`; response attestation/assertion fields required); incomplete/garbage → **422** (was free `dict`; late opaque **400**). Security **Passkey name** + login passkey path; serializers keep `type: public-key`.
- **Email test body OpenAPI (BR-20.3):** `EmailTestRequest` `extra=forbid`; optional `to` ∈ `EmailStr`; unknown keys → **422**.
- **WebAuthn clientExtensionResults OpenAPI (BR-19):** `clientExtensionResults` ∈ `WebAuthnClientExtensionResultsValue` (must be a JSON object; ≤32 keys; open-ended extension names preserved); list/string/number/`true` → **422** (was free `dict[str, Any]`). Security + Login passkey serializers send `getClientExtensionResults()` object.
- **WebAuthn transports OpenAPI (BR-19):** `WebAuthnAttestationResponse.transports` ∈ `AuthenticatorTransportListValue` (`usb`|`nfc`|`ble`|`internal`|`hybrid`|`smart-card`; strip/lower; legacy `cable`→`hybrid`); omit/`null` OK; unknown token / non-list → **422** (was free `list[str]`). Security `credentialToJson` filters to the allow-list.
- **Stock count bodies OpenAPI (BR-5.2):** `StockCountCreate` / `StockCountItemUpdate` / `StockCountItemsUpdate` / `StockCountCancel` `extra=forbid`; unknown keys → **422**. Inventory Counts **Save count lines** / cancel / create already labeled.
- **Auth session bodies OpenAPI (BR-19):** `Login` / `TwoFactorConfirm` / `Verify` / `Disable` / `RefreshRequest` / `PasswordResetRequest` / `Confirm` / `EmailVerifyConfirm` / `ResendVerificationRequest` / `ProfileUpdate` `extra=forbid`; unknown keys → **422**.
- **Sale line bodies OpenAPI (BR-7 / BR-8.1):** `LineItem` + `SalesInvoiceItemCreate` `extra=forbid`; unknown nested line keys → **422**.
- **Inventory stock mutation + notification prefs OpenAPI:** `StockAdjust` / `StockMove` / `StockOut` / `OpeningStockLine` / `OpeningStockCreate` + `NotificationPreferencesUpdate` `extra=forbid`; unknown keys → **422**.
- **Sales document create bodies OpenAPI (BR-7.2 / BR-7.3 / BR-7.4):** `SalesInvoiceCreate` / `SalesQuotationCreate` / `SalesOrderCreate` `extra=forbid`; unknown keys → **422** (nested `SalesInvoiceItemCreate` already forbid).
- **Sales return create bodies OpenAPI (BR-7.5):** `SalesReturnCreate` + `SalesReturnItemCreate` + `SalesReturnPost` `extra=forbid`; unknown keys → **422**.
- **Purchase order create bodies OpenAPI (BR-6.3):** `PurchaseOrderCreate` + `PurchaseOrderItemCreate` + `PurchaseOrderAmend` `extra=forbid`; unknown keys → **422**.
- **Purchase invoice create bodies OpenAPI (BR-6.5):** `PurchaseInvoiceCreate` + `PurchaseInvoiceItemCreate` + `PurchaseInvoiceUpdate` `extra=forbid`; unknown keys → **422**.
- **Expense create/update bodies OpenAPI (BR-9.2):** `ExpenseCreate` / `ExpenseUpdate` `extra=forbid`; unknown keys → **422**.
- **Journal create bodies OpenAPI (BR-10.2):** `JournalCreate` + `JournalLineCreate` `extra=forbid`; unknown keys → **422**.
- **Party create/update bodies OpenAPI (BR-7.1 / BR-6.1):** `PartyCreate` / `PartyUpdate` + `PartyContactCreate` / `PartyContactUpdate` `extra=forbid`; unknown keys → **422**.
- **Product create/update bodies OpenAPI (BR-5.1):** `ProductCreate` / `ProductUpdate` `extra=forbid`; unknown keys → **422**.
- **Store / warehouse create/update bodies OpenAPI (BR-2.5 / BR-5.2):** `StoreCreate` / `StoreUpdate` + `WarehouseCreate` / `WarehouseUpdate` `extra=forbid`; unknown keys → **422**.
- **Credit payment / limit bodies OpenAPI (BR-11):** `CustomerPaymentCreate` / `SupplierPaymentCreate` + `CreditLimitUpdate` / `CreditLimitOverrideBody` `extra=forbid`; unknown keys → **422**.
- **COA / opening balance / cash transfer bodies OpenAPI (BR-10):** `AccountCreate` / `AccountUpdate` + `OpeningBalanceCreate` / `OpeningBalanceLine` + `CashTransferCreate` `extra=forbid`; unknown keys → **422**.
- **Bank connection / cheque lifecycle bodies OpenAPI (BR-10.3):** `BankConnectionCreate` / `BankConnectionUpdate` + `ChequeLifecycleReason` `extra=forbid`; unknown keys → **422**.
- **GRN create bodies OpenAPI (BR-6.4):** `GrnCreate` + `GrnItemCreate` `extra=forbid`; unknown keys → **422**.
- **Purchase request / return bodies OpenAPI (BR-6.2 / BR-6.6):** `PurchaseRequestCreate` / `Item` / `Reject` / `Convert` + `PurchaseReturnCreate` / `Item` / `Cancel` `extra=forbid`; unknown keys → **422**.
- **Tax create/update/calculate bodies OpenAPI (BR-12):** `TaxCreate` / `TaxUpdate` / `TaxCalculateRequest` `extra=forbid`; unknown keys → **422**.
- **Webhook / report schedule bodies OpenAPI (BR-17 / BR-14):** `WebhookCreate` / `WebhookUpdate` + `ReportScheduleCreate` / `ReportScheduleUpdate` `extra=forbid`; unknown keys → **422**.
- **Recurring expense / expense category bodies OpenAPI (BR-9):** `RecurringExpenseCreate` / `Update` + `ExpenseCategoryCreate` / `Update` + `ExpenseReject` `extra=forbid`; unknown keys → **422**.
- **Stock transfer / document cancel bodies OpenAPI:** `StockTransferCreate` / `Item` / `Reject` + `PurchaseOrderCancel` / `PurchaseInvoiceCancel` / `SalesQuotationReject` / `SalesOrderConfirm` / `SalesOrderCancel` / `SalesInvoiceCancel` / `SalesReturnCancel` `extra=forbid`; unknown keys → **422**.
- **Tenant create/update bodies OpenAPI (BR-1 / BR-2):** `TenantCreate` / `TenantProfileUpdate` / `TenantSuspendRequest` / `TenantModulesUpdate` / `TenantStoreLimitUpdate` / `TenantMaxStoresOverrideUpdate` `extra=forbid`; unknown keys → **422**.
- **User / platform staff bodies OpenAPI (BR-3):** `UserCreate` / `UserUpdate` + `PlatformStaffUpdate` `extra=forbid`; unknown keys → **422**.
- **Branch / department bodies OpenAPI (BR-2.5):** `BranchCreate` / `BranchUpdate` + `DepartmentCreate` / `DepartmentUpdate` `extra=forbid`; unknown keys → **422**.
- **Catalog master bodies OpenAPI (BR-5.1):** `ProductCategoryCreate` / `Update` + `BrandCreate` / `Update` + `UnitOfMeasureCreate` / `Update` / `UnitConvertPreview` + `ProductVariantCreate` / `Update` + `ProductImagePrimaryUpdate` `extra=forbid`; unknown keys → **422**.
- **Customer group / low-stock suggestion bodies OpenAPI (BR-7.1 / BR-5.2):** `CustomerGroupCreate` / `Update` + `LowStockSuggestionsCreate` / `LowStockSuggestionLine` `extra=forbid`; unknown keys → **422**.
- **Settings / period / POS tender line bodies OpenAPI:** `StoreDrawerSettingsUpdate` / reorder + FEFO / early-pay / numbering / sales/purchasing/accounting/POS/print/backup settings + `FxAutoRefreshUpdate` / `BankAutoClearBody` / `PeriodCloseBody` / `PeriodReopenBody` / `PosPaymentLine` `extra=forbid`; unknown keys → **422**.
- **Tenant subscription assign body OpenAPI (BR-1):** `TenantSubscriptionAssign` `extra=forbid`; unknown keys → **422**.
- **Platform grant/revoke access bodies OpenAPI:** `PlatformGrantAccess` / `PlatformRevokeAccess` `extra=forbid`; unknown keys → **422**.
- **Expense decision / recurring skip bodies OpenAPI (BR-9):** `ExpenseDecision` / `RecurringSkipNext` `extra=forbid`; unknown keys → **422**.
- **POS drawer / session bodies OpenAPI (BR-8):** `PosDrawerOpen` / `PosSessionOpen` / `PosSessionClose` `extra=forbid`; unknown keys → **422**.
- **Journal unpost body OpenAPI (BR-10.2):** `JournalUnpost` `extra=forbid`; unknown keys → **422**.
- **WebAuthn Base64UrlValue OpenAPI (BR-19):** Introduce `Base64UrlValue` (strip; `A-Za-z0-9_-` + optional `=` pad; no spaces/`+`/`/`/`://`/`@`). Attestation `clientDataJSON` / `attestationObject` ∈ `Base64UrlValue`; blank/standard-b64/URL → **422** (was free `str` `min_length=1`; garbage reached verify as opaque **400**).
- **WebAuthn assertion Base64Url OpenAPI (BR-19):** Assertion `clientDataJSON` / `authenticatorData` / `signature` ∈ `Base64UrlValue`; blank/garbage → **422** (was free `str` `min_length=1`).
- **WebAuthn credential id/rawId OpenAPI (BR-19):** Registration + authentication credential `id` / `rawId` ∈ `Base64UrlValue`; blank/`!!!`/`http://…`/`+`/`/` → **422** (was free `str` `min_length=1`). Security + Login `bufferToBase64url` serializers.
- **WebAuthn userHandle Base64Url OpenAPI (BR-19):** Assertion `userHandle` ∈ `Base64UrlValue | None`; omit/`null` OK; blank/garbage → **422** (was unconstrained `str`). Login sends `null` when absent.
- **Profile phone Save omit OpenAPI (BR-15.2 / BR-19):** Company **Save my profile** omits blank `phone` (was raw `profilePhone` → blank **422**). Same omit pattern as `full_name`.
- **POS customer_name trim OpenAPI (BR-8.1):** POS Complete sale sends `customer_name: name.trim() || null` (was `name || null`; whitespace-only → **422**).
- **Document numbering nested forbid OpenAPI (BR-20.4):** `DocumentNumberingFields` `extra=forbid`; unknown nested keys under `*_numbering` → **422** (parent settings forbid alone accepted junk).
- **Company email Save omit OpenAPI (BR-2):** Company **Company email** (`aria-label`); Save omits blank/whitespace `email` (was raw `tenant.email` → blank **422** from `EmailStr`). Same omit pattern as phone/website.
- **2FA code FE trim OpenAPI (BR-19):** Login verify sends `code: totpCode.trim()`; login body `totp_code: totpCode.trim() || null`; Security confirm / backup-codes / disable send `code.trim()` (was raw `code`/`totpCode`; whitespace-only → **422** after schema strip).
- **User phone FE trim OpenAPI (BR-3):** Users create sends `phone: form.phone.trim() || null` (was `form.phone || null`; whitespace-only → **422**).
- **Webhook URL FE trim OpenAPI (BR-18.6):** Integrations create sends `url: hookUrl.trim()` (was raw `hookUrl`; padded/whitespace-only → **422** after schema strip).
- **Bank feed URL FE trim OpenAPI (BR-10.3):** Accounting Reconcile Connect sends `feed_url: connFeedUrl.trim() || null` for `http_json` (was raw `connFeedUrl`; whitespace-only → **422**).
- **Customer email FE trim OpenAPI (BR-7.1):** Sales create sends `email: customerEmail.trim() || null`; **Customer email** (`aria-label`) (was raw `customerEmail || null`; whitespace-only → **422** from `EmailStr`).
- **Login / reset email FE trim OpenAPI (BR-19):** Login + resend-verification send `email.trim()`; Login **Login email** (`aria-label`). Forgot password sends `email.trim()`; **Password reset email** (`aria-label`).
- **AI prediction line notes FE trim OpenAPI (BR-21.4):** Create draft PR(s) sends line `notes: String(x.notes || '').trim() || null` (was `x.notes || null`; whitespace/`!!!` from prediction payload → **422**).
- **Company company_name FE trim OpenAPI (BR-20.1):** Company Save sends `company_name: String(tenant.company_name || '').trim()` (was raw; whitespace-padded names relied on schema strip only).
- **Expense OCR Apply FE trim OpenAPI (BR-9.2):** Expenses OCR Apply trims payee/description/reference/date and omits blank after trim; **Expense OCR payee** / **description** / **reference** (`aria-label`s).
- **AI create-expense OCR fields FE trim OpenAPI (BR-21.8):** Create draft expense sends `payee` / `reference` / `category` via `String(…).trim() || null` (was raw extract fields; whitespace → **422**).
- **AI create-PI OCR fields FE trim OpenAPI (BR-21.8):** Create draft purchase invoice sends `supplier_invoice_number` / `notes` via `String(…).trim() || null`.
- **Supplier email aria-label OpenAPI (BR-6.1):** Purchasing **Supplier email** (`aria-label`); create already sends `supplierEmail.trim() || null`.
- **User email aria-label OpenAPI (BR-3):** Users **User email** (`aria-label`); create already sends `form.email.trim()`.
- **Sales document numbering prefix aria OpenAPI (BR-20.4):** Sales **Invoice** / **Quotation** / **Sales order** / **Sales return** / **Credit note** / **Payment receipt number prefix** (+ next-number) `aria-label`s.
- **Purchasing document numbering prefix aria OpenAPI (BR-20.4):** Purchasing **Purchase order** / **GRN** / **Purchase invoice** / **Purchase request** / **Purchase return** / **Debit note** / **Supplier payment number prefix** (+ next-number) `aria-label`s.
- **Expense number prefix aria OpenAPI (BR-20.4):** Expenses **Expense number prefix** + **Expense next number** (`aria-label`s).
- **Inventory document numbering prefix aria OpenAPI (BR-20.4):** Inventory **Stock transfer** / **Stock count** / **Opening stock number prefix** (+ next-number) `aria-label`s.
- **POS document numbering prefix aria OpenAPI (BR-20.4):** POS **POS sale number prefix** / **POS session number prefix** (+ next-number) `aria-label`s.
- **Company / Platform industry aria OpenAPI (BR-20.1):** Company **Company industry** + Platform **Tenant industry** selects (`aria-label`s); schema already `IndustryValue`.
- **Company date format aria OpenAPI (BR-20.2):** Company **Company date format** select (`aria-label`); schema Literal already.
- **Company decimal separator aria OpenAPI (BR-20.2):** Company **Company decimal separator** select (`aria-label`).
- **Company thousand separator aria OpenAPI (BR-20.2):** Company **Company thousand separator** select (`aria-label`).
- **Company time format aria OpenAPI (BR-20.2):** Company **Company time format** select (`aria-label`).
- **Backup frequency aria OpenAPI (BR-16):** Backup **Backup frequency** select (`aria-label`).
- **Recurring frequency aria OpenAPI (BR-9.5):** Expenses **Recurring frequency** select (`aria-label`).
- **Report schedule frequency aria OpenAPI (BR-14):** Reports **Report schedule frequency** select (`aria-label`).
- **Report schedule format aria OpenAPI (BR-14):** Reports **Report schedule format** select (`aria-label`).
- **Report schedule report type aria OpenAPI (BR-14):** Reports **Report schedule report type** select (`aria-label`).
- **Expense payment method aria OpenAPI (BR-9.2):** Expenses **Expense payment method** + **Recurring payment method** selects (`aria-label`s).
- **Tax rate type aria OpenAPI (BR-12.1):** Tax **Tax rate type** select (`aria-label`).
- **Tax pricing mode aria OpenAPI (BR-12.1):** Tax **Tax pricing mode** select (`aria-label`).
- **Product supply class aria OpenAPI (BR-5 / BR-12):** Inventory **Product supply class** + **Edit product supply class** selects (`aria-label`s).
- **Warehouse type aria OpenAPI (BR-2.4):** Multi-Store **Warehouse type** select (`aria-label`).
- **Cash drawer mode aria OpenAPI (BR-8.1):** Multi-Store **Cash drawer mode** select (`aria-label`).
- **Liquid account kind aria OpenAPI (BR-10.3):** Accounting **Liquid account kind** select (`aria-label`).
- **Cash transfer kind aria OpenAPI (BR-10.3):** Accounting **Cash transfer kind** select (`aria-label`).
- **Bank connection provider aria OpenAPI (BR-10.3):** Accounting **Bank connection provider** select (`aria-label`).
- **User record scope aria OpenAPI (BR-3.3):** Users **User record scope** select (`aria-label`).
- **POS payment method aria OpenAPI (BR-8.1):** POS **POS payment method** select (`aria-label`).
- **POS receipt paper aria OpenAPI (BR-8 / BR-20.4):** POS **POS receipt paper** select (`aria-label`).
- **Balance sheet compare aria OpenAPI (BR-14.5):** Reports **Balance sheet compare** select (`aria-label`).
- **Report schedule weekday aria OpenAPI (BR-14):** Reports **Report schedule weekday** select (`aria-label`; body `weekday` 0–6).
- **Customer profile type aria OpenAPI (BR-7.1):** Sales **Customer profile type** select (`aria-label`).
- **Customer status aria OpenAPI (BR-7.1):** Sales **Customer status** select (`aria-label`).
- **Supplier profile type aria OpenAPI (BR-6.1):** Purchasing **Supplier profile type** select (`aria-label`).
- **Supplier status aria OpenAPI (BR-6.1):** Purchasing **Supplier status** select (`aria-label`).
- **Purchase return reason aria OpenAPI (BR-6.6):** Purchasing **Purchase return reason** select (`aria-label`).
- **Subscription package aria OpenAPI:** Platform **Subscription package** select (`aria-label`).
- **Subscription term unit aria OpenAPI:** Platform **Subscription term unit** select (`aria-label`).
- **Platform grant role aria OpenAPI:** Platform Staff **Platform grant role** select (`aria-label`).
- **Platform staff change role aria OpenAPI:** Platform Staff row **Change platform staff role** select (`aria-label`).
- **PO amend unit aria OpenAPI (BR-6.3):** Purchasing Amend **PO unit** select (`aria-label`; same as Create).
- **Cash drawer store aria OpenAPI (BR-8.1):** Multi-Store **Cash drawer store** select (`aria-label`).
- **Edit account select aria OpenAPI (BR-10.1):** Accounting **Edit account** select (`aria-label`; pairs with **Edit account name**).
- **X-Tenant-ID header OpenAPI:** Optional `X-Tenant-ID` ∈ `UuidIdValue` (strip; lower; valid UUID); omit → JWT/key tenant alone; blank/slug/`!!!`/`http://…`/non-UUID → **422** (was free `str`; slug/`!!!` reached cross-tenant **403**). UUID ≠ claims tenant → **403**. Rate-limit middleware still buckets on raw header string (not auth).
- **X-API-Key header OpenAPI (BR-18.1):** Optional `X-API-Key` ∈ `ApiKeyHeaderValue` (strip; `rdk_…`; 1–200; ≥1 letter/digit after prefix; no `://`/`@`/spaces); omit → JWT Bearer path; blank/`!!!`/`http://…`/non-`rdk_` → **422** (was free `str`; garbage reached key lookup **401**). Authenticity remains hashed key lookup (**401**). `Authorization: Bearer rdk_…` still accepted (shape soft-path).
- **Multi-tenant headers docs honesty OpenAPI:** Appendix B / API Keys auth headers document `X-Tenant-ID` as JWT/key **UUID** (not slug `tenant_abc123`).
- **Response envelope honesty OpenAPI:** Success `env()` body is `{ success, data, message }` only — no JSON `timestamp` / `request_id`; correlation via **`X-Request-ID`** header.
- **List pagination honesty OpenAPI:** Most lists return unpaginated `data: T[]` (no global cursor/`items`+`pagination` contract).
- **Money JSON number honesty OpenAPI:** Request Values and response serializers use JSON **numbers** (not decimal strings) for money/qty; pilots use `honesty.money_json` (Decimal→finite float) on sales invoice/quotation/order/return (+ tax_breakdown), PO/PI/PR/GRN (+ PI tax_breakdown), expense/recurring, cheque, journal, POS payment/session, cash transfer/account (+ cash transfer audit amount), bank recon (+ unmatched journal debit/credit/signed_amount + statement arithmetic closing), PR/stock-transfer/stock-count qty, FX rate, product/variant/batch, tax/budget/discount/unit/tenant/warehouse, credit aging/statement/history, POS receipt, invoice print, inventory/store warehouse stock (+ store reorder-policy), stock-in/out responses, COA opening totals, reports sales daily/monthly/by-product/customer/salesperson/store/department + returns + inventory balance/valuation/movements/low-stock/expiry/transfers/stock-counts + purchases summary/pending/returns/suppliers + expenses summary + budget-vs-actual + cash-flow + balance sheet + trial balance + P&L, dashboard KPIs, AI inventory/sales/expenses/customer/documents (+ amount_mismatch flags), tax calc/report/filing pack + GH VAT return, party credit/balance, opening-stock response, low-stock purchase suggestions, expense OCR suggestion amount serializers, webhook sale/expense/PO/stock (+ stock.low) amount pilots, bank feed normalize/mock/sync amounts, sales-return discard audit qty, and notification payment-due / recurring-expense-due amounts.
- **Error envelope honesty OpenAPI:** Errors use FastAPI `detail` shapes; no success-style `{ success:false, error, request_id }` body field.
- **Request Content-Type honesty OpenAPI:** Default JSON `application/json`; multipart uploads are documented exceptions (not “all requests must be JSON”).
- **Response media-type honesty OpenAPI:** `env()` wraps JSON successes; CSV/PDF/XLSX/PNG/HTML/metrics/file downloads are non-envelope responses.
- **Date input honesty OpenAPI:** Shared `IsoDateQueryValue` accepts `YYYY-MM-DD` or ISO datetime (not only `YYYY-MM-DDTHH:MM:SSZ`).
- **HTTP Methods honesty OpenAPI:** `PUT` only on three upsert routes (FX rates, WH reorder, store reorder-policy); updates are primarily `PATCH`.
- **Product lookup list shape honesty OpenAPI (BR-18.2):** Lookup returns `{ q, barcode, count, items }` with bounded `limit` — not a bare array / not global cursor pagination.
- **Error code catalog honesty OpenAPI:** Docs list real `detail` / `detail.code` values; metrics status→code maps (e.g. `VALIDATION_ERROR`) are not response body fields.
- **Expense reject reason defense-in-depth OpenAPI (BR-9.3):** Service `reject_expense` uses `require_honest_narrative` (**400** on blank/URL/punctuation) matching OpenAPI `ExpenseRejectReasonValue` (**422**).
- **Recurring skip reason defense-in-depth OpenAPI (BR-9.5):** Service `skip_next_recurring` uses `require_honest_narrative` (**400**) matching `RecurringSkipReasonValue` (**422**).
- **Journal unpost reason defense-in-depth OpenAPI (BR-10.2):** Service `unpost_journal_entry` uses `require_honest_narrative` (**400**) matching `JournalUnpostReasonValue` (**422**).
- **Tenant suspend reason defense-in-depth OpenAPI (BR-1 / BR-19):** `POST /tenants/me/suspend` + `POST /tenants/{ref}/suspend` use `require_honest_narrative` (**400**) matching `TenantSuspendReasonValue` (**422**).
- **Sales invoice money_json Decimal pilot OpenAPI (BR-7.4):** `serialize_invoice` money/qty fields use `honesty.money_json` (Decimal→finite JSON number; not decimal strings).
- **Tenant company name aria OpenAPI (BR-1):** Platform create **Tenant company name** input (`aria-label`).
- **Tenant currency aria OpenAPI (BR-1 / BR-2.6):** Platform create **Tenant currency** input (`aria-label`).
- **Tenant admin email aria OpenAPI (BR-1):** Platform create **Tenant admin email** input (`aria-label`).
- **Subscription term length aria OpenAPI:** Platform **Subscription term length** input (`aria-label`).
- **Store entitlement override aria OpenAPI:** Platform **Store entitlement override** + **Store entitlement override draft** inputs (`aria-label`s).
- **Backup retention hour aria OpenAPI (BR-16):** Backup **Backup retention count** + **Backup hour UTC** inputs (`aria-label`s).
- **POS split tender aria OpenAPI (BR-8.1):** POS **POS cash tender** + **POS card tender** inputs (`aria-label`s).
- **Period close/reopen reason defense-in-depth OpenAPI (BR-10.2):** Service `close_books` / `reopen_books` use `require_honest_narrative` (**400**) matching `PeriodCloseReasonValue` (**422**).
- **Cheque bounce/cancel reason defense-in-depth OpenAPI (BR-10.4):** Service `bounce_cheque` / `cancel_cheque` use `require_honest_narrative` (**400**) matching `ChequeLifecycleReasonValue` (**422**).
- **Product CSV import free-text honesty OpenAPI (BR-5.1 / BR-18.2):** CSV validate path applies `ProductNameValue` / `ProductSkuValue` / `ProductDescriptionValue` (TypeAdapter) so `!!!`/`http://…`/bad SKU fail the row report (was strip-only name/sku required).
- **Company SMTP port aria OpenAPI (BR-20.3):** Company **Company SMTP port** input (`aria-label`).
- **Stock quantity aria OpenAPI (BR-5.2):** Inventory **Stock-in quantity** + **Opening stock quantity** + **Stock adjustment quantity** + **Stock-out quantity** inputs (`aria-label`s).
- **Quotation reject reason defense-in-depth OpenAPI (BR-7.2):** Service `reject_quotation` uses `require_honest_narrative` (**400**) matching `SalesQuotationRejectReasonValue` (**422**).
- **Purchase request reject reason defense-in-depth OpenAPI (BR-6.2):** Service `reject_request` uses `require_honest_narrative` (**400**) matching `PurchaseRequestRejectReasonValue` (**422**).
- **Stock transfer reject/cancel reason defense-in-depth OpenAPI (BR-5.2 / BR-13.2):** `reject_transfer` / `cancel_transfer` use `require_honest_narrative` (**400**) matching `StockTransferRejectReasonValue` (**422**).
- **Sales invoice cancel reason defense-in-depth OpenAPI (BR-7.4):** `cancel_sales_invoice` uses `require_honest_narrative` (**400**) matching `SalesInvoiceCancelReasonValue` (**422**).
- **Sales order cancel reason defense-in-depth OpenAPI (BR-7.3):** `cancel_order` uses `require_honest_narrative` (**400**) matching `SalesOrderCancelReasonValue` (**422**).
- **Sales return cancel reason defense-in-depth OpenAPI (BR-7.5):** `cancel_return` uses `require_honest_narrative` (**400**) matching `SalesReturnCancelReasonValue` (**422**).
- **PO cancel reason defense-in-depth OpenAPI (BR-6.3):** `cancel_purchase_order` uses `require_honest_narrative` (**400**) matching `PurchaseOrderCancelReasonValue` (**422**).
- **PO amend reason defense-in-depth OpenAPI (BR-6.3):** `amend_purchase_order` uses `require_honest_narrative` (**400**) matching `PurchaseOrderAmendReasonValue` (**422**).
- **PI cancel reason defense-in-depth OpenAPI (BR-6.5):** `cancel_purchase_invoice` uses `require_honest_narrative` (**400**) matching `PurchaseInvoiceCancelReasonValue` (**422**).
- **Purchase return cancel reason defense-in-depth OpenAPI (BR-6.6):** `cancel_purchase_return` uses `require_honest_narrative` (**400**) matching `PurchaseReturnCancelReasonValue` (**422**).
- **Stock count cancel reason defense-in-depth OpenAPI (BR-5.2):** `cancel_count` uses `require_honest_narrative` (**400**) matching `StockCountCancelReasonValue` (**422**).
- **GRN rejection reason defense-in-depth OpenAPI (BR-6.4):** `create_grn` applies `require_honest_narrative` when `rejected_qty > 0` (**400**) matching `GrnRejectionReasonValue` (**422**).
- **Credit override reason defense-in-depth OpenAPI (BR-11.1):** `enforce_customer_credit_limit` uses `require_honest_narrative` (**400** `CREDIT_OVERRIDE_REASON_REQUIRED`) matching `CreditOverrideReasonValue` (**422**).
- **POS drawer open reason defense-in-depth OpenAPI (BR-8.1):** Manual `open_drawer(require_specific_reason=True)` uses `require_honest_narrative` (min 3) + placeholder reject (**400** `DRAWER_REASON_REQUIRED`) matching `PosDrawerOpenReasonValue` (**422**).
- **Cheque money_json Decimal pilot OpenAPI (BR-10.4):** `serialize_cheque.amount` uses `honesty.money_json`.
- **Expense money_json Decimal pilot OpenAPI (BR-9.2 / BR-9.5):** `serialize_expense` / `serialize_recurring` `amount` use `honesty.money_json`.
- **Journal money_json Decimal pilot OpenAPI (BR-10.2):** `serialize_journal` totals/line debit|credit use `honesty.money_json`.
- **Quotation money_json Decimal pilot OpenAPI (BR-7.2):** `serialize_quotation` money/qty fields use `honesty.money_json`.
- **Purchase order money_json Decimal pilot OpenAPI (BR-6.3):** `serialize_po` money/qty fields use `honesty.money_json`.
- **Payment FX rate aria OpenAPI (BR-2.6 / BR-11):** Credit **Payment FX rate** input (`aria-label`).
- **Payment terms days aria OpenAPI (BR-11):** Credit **Customer payment terms days** + **Supplier payment terms days** inputs (`aria-label`s).
- **Customer credit limit aria OpenAPI (BR-11.1):** Sales **Customer credit limit** input (`aria-label`).
- **Purchase request quantity aria OpenAPI (BR-6.2):** Purchasing **Purchase request quantity** input (`aria-label`).
- **Purchase order qty/price aria OpenAPI (BR-6.3):** Purchasing **Purchase order quantity** + **Purchase order unit price** inputs (`aria-label`s).
- **Purchase invoice qty/price aria OpenAPI (BR-6.5):** Purchasing **Purchase invoice quantity** + **Purchase invoice unit price** inputs (`aria-label`s).
- **Tax calculator amount aria OpenAPI (BR-12.1):** Tax **Tax calculator amount** input (`aria-label`).
- **Optional narrative defense-in-depth OpenAPI:** `honesty.optional_honest_narrative` — omit/`None`/blank → `None`; non-blank garbage/URL/punctuation → **400** (mirrors optional OpenAPI narrative Values).
- **Expense approve comment defense-in-depth OpenAPI (BR-9.3):** `approve_expense` uses `optional_honest_narrative` (**400**) matching `ExpenseApproveCommentValue` (**422**).
- **POS shift close notes defense-in-depth OpenAPI (BR-8.2):** `close_session` uses `optional_honest_narrative` (**400**) matching `PosSessionCloseNotesValue` (**422**).
- **Cash transfer notes defense-in-depth OpenAPI (BR-10.3):** `create_transfer` uses `optional_honest_narrative` (**400**) matching `CashTransferNotesValue` (**422**).
- **Stock count notes defense-in-depth OpenAPI (BR-5.2):** `create_count` uses `optional_honest_narrative` (**400**) matching `StockCountNotesValue` (**422**).
- **Sales order money_json Decimal pilot OpenAPI (BR-7.3):** `serialize_order` money/qty/reserved fields use `honesty.money_json`.
- **Sales return money_json Decimal pilot OpenAPI (BR-7.5):** `serialize_return` money/qty fields use `honesty.money_json`.
- **Purchase invoice money_json Decimal pilot OpenAPI (BR-6.5):** `serialize_purchase_invoice` money/qty/FX fields use `honesty.money_json`.
- **Purchase return money_json Decimal pilot OpenAPI (BR-6.6):** `serialize_purchase_return` money/qty fields use `honesty.money_json`.
- **GRN money_json Decimal pilot OpenAPI (BR-6.4):** `serialize_grn` received/accepted/rejected qty use `honesty.money_json`.
- **Cash account/transfer money_json Decimal pilot OpenAPI (BR-10.3):** `serialize_account` balances + `serialize_transfer.amount` use `honesty.money_json`.
- **Bank statement money_json Decimal pilot OpenAPI (BR-10.3):** `serialize_statement` balances + `serialize_line.amount` (+ liquid account balances) use `honesty.money_json`.
- **POS payment/drawer money_json Decimal pilot OpenAPI (BR-8.1 / BR-8.2):** `serialize_payment.amount` + `drawer_summary` cash totals use `honesty.money_json`.
- **FX rate money_json Decimal pilot OpenAPI (BR-2.6):** `serialize_rate.rate_to_base` uses `honesty.money_json`.
- **Purchase request qty money_json Decimal pilot OpenAPI (BR-6.2):** `serialize_request` line `quantity` uses `honesty.money_json`.
- **Stock count qty money_json Decimal pilot OpenAPI (BR-5.2):** `serialize_item` expected/counted qty use `honesty.money_json`.
- **Stock transfer qty money_json Decimal pilot OpenAPI (BR-5.2 / BR-13.2):** `serialize_transfer` item qty/shipped/received use `honesty.money_json`.
- **Company inactivity timeout aria OpenAPI (BR-19.3):** Company **Company inactivity timeout minutes** input (`aria-label`).
- **Company store allocation aria OpenAPI:** Multi-Store **Company store allocation** input (`aria-label`).
- **Report schedule hour UTC aria OpenAPI (BR-14):** Reports **Report schedule hour UTC** input (`aria-label`).
- **Sales customer payment terms days aria OpenAPI (BR-11):** Sales **Customer payment terms days** input (`aria-label`).
- **Purchase order line discount aria OpenAPI (BR-6.3):** Purchasing **Purchase order line discount** input (`aria-label`).
- **PO amend unit price/discount aria OpenAPI (BR-6.3):** Purchasing **PO amend unit price** + **PO amend line discount** inputs (`aria-label`s).
- **Purchase invoice FX rate aria OpenAPI (BR-6.5 / BR-2.6):** Purchasing **Purchase invoice FX rate** input (`aria-label`).
- **Purchase invoice tax rate percent aria OpenAPI (BR-6.5):** Purchasing **Purchase invoice tax rate percent** input (`aria-label`).
- **Stock count line notes defense-in-depth OpenAPI (BR-5.2):** `update_count_items` uses `optional_honest_narrative` (**400**) matching `StockCountItemNotesValue` (**422**); explicit `null` clears.
- **Purchase order notes defense-in-depth OpenAPI (BR-6.3):** `create_purchase_order` / `amend_purchase_order` use `optional_honest_narrative` (**400**) matching `PurchaseOrderNotesValue` (**422**).
- **GRN notes defense-in-depth OpenAPI (BR-6.4):** `create_grn` uses `optional_honest_narrative` (**400**) matching `GrnNotesValue` (**422**).
- **Purchase return notes defense-in-depth OpenAPI (BR-6.6):** `create_purchase_return` uses `optional_honest_narrative` (**400**) matching `PurchaseReturnNotesValue` (**422**).
- **Purchase invoice notes defense-in-depth OpenAPI (BR-6.5):** `create_purchase_invoice` + OCR draft apply use `optional_honest_narrative` (**400**) matching `PurchaseInvoiceNotesValue` (**422**).
- **Sales document notes defense-in-depth OpenAPI (BR-7.2 / BR-7.3 / BR-7.4):** Quotation / order / invoice create use `optional_honest_narrative` (**400**) matching `SalesDocumentNotesValue` (**422**).
- **Sales return notes defense-in-depth OpenAPI (BR-7.5):** `create_return` uses `optional_honest_narrative` (**400**) matching `SalesReturnNotesValue` (**422**).
- **Payment notes defense-in-depth OpenAPI (BR-11):** Customer / supplier payment create use `optional_honest_narrative` (**400**) matching `PaymentNotesValue` (**422**); auto-alloc / early-discount system notes remain when user notes omitted.
- **Stock transfer notes defense-in-depth OpenAPI (BR-5.2 / BR-13.2):** `create_transfer` uses `optional_honest_narrative` (**400**) matching `StockTransferNotesValue` (**422**).
- **Purchase request notes defense-in-depth OpenAPI (BR-6.2):** `create_request` header + line notes use `optional_honest_narrative` (**400**) matching `PurchaseRequestNotesValue` (**422**).
- **Bank statement notes/line description defense-in-depth OpenAPI (BR-10.3):** `create_statement` notes + line `description` use `optional_honest_narrative` (**400**) matching `BankStatementNotesValue` / `BankStatementLineDescriptionValue` (**422**).
- **Bank clear-group notes defense-in-depth OpenAPI (BR-10.3):** `create_clearing_group` uses `optional_honest_narrative` (**400**) matching `BankClearGroupNotesValue` (**422**).
- **Backup notes defense-in-depth OpenAPI (BR-16):** `create_backup` uses `optional_honest_narrative` (**400**) matching `BackupNotesValue` (**422**).
- **Webhook description defense-in-depth OpenAPI (BR-18.6):** `create_endpoint` / `update_endpoint` use `optional_honest_narrative` (**400**) matching `WebhookDescriptionValue` (**422**).
- **Brand description defense-in-depth OpenAPI (BR-5.1):** `create_brand` / `update_brand` use `optional_honest_narrative` (**400**) matching `BrandDescriptionValue` (**422**).
- **Stock movement notes defense-in-depth OpenAPI (BR-5.2):** `stock_in_with_batch` / `stock_out_with_batch` / `apply_stock_change` use `optional_honest_narrative` (**400**) matching StockIn/Out/AdjustNotesValue (**422**).
- **Opening stock notes defense-in-depth OpenAPI (BR-5.2):** `post_opening_stock` header + line notes use `optional_honest_narrative` (**400**) matching `OpeningStockNotesValue` (**422**).
- **Opening balance notes defense-in-depth OpenAPI (BR-10.1):** `post_coa_opening_balances` uses `optional_honest_narrative` (**400**) matching `OpeningBalanceNotesValue` (**422**).
- **Journal description defense-in-depth OpenAPI (BR-10.2):** `post_journal_entry` description uses `require_honest_narrative` (min 2) + line `description` uses `optional_honest_narrative` (**400**) matching `JournalDescriptionValue` / `JournalLineDescriptionValue` (**422**).
- **Product money_json Decimal pilot OpenAPI (BR-5.1):** `serialize_product` cost/selling/stock/reorder/dimensions use `honesty.money_json`.
- **Variant money_json Decimal pilot OpenAPI (BR-5.1):** `serialize_variant` cost/selling/stock use `honesty.money_json`.
- **Batch qty money_json Decimal pilot OpenAPI (BR-5.2):** `serialize_batch.quantity` uses `honesty.money_json`.
- **Tax rate money_json Decimal pilot OpenAPI (BR-12.1):** `serialize_tax_rate.rate` uses `honesty.money_json`.
- **Expense category budget money_json Decimal pilot OpenAPI (BR-9.1):** `serialize_category.budget_amount` uses `honesty.money_json`.
- **Customer group discount money_json Decimal pilot OpenAPI (BR-7.1):** `serialize_group.discount_percent` uses `honesty.money_json`.
- **Unit conversion money_json Decimal pilot OpenAPI (BR-5.1):** `serialize_unit.conversion_ratio` uses `honesty.money_json`.
- **Tenant threshold money_json Decimal pilot OpenAPI (BR-9.3 / BR-11):** `serialize_tenant` expense approval / L2 / early-pay % use `honesty.money_json`.
- **Warehouse capacity money_json Decimal pilot OpenAPI (BR-2.4):** `serialize_warehouse.capacity` uses `honesty.money_json`.
- **Variant selling price aria OpenAPI (BR-5.1):** Inventory Variants **Variant selling price …** input (`aria-label`).
- **Opening stock unit cost aria OpenAPI (BR-5.2):** Inventory Opening **Opening stock unit cost** input (`aria-label`).
- **Stock transfer quantity aria OpenAPI (BR-5.2 / BR-13.2):** Inventory + Multi-Store **Stock transfer quantity** inputs (`aria-label`s).
- **Supplier payment terms days aria OpenAPI (BR-6.1):** Purchasing **Supplier payment terms days** input (`aria-label`).
- **Purchase return quantity aria OpenAPI (BR-6.6):** Purchasing **Purchase return quantity** input (`aria-label`).
- **Sales invoice FX rate aria OpenAPI (BR-7.4 / BR-2.6):** Sales **Sales invoice FX rate** input (`aria-label`).
- **Sales invoice pay amount aria OpenAPI (BR-7.4 / BR-11):** Sales Invoices **Sales invoice pay amount …** input (`aria-label`).
- **Expense description defense-in-depth OpenAPI (BR-9.2 / BR-9.5):** `create_expense` / `update_expense` / recurring create+update use `optional_honest_narrative` (**400**) matching `ExpenseDescriptionValue` (**422**); blank → empty narrative.
- **Expense payee defense-in-depth OpenAPI (BR-9.2 / BR-9.5):** expense + recurring create/update use `optional_honest_narrative` (**400**) matching `ExpensePayeeValue` (**422**); `clear_payee` still clears.
- **Expense reference defense-in-depth OpenAPI (BR-9.2):** `create_expense` / `update_expense` use `optional_honest_narrative` (**400**) matching `ExpenseReferenceValue` (**422**); create omit/`null` → auto `EXP-YYYY-NNNN`.
- **Cash transfer reference defense-in-depth OpenAPI (BR-10.3):** `create_transfer` uses `optional_honest_narrative` (**400**) matching `CashTransferReferenceValue` (**422**); omit → auto `XFER-YYYY-NNNN`.
- **Journal reference defense-in-depth OpenAPI (BR-10.2):** `post_journal_entry` uses `optional_honest_narrative` (**400**) matching `JournalReferenceValue` (**422**).
- **Payment reference defense-in-depth OpenAPI (BR-11):** Customer / supplier payment create use `optional_honest_narrative` (**400**) matching `PaymentReferenceValue` (**422**).
- **POS payment reference defense-in-depth OpenAPI (BR-8.1):** `resolve_sale_payments` tender `reference` uses `optional_honest_narrative` (**400**) matching `PaymentReferenceValue` (**422**).
- **Opening balance reference defense-in-depth OpenAPI (BR-10.1):** `post_coa_opening_balances` uses `optional_honest_narrative` (**400**) matching `OpeningBalanceReferenceValue` (**422**); omit → auto `COA-OPEN-YYYYMMDD`.
- **Opening stock reference defense-in-depth OpenAPI (BR-5.2):** `post_opening_stock` uses `optional_honest_narrative` (**400**) matching `OpeningStockReferenceValue` (**422**); omit → auto `OS-YYYY-NNNN`.
- **Product description defense-in-depth OpenAPI (BR-5.1):** `POST|PATCH /products` use `optional_honest_narrative` (**400**) matching `ProductDescriptionValue` (**422**); PATCH `null` clears.
- **Approval matrix label defense-in-depth OpenAPI (BR-9.3 / PR):** Expense + PR `normalize_approval_matrix` labels use `optional_honest_narrative` (**400**) matching `ApprovalLevelLabelValue` (**422**); blank → `Level N`.
- **Purchase request department defense-in-depth OpenAPI (BR-6.2):** `create_request` uses `optional_honest_narrative` (**400**) matching `PurchaseRequestDepartmentValue` (**422**).
- **Bank statement line external_ref defense-in-depth OpenAPI (BR-10.3):** `create_statement` line `external_ref` uses `optional_honest_narrative` (**400**) matching `BankStatementLineExternalRefValue` (**422**).
- **Print header/footer text defense-in-depth OpenAPI (BR-20.4):** `apply_print_branding_update` header/footer use `optional_honest_narrative` (**400**) matching `PrintHeaderTextValue` / `PrintFooterTextValue` (**422**); `null` clears.
- **Credit AR aging money_json Decimal pilot OpenAPI (BR-11):** `ar_aging` party credit/balance + document due/FX use `honesty.money_json`.
- **Credit AP aging money_json Decimal pilot OpenAPI (BR-11):** `ap_aging` party credit/balance + document due use `honesty.money_json`.
- **Credit statement money_json Decimal pilot OpenAPI (BR-11):** `customer_statement` / `supplier_statement` balances + line debit/credit/due use `honesty.money_json`.
- **Credit history money_json Decimal pilot OpenAPI (BR-7.1 / BR-6.1):** Customer / supplier history money fields use `honesty.money_json`.
- **POS receipt money_json Decimal pilot OpenAPI (BR-8):** `build_receipt_payload` totals + line qty/price use `honesty.money_json`.
- **Invoice print money_json Decimal pilot OpenAPI (BR-7.4 / BR-20.4):** Invoice print payload money/qty fields use `honesty.money_json`.
- **Inventory warehouse stock money_json Decimal pilot OpenAPI (BR-5.2 / BR-5.4):** Warehouse stock list/reorder + per-product warehouse stock qty/reorder use `honesty.money_json`.
- **Stock-in/out response money_json Decimal pilot OpenAPI (BR-5.2):** `stock_in_with_batch` / `stock_out_with_batch` return qty/cost use `honesty.money_json`.
- **Opening balance response money_json Decimal pilot OpenAPI (BR-10.1):** `post_coa_opening_balances` totals/plug use `honesty.money_json`.
- **Store inventory stock money_json Decimal pilot OpenAPI (BR-13):** Multi-store inventory list qty/reorder use `honesty.money_json`.
- **Product cost price aria OpenAPI (BR-5.1):** Inventory Create **Product cost price** input (`aria-label`).
- **Edit product cost price aria OpenAPI (BR-5.1):** Inventory Edit **Edit product cost price** input (`aria-label`).
- **Variant cost price aria OpenAPI (BR-5.1):** Inventory Variants **Variant cost price …** input (`aria-label`).
- **Batch number defense-in-depth OpenAPI (BR-5.2 / BR-6.4):** `stock_in_with_batch` (+ GRN path) uses `optional_honest_narrative` (**400**) matching `BatchNumberValue` (**422**).
- **Stock-in reference_type defense-in-depth OpenAPI (BR-5.2):** `stock_in_with_batch` uses `optional_honest_narrative` (**400**) matching `StockInReferenceTypeValue` (**422**); lowercased when set.
- **Stock movement reference_id defense-in-depth OpenAPI (BR-5.2):** `stock_in_with_batch` / `stock_out_with_batch` (+ `POST /inventory/stock-out`) use `optional_honest_narrative` (**400**) matching `StockMovementReferenceIdValue` (**422**).
- **POS customer_name defense-in-depth OpenAPI (BR-8.1):** POS sale create uses `optional_honest_narrative` (**400**) matching `PosCustomerNameValue` (**422**); omit → walk-in / party name.
- **Bank connection display_name defense-in-depth OpenAPI (BR-10.3):** `create_connection` / `update_connection` use `optional_honest_narrative` (**400**) matching `BankConnectionDisplayNameValue` (**422**).
- **Bank external_account_id defense-in-depth OpenAPI (BR-10.3):** `create_connection` / `update_connection` use `optional_honest_narrative` (**400**) matching `BankExternalAccountIdValue` (**422**).
- **Party code defense-in-depth OpenAPI (BR-6.1 / BR-7.1):** `_normalize_party_profile` uses `optional_honest_narrative` (**400**) matching `PartyCodeValue` (**422**).
- **Party category defense-in-depth OpenAPI (BR-6.1 / BR-7.1):** `_normalize_party_profile` uses `optional_honest_narrative` (**400**) matching `PartyCategoryValue` (**422**).
- **Party address defense-in-depth OpenAPI (BR-6.1 / BR-7.1):** `_normalize_party_profile` uses `optional_honest_narrative` (**400**) matching `AddressValue` (**422**).
- **Cheque number defense-in-depth OpenAPI (BR-11 / BR-10.4):** `create_from_customer_payment` / `create_from_supplier_payment` use `optional_honest_narrative` (**400**) matching `ChequeNumberValue` (**422**); omit → reference/payment_number fallback.
- **Cheque bank_name defense-in-depth OpenAPI (BR-11 / BR-10.4):** cheque create-from-payment uses `optional_honest_narrative` (**400**) matching `BankNameValue` (**422**).
- **Sales daily report money_json Decimal pilot OpenAPI (BR-14.1):** `sales_daily` revenue/tax/discount fields use `honesty.money_json`.
- **Sales monthly report money_json Decimal pilot OpenAPI (BR-14.1):** `sales_monthly` totals + daily revenue use `honesty.money_json`.
- **Inventory balance report money_json Decimal pilot OpenAPI (BR-14.2):** `inventory_balance` qty/cost/value totals use `honesty.money_json`.
- **Inventory valuation report money_json Decimal pilot OpenAPI (BR-14.2):** `inventory_valuation` totals use `honesty.money_json`.
- **Inventory movements report money_json Decimal pilot OpenAPI (BR-14.2 / BR-5.3):** `inventory_movements` qty before/after use `honesty.money_json`.
- **Budget vs actual report money_json Decimal pilot OpenAPI (BR-14.4):** expenses budget-vs-actual budget/actual/variance use `honesty.money_json`.
- **Dashboard money_json Decimal pilot OpenAPI (BR-4):** `build_dashboard` sales/purchases/expenses + recent sale totals use `honesty.money_json`.
- **AI inventory predictions money_json Decimal pilot OpenAPI (BR-21.3 / BR-21.4):** forecast stock/reorder/sold/velocity/order qty use `honesty.money_json`.
- **AI sales analysis money_json Decimal pilot OpenAPI (BR-21.5):** monthly series + RFM monetary + forecast use `honesty.money_json`.
- **AI expenses analysis money_json Decimal pilot OpenAPI (BR-21.6):** budget alerts + unusual amounts + category budgets use `honesty.money_json`.
- **AI customer assist money_json Decimal pilot OpenAPI (BR-21.9):** open balance / credit limit use `honesty.money_json`.
- **Tax calculate money_json Decimal pilot OpenAPI (BR-12.1):** `compute_tax_breakdown` net/tax/gross/rate use `honesty.money_json`.
- **Tax report money_json Decimal pilot OpenAPI (BR-12):** `tax_report` output/input/net payable + taxable nets use `honesty.money_json`.
- **Opening stock response money_json Decimal pilot OpenAPI (BR-5.2):** `post_opening_stock` line `unit_cost` / `line_value` use `honesty.money_json`.
- **Tax reverse charge aria OpenAPI (BR-12.1):** Tax Create rate **Tax reverse charge** checkbox (`aria-label`).
- **Tax calculate button aria OpenAPI (BR-12.1):** Tax Calculator **Calculate tax** button (`aria-label`).
- **Sales by product report money_json Decimal pilot OpenAPI (BR-14.1):** `sales_by_product` qty/revenue totals use `honesty.money_json`.
- **Sales by customer report money_json Decimal pilot OpenAPI (BR-14.1):** `sales_by_customer` invoice/POS revenue/tax use `honesty.money_json`.
- **Sales returns report money_json Decimal pilot OpenAPI (BR-14.1):** `sales_returns_summary` amounts/qty use `honesty.money_json`.
- **Sales by salesperson report money_json Decimal pilot OpenAPI (BR-14.1):** `sales_by_salesperson` revenue/tax use `honesty.money_json`.
- **Sales by store report money_json Decimal pilot OpenAPI (BR-14.1 / BR-2.5):** `sales_by_store` revenue/tax use `honesty.money_json`.
- **Sales by department report money_json Decimal pilot OpenAPI (BR-14.1 / BR-2.5):** `sales_by_department` revenue/tax use `honesty.money_json`.
- **Inventory low-stock report money_json Decimal pilot OpenAPI (BR-14.2):** `inventory_low_stock` stock/reorder/suggested qty use `honesty.money_json`.
- **Inventory expiry report money_json Decimal pilot OpenAPI (BR-14.2):** `inventory_expiry` batch qty totals use `honesty.money_json`.
- **Inventory transfers report money_json Decimal pilot OpenAPI (BR-13.2):** `inventory_transfers` qty/shipped/received use `honesty.money_json`.
- **Inventory stock-counts report money_json Decimal pilot OpenAPI (BR-5.2):** `inventory_stock_counts` variance qty use `honesty.money_json`.
- **Purchases summary report money_json Decimal pilot OpenAPI (BR-14.3):** `purchases_summary` total/outstanding use `honesty.money_json`.
- **Purchases pending orders report money_json Decimal pilot OpenAPI (BR-14.3):** `purchases_pending_orders` amounts/qty use `honesty.money_json`.
- **Purchases returns report money_json Decimal pilot OpenAPI (BR-14.3):** `purchases_returns_summary` amounts/qty use `honesty.money_json`.
- **Purchases by supplier report money_json Decimal pilot OpenAPI (BR-14.3):** `purchases_by_supplier` totals use `honesty.money_json`.
- **Expenses summary report money_json Decimal pilot OpenAPI (BR-14.4):** `expenses_summary` amount by category use `honesty.money_json`.
- **Cash flow report money_json Decimal pilot OpenAPI (BR-14.5):** `cash_flow` inflows/outflows/section nets use `honesty.money_json`.
- **Balance sheet report money_json Decimal pilot OpenAPI (BR-14.5):** balance sheet section balances + compare deltas use `honesty.money_json`.
- **Trial balance money_json Decimal pilot OpenAPI (BR-14.5 / BR-10.6):** `trial_balance` debit/credit/balance use `honesty.money_json`.
- **Profit and loss money_json Decimal pilot OpenAPI (BR-14.5 / BR-10.6):** `profit_and_loss` / `_pnl_pack` revenue/COGS/expenses/net use `honesty.money_json`.
- **Party credit/balance money_json Decimal pilot OpenAPI (BR-6.1 / BR-7.1):** `_serialize_party` `credit_limit` / `balance` use `honesty.money_json`.
- **Opening stock list qty money_json Decimal pilot OpenAPI (BR-5.2):** opening-stock movement list qty before/after use `honesty.money_json`.
- **Report export CSV aria OpenAPI (BR-14):** Reports **Export report CSV** button (`aria-label`).
- **Report export Excel aria OpenAPI (BR-14):** Reports **Export report Excel** button (`aria-label`).
- **Report export PDF aria OpenAPI (BR-14):** Reports **Export report PDF** button (`aria-label`).
- **PO delivery_address defense-in-depth OpenAPI (BR-6.3):** `create_purchase_order` / `amend_purchase_order` use `optional_honest_narrative` (**400**) matching `AddressValue` (**422**).
- **SO delivery_address defense-in-depth OpenAPI (BR-7.3):** `create_order` / `confirm_order` use `optional_honest_narrative` (**400**) matching `AddressValue` (**422**).
- **Party contact name defense-in-depth OpenAPI (BR-6.1 / BR-7.1):** `create_contact` / `update_contact` use `require_honest_narrative` (**400**) matching `PartyContactNameValue` (**422**).
- **Party contact designation defense-in-depth OpenAPI (BR-6.1 / BR-7.1):** contact create/update use `optional_honest_narrative` (**400**) matching `PartyContactDesignationValue` (**422**).
- **Supplier invoice number defense-in-depth OpenAPI (BR-6.5):** `create_purchase_invoice` + draft OCR apply + AI create-PI use `optional_honest_narrative` (**400**) matching `SupplierInvoiceNumberValue` (**422**).
- **Store address defense-in-depth OpenAPI (BR-2.3):** `create_store` / `update_store` use `optional_honest_narrative` (**400**) matching `AddressValue` (**422**).
- **Warehouse address defense-in-depth OpenAPI (BR-2.4):** `create_warehouse` / `update_warehouse` use `optional_honest_narrative` (**400**) matching `AddressValue` (**422**).
- **Branch address defense-in-depth OpenAPI (BR-2.2):** `create_branch` / `update_branch` use `optional_honest_narrative` (**400**) matching `AddressValue` (**422**).
- **Tax filing pack money_json Decimal pilot OpenAPI (BR-12):** `tax_filing_pack` box/schedule/top-level amounts use `honesty.money_json`.
- **Tax filing GH VAT money_json Decimal pilot OpenAPI (BR-12):** `gh_vat.map_return` box amounts use `honesty.money_json`.
- **Low-stock suggestions money_json Decimal pilot OpenAPI (BR-6.2):** `list_low_stock_suggestions` stock/reorder/suggested qty use `honesty.money_json`.
- **AI document PO match money_json Decimal pilot OpenAPI (BR-21.8):** Analyze PO match `total_amount` uses `honesty.money_json`.
- **AI document create draft money_json Decimal pilot OpenAPI (BR-21.8):** Create-expense audit amount + create-PI PO line qty/price/tax/discount use `honesty.money_json`.
- **Tax filing export CSV aria OpenAPI (BR-12):** Tax **Export tax filing CSV** button (`aria-label`).
- **Tax filing export Excel aria OpenAPI (BR-12):** Tax **Export tax filing Excel** button (`aria-label`).
- **Tax filing export PDF aria OpenAPI (BR-12):** Tax **Export tax filing PDF** button (`aria-label`).
- **Tax Ghana VAT export Excel aria OpenAPI (BR-12):** Tax **Export Ghana VAT Excel** button (`aria-label`).
- **Tax Ghana VAT export CSV aria OpenAPI (BR-12):** Tax **Export Ghana VAT CSV** button (`aria-label`).
- **Tax Ghana VAT export PDF aria OpenAPI (BR-12):** Tax **Export Ghana VAT PDF** button (`aria-label`).
- **Company company_name defense-in-depth OpenAPI (BR-20.1):** `update_profile` uses `require_honest_narrative` (min 2) (**400**) matching `CompanyNameValue` (**422**).
- **Company legal_name defense-in-depth OpenAPI (BR-20.1):** `update_profile` uses `optional_honest_narrative` (min 2) (**400**) matching `LegalNameValue` (**422**).
- **Company contact_person defense-in-depth OpenAPI (BR-20.1):** `update_profile` uses `optional_honest_narrative` (**400**) matching `ContactPersonValue` (**422**).
- **Company address defense-in-depth OpenAPI (BR-20.1):** `update_profile` headquarters `address` uses `optional_honest_narrative` (**400**) matching `AddressValue` (**422**).
- **Company billing_address defense-in-depth OpenAPI (BR-20.1):** `update_profile` uses `optional_honest_narrative` (**400**) matching `AddressValue` (**422**).
- **Company shipping_address defense-in-depth OpenAPI (BR-20.1):** `update_profile` uses `optional_honest_narrative` (**400**) matching `AddressValue` (**422**).
- **Company registration_number defense-in-depth OpenAPI (BR-20.1):** `update_profile` uses `optional_honest_narrative` (**400**) matching `RegistrationNumberValue` (**422**).
- **Company tax_registration_number defense-in-depth OpenAPI (BR-20.1):** `update_profile` uses `optional_honest_narrative` (**400**) matching `TaxRegistrationNumberValue` (**422**).
- **Store name defense-in-depth OpenAPI (BR-13.1):** `create_store` / `update_store` use `require_honest_narrative` (**400**) matching `StoreNameValue` (**422**).
- **Warehouse name defense-in-depth OpenAPI (BR-2.4 / BR-13):** `create_warehouse` / `update_warehouse` use `require_honest_narrative` (**400**) matching `WarehouseNameValue` (**422**).
- **Branch name defense-in-depth OpenAPI (BR-2.3 / BR-13):** `create_branch` / `update_branch` use `require_honest_narrative` (min 2) (**400**) matching `BranchNameValue` (**422**).
- **Department name defense-in-depth OpenAPI (BR-2.4 / BR-13):** `create_department` / `update_department` use `require_honest_narrative` (min 2) (**400**) matching `DepartmentNameValue` (**422**).
- **Account name defense-in-depth OpenAPI (BR-10.3):** `create_account` / `update_account` use `require_honest_narrative` (**400**) matching `AccountNameValue` (**422**).
- **Account bank_name defense-in-depth OpenAPI (BR-10.3):** account create/update use `optional_honest_narrative` (**400**) matching `BankNameValue` (**422**); bank liquid still requires name (**400**).
- **Account bank_branch defense-in-depth OpenAPI (BR-10.3):** account create/update use `optional_honest_narrative` (**400**) matching `BankBranchValue` (**422**).
- **Party name defense-in-depth OpenAPI (BR-6.1 / BR-7.1):** `_normalize_party_profile` uses `require_honest_narrative` (**400**) matching `PartyNameValue` (**422**).
- **Expense OCR amount money_json Decimal pilot OpenAPI (BR-9.2):** `parse_receipt_text` suggestion `amount` uses `honesty.money_json`.
- **Report sales daily CSV aria OpenAPI (BR-14.1):** Reports **Export sales daily CSV** button (`aria-label`).
- **Report salespeople Excel aria OpenAPI (BR-14.1):** Reports **Export salespeople Excel** button (`aria-label`).
- **Report trial balance Excel aria OpenAPI (BR-14.5):** Reports **Export trial balance Excel** button (`aria-label`).
- **Report trial balance CSV aria OpenAPI (BR-14.5):** Reports **Export trial balance CSV** button (`aria-label`).
- **Report P&L PDF aria OpenAPI (BR-14.5):** Reports **Export P&L PDF** button (`aria-label`).
- **Report sales customers Excel aria OpenAPI (BR-14.1):** Reports **Export sales customers Excel** button (`aria-label`).
- **Report sales by store Excel aria OpenAPI (BR-14.1 / BR-2.5):** Reports **Export sales by store Excel** button (`aria-label`).
- **Product name defense-in-depth OpenAPI (BR-5.1):** `POST|PATCH /products` use `require_honest_narrative` (**400**) matching `ProductNameValue` (**422**).
- **Variant name defense-in-depth OpenAPI (BR-5.1):** `create_variant` / `update_variant` use `require_honest_narrative` (**400**) matching `VariantNameValue` (**422**).
- **Variant attributes defense-in-depth OpenAPI (BR-5.1):** `_clean_attr` uses `optional_honest_narrative` (**400**) matching `VariantAttrValue` (**422**).
- **Category name defense-in-depth OpenAPI (BR-5.1):** `create_category` / `update_category` use `require_honest_narrative` (**400**) matching `CategoryNameValue` (**422**).
- **Brand name defense-in-depth OpenAPI (BR-5.1):** `create_brand` / `update_brand` use `require_honest_narrative` (**400**) matching `BrandNameValue` (**422**).
- **Unit name defense-in-depth OpenAPI (BR-5.1):** `create_unit` / `update_unit` use `require_honest_narrative` (**400**) matching `UnitNameValue` (**422**).
- **Customer group name defense-in-depth OpenAPI (BR-7.1):** `create_group` / `update_group` use `require_honest_narrative` (**400**) matching `CustomerGroupNameValue` (**422**).
- **Custom role label defense-in-depth OpenAPI (BR-3.2):** `create_custom_role` / `update_custom_role` use `require_honest_narrative` (**400**) matching `CustomRoleLabelValue` (**422**).
- **Tax rate name defense-in-depth OpenAPI (BR-12.1):** `POST /tax/rates` + `update_tax_rate` use `require_honest_narrative` (**400**) matching `TaxRateNameValue` (**422**).
- **Expense category name defense-in-depth OpenAPI (BR-9.1):** `POST /expenses/categories` + `update_category` use `require_honest_narrative` (**400**) matching `ExpenseCategoryNameValue` (**422**).
- **User full_name defense-in-depth OpenAPI (BR-3 / BR-19):** `POST /users` + `PATCH /users/{id}` + `PATCH /me` use `require_honest_narrative` (**400**) matching `UserFullNameValue` (**422**).
- **Platform staff full_name defense-in-depth OpenAPI:** `create_platform_staff` / `update_platform_staff` use `require_honest_narrative` (**400**) matching `PlatformStaffFullNameValue` (**422**).
- **API key name defense-in-depth OpenAPI (BR-18.1):** `create_key` uses `require_honest_narrative` (min 2) (**400**) matching `ApiKeyNameValue` (**422**).
- **Report schedule name defense-in-depth OpenAPI (BR-14):** `create_schedule` / `update_schedule` use `require_honest_narrative` (min 2) (**400**) matching `ReportScheduleNameValue` (**422**).
- **AI report template name defense-in-depth OpenAPI (BR-21.7):** `create_template` uses `require_honest_narrative` (**400**) matching `AiReportTemplateNameValue` (**422**).
- **Bank recon unmatched journal money_json Decimal pilot OpenAPI (BR-10.3):** Unmatched journal-line candidates `debit`/`credit`/`signed_amount` use `honesty.money_json`.
- **Report sales returns Excel aria OpenAPI (BR-14.1):** Reports **Export sales returns Excel** button (`aria-label`).
- **Report sales returns CSV aria OpenAPI (BR-14.1):** Reports **Export sales returns CSV** button (`aria-label`).
- **Report inventory valuation Excel aria OpenAPI (BR-14.2):** Reports **Export inventory valuation Excel** button (`aria-label`).
- **Report inventory movements Excel aria OpenAPI (BR-14.2 / BR-5.3):** Reports **Export inventory movements Excel** button (`aria-label`).
- **Report inventory movements CSV aria OpenAPI (BR-14.2 / BR-5.3):** Reports **Export inventory movements CSV** button (`aria-label`).
- **Report inventory expiry Excel aria OpenAPI (BR-14.2):** Reports **Export inventory expiry Excel** button (`aria-label`).
- **Report inventory expiry CSV aria OpenAPI (BR-14.2):** Reports **Export inventory expiry CSV** button (`aria-label`).
- **Report inventory transfers Excel aria OpenAPI (BR-13.2):** Reports **Export inventory transfers Excel** button (`aria-label`).
- **Report inventory transfers CSV aria OpenAPI (BR-13.2):** Reports **Export inventory transfers CSV** button (`aria-label`).
- **Report inventory stock counts Excel aria OpenAPI (BR-5.2):** Reports **Export inventory stock counts Excel** button (`aria-label`).
- **Report inventory stock counts CSV aria OpenAPI (BR-5.2):** Reports **Export inventory stock counts CSV** button (`aria-label`).
- **Report purchases pending orders Excel aria OpenAPI (BR-14.3):** Reports **Export purchases pending orders Excel** button (`aria-label`).
- **Report purchases pending orders CSV aria OpenAPI (BR-14.3):** Reports **Export purchases pending orders CSV** button (`aria-label`).
- **Report purchases returns Excel aria OpenAPI (BR-14.3):** Reports **Export purchases returns Excel** button (`aria-label`).
- **Report purchases returns CSV aria OpenAPI (BR-14.3):** Reports **Export purchases returns CSV** button (`aria-label`).
- **Passkey name defense-in-depth OpenAPI (BR-19 / WebAuthn):** `verify_registration` uses `optional_honest_narrative` (**400**) matching `PasskeyNameValue` (**422**); omit/`null`/blank → `"Passkey"`.
- **Company SMTP from_name defense-in-depth OpenAPI (BR-20.3):** `apply_email_settings_update` uses `optional_honest_narrative` (**400**) matching `SmtpFromNameValue` (**422**).
- **Company SMS account_sid defense-in-depth OpenAPI (BR-15.2):** `apply_sms_settings_update` uses `optional_honest_narrative` (**400**) matching `TwilioAccountSidValue` (**422**).
- **POS sale create money_json Decimal pilot OpenAPI (BR-8.1):** `POST /pos/sales` response `subtotal`/`tax`/`total`/`discount_amount`/`line_discounts` use `honesty.money_json`.
- **POS sale webhook money_json Decimal pilot OpenAPI (BR-8.1 / BR-18.6):** POS `sale.created` / `sale.paid` webhook `amount` (+ tender amounts) use `honesty.money_json`.
- **POS product search money_json Decimal pilot OpenAPI (BR-8.1):** `GET /pos/products/search` `selling_price`/`stock_qty`/`tax_rate_pct` use `honesty.money_json`.
- **Customer payment response money_json Decimal pilot OpenAPI (BR-11):** `POST /sales/payments` + nested customer payment responses use `honesty.money_json` for amount/FX.
- **Supplier payment response money_json Decimal pilot OpenAPI (BR-11):** `POST /purchasing/payments` (supplier) amount/discount/FX use `honesty.money_json`.
- **POS session payment_breakdown money_json Decimal pilot OpenAPI (BR-8.2):** Session Z-report `payment_breakdown` cash/card/other/total use `honesty.money_json`.
- **AI sales analysis money_json Decimal pilot OpenAPI (BR-21.5):** Invoice/POS basket `total` fields use `honesty.money_json`.
- **Stock adjustment money_json Decimal pilot OpenAPI (BR-5.2):** `POST /inventory/adjust` response `stock_qty` uses `honesty.money_json`.
- **Product warehouse-stock reorder money_json Decimal pilot OpenAPI (BR-5.2 / BR-18.2):** `GET /products/{id}/warehouse-stock` consolidated `reorder_level` uses `honesty.money_json`.
- **Sales invoice post webhook money_json Decimal pilot OpenAPI (BR-7.4 / BR-18.6):** `sale.created` webhook `amount` uses `honesty.money_json`.
- **Expense approve webhook money_json Decimal pilot OpenAPI (BR-9.3 / BR-18.6):** `expense.approved` webhook `amount` uses `honesty.money_json`.
- **AR payment webhook money_json Decimal pilot OpenAPI (BR-11 / BR-18.6):** `sale.paid` webhook `amount` uses `honesty.money_json`.
- **PO amend snapshot money_json Decimal pilot OpenAPI (BR-6.3):** Amendment before/after header + line money/qty snapshots use `honesty.money_json`.
- **Inventory Export products CSV aria OpenAPI (BR-18.2):** Inventory Lookup **Export products CSV** button (`aria-label`).
- **Inventory Download product CSV template aria OpenAPI (BR-18.2):** Inventory Lookup **Download product CSV template** button (`aria-label`).
- **Audit Export CSV aria OpenAPI (BR-17):** Audit **Export audit CSV** button (`aria-label`).
- **Company SMTP username defense-in-depth OpenAPI (BR-20.3):** `apply_email_settings_update` username strip + letter/digit + no `://` (**400**; `@` email logins allowed) matching `SmtpUsernameValue` (**422**).
- **Company SMTP password defense-in-depth OpenAPI (BR-20.3):** `apply_email_settings_update` uses `optional_honest_narrative` + no spaces (**400**) matching `SmtpPasswordValue` (**422**); blank → keep prior; `clear_password` still clears.
- **Company SMS auth_token defense-in-depth OpenAPI (BR-15.2):** `apply_sms_settings_update` uses `optional_honest_narrative` + no spaces (**400**) matching `TwilioAuthTokenValue` (**422**); blank → keep prior; `clear_auth_token` still clears.
- **Webhook secret defense-in-depth OpenAPI (BR-18.6):** `create_endpoint` uses `optional_honest_narrative` (**400**) matching `WebhookSecretValue` (**422**); omit/`null`/blank → auto-generate `whsec_…`; non-`whsec_` min-length remains.
- **AI document expense description defense-in-depth OpenAPI (BR-21.8 / BR-9.2):** `create_expense_from_extract` uses `optional_honest_narrative` (**400**) matching `ExpenseDescriptionValue` (**422**); blank → OCR default narrative.
- **AI document expense payee defense-in-depth OpenAPI (BR-21.8 / BR-9.2):** OCR draft expense uses `optional_honest_narrative` (**400**) matching `ExpensePayeeValue` (**422**).
- **AI document expense reference defense-in-depth OpenAPI (BR-21.8 / BR-9.2):** OCR draft expense uses `optional_honest_narrative` (**400**) matching `ExpenseReferenceValue` (**422**).
- **AI document purchase invoice notes defense-in-depth OpenAPI (BR-21.8 / BR-6.5):** OCR draft PI (`create_purchase_invoice_from_extract`) uses `optional_honest_narrative` (**400**) matching `PurchaseInvoiceNotesValue` (**422**); blank → `OCR draft PI from {po}`.
- **Sales invoice tax_breakdown money_json Decimal pilot OpenAPI (BR-7.4 / BR-12):** `serialize_invoice` nested `tax_breakdown` line/by_rate/component totals use `honesty.money_json`.
- **Purchase invoice tax_breakdown money_json Decimal pilot OpenAPI (BR-6.5 / BR-12):** `serialize_purchase_invoice` nested `tax_breakdown` money fields use `honesty.money_json`.
- **PO created webhook money_json Decimal pilot OpenAPI (BR-6.3 / BR-18.6):** `purchase.order.created` webhook `total_amount` uses `honesty.money_json`.
- **Stock in webhook money_json Decimal pilot OpenAPI (BR-5.2 / BR-18.6):** `stock.in` webhook `quantity`/`stock_qty` use `honesty.money_json`.
- **Stock out webhook money_json Decimal pilot OpenAPI (BR-5.2 / BR-18.6):** `stock.out` webhook `quantity`/`stock_qty` use `honesty.money_json`.
- **Store reorder policy money_json Decimal pilot OpenAPI (BR-13 / BR-5.2):** `set_store_reorder_policy` response qty/reorder fields use `honesty.money_json`.
- **AI document amount flag money_json Decimal pilot OpenAPI (BR-21.8):** Analyze `amount_mismatch` flag `extracted`/`expected` use `honesty.money_json`.
- **Bank recon statement arithmetic money_json Decimal pilot OpenAPI (BR-10.3):** `STATEMENT_ARITHMETIC` 409 `expected_closing`/`statement_closing` use `honesty.money_json`.
- **Users Download CSV template aria OpenAPI (BR-3):** Users Bulk import **Download user CSV template** button (`aria-label`).
- **Sales Print A4 aria OpenAPI (BR-7.4 / BR-20.4):** Sales Invoices **Print sales invoice A4** button (`aria-label`).
- **Sales Print thermal aria OpenAPI (BR-7.4 / BR-20.4):** Sales Invoices **Print sales invoice thermal** button (`aria-label`).
- **Backup Download aria OpenAPI (BR-16):** Backup **Download backup** button (`aria-label`).
- **Expenses Download attachment aria OpenAPI (BR-9.2):** Expenses **Download expense attachment** button (`aria-label`).
- **Purchasing Download PI attachment aria OpenAPI (BR-6.5):** Purchasing Invoices **Download purchase invoice attachment** button (`aria-label`).
- **Accounting Download journal attachment aria OpenAPI (BR-10.2):** Accounting **Download journal attachment** button (`aria-label`).
- **Accounting Import bank CSV/OFX aria OpenAPI (BR-10.3):** Accounting Reconcile **Import bank statement CSV or OFX** button (`aria-label`).
- **Bank connection access_token defense-in-depth OpenAPI (BR-10.3):** `create_connection` / `update_connection` use `optional_honest_narrative` + no spaces (**400**) matching `BankAccessTokenValue` (**422**); blank → no token / no change; `clear_credentials` still clears.
- **Company SMTP host defense-in-depth OpenAPI (BR-20.3):** `apply_email_settings_update` uses `validate_smtp_host_value` (**400**) matching `SmtpHostValue` (**422**).
- **Company SMS from_number defense-in-depth OpenAPI (BR-15.2):** `apply_sms_settings_update` uses `validate_e164_phone_value` (**400**) matching `E164PhoneValue` (**422**).
- **Cash drawer host defense-in-depth OpenAPI (BR-8.1):** `PATCH /stores/{id}/drawer` uses `validate_smtp_host_value` (**400**) matching `SmtpHostValue` (**422**); blank → null; network mode still requires host.
- **AI report prompt defense-in-depth OpenAPI (BR-21.7):** `parse_prompt` uses `require_honest_narrative` (**400**) matching `AiReportPromptValue` (**422**); injection checks remain.
- **AI report period defense-in-depth OpenAPI (BR-21.7):** `generate_report` structured path uses `optional_honest_narrative` (**400**) matching `AiReportPeriodValue` (**422**).
- **Stock low product webhook money_json Decimal pilot OpenAPI (BR-5.2 / BR-18.6):** `stock.low` product webhook `stock_qty`/`reorder_level` use `honesty.money_json`.
- **Stock low warehouse webhook money_json Decimal pilot OpenAPI (BR-5.2 / BR-18.6):** `stock.low` warehouse webhook qty/reorder/suggested_order use `honesty.money_json`.
- **Bank feed normalize money_json Decimal pilot OpenAPI (BR-10.3):** `_normalize_txn` debit/credit/amount use `honesty.money_json`.
- **Bank mock feed money_json Decimal pilot OpenAPI (BR-10.3):** Mock deposit/withdrawal amounts use `honesty.money_json`.
- **Bank sync statement balances money_json Decimal pilot OpenAPI (BR-10.3):** Sync imported statement open/close/net use `honesty.money_json`.
- **Cash transfer audit money_json Decimal pilot OpenAPI (BR-10.3):** Cash transfer audit `details.amount` uses `honesty.money_json`.
- **Sales return discard audit money_json Decimal pilot OpenAPI (BR-7.5):** `return_discarded` audit qty + post settlement totals use `honesty.money_json`.
- **Notification payment due money_json Decimal pilot OpenAPI (BR-15.2 / BR-11):** AR/AP payment-due scan due amounts use `honesty.money_json`.
- **Notification recurring expense due money_json Decimal pilot OpenAPI (BR-9.5 / BR-15.2):** Recurring expense due message amount uses `honesty.money_json`.
- **Users Validate CSV import aria OpenAPI (BR-3):** Users Bulk import **Validate user CSV import** button (`aria-label`).
- **Users Import valid CSV rows aria OpenAPI (BR-3):** Users Bulk import **Import valid user CSV rows** button (`aria-label`).
- **Inventory Generate product barcode aria OpenAPI (BR-5.1):** Inventory Edit **Generate product barcode** button (`aria-label`).
- **Inventory Print product barcode label aria OpenAPI (BR-5.1):** Inventory Edit **Print product barcode label** button (`aria-label`).
- **Inventory Validate product CSV aria OpenAPI (BR-18.2):** Inventory Import **Validate product CSV import** button (`aria-label`).
- **Inventory Import valid product CSV aria OpenAPI (BR-18.2):** Inventory Import **Import valid product CSV rows** button (`aria-label`).
- **Inventory Generate variant barcode aria OpenAPI (BR-5.1):** Inventory Variants **Generate variant barcode** button (`aria-label`).
- **Inventory Print variant barcode label aria OpenAPI (BR-5.1):** Inventory Variants **Print variant barcode label** button (`aria-label`).
- **Integrations Copy API key aria OpenAPI (BR-18.1):** Integrations **Copy API key** button (`aria-label`).
- **Integrations Copy webhook signing secret aria OpenAPI (BR-18.6):** Integrations **Copy webhook signing secret** button (`aria-label`).
- **Integrations Retry webhook delivery aria OpenAPI (BR-18.6):** Integrations Deliveries **Retry webhook delivery** button (`aria-label`).
- **Integrations Refresh webhook deliveries aria OpenAPI (BR-18.6):** Integrations **Refresh webhook deliveries** button (`aria-label`).
- **Accounting Sync bank connection aria OpenAPI (BR-10.3):** Accounting Reconcile **Sync bank connection** button (`aria-label`).
- **Notifications Scan due alerts aria OpenAPI (BR-4.4 / BR-15.2):** Notifications **Scan due notification alerts** button (`aria-label`).
- **Jobs Refresh list aria OpenAPI (BR-Celery):** Jobs **Refresh jobs list** button (`aria-label`).
- **Reports Run schedule now aria OpenAPI (BR-14):** Reports Email schedules **Run report schedule** button (`aria-label`).
- **Reports Toggle schedule enabled aria OpenAPI (BR-14):** Reports Email schedules **Enable/Disable report schedule** button (`aria-label`).
- **Credit Save early pay terms aria OpenAPI (BR-11):** Credit **Save early pay terms** button (`aria-label`).
- **Stores Save store allocation aria OpenAPI (BR-13):** Multi-Store **Save store limit allocation** button (`aria-label`).
- **Login Resend verification email aria OpenAPI (BR-19):** Login **Resend verification email** button (`aria-label`).
- **Report export date Query OpenAPI (BR-14):** `GET /reports/export` Query `from_date` / `to_date` / `date` / `as_of` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound / live as_of fallbacks; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Reports shared **Report From/To/as of date** inputs (`aria-label`s).
- **Tax date Query OpenAPI:** `GET /reports/tax` + `GET /reports/tax/filing` Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Tax **Tax From/To date** inputs (`aria-label`s).
- **Expenses date Query OpenAPI (BR-14.4):** `GET /reports/expenses/summary` + `GET /reports/expenses/budget-vs-actual` Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Reports **Expenses** tab uses shared **Report From/To date** inputs (`aria-label`s).
- **Sales products/customers date Query OpenAPI (BR-14.1):** `GET /reports/sales/products` + `GET /reports/sales/customers` Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Reports **Sales** / **Customers** tabs use shared **Report From/To date** inputs (`aria-label`s).
- **Purchases summary/suppliers date Query OpenAPI (BR-14.3):** `GET /reports/purchases/summary` + `GET /reports/purchases/suppliers` Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Reports **Purchases** tab uses shared **Report From/To date** inputs (`aria-label`s).
- **Purchases pending/returns date Query OpenAPI (BR-14.3):** `GET /reports/purchases/pending-orders` + `GET /reports/purchases/returns` Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Reports **Purchases** tab uses shared **Report From/To date** inputs (`aria-label`s).
- **Sales returns/salesperson date Query OpenAPI (BR-14.1):** `GET /reports/sales/returns` + `GET /reports/sales/salesperson` Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Reports **Sales** / **Salespeople** tabs use shared **Report From/To date** inputs (`aria-label`s).
- **Sales by-store/by-department date Query OpenAPI (BR-14.1 / BR-2.5):** `GET /reports/sales/by-store` + `GET /reports/sales/by-department` Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Reports **Stores** / **Departments** tabs use shared **Report From/To date** inputs (`aria-label`s).
- **Inventory transfers/stock-counts date Query OpenAPI (BR-13.2 / BR-5.2):** `GET /reports/inventory/transfers` + `GET /reports/inventory/stock-counts` Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Reports **Inventory** tab uses shared **Report From/To date** inputs (`aria-label`s).
- **Customer/supplier history date Query OpenAPI (BR-7.1 / BR-6.1):** `GET /customers/{id}/history` + `GET /suppliers/{id}/history` Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → no bound; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent omit; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Credit **History From/To date** inputs (`aria-label`s).
- **AI sales/expenses analysis date Query OpenAPI (BR-21.5 / BR-21.6):** `GET /ai/sales/analysis` + `GET /ai/expenses/analysis` Query `from_date` / `to_date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → service default ~90d window; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent default; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). AI **Analysis From/To date** inputs (`aria-label`s).
- **Sales daily date Query OpenAPI (BR-14.1):** `GET /reports/sales/daily` Query `date` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit → today UTC; blank/`not-a-date`/`01/02/2024` → **422** (blank was silent today; invalid was late service **400**). Service `parse_date` remains defense-in-depth (**400**). Reports Sales passes shared **Report To date** (fallback **From**) as `date` (`aria-label`s).
- **Barcode symbology OpenAPI (BR-5.1):** Query `symbology` on product/variant barcode generate/png/label ∈ code128|ean13|upca (`Literal` + strip/lower). Generate omit → `code128`; blank/unknown → **422** (no silent code128 from `""`). png/label omit → auto-detect. Service `normalize_symbology` remains defense-in-depth. Inventory barcode symbology select.
- **Platform grant role OpenAPI:** `PlatformGrantAccess.role` ∈ super_admin|platform_owner|platform_admin|platform_support|platform_finance (`Literal` + strip/lower); omit → `platform_support`; blank/invalid → **422** (no silent support from `""`). Service `grant_dashboard_access` remains defense-in-depth. Platform Staff **Platform grant role** select (`aria-label`).
- **Platform staff create/update role OpenAPI:** `PlatformStaffCreate` / `PlatformStaffUpdate.role` same `PlatformRoleValue` Literal; create omit → `platform_support`; blank/invalid → **422** (no silent support from API `role or "platform_support"`). Service create/update remain defense-in-depth. Platform Staff **Platform staff role** + row **Change platform staff role** selects (`aria-label`s).
- **Platform revoke fallback_role OpenAPI:** `PlatformRevokeAccess.fallback_role` ∈ company_admin|store_manager|sales_officer|inventory_officer|accountant|cashier (`Literal` + strip/lower); omit → `company_admin`; blank/invalid/platform → **422** (no silent company_admin from `""`). Service `revoke_dashboard_access` remains defense-in-depth. Platform Staff Revoke hardcodes `company_admin`.
- **Custom role base_role OpenAPI (BR-3.x):** `CustomRoleCreate.base_role` ∈ clonable system roles (`Literal` + strip/lower; excludes `super_admin`); omit/null OK when `permissions` set; blank/unknown/`super_admin` → **422**. Service `create_custom_role` remains defense-in-depth. Users **Clone from** select.
- **Custom role permissions OpenAPI (BR-3.2):** `CustomRoleCreate` / `Update.permissions` typed `dict[str, list[ApiKeyPermissionAction]]` (`extra=forbid` bodies; modules ∈ `ASSIGNABLE_MODULES`; actions ∈ read|write|approve|*). Unknown keys / empty map / unknown module|action / `*:*` → **422** (was late service **400**). Omit permissions + `base_role` still clones. Service `normalize_permissions` remains defense-in-depth (**422**). Users **Create custom role** controls (`aria-label`s).
- **Tax filing jurisdiction Query OpenAPI:** `GET /reports/tax/filing` + `GET /reports/export` Query `jurisdiction` ∈ `GH` (`TaxFilingJurisdictionValue` / `tax_filings.SUPPORTED`; strip/upper); omit → tenant default / export default; blank/unsupported → **422** (blank was silent omit; unsupported was late **400**). Service `normalize_jurisdiction` remains defense-in-depth for tenant-default path. Tax **Tax filing jurisdiction filter** (`filingJurisdictionFilter`).
- **Company tax_jurisdiction OpenAPI:** `TenantProfileUpdate.tax_jurisdiction` same `TaxFilingJurisdictionValue` (`GH` / `SUPPORTED`; strip/upper); omit = no change; blank/unsupported → **422** (was free `str`; length-only late **400**; unsupported could persist). Service `update_tenant_profile` remains defense-in-depth vs `SUPPORTED`. Company **Tax jurisdiction** select.
- **FX currency_code OpenAPI (BR-2.6):** `PUT|DELETE /credit/exchange-rates/{currency_code}` Path + `ExchangeRateUpsert.currency_code` (+ refresh `currencies[]`) ∈ 3-letter ISO (`CurrencyCodeValue`; strip/upper; `extra=forbid` bodies); blank/non-ISO → **422** (was late service **400**). Service `normalize_currency` remains defense-in-depth. Credit **Exchange rates** (`aria-label`s).
- **Approval matrix roles OpenAPI (BR-9.3 / PR):** `ApprovalLevelUpdate` / `PurchaseApprovalLevelUpdate.roles[]` ∈ `SystemRoleValue` (`rbac.VALID_ROLES`; strip/lower; `extra=forbid` settings bodies); blank/unknown role → **422** (was late service **400**). Services `normalize_approval_matrix` remain defense-in-depth. Expenses **Approval matrix** + Purchasing **PR approval matrix** (`aria-label`s + system-role datalist).
- **Approval matrix label OpenAPI (BR-9.3 / PR):** `ApprovalLevelUpdate.label` + `PurchaseApprovalLevelUpdate.label` ∈ `ApprovalLevelLabelValue` (strip; 1–120; ≥1 letter/digit; no `://`/`@`); omit/`null` → no label; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist in tenant approval matrix JSON). Expenses **Expense approval level N label** + Purchasing **PR approval level N label** (`aria-label`s); Save sends `null` when blank.
- **Store operating_hours OpenAPI (BR-2.3):** `StoreCreate` / `StoreUpdate.operating_hours` typed `StoreOperatingHours` (`extra=forbid`; days ∈ mon…sun; each `StoreDayHours` closed or HH:MM open before close); unknown day / bad time / open≥close → **422** (was late service **400**). Service `normalize_operating_hours` remains defense-in-depth. Multi-Store **Operating hours** editor (`aria-label`s).
- **Store hours open/close OpenAPI (BR-2.3):** `StoreDayHours.open` / `close` ∈ `StoreHoursTimeValue` (strip; `HH:MM` 24h `^([01]\d|2[0-3]):([0-5]\d)$`); omit/`null` OK (still required by day model when not `closed`); blank/`!!!`/`9:00`/`25:00`/`http://…` → **422** (was free `str`; OpenAPI unconstrained string; blank with `closed` was kept as `""`; garbage failed only via day model_validator / service **400**). Multi-Store **Store {Day} open time** / **Store {Day} close time** inputs (`aria-label`s). Service `normalize_operating_hours` remains defense-in-depth.
- **Company currency OpenAPI (BR-2.6):** `TenantCreate.currency` / `TenantProfileUpdate.currency` ∈ 3-letter ISO (`CurrencyCodeValue`; strip/upper); create omit → `GHS`; PATCH omit = no change; blank/non-ISO → **422** (was free `str`; create unchecked; profile length-only late **400**). Service `normalize_currency` remains defense-in-depth on profile update. Company **Currency** select.
- **Company fiscal_year_start OpenAPI:** `TenantProfileUpdate.fiscal_year_start` ∈ valid calendar `MM-DD` (`FiscalYearStartValue`; strip); omit = no change; blank/invalid → **422** (was free `str`; length-only late **400**). Service `parse_fiscal_mmdd` remains defense-in-depth. Company **Fiscal year start** input.
- **Company timezone OpenAPI:** `TenantProfileUpdate.timezone` ∈ valid IANA key (`TimezoneValue`; strip + `ZoneInfo`); omit = no change; blank/`Foo/Bar`/`UTC+0` → **422** (was free `str`; blank late **400**; garbage could persist). Service `ZoneInfo` remains defense-in-depth. Company **Timezone** select.
- **Role key shape OpenAPI (BR-3.1 / BR-3.2):** `UserCreate` / `UserUpdate.role` + `CustomRoleCreate.key` ∈ `RoleKeyValue` (`custom_roles.ROLE_KEY_RE`; strip/lower); create omit → `cashier`; PATCH omit = no change; blank/`A`/`Cashier!` → **422** (was free `str`; blank late **400**). Unknown role / system-key collision on custom create remain service **400**. Users **User role** / **Change role** selects (`aria-label`s).
- **Document/payment currency OpenAPI (BR-2.6):** `SalesInvoiceCreate` / `PurchaseInvoiceCreate` / `CustomerPaymentCreate` / `SupplierPaymentCreate.currency` ∈ 3-letter ISO (`CurrencyCodeValue | null`); omit/`null` → tenant base via `resolve_rate`; blank/`ghs`/`EURO` → **422** (was free `str`; blank silently base). Service `normalize_currency` / `resolve_rate` remain defense-in-depth. Sales + Purchasing **Currency** inputs (`aria-label`s).
- **Platform staff + reports:** Roles `platform_owner` / `platform_admin` / `platform_support` / `platform_finance` (+ legacy `super_admin`). APIs `GET|POST /platform/staff`, `PATCH /platform/staff/{id}`, `GET /platform/roles`, `GET /platform/reports` (+ `/summary|/subscriptions|/packages|/trials`). UI: `/platform/staff`, `/platform/reports`. Create staff only via `/platform/staff` (not tenant `/users`).
- Rate-limit tests: force `RATE_LIMIT_BACKEND=memory` + `rate_limiter.reset_for_tests()` under TestClient to avoid Redis event-loop flakes.
- **Metrics / request logs:** `GET /api/v1/metrics` (Prometheus text; `METRICS_ENABLED`); structured JSON via `ribdigi.request` + `X-Request-ID` (`REQUEST_LOG_ENABLED`). Operator scrape/alerts: `ops/prometheus/`, Grafana examples: `ops/grafana/`. See `docs/OPS_MONITORING_MVP.md`.
- **Kubernetes:** Helm chart `helm/ribdigi/` + flat `k8s/`; probes are `/api/v1/health` (live) and `/api/v1/health/ready` (ready). Operator smoke: `ops/k8s/staging-smoke.sh.example`. Main CI stays deploy-free — see `docs/K8S_DEPLOY_MVP.md`.
- **Load tests:** `cd backend && PYTHONPATH=. python -m loadtest.run_baseline --smoke` / `--ci-capacity`. ASGI proof via `test_load_capacity_c1.py`. Against a live API, keep concurrency low or raise `RATE_LIMIT_*` — auth bucket is easy to trip (`429`). Targets/docs: `docs/LOAD_TEST_BASELINE.md`.
- **Logical DR drill:** `POST /backup` → corrupt → dry-run → `confirm_text=RESTORE` apply → `POST /backup/{id}/verify`. Cross-tenant backup ids must 404. Runbook: `docs/DR_LOGICAL_BACKUP_RUNBOOK.md`.
- **AI packaging:** Chat is fail-closed (`503`) until `AI_ENABLED` + approved provider + key. Rule-based insights work without an LLM. Use `GET /ai/status` and `GET /ai/queries` (tenant-scoped; hashed/redacted prompts). **Dashboard insights:** `GET /ai/insights` composes sales spike/drop, expense anomaly, and restock action signals (same builder as digests). **Weekly insight digest:** Celery job `send_weekly_ai_insight_digest` runs Monday 07:00 UTC; `POST /ai/insights/digest` sends the signed-in user's tenant-scoped preview. **AI Security Monitor:** `GET /ai/security/alerts?scan=true` or `POST /ai/security/scan`; Celery job `scan_ai_security_alerts` (restart worker/beat after pull). **Inventory predictions:** `GET /ai/inventory/predictions`, `GET /ai/inventory/low-stock-prediction`, `POST .../requests` (draft PRs; needs `purchasing:write`) — `docs/AI_INVENTORY_MVP.md`. **Sales/expense analysis:** `GET /ai/sales/analysis`, `GET /ai/expenses/analysis` — `docs/AI_SALES_EXPENSE_MVP.md`. **Report generator:** `POST /ai/reports/generate` / `export` + templates — `docs/AI_REPORT_GENERATOR_MVP.md`. **Customer assistant:** `POST /ai/customer/assist` — `docs/AI_CUSTOMER_MVP.md`. **Document assistant:** `POST /ai/documents/analyze` (multipart OCR + party/PO match) — `docs/AI_DOCUMENT_MVP.md`. Do not claim full BR-21 chat complete — see `docs/AI_SECURITY_MVP.md`.
- Do not commit `.devdata/` or `dump.rdb`.
- **Notification center (BR-4.4 / BR-15.2):** Shell topbar **Alerts** opens a dropdown (`GET /notifications?status=unread&limit=8`) with badge from `GET /notifications/unread-count`. Full page `/notifications` supports `?status=` + `?category=` chips and `PATCH .../read|unread`. Prefs: `GET|PATCH /notifications/settings`.
- **Notification list query OpenAPI (BR-4.4):** `GET /notifications` `status` ∈ unread|read; `category` ∈ `VALID_CATEGORIES` (`Literal` + strip/lower). Omit → all; blank/invalid → **422** (no silent empty filter). Service `list_notifications` remains defense-in-depth. Notifications Unread/All + category chips (aligned with prefs keys).
- **Notification preferences OpenAPI (BR-4.4 / BR-15.2):** `PATCH /notifications/settings` body `{ preferences }` uses typed `NotificationPreferencesMap` (known categories only) + `NotificationChannelPrefs` (`dashboard`|`email`|`sms` bools; `extra=forbid`). Unknown category/channel → **422** (no silent drop via merge). Service `update_preferences` remains defense-in-depth. Notifications **Channel preferences** toggles.
- **AI document_type OpenAPI (BR-21.8):** `POST /ai/documents/analyze` Form `document_type` ∈ receipt|invoice|purchase_order|auto (`Literal` + strip/lower); omit → `auto`; blank/`""`/invalid → **422** (Form default is `None` so empty field is not silently remapped to auto). Service `analyze_upload` remains defense-in-depth. AI **Document type** select.
- **AI chat body OpenAPI (BR-21.1):** `POST /ai/chat` body typed `AiChatBody` (`extra=forbid`; `message` or `prompt` ∈ `AiChatMessageValue` strip; 1–16000; ≥1 letter/digit; no `://`/`@`; omit both / blank/`!!!`/`http://…` → **422**; optional `context` same Value — omit/`null` OK; blank/garbage → **422**; optional `conversation_id` ∈ `UuidIdValue` — omit/`null` OK; blank/`!!!`/`http://…`/non-UUID → **422**; was free `str` strip-blank→omit). Unknown keys → **422** (blank was late service **400**; punctuation/URL could reach parse). Service `parse_chat_message` remains defense-in-depth. AI **Ask AI chat** controls (`aria-label` message + Ask; Ask gated on trim).
- **AI chat conversation_id OpenAPI (BR-21.1):** `AiChatBody.conversation_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` OK; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str` with strip-blank→omit; garbage could pass through). AI UI currently omits (single-turn Ask).
- **AI customer assist body OpenAPI (BR-21.9):** `POST /ai/customer/assist` body typed `AiCustomerAssistBody` (`extra=forbid`; optional `customer_id` ∈ `UuidIdValue` strip/lower; omit/`null` → overview / all-customers path; blank/`!!!`/`http://…`/non-UUID → **422** — was free `str` with blank coerced to omit; optional `query`/`message` ∈ `AiChatMessageValue` same honesty; omit/`null` → overview; blank/`!!!`/`http://…` → **422** — was free `str` stripped to null / garbage could silently become overview). Unknown keys → **422**. Service `customer_assist` remains defense-in-depth. AI **AI customer assist customer** select + **Customer assist** button (`aria-label`s); Customer assist sends `customer_id` trim or `null` when blank.
- **AI chat message OpenAPI (BR-21.1 / BR-21.9):** shared `AiChatMessageValue` on chat `message`|`prompt`|`context` + customer-assist `query`|`message`. Strip; 1–16000 (align `AI_MAX_MESSAGE_CHARS`); ≥1 letter/digit; no `://`/`@`; blank/`!!!`/`http://…` → **422**. AI shared textarea (`aria-label` **AI chat message**).
- **AI reports generate body OpenAPI (BR-21.7):** `POST /ai/reports/generate` body typed `AiReportsGenerateBody` (`extra=forbid`; require `prompt`|`template_id`|`report_type`; optional `prompt` ∈ `AiReportPromptValue` strip; 1–16000; ≥1 letter/digit; no `://`/`@`; omit/`null` OK with template_id|report_type; blank/`!!!`/`http://…` → **422**; optional `template_id` ∈ `UuidIdValue` — omit/`null` OK with prompt|report_type; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str` strip-blank→omit); optional `period` ∈ `AiReportPeriodValue` strip; 1–80; ≥1 letter/digit; no `://`/`@`; omit/`null` → service/prompt default; blank/`!!!`/`http://…` → **422** (was free `str` soft-nulled on blank; punctuation/URL could reach period_label); `format` ∈ csv|pdf|xlsx; `report_type` ∈ EXPORTABLE). Unknown keys / missing intent / bad format|type|prompt|period|template_id → **422** (format garbage was silent csv; unknown type was late **400**; free prompt/period could reach parse). Service `generate_report` remains defense-in-depth. AI **Generate AI report** + **AI report period** (`aria-label`s); generate sends `null` when period blank (prompt path; template_id unused in UI).
- **AI reports generate template_id OpenAPI (BR-21.7):** `AiReportsGenerateBody.template_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` OK with prompt|report_type; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str` strip-blank→omit; garbage could reach template lookup). Existence remains tenant-scoped (**404**).
- **AI report period OpenAPI (BR-21.7):** `AiReportsGenerateBody.period` ∈ `AiReportPeriodValue` (strip; 1–80; ≥1 letter/digit; no `://`/`@`); omit/`null` → service/prompt default; blank/`!!!`/`http://…` → **422** (was free `str`; blank soft-nulled; garbage could reach `period_label` / `parse_prompt`). AI **AI report period** input (`aria-label`); Generate sends `null` when blank.
- **AI reports export period OpenAPI (BR-21.7):** `AiReportsExportBody.period` ∈ same `AiReportPeriodValue`; omit/`null` → service/prompt default; blank/`!!!`/`http://…` → **422** (field was absent — unknown `period` → **422** via `extra=forbid`; export FE ignored the shared period input). AI **AI report period** + **Export AI report**; Export sends `null` when blank. `export_from_intent` passes period into `generate_report`.
- **AI reports export body OpenAPI (BR-21.7):** `POST /ai/reports/export` body typed `AiReportsExportBody` (`extra=forbid`; require `prompt`|`template_id`|`report_type`; optional `prompt` ∈ `AiReportPromptValue` same honesty; optional `template_id` ∈ `UuidIdValue` same honesty as generate; optional `period` ∈ `AiReportPeriodValue`; `format` omit → csv). Unknown keys / missing intent / bad format|type|prompt|period|template_id → **422** (was free `dict` with `or "csv"`). Service `export_from_intent` remains defense-in-depth. AI **Export AI report** (`aria-label` Export CSV).
- **AI reports export template_id OpenAPI (BR-21.7):** `AiReportsExportBody.template_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` OK with prompt|report_type; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str` strip-blank→omit; garbage could reach template lookup). Same honesty as generate; existence remains tenant-scoped (**404**).
- **AI report template create body OpenAPI (BR-21.7):** `POST /ai/reports/templates` body typed `AiReportTemplateCreateBody` (`extra=forbid`; `name` ∈ `AiReportTemplateNameValue`; required `prompt` ∈ `AiReportPromptValue` strip; 1–16000; ≥1 letter/digit; no `://`/`@`; blank/`!!!`/`http://…` → **422** — was free `str` min_length=1; punctuation/URL could persist; optional `format` ∈ csv|pdf|xlsx). Unknown keys / bad name|prompt / bad format → **422**. Service `create_template` remains defense-in-depth. AI **Save AI report template** controls (`aria-label` name + Save; Save gated on trim name + prompt — no silent default prompt).
- **AI report prompt OpenAPI (BR-21.7):** shared `AiReportPromptValue` on template create (required) + generate/export (optional when other intent present). Strip; 1–16000 (align `AI_MAX_MESSAGE_CHARS`); ≥1 letter/digit; no `://`/`@`; blank/`!!!`/`http://…` → **422**. AI shared prompt textarea (`aria-label` **AI chat message**).
- **AI low-stock prediction requests body OpenAPI (BR-21.4):** `POST /ai/inventory/low-stock-prediction/requests` body typed `AiLowStockPredictionRequestsBody` (`extra=forbid`; `days_ahead` 1–365 omit→14; `min_confidence` 0–1 omit→0; optional `lines`/`include_open`). Optional header `notes` + nested `AiLowStockPredictionLine.notes` ∈ `PurchaseRequestNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`; omit/`null` → no notes; blank/`!!!`/`http://…` → **422** — was free `str` silently stripped to null). Unknown keys / bad days|confidence → **422**. Service `create_requests_from_predictions` remains defense-in-depth. AI **AI low-stock prediction notes** + **Create draft PR(s)** + Include open PRs (`aria-label`s); Create sends header `null` when blank; line notes `String(x.notes || '').trim() || null`.
- **AI low-stock prediction line OpenAPI (BR-21.4):** nested `lines[]` typed `AiLowStockPredictionLine` (`extra=forbid`; required `product_id` ∈ `UuidIdValue`; optional `warehouse_id` / `preferred_supplier_id` ∈ `UuidIdValue`; optional confidence 0–1, order qty ≥0; optional `notes` ∈ `PurchaseRequestNotesValue`; optional `risk_reason` ∈ `AiPredictionRiskReasonValue` — strip; 1–500; ≥1 letter/digit; no `://`/`@`; omit/`null` → service defaults generated line note to `at_risk`; blank/`!!!`/`http://…` → **422** — was free `str` stripped to null / garbage could embed into draft PR line notes). Blank/`!!!`/`http://…`/non-UUID on FK ids → **422** (was free `str`; blank silently stripped to null). Unknown line keys / bad qty|confidence → **422** (was free `list[dict]`). AI **AI prediction risk reason** override + Create draft PR(s) slim prediction rows to the allow-list (`null` when blank; FK ids trimmed).
- **AI low-stock prediction line product_id OpenAPI (BR-21.4):** `AiLowStockPredictionLine.product_id` ∈ `UuidIdValue` (strip; lower; valid UUID); blank/`!!!`/`http://…`/non-UUID → **422** (was free `str` with blank→None strip). Existence remains tenant-scoped product lookup (**404**). AI Create draft PR(s) sends trim.
- **AI low-stock prediction line warehouse_id OpenAPI (BR-21.4):** `AiLowStockPredictionLine.warehouse_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no warehouse; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; blank silently stripped to null). AI Create draft PR(s) sends trim or `null`.
- **AI low-stock prediction line preferred_supplier_id OpenAPI (BR-21.4):** `AiLowStockPredictionLine.preferred_supplier_id` ∈ `UuidIdValue` (strip; lower; valid UUID); omit/`null` → no preference; blank/`!!!`/`http://…`/non-UUID → **422** (was free `str`; blank silently stripped to null). AI Create draft PR(s) sends trim or `null`.
- **Low-stock suggestion notes OpenAPI (BR-6.2):** `POST /purchasing/requests/from-low-stock` `LowStockSuggestionsCreate.notes` + `LowStockSuggestionLine.notes` ∈ `PurchaseRequestNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → service default header note / no line notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist). Reports Inventory **Low-stock suggestion notes** input (`aria-label`); Create draft PR sends `null` when blank (no longer hardcodes `"Created from low-stock suggestions"`).
- **Credit aging kind OpenAPI (BR-11):** `GET /credit/aging` Query `kind` ∈ receivable|payable (`Literal` + strip/lower); omit → `receivable`; blank/invalid → **422** (no silent AR for `Payable`/`""`/garbage). Credit **Receivables** / **Payables** toggle.
- **Inventory valuation method OpenAPI (BR-14.2):** `GET /reports/inventory/valuation` Query `method` ∈ `standard` only (`Literal` + strip/lower); omit → `standard`; blank/fifo/lifo/average/invalid → **422** (no silent standard from `""`). Service `inventory_valuation` remains defense-in-depth **400**. Reports Inventory **Valuation method** select.
- **Cheque list query OpenAPI (BR-10.4):** `GET /accounting/cheques` Query `direction` ∈ received|issued; `status` ∈ pending|deposited|cleared|bounced|cancelled (`Literal` + strip/lower). Omit → all; blank/invalid → **422** (no silent empty filter). Service `list_cheques` remains defense-in-depth. Accounting Cheques **Direction** / **Status** filters.
- **Journal manage status Query OpenAPI (BR-10.2):** `GET /accounting/journal-entries` Query `status` ∈ posted|unposted (`JournalStatusFilterValue`); omit → all; blank/invalid → **422**. Allow-list defense-in-depth on list handler. Accounting Ledger **Journal status filter** (`journalManageFilter`; client filter over full list cache).
- **Cash transfer kind Query OpenAPI (BR-10.3):** `GET /accounting/transfers` Query `kind` ∈ transfer|deposit|withdrawal (`CashTransferKindFilterValue`); omit → all; blank/invalid → **422**. Service `list_transfers` / `TRANSFER_KINDS` remain defense-in-depth. Accounting Cash & Bank **Cash transfer kind filter** (`xferKindManageFilter`; client filter over full list cache).
- **POS shift status Query OpenAPI (BR-8.2):** `GET /pos/sessions` Query `status` ∈ open|closed (`PosSessionStatusFilterValue`); omit → all; blank/invalid → **422**. Allow-list defense-in-depth on list handler. POS **POS shift status filter** (`shiftManageFilter`; client filter over full list cache).
- **POS shift close notes OpenAPI (BR-8.2):** `PosSessionClose.notes` ∈ `PosSessionCloseNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on `PosSession.notes` Text). POS **POS shift close notes** input (`aria-label`); Close shift sends `null` when blank.
- **Stock count report status OpenAPI (BR-5.2):** `GET /reports/inventory/stock-counts` Query `status` ∈ draft|completed|cancelled (`Literal` + strip/lower); omit → `completed`; blank/invalid → **422** (no silent empty filter / blank→all). Service `inventory_stock_counts` remains defense-in-depth. Reports Inventory **Count status** select.
- **Stock count manage status Query OpenAPI (BR-5.2):** `GET /inventory/stock-counts` Query `status` ∈ draft|completed|cancelled (same `StockCountReportStatusValue`); omit → all; blank/invalid → **422**. Service `list_counts` remains defense-in-depth. Inventory Counts **Stock count status filter** (`countManageFilter`; client filter over full list cache).
- **Transfer manage status Query OpenAPI (BR-5.2 / BR-13.2):** `GET /inventory/stock-transfers` + `GET /stores/transfers` Query `status` ∈ draft|requested|in_transit|received|cancelled (same `TransferReportStatusValue`); omit → all; blank/invalid → **422**. Service `list_transfers` remains defense-in-depth. Inventory + Multi-Store **Stock transfer status filter** (`transferManageFilter`; client filter over full list cache).
- **Return manage status Query OpenAPI (BR-7.5 / BR-6.6):** `GET /sales/returns` + `GET /purchasing/returns` Query `status` ∈ draft|posted|cancelled (same `ReturnReportStatusValue`); omit → all; blank/invalid → **422**. Allow-list defense-in-depth on list handlers. Sales + Purchasing **Return status filter** (`returnManageFilter`; client filter over full list cache).
- **PO manage status Query OpenAPI (BR-6.3):** `GET /purchasing/orders` Query `status` ∈ draft|sent|partially_received|received|cancelled (`PurchaseOrderStatusValue`; superset of pending-report Literal); omit → all; blank/invalid → **422**. Allow-list `PO_MANAGE_STATUSES` defense-in-depth. Purchasing Orders **Purchase order status filter** (`poManageFilter`; client filter over full list cache).
- **Sales invoice manage status Query OpenAPI (BR-7.4):** `GET /sales/invoices` Query `status` ∈ draft|posted|sent|partial|paid|overdue|cancelled (`SalesInvoiceStatusValue`); omit → all; blank/invalid → **422**. Allow-list `SI_MANAGE_STATUSES` defense-in-depth. Sales Invoices **Sales invoice status filter** (`invoiceManageFilter`; client filter over full list cache).
- **PR manage status Query OpenAPI (BR-6.2):** `GET /purchasing/requests` Query `status` ∈ draft|pending|approved|rejected|converted (`PurchaseRequestStatusValue`); omit → all; blank/invalid → **422**. Allow-list `PR_MANAGE_STATUSES` defense-in-depth. Purchasing Requests **Purchase request status filter** (`prManageFilter`; client filter over full list cache).
- **Quotation manage status Query OpenAPI (BR-7.2):** `GET /sales/quotations` Query `status` ∈ draft|sent|accepted|rejected|expired|converted (`SalesQuotationStatusValue`); omit → all; blank/invalid → **422**. Allow-list `QT_MANAGE_STATUSES` defense-in-depth. Sales Quotations **Quotation status filter** (`quotationManageFilter`; client filter over full list cache).
- **Sales order manage status Query OpenAPI (BR-7.3):** `GET /sales/orders` Query `status` ∈ draft|confirmed|processing|shipped|delivered|invoiced|cancelled (`SalesOrderStatusValue`); omit → all; blank/invalid → **422**. Allow-list `SO_MANAGE_STATUSES` defense-in-depth. Sales Orders **Sales order status filter** (`orderManageFilter`; client filter over full list cache).
- **PI manage status Query OpenAPI (BR-6.5):** `GET /purchasing/invoices` Query `status` ∈ draft|unpaid|partial|paid|overdue|cancelled (`PurchaseInvoiceStatusValue`); omit → all; blank/invalid → **422**. Allow-list `PI_MANAGE_STATUSES` defense-in-depth. Purchasing Invoices **Purchase invoice status filter** (`piManageFilter`; client filter over full list cache).
- **Transfer report status OpenAPI (BR-13.2):** `GET /reports/inventory/transfers` Query `status` ∈ draft|requested|in_transit|received|cancelled (`Literal` + strip/lower); omit → all; blank/invalid → **422** (was late **400**). Service `inventory_transfers` remains defense-in-depth. Reports Inventory **Transfer status** select.
- **Pending PO report status OpenAPI (BR-14.3):** `GET /reports/purchases/pending-orders` Query `status` ∈ draft|sent|partially_received (`Literal` + strip/lower); omit → all pending; blank/`received`/`cancelled`/invalid → **422** (was late **400**). Service `purchases_pending_orders` remains defense-in-depth. Reports Purchases **Pending status** select.
- **Return report status OpenAPI (BR-14.1 / BR-14.3):** `GET /reports/sales/returns` + `GET /reports/purchases/returns` Query `status` ∈ draft|posted|cancelled (`Literal` + strip/lower); omit → all; blank/invalid → **422** (was late **400**). Services remain defense-in-depth. Reports Sales/Purchases **Return status** selects.
- **Return report reason OpenAPI (BR-14.1 / BR-14.3):** same endpoints Query `reason` — sales ∈ damaged|wrong_item|defective|customer_change|other; purchases ∈ damaged|wrong_item|expiry|quality|other (`Literal` + strip/lower); omit → all; blank/invalid → **422** (was late **400**). Services remain defense-in-depth. Reports Sales/Purchases **Return reason** selects.
- **Platform tenant status Query OpenAPI (BR-1 / BR-19):** `GET /tenants` Query `status` ∈ trial|active|grace|suspended (`Literal` + strip/lower); omit → all; blank/invalid → **422** (was late **400**). Service `list_tenants` remains defense-in-depth. Platform console **Tenant status** chips (client filter over full `GET /tenants` cache; API also supports `?status=`).
- **Branded emails (BR-15.2):** `emailer.render_branded_html` wraps outbound HTML (verification, reset, notifications, digests, QT/PO/INV, test, receipt email) with company name + optional logo/print header/footer from Company → Print branding.
- **Print branding OpenAPI (BR-20.4):** `PrintBrandingUpdate.default_invoice_template` ∈ a4|thermal; `default_receipt_paper` ∈ 58mm|80mm (`Literal`); omit = no change; blank/invalid → **422**. `header_text` ∈ `PrintHeaderTextValue` (strip; 1–200; ≥1 letter/digit; no `://`/`@`); `footer_text` ∈ `PrintFooterTextValue` (strip; 1–300; same honesty); omit → no change; `null` → clear; blank/`!!!`/`http://…` → **422** (was free `str` max_length only — blank/garbage could persist on print branding / emails). Company Print branding **Print branding header text** / **footer text** + **Save print branding** (`aria-label`s); Save sends `null` when blank. Read path still coerces garbage template/paper to defaults.
- **Print/receipt query OpenAPI (BR-20.4 / BR-8):** Invoice `GET .../print` `template`/`format`/`paper` and POS `GET .../receipt` + `POST .../receipt/send` `format`/`paper`/`channel` use Query Literals (+ strip/lower). Omit uses branding/defaults; blank/invalid → **422** (no silent pdf/json/email/80mm or branding fallback from garbage). POS **POS receipt paper** select (`aria-label`); Sales print controls.
- **Financial report location filters (BR-14.5):** P&L, cash-flow, **balance sheet**, and **trial balance** accept `store_id` / `branch_id` ∈ `UuidIdValue` (omit/`null` → all; blank/`!!!`/`http://…`/non-UUID → **422** — was free `str`; attributable journals via `_pnl_journal_ids_for_stores`). Reports UI tabs pass the same filters (trim + aria-labels).
- **Backup failure alerts (BR-16.2):** Scheduled/manual backup failures create an in-app **Backup failed** system notification for company/super admins. `run_scheduled_backup_if_due` returns `ran=false` with `reason=failed|dir_not_writable` (never raises for those paths).
- **POS tenders (BR-8.1):** `normalize_payment_method` accepts `digital_wallet` / `mobile_money` / `momo` as `wallet`. Unknown/blank are **rejected** (strict default; OpenAPI `PosSaleCreate` / `PosPaymentLine` Literals → **422**; no silent `other`). Soft `strict=False` remains for legacy reads only. Product `PATCH` audit details include `changes.{field}.{before,after}` (BR-17.1).
- **POS payment_method OpenAPI (BR-8.1):** `PosSaleCreate.payment_method` ∈ cash|card|wallet|credit|other|split; tender lines omit `split`. Omit → cash; blank/invalid → **422**. POS **POS payment method** select (`aria-label`).
- **POS/sale status OpenAPI (BR-8.1):** `PosSaleCreate` / `TransactionCreate.status` ∈ `completed` (`Literal`); omit → `completed`; blank/invalid → **422** (no garbage persist on `transactions.status`). POS UI hardcodes completed.
- **Onboarding / purchasing BR sync:** `POST /tenants` / `PATCH /tenants/me` validate `industry` via schema `Literal` (+ case coerce) → blank/invalid **422**; service `normalize_industry` (`VALID_INDUSTRIES`) remains defense-in-depth. Tenancy is shared-schema + `tenant_id` (`docs/ADR_001_TENANCY.md`); industry-specific COA packs remain open under BR-1.5. Purchasing BR-6.3–6.6 lifecycle (PO↔PR, GRN partial/post-on-create, PI attachment/AP, returns stock-out) is implemented — prefer docs citations over new modules unless a real gap appears.
- **Company format OpenAPI (BR-20.2):** `TenantProfileUpdate` `tax_filing_period` / `date_format` / `decimal_separator` / `thousand_separator` / `time_format` are schema Literals (omit = no change; blank/invalid → **422**). `thousand_separator` allows `""`/`none`. Company Regional formatting + filing period selects: **Tax filing period** / **Company date format** / **Company decimal separator** / **Company thousand separator** / **Company time format** (`aria-label`s).
- **Multi-currency (BR-2.6):** Credit exchange rates + refresh/auto; base via Company currency; SI/PI and payments accept `currency`/`exchange_rate` (Purchasing invoice create UI mirrors Sales).
- **Domain APIs (BR-18.2–18.5):** Catalog/sales/purchasing/customer surfaces are live under JWT + API keys. Integrator barcode lookup: `GET /inventory/products/lookup` (`inventory:read`); per-product stock: `GET /products/{id}/warehouse-stock`. Inventory UI **Lookup** tab + **Export products CSV** (`GET /products/export`). POS wedge search remains `GET /pos/products/search`.
- **API key status Query OpenAPI (BR-18.1):** `GET /api-keys` Query `status` ∈ active|revoked|expired (`Literal` + strip/lower); omit → all; blank/invalid → **422** (was late **400**). Service `list_keys` remains defense-in-depth. Integrations **API key status filter** All / Active / Revoked / Expired (`apiKeyManageFilter`; client filter over full `GET /api-keys` cache).
- **API key create OpenAPI (BR-18.1):** `POST /api-keys` body typed `ApiKeyCreate` (`extra=forbid`; `name` ∈ `ApiKeyNameValue` 2–120 honesty; optional `expires_at`; optional `permissions` module→`read|write|approve|*`). Unknown top-level keys / bad name / invalid expiry / unknown module|action → **422** (was late **400** via free `dict`). Omit/`{}` permissions → default read map. Service `create_key` / `normalize_permissions` remain defense-in-depth. Integrations create controls (`aria-label` name/expiry/create).
- **API key expires_at OpenAPI (BR-18.1):** `ApiKeyCreate.expires_at` ∈ `IsoDateQueryValue` (strip; `YYYY-MM-DD` or ISO datetime); omit/`null` → no expiry; blank/`not-a-date`/`01/02/2024` → **422** (was free `datetime`; OpenAPI date-time; padded dates inconsistent). API `reports.parse_datetime` keeps clock time (defense-in-depth **400**). Integrations **API key expiry** input (`aria-label`); create omits blank.
- **Webhook deliveries (BR-18.6):** Integrations **Deliveries** loads `GET /webhooks/{id}/deliveries`; **Retry** calls `POST .../deliveries/{id}/retry` for `pending_retry`/`failed`. Manage list filter All / Active / Inactive (`webhookManageFilter`; client filter over full `GET /webhooks` cache; API also supports `?is_active=` / `active_only=`). **Delivery status Query OpenAPI:** `GET .../deliveries` Query `status` ∈ pending|pending_retry|delivered|failed (`Literal` + strip/lower); omit → all; blank/invalid → **422**. Service `list_deliveries` remains defense-in-depth. Integrations **Webhook delivery status filter** (`deliveryStatusFilter`; client filter over loaded delivery cache). Celery still runs `retry_due_webhooks` on schedule. Subscriber verify: API docs §17.4 + Integrations **Verify signature** (HMAC over `t.` + raw body, 300s skew). Live emits: `webhook.test`, `sale.created` (invoice post + POS), `sale.paid` (AR payment + POS with no credit tender), `customer.created`, `supplier.created`, `purchase.order.created`, `purchase.grn.received`, `expense.approved`, `stock.low`, `stock.in` (skips `reference_type=grn`; use `purchase.grn.received`), `stock.out` (skips `pos_sale`/`sales_invoice`; use `sale.created`), `tenant.suspended` (emit inside `suspend_tenant`; Celery retries skip suspended tenants), `user.login` (via `create_session(..., login_method=…)`; refresh does not emit).
- **Bank feed soft-deactivate (BR-10.3):** Accounting Reconcile **Activate** / **Deactivate** calls `PATCH /accounting/bank-connections/{id}` `{ is_active }`. Manage list filter All / Active / Inactive (`connectionManageFilter`; client filter over full `GET /accounting/bank-connections` cache; API also supports `?is_active=`). Inactive connections skip Celery auto-sync and Sync returns 400 (unless `force`). **Remove** still hard-deletes.
- **Bank connection provider OpenAPI (BR-10.3):** `BankConnectionCreate` / `Update.provider` ∈ mock|http_json (`Literal`); create omit → `mock`; blank/invalid → **422** (no silent mock from `""`). Service `_normalize_provider` remains defense-in-depth (`mock` still blocked in production). Accounting **Bank connection provider** select (`aria-label`).
- **Cash transfer kind OpenAPI (BR-10.3):** `CashTransferCreate.kind` ∈ transfer|deposit|withdrawal (`Literal`); omit → `transfer`; blank/invalid → **422** (no silent transfer from `""`). Accounting **Cash transfer kind** select (`aria-label`).
- **Cash transfer reference OpenAPI (BR-10.3):** `CashTransferCreate.reference` ∈ `CashTransferReferenceValue` (strip; 1–80; ≥1 letter/digit; no `://`/`@`); omit/`null` → auto `XFER-YYYY-NNNN`; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently auto-numbered / garbage could persist). Accounting Cash & Bank **Cash transfer reference** input (`aria-label`); Post sends `null` when blank.
- **Opening balance reference OpenAPI (BR-10.1):** `OpeningBalanceCreate.reference` ∈ `OpeningBalanceReferenceValue` (strip; 1–100; ≥1 letter/digit; no `://`/`@`); omit/`null` → auto `COA-OPEN-YYYYMMDD`; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently auto-labeled / garbage could persist on journal `reference`). Accounting Ledger **Opening balance reference** input (`aria-label`); Post sends `null` when blank.
- **Opening balance notes OpenAPI (BR-10.1):** `OpeningBalanceCreate.notes` ∈ `OpeningBalanceNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → default journal description; blank/`!!!`/`http://…` → **422** (was free `str`; blank fell through to default / garbage could persist on `JournalEntry.description`). Accounting Ledger **Opening balance notes** input (`aria-label`); Post sends `null` when blank (was hardcoded `"COA opening balances"`).
- **Cash transfer notes OpenAPI (BR-10.3):** `CashTransferCreate.notes` ∈ `CashTransferNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently dropped / garbage could persist). Accounting Cash & Bank **Cash transfer notes** input (`aria-label`); Post sends `null` when blank.
- **Account type / liquid_kind OpenAPI (BR-10.3):** `AccountCreate.account_type` ∈ asset|liability|equity|income|expense (`Literal`; omit → `asset`; blank/invalid → **422**). `liquid_kind` ∈ cash|bank|null (`Literal`; omit/null = non-liquid; blank/invalid → **422**). Service create_account remains defense-in-depth. Accounting **Liquid account kind** select (`aria-label`).
- **Store drawer_mode OpenAPI (BR-8.1):** `StoreDrawerSettingsUpdate.drawer_mode` ∈ none|mock|network|browser_bridge (`Literal`); omit = no change on PATCH; blank/invalid → **422** (no silent `none` from `""`). Service `normalize_mode` remains defense-in-depth. Multi-Store **Cash drawer mode** select (`aria-label`).
- **Party manage status filter (BR-6.1 / BR-7.1):** Sales / Purchasing Manage dropdowns filter All / Active / Inactive (client filter over full `GET /customers` / `GET /suppliers` cache; API also supports `?status=`). Document pickers remain active-only.
- **Party list status Query OpenAPI (BR-6.1 / BR-7.1):** `GET /customers` / `GET /suppliers` Query `status` ∈ active|inactive (`Literal` + strip/lower); omit → all; blank/invalid → **422** (was late **400**). `_PARTY_STATUSES` remains defense-in-depth. Sales/Purchasing Manage status filters already wired.
- **Movement type Query OpenAPI (BR-5.3):** `GET /inventory/movements` + `GET /reports/inventory/movements` Query `movement_type` ∈ stock_in|stock_out|opening_stock|adjustment|transfer_out|transfer_in|transfer_cancel (`Literal` + strip/lower); omit → all; blank/invalid → **422** (no silent empty filter). Service `inventory_movements` remains defense-in-depth. Inventory **Movements** type select.
- **Movement reason Query OpenAPI (BR-5.2/5.3):** same movement list endpoints Query `reason` ∈ damage|theft|expiry|found|lost (`Literal` + strip/lower); omit → all; blank/invalid → **422** (no silent empty filter). Service remains defense-in-depth. Inventory **Movements** reason select.
- **Party profile_type / status OpenAPI (BR-6.1 / BR-7.1):** `PartyCreate` / `Update.profile_type` uses OpenAPI union `Literal` (walk_in|registered|trade|manufacturer|service|other); create omit → `registered`; blank/unknown → **422** (no silent registered from `""`). Kind allow-list still rejects cross-kind values (e.g. customer+trade → **400**). `status` ∈ active|inactive (`Literal`). Sales **Customer profile type** / **Customer status** + Purchasing **Supplier profile type** / **Supplier status** selects (`aria-label`s).
- **Product manage status filter (BR-5.1):** Inventory Selected product + Products table filter All / Active / Inactive (`productManageFilter`; client filter over full `GET /products` cache; API also supports `?is_active=`). Sale / POS / Purchasing pickers remain active-only.
- **Product tax_supply_class OpenAPI (BR-5.1 / BR-12.1):** `ProductCreate` / `Update.tax_supply_class` ∈ standard|zero_rated|exempt (`Literal`); create omit → `standard`; blank/invalid → **422** (no silent standard from garbage). Create path uses `normalize_supply_class(..., strict=True)`; soft normalize remains for internal/legacy reads. Inventory **Product supply class** + **Edit product supply class** selects (`aria-label`s).
- **Multi-Store manage status filters (BR-2.2–2.5):** Stores / Warehouses / Branches / Departments tables filter All / Active / Inactive; create/edit pickers stay active-only. API: `GET /stores?is_active=`, `GET /warehouses?is_active=` (branches/departments already had `?is_active=`).
- **Warehouse type OpenAPI (BR-2.4):** `WarehouseCreate` / `WarehouseUpdate.warehouse_type` ∈ retail|bulk|cold_storage|other (`Literal`); create omit → `retail`; blank/invalid → **422** (no silent retail from `""`). Multi-Store **Warehouse type** select (`aria-label`).
- **Tax rate manage status filter (BR-12.1):** Tax rates table filter All / Active / Inactive (`taxRateManageFilter`; client filter over full `GET /tax/rates` cache; API also supports `?is_active=`). Category/product tax pickers stay active-only.
- **Tax pricing_mode / tax_type OpenAPI (BR-12.1):** `TaxCreate` / `TaxUpdate` / `TaxCalculateRequest`: `pricing_mode` ∈ exclusive|inclusive (`Literal`); `tax_type` ∈ vat|gst|sales_tax|custom (`Literal`). Create omit → exclusive / vat; blank/invalid → **422** (no silent exclusive for unknown modes; no free-string tax_type). Service `normalize_pricing_mode` / `normalize_tax_type` remain defense-in-depth. Tax **Tax rate type** + **Tax pricing mode** selects (`aria-label`s).
- **Tax components basis OpenAPI (BR-12.1):** `TaxCreate` / `TaxUpdate` / `TaxCalculateRequest.components[]` typed `TaxComponent` (`extra=forbid`; `rate` ≥0; `basis` ∈ net|compound strip/lower, omit→`net`; optional `code`/`name`). Blank/invalid basis / unknown keys / non-objects → **422** (was free `list[dict]`; blank basis silently `net`; bad basis late **400**). Service `normalize_components` remains defense-in-depth. Tax **Tax rate components JSON** textarea (`aria-label`).
- **Tax component code/name OpenAPI (BR-12.1):** `TaxComponent.code` ∈ `TaxComponentCodeValue` (strip; 1–40; ≥1 letter/digit; no `://`/`@`); `TaxComponent.name` ∈ `TaxComponentNameValue` (strip; 1–80; ≥1 letter/digit; no `://`/`@`); omit/`null` OK (service falls back to name/`cN` or code); blank/`!!!`/`http://…` → **422** (was free `str`; blank silently fell through to name/`cN`/code; garbage could persist). Tax **Tax rate components JSON** textarea (`aria-label`; create trims blank code/name to `null`). Service `normalize_components` remains defense-in-depth.
- **Expense category manage status filter (BR-9.1):** Expenses Category budgets table filter All / Active / Inactive (`categoryManageFilter`; client filter over full `GET /expenses/categories` cache; API also supports `?is_active=`). Create/recurring pickers stay active-only.
- **Customer group manage status filter (BR-7.1):** Sales Customer groups list filter All / Active / Inactive (`groupManageFilter`; client filter over full `GET /customers/groups` cache; API also supports `?is_active=`). Assign/create pickers stay active-only.
- **Catalog manage status filters (BR-5.1):** Inventory Catalog categories / brands / units lists filter All / Active / Inactive; create/parent/product pickers stay active-only. API: `GET /catalog/categories|brands|units?is_active=`.
- **Product variant manage status filter (BR-5.1):** Inventory Variants table filter All / Active / Inactive (`variantManageFilter`; client filter over full `GET /products/{id}/variants` cache; API also supports `?is_active=`). Sale / stock / batch pickers stay active-only.
- **Recurring expense manage status filter (BR-9.5):** Expenses Recurring table filter All / Active / Inactive (`recurringManageFilter`; client filter over full `GET /expenses/recurring` cache; API also supports `?is_active=`). Generate / Skip next remain active-only schedules.
- **Recurring frequency OpenAPI (BR-9.5):** `RecurringExpenseCreate` / `Update.frequency` ∈ daily|weekly|monthly|yearly (`Literal`); create omit → `monthly`; blank/invalid → **422** (no silent monthly from `""`). Expenses **Recurring frequency** select (`aria-label`).
- **Expense payment_method OpenAPI (BR-9.2):** `ExpenseCreate` / `Update` / `AiDocumentExpenseCreate` / recurring create+update `payment_method` ∈ cash|bank_transfer|card|cheque (`Literal` + aliases); create omit → `cash` (recurring → `bank_transfer`); blank/invalid → **422**. Service `normalize_expense_payment_method` is defense-in-depth. Expenses **Expense payment method** + **Recurring payment method** selects (`aria-label`s).
- **Expense status Query OpenAPI (BR-9.2):** `GET /expenses` Query `status` ∈ pending|approved|rejected (`Literal` + strip/lower); omit → all; blank/invalid → **422**. Endpoint keep allow-list defense-in-depth. Expenses **Expense status filter** All / Pending / Approved / Rejected (`expenseManageFilter`; client filter over full `GET /expenses` cache).
- **AR/AP payment_method OpenAPI (BR-11):** `CustomerPaymentCreate` / `SupplierPaymentCreate` / `SalesReturnPost` use the same settlement `Literal` (AR omit → `cash`; AP omit → `bank_transfer`; blank/invalid → **422**). Credit UI Method select matches.
- **Recurring Skip-next reason (BR-9.5):** Expenses Recurring **Skip next reason** input (required) → `POST /expenses/recurring/{id}/skip-next` `{ reason }` ∈ `RecurringSkipReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could persist on audit). Audit `recurring_expense_skipped.details.reason` (+ previous/new `next_run_at`). Does **not** mutate schedule `description` (generate template). Expenses **Skip next reason** (`aria-label`).
- **User manage status filter (BR-3.1):** Users table filter All / Active / Inactive (`userManageFilter`; client filter over full `GET /users` cache; API also supports `?is_active=`). Activate/Deactivate unchanged (DELETE soft-deactivate / PATCH reactivate).
- **Record scope OpenAPI (BR-3.3):** `UserCreate` / `UserUpdate` / `CustomRoleCreate` / `CustomRoleUpdate.record_scope` ∈ own|department|branch|all (`Literal`); omit = role/base default; blank/invalid → **422** (no silent `all` from `""`). Users **User record scope** + row **Edit user record scope** selects (`aria-label`s). Service `normalize_record_scope` remains defense-in-depth.
- **Quotation Reject UI (BR-7.2):** Sales Quotations **Reject** calls `POST /sales/quotations/{id}/reject` for `draft`/`sent` (same gate as Accept). Status → `rejected`; further accept/reject → 409. Convert/Email unchanged. Past `valid_until` reject/accept/send/convert flip to `expired` then 409.
- **Quotation auto-expire (BR-7.2):** `scan_quotation_expiry` / `POST /notifications/scan-due` (and Celery beat) flips past-due `draft`/`sent` quotations to `status=expired` (even when an unread T−1 notify already exists). T−1 window still notifies only. Sales UI hides Accept/Reject/Convert once status is `expired`.
- **Multi-line manual journal UI (BR-10.2):** Accounting Manual journal posts dynamic `lines[]` (default 2; Add/Remove; Σ debit = Σ credit ±0.01). API already accepted N≥2 lines.
- **COA account soft-deactivate (BR-10.1):** Accounting Chart of accounts **Activate** / **Deactivate** via `PATCH /accounting/accounts/{id}` `{ is_active }`. Manage filter All/Active/Inactive (`accountManageFilter`; `GET /accounting/accounts?is_active=`). Inactive blocked on journal / transfer / opening / expense GL / liquid pickers. Alembic `20260815_0102`.
- **Stock count Cancel reason (BR-5.2):** Inventory Counts **Cancel reason** input (required) → `POST /inventory/stock-counts/{id}/cancel` `{ reason }` ∈ `StockCountCancelReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could be appended to count `notes` / audit). Appended to count `notes` (`Cancel: …`) + audit `stock_count_cancelled.details.reason`. Draft only; no variance movements. Notes column shown in the counts table. Inventory **Stock count cancel reason** (`aria-label`) + `Cancel stock count ${id}`.
- **Stock count notes OpenAPI (BR-5.2):** `StockCountCreate.notes` ∈ `StockCountNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently dropped / garbage could persist on `StockCount.notes` Text). Inventory Counts **Stock count notes** input (`aria-label`); Create draft sends `null` when blank.
- **Stock count item notes OpenAPI (BR-5.2):** `StockCountItemUpdate.notes` ∈ `StockCountItemNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit → no change; `null` → clear; blank/`!!!`/`http://…` → **422** (was free `str`; blank silently dropped via strip-to-None / garbage could persist). PATCH uses `exclude_unset` so omit ≠ null. Inventory Counts active draft **Stock count line notes** inputs (`aria-label`); Save counts sends `null` when blank.
- **Purchase request Reject reason (BR-6.2):** Purchasing Requests **Reject reason** input (required) → `POST /purchasing/requests/{id}/reject` `{ reason }` ∈ `PurchaseRequestRejectReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could persist on `PurchaseRequest.rejection_reason`). No hardcoded `"Rejected from purchasing UI"`. Table shows `rejection_reason`. Purchasing **Purchase request reject reason** (`aria-label`). Reject is role-gated to the awaiting approval level; creators cannot self-reject.
- **PO Cancel reason (BR-6.3):** Purchasing Orders **Cancel reason** input (required) → `POST /purchasing/orders/{id}/cancel` `{ reason }` ∈ `PurchaseOrderCancelReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could be appended to PO `notes` / audit). Appended to PO `notes` (`Cancel: …`) + audit `po_cancelled.details.reason`. Notes column shown in the orders table. Purchasing **Purchase order cancel reason** (`aria-label`) + `Cancel purchase order ${id}`.
- **PO Amend reason (BR-6.3):** Purchasing Orders amend panel **Required amendment reason** → `POST /purchasing/orders/{id}/amend` `{ reason }` ∈ `PurchaseOrderAmendReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could persist on amendment history / audit `po_amended.details.reason`). Stored on amendment history + audit. Purchasing **Purchase order amend reason** (`aria-label`) + **Save purchase order amendment**.
- **PI Cancel reason (BR-6.5):** Purchasing Invoices **Cancel reason** input (required) → `POST /purchasing/invoices/{id}/cancel` `{ reason }` ∈ `PurchaseInvoiceCancelReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could be appended to invoice `notes` / audit). Appended to invoice `notes` (`Cancel: …`) + audit `pi_cancelled.details.reason`. Notes column shown in the invoices table. Already-cancelled remains idempotent. Purchasing **Purchase invoice cancel reason** (`aria-label`) + `Cancel purchase invoice ${id}`.
- **Purchase return Cancel reason (BR-6.6):** Purchasing Returns **Cancel reason** input (required) → `POST /purchasing/returns/{id}/cancel` `{ reason }` ∈ `PurchaseReturnCancelReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could be appended to return `notes` / audit). Appended to draft return `notes` (`Cancel: …`) + audit `purchase_return_cancelled.details.reason`. Notes column shown in the returns table. Draft only; no stock/AP. Purchasing **Purchase return cancel reason** (`aria-label`) + `Cancel purchase return ${id}`.
- **SO Cancel reason (BR-7.3):** Sales Orders **Cancel reason** input (required) → `POST /sales/orders/{id}/cancel` `{ reason }` ∈ `SalesOrderCancelReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could be appended to order `notes` / audit). Appended to order `notes` (`Cancel: …`) + audit `so_cancelled.details.reason`. Notes column shown in the orders table. Cancel still releases reservations through processing. Sales **Sales order cancel reason** (`aria-label`) + `Cancel sales order ${id}`.
- **SI Cancel reason (BR-7.4):** Sales Invoices **Cancel reason** input (required) → `POST /sales/invoices/{id}/cancel` `{ reason }` ∈ `SalesInvoiceCancelReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could be appended to invoice `notes` / audit). Appended to draft invoice `notes` (`Cancel: …`) + audit `invoice_cancelled.details.reason`. Notes column shown in the invoices table. Draft only. Sales **Sales invoice cancel reason** (`aria-label`) + `Cancel sales invoice ${id}`.
- **SR Cancel reason (BR-7.5):** Sales Returns **Cancel reason** input (required) → `POST /sales/returns/{id}/cancel` `{ reason }` ∈ `SalesReturnCancelReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could be appended to return `notes` / audit). Appended to draft return `notes` (`Cancel: …`) + audit `sales_return_cancelled.details.reason`. Notes column shown in the returns table. Draft only; no stock/AR. Sales **Sales return cancel reason** (`aria-label`) + `Cancel sales return ${id}`.
- **Stock transfer Reject reason (BR-5.2/5.4):** Inventory Transfers **Reject reason** input (required) → `POST /inventory/stock-transfers/{id}/reject` `{ reason }` ∈ `StockTransferRejectReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could persist on `StockTransfer.rejection_reason`). Status → `cancelled` with `rejection_reason` shown in the table. **Cancel** also requires typed reason → same `rejection_reason` / same Value type. Inventory **Stock transfer reject reason** (`aria-label`). Approve/ship/receive unchanged.
- **Multi-store transfer Reject reason (BR-13.2):** Multi-Store Transfers **Reject reason** input (required) → `POST /stores/transfers/{id}/reject` `{ reason }` ∈ `StockTransferRejectReasonValue` (same as Inventory). Backend requires non-empty reason; status → `cancelled` with `rejection_reason` column. **Cancel** also requires typed reason → same field. Multi-Store **Stock transfer reject reason** (`aria-label`). Same helpers as inventory WH transfers.
- **Platform Suspend reason:** Platform console **Suspend reason** input (required) → `POST /tenants/{slug|id}/suspend` `{ reason }` ∈ `TenantSuspendReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could persist on `Tenant.suspended_reason`). Stores `suspended_reason`. Replaces `window.prompt`. Confirm dialog kept. Activate clears reason. Platform **Tenant suspend reason** (`aria-label`) + `Suspend tenant ${id}`.
- **Company self-suspend reason:** Company page **Suspend reason** input (required) → `POST /tenants/me/suspend` `{ reason }` ∈ `TenantSuspendReasonValue` (same Value type). Backend requires non-empty reason; no hardcoded `"Admin requested"`. Confirm still warns sessions will be revoked; tokens cleared client-side after success. Company **Tenant suspend reason** (`aria-label`) + **Suspend company**.
- **Quotation Reject reason (BR-7.2):** Sales Quotations **Reject reason** input (required) → `POST /sales/quotations/{id}/reject` `{ reason }` ∈ `SalesQuotationRejectReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could persist on `SalesQuotation.rejection_reason`). Alembic `20260815_0103`. Sales **Quotation reject reason** (`aria-label`). Accept/convert unchanged.
- **Expense Reject reason (BR-9.3):** Expenses **Reject reason** input (required) → `POST /expenses/{id}/reject` `{ reason }` ∈ `ExpenseRejectReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could persist on `Expense.rejection_reason`). No hardcoded `"Rejected"` fallback. Table **Reject reason** column. Expenses **Expense reject reason** (`aria-label`). Approve uses optional typed **Approve comment** (no hardcoded `"Approved"`) → `approval_comment`.
- **Expense approve comment OpenAPI (BR-9.3):** `ExpenseDecision.comment` ∈ `ExpenseApproveCommentValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no typed comment (service may still set a level-awaiting system note); blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on `approval_comment`). Expenses **Expense approve comment** input (`aria-label`); Approve omits blank.
- **Sales return reason (BR-7.5):** Sales Returns **Select reason** (required; empty default) → `POST /sales/returns` `{ reason }` ∈ damaged|wrong_item|defective|customer_change|other (schema `Literal` → omit/blank/invalid **422**). No silent default to `other`.
- **Sales return notes OpenAPI (BR-7.5):** `SalesReturnCreate.notes` ∈ `SalesReturnNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on `SalesReturn.notes` Text). Sales Returns **Sales return notes** input (`aria-label`); Create return sends `null` when blank.
- **Sales document notes OpenAPI (BR-7.2 / BR-7.3 / BR-7.4):** `SalesQuotationCreate` / `SalesOrderCreate` / `SalesInvoiceCreate.notes` ∈ `SalesDocumentNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on QT/SO/SI notes Text). Sales Create sale **Sales document notes** input (`aria-label`); Create quotation/order/invoice send `null` when blank.
- **Purchase return notes OpenAPI (BR-6.6):** `PurchaseReturnCreate.notes` ∈ `PurchaseReturnNotesValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` → no notes; blank/`!!!`/`http://…` → **422** (was free `str`; blank/garbage could persist on `PurchaseReturn.notes` Text). Purchasing Returns **Purchase return notes** input (`aria-label`); Draft return sends `null` when blank.
- **Coded reason OpenAPI Literals:** `PurchaseReturnCreate.reason`, `SalesReturnCreate.reason`, and `StockAdjust.reason` use `typing.Literal` so omit/blank/invalid codes → **422** (service enum checks remain defense in depth). Purchasing **Purchase return reason** select (`aria-label`).
- **Sales return condition (BR-7.5):** Sales Returns **Select condition** (required) → `items[].condition` ∈ sellable|discard (schema `Literal` → omit/blank/invalid **422**). No silent sellable/discard from Restock checkbox.
- **Sales return settlement_method OpenAPI (BR-7.5):** `SalesReturnPost.settlement_method` ∈ adjust|refund (`Literal`); omit OK (service defaults `adjust` when no excess AR); blank/invalid → **422** (no silent garbage). Excess AR still requires explicit method → **400** `SETTLEMENT_REQUIRED`. Sales **Post credit** / **Post + refund**.
- **Stock Out / Adjust reason (BR-5.2):** Inventory Stock Out **Select reference type** (required; empty default) → `reference_type` ∈ sale|transfer|adjustment|damage|internal|other (`StockOut` schema `Literal` → omit/blank/invalid **422**) — no silent `other`. Stock Adjust **Select reason** (required; empty default) → `reason` ∈ damage|theft|expiry|found|lost — no silent `damage`.
- **Cheque bounce/cancel reason (BR-10.4):** Accounting Cheques **Bounce / Cancel reason** input (required) → `POST /accounting/cheques/{id}/bounce|cancel` body `{ reason }` ∈ `ChequeLifecycleReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could be appended to cheque `notes` / journal). Appended to notes + journal description. Deposit/Clear unchanged. Notes column shown in the table. Accounting **Cheque bounce cancel reason** (`aria-label`) + `Bounce cheque ${id}` / `Cancel cheque ${id}`.
- **Journal Unpost reason (BR-10.2):** Accounting Recent journals **Unpost reason** input (required) → `POST /accounting/journal-entries/{id}/unpost` `{ reason }` ∈ `JournalUnpostReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could be appended to journal `description` / audit). Appended to journal description (`Unpost: …`) + audit `journal_unposted.details.reason`. No empty-body unpost. Accounting **Journal unpost reason** (`aria-label`) + `Unpost journal ${id}`.
- **Period close/reopen reason (BR-10.2):** Accounting Period close **Close / reopen reason** input (required) → `POST /accounting/period/close|reopen` `{ reason }` ∈ `PeriodCloseReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/blank/`!!!`/`http://…` → **422** (was free `str` `min_length=1` only — whitespace still reached service **400**; garbage could land in audit `period_closed` / `period_reopened` `details.reason`). (+ `through_date`). Accounting **Period close or reopen reason** (`aria-label`) + **Close books** / **Reopen books**.
- **Credit override reason (BR-11.1):** Sales Invoices + POS **Credit override reason** input (required on over-limit confirm) → `override_reason` ∈ `CreditOverrideReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`) on invoice post / POS sale / legacy `TransactionCreate`. Omit/`null` OK when not overriding; blank/`!!!`/`http://…` → **422**; required when `override_credit_limit=true` (was free `str` `max_length=500` only — whitespace failed model_validator, but garbage could land in audit `credit_limit_override.details.reason`). Service still enforces `CREDIT_OVERRIDE_REASON_REQUIRED` for defense in depth. Needs `credit:approve`. Sales/POS **Credit override reason** (`aria-label`) + Sales **Post sales invoice ${id}**.
- **GRN reject reason OpenAPI (BR-6.4):** `GrnItemCreate.rejection_reason` ∈ `GrnRejectionReasonValue` (strip; 1–500; ≥1 letter/digit; no `://`/`@`); omit/`null` OK when no reject; blank/`!!!`/`http://…` → **422** (was free `str`; blank failed model_validator when rejected, but garbage could persist on GRN line). `model_validator` still requires reason when `rejected_qty > 0` (or inferred reject from accepted < received). Purchasing Post GRN **GRN rejection reason ${po_item_id}** (`aria-label`) + **Post GRN**; UI pre-checks before POST; service still requires reason as defense in depth.
- **Reject/suspend reason OpenAPI hygiene:** `TenantSuspendRequest.reason` ∈ `TenantSuspendReasonValue`; `StockTransferReject.reason` ∈ `StockTransferRejectReasonValue`; `ExpenseReject.reason` ∈ `ExpenseRejectReasonValue`; `PurchaseRequestReject.reason` ∈ `PurchaseRequestRejectReasonValue`; `SalesQuotationReject.reason` ∈ `SalesQuotationRejectReasonValue` (full narrative honesty → **422** on blank/garbage). Expense approve keeps optional `ExpenseDecision.comment` only (no silent `comment` fallback on reject).
- **Action button hover colors:** Lifecycle clicks use `.btn-ok` (green Accept/Approve/Activate/Deposit/Clear/Post/Receive/Ship/Reopen/Confirm/Process/Deliver/Submit/→ Order/→ Invoice) and `.btn-danger` (red Reject/Cancel/Suspend/Bounce/Unpost/Deactivate/Close books/Skip next). Styled in `frontend/app/globals.css` (table + card; light/dark). Soft Activate/Deactivate on Users, Inventory (product/variant/catalog), Stores (branch/dept/store/WH), Accounting COA + bank connections, Tax rates, Sales customer/group, Purchasing supplier, Expenses category match the same classes (recurring expenses already did). Residual **Post** actions also use `btn-ok`: Accounting Post balanced entry / Post opening balances / cash-transfer Post; Inventory Post opening stock / Post adjustment / Post stock out. Expenses **Submit expense** also uses `btn-ok`.
- **Brand palette (UI):** CSS tokens in `frontend/app/globals.css` `:root` — brand `#7FA347` / dark `#658338` / light `#C6D9A8`; forest `#003024` / deep `#001A14`; deep-green `#13612E`; ink `#10211B`; muted `#4D5D56`; line `#D7E0D8`; mist `#F3F6F1`; sand `#F7F4EC`; white `#FFFFFF`. Use vars (`var(--brand)`, …) for new UI; primary CTAs `.btn-primary` / `.login-primary` / `.tpos-btn-primary`.
- **POS drawer reason (BR-8.1):** POS shift header **Drawer reason** input (required) → `POST /pos/sessions/{id}/drawer/open` `{ reason }` ∈ `PosDrawerOpenReasonValue` (strip; 3–200; ≥1 letter/digit; no `://`/`@`; rejects placeholders `manual`/`n/a`/`na`/`none`/`test`); omit/blank/`!!!`/`http://…`/placeholder → **422** (was free `str` `min_length=1` only — whitespace/placeholders still reached service **400**; garbage could be logged on drawer open). Replaces `window.prompt`. Auto-open on cash sale unchanged (`pos_sale:{id}`). POS **Cash drawer open reason** (`aria-label`) + **Open cash drawer**.
- **Custom role soft-deactivate (BR-3.2):** Users **Activate** / **Deactivate** call `PATCH /roles/{key}` `{ is_active }`. Manage list uses `GET /roles?include_inactive=true`. Manage list filter All / Active / Inactive (`roleManageFilter`; client filter over full roles cache). Default catalog hides inactive; new assignment returns 400; existing assignees keep the role until reassigned. **Delete** still 409 while any user is assigned. Assignment pickers remain active-only (except current role).

## Subscription-Based Multi-Store Architecture

```
RIBDIGI HOUSE
      ↓
Subscription Package (max_stores; NULL = unlimited)
      ↓
Tenant (= Company in this product)
      ↓
Store / Branch / Warehouse
```

- **Company == Tenant.** There is no child Company entity; `tenants.company_name` is the company profile. Multi-company-under-tenant allocation is N/A until that model exists.
- **RIBDIGI HOUSE** sets package catalog limits and optional per-tenant `max_stores_override`.
- **Tenant Admin (`company_admin`)** may set `store_limit` ≤ subscription entitlement (company allocation).
- **Effective limit** = `min(store_limit, entitlement)` when both finite; `NULL` means unlimited.
- **Counts** use **active** stores for create/activate. Downgrades never delete stores; `over_entitlement` is surfaced and create/activate is blocked until resolved.
- Enforcement is backend-only (`store_entitlements` + tenant row lock). Do not rely on frontend button disable alone.
- Reuse existing `stores:read|write` RBAC + package `stores` module gate; do not invent parallel permission strings unless product expands RBAC verbs.
